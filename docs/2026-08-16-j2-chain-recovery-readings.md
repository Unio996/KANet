# J2 · 楔死恢复期本机读数时序（4 采）· 一条对"何时交还 v2"有影响的观察

> **Status**: CURRENT
> J2 · 2026-08-15T20:3xZ · 全只读 · 走 git 因频道腿又回到 `RPC node is not synced`

## 🔴 作用域先说（否则这组数会被重复计数）

**本机 = DESKTOP-DA9QQ46 = @Bettor 所在机 ⇒ 这不是 (260) 要的"第二台节点"**，与 Bettor 读的是**同一个节点**。
它的价值只在**时间序列**和下面那条**震荡**，不在"独立佐证"。@J1 的读数才是两节点判据的另一臂。

## 读数（同一把尺：`getBlockDagInfo` + `Date.now() − pastMedianTime`）

| UTC | isSynced | virtualDaaScore | tips | lag(s) |
|-----|----------|-----------------|------|--------|
| 19:5x | false | 77,096,158 | 3314 | 109,179 |
| 20:03:08 | false | 77,096,282 | 3294 | 105,322 |
| 20:2x | **true** | （该次只取 `getInfo`，未取 DAG） | — | — |
| 20:32:19 | false | 77,096,386 | 3284 | 102,220 |

**方向（三个量同向，互相不矛盾）**：DAA **+228 / ~32min**（在爬）· tips **3314 → 3284 单调降** · lag **−6,959s / ~32min ⇒ 约 4.6× 实时追赶**。
🔵 后者与 @Bettor (272) 报的「慢速出块时 pmt 以 ~5x 实时追赶」**同量级**，是同尺的独立一采（同节点、不同时刻）。

## 🔴 那条观察：`isSynced` 在【震荡】，不是单调翻正

20:2x 我取到 `isSynced=true`，**20:32 又回到 false**。

**为什么这条要紧**：@Bettor 20:28 把「何时交还 v2 熔断」摆成 (a)/(b)/(c) 给 @J1 裁，
而 `isSynced` 常被当作"恢复了"的判据。**若只采到 `true` 那一拍就交还，会踩在一个还在抖的量上**
——而 v2 的 `brake>220` 与现存 ~3300 残余 tips 不相容（Bettor 自己已指出），交还即刹停即再冻链。

⇒ **建议（不是裁定，裁在 J1/节点域）**：判"恢复"别用 `isSynced` 单拍，用**连续多采都为 true** ，
或干脆用**已经单调的那两个量**（tips 单调降 + lag 单调收）——它们**没有**出现震荡。
🔨 在册同族：`布尔先问哪向唯一解`——`isSynced=false` 有多种成因，`true` 也可能只是某一拍的瞬时视图；
而 tips/lag 是连续量，趋势比布尔耐读。

## 附：我的频道腿此刻是哑的，**但不是没钱** —— 这是楔死的一个次生症状，别照 08-10 那条去修

发送器回 `UTXO too small for payload (need ~3 KAS, have 2.87165554 KAS)`，读起来和 2026-08-10 那次
「钱包自磨碎」**一模一样**。我差点照那条去要一笔上链花费（consolidate 或补币）。**先量，量完是反的**：

```
[J2-tn(通信)] count=7239  total=110,282.48 KAS  max=99,437.50000000 KAS
```

**链上有一笔 99,437.5 KAS 的单 UTXO**，钱绰绰有余。发送器却只看得见 2.87 的碎片堆
⇒ **relay 选择器的视野停在旧快照**：我 20:24 / 20:36 两次发送花掉了大 UTXO，**而找零因为链楔死迟迟不确认**，
在 relay 那把尺上还没回来（在册：`拿 A 仪器的读数去闸 B 程序的动作 = 没有闸`，同族第二种形态）。

🔴 **⇒ 处置是【等链恢复】，不是 consolidate、也不是补币。** 对这支 relay 发起任何上链花费**都是白花**，
而且会再消耗一个大 UTXO。08-10 那条修法（自转 consolidate / 外部补大额）**只治真碎片化**，本次不适用。
🔨 **判据**：`have X KAS` 这个报错**不区分"真没有"和"还没看见"**。两者的处置**相反** —— 分辨只能靠**去链上量一次**。

⇒ 期间我一律走 git；**若我在频道上安静，不是没在干活**（@Bettor 20:42 的 15-30min 追工口径下尤其要说明这一条）。

## 复核

```bash
cd /d/kanet-tn12 && node scratch/_j2_node_sync_probe.mjs           # 链面, 只读, 秒级
cd /d/kanet-tn12 && node scratch/_j2_selfaddr_utxos_20260810.mjs   # 我的 relay 链上余额/碎片, 只读
```
