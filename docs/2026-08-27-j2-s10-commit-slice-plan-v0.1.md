# §10 v1 · commit 切片计划 v0.1（报备层 · 零落码 · 每片可独立 NWT diff 审）

> **Status**: DRAFT v0.1 · J2 2026-08-27 · 交付 Bettor/NWT 审（切片前置） · Bettor 派工（D-013 §1 "§10 GO"）· 依据：设计 `docs/2026-08-19-s10-pubkey-identity-design.md`@847bcf22（Codex MSG-250 五项 CLOSED）+ 实施计划 `docs/2026-08-27-j2-s10-impl-plan-v0.1.md` v0.2 = 70761d33（NWT 深审 PASS-WITH-NOTES e128a735）。
> **NWT e128a735 五条 notes 吸收核对**（v0.2 已全部吸收，无未吸收项）：① ⑦ 作用域收紧（关跨节点/远程；同机仍 loopback）→ v0.2 头注 + #9；② `local_relay_id` 列不存、映射活算 `XOnlyPublicKey.fromAddress` → #3；③ in-tx 读 `relay_nodes` 同 `.immediate` 写锁一致性显式句 → #4 TOCTOU 补注；④ "承重是绑定、验签冗余"口径 → #4；⑤ 回滚形状（S10 in-tx 失败整笔回滚含 A2 INSERT、挑战不消费、可重试）→ #4 回滚补注。NWT §47 "C4 builder 碰钥按 3c876765 那套 grep 零片段再卡" = 审查动作，落在 C4 审点。
> **范围铁律（D-013 §1）**：register-only；rotate/revoke/迁移不含；不做自动签发口；不部署对外、不动 Owner 实例 §0 墙；不改 A2 六字段语义（S10 独立信封）。**代码入库 ≠ live**（D-005 独立迁移 Owner 另拍）。
> **流程**：每片 pathspec commit **不推**，附 `node <case>` 输出 + lint 0 ⇒ NWT diff 审 GREEN ⇒ Bettor 推 ⇒ 累计到可审单元发 Codex 桥（MSG-285）。READY（步 0）到时暂停切片、先跑 (17) 清单，完后回来。铁律：列名/函数名/行号全部查了再写（本页坐标 `[SRC 2026-08-27]` 均本机 grep 实核）。

## 0 · 现状坐标（实核，切片依据）
- `kasia-console/src/lib/u1-registration.mjs`：`REG_REJECT` :98-112（冻结对象，值 = 键名字符串）；`_RegTxError` :115；`deriveCustody` :125；`registerIdentity` :150（生产入口，无注入参数）；`__testOnlyRegisterIdentityWithInjections` :168；`_registerIdentityImpl` :172；预筛序 = ① custodyPre :179 → ②-a binding :183 → ②-c store :190-205 → ②-d 读 record :212 → ②-b PoP :215 → ③ `runTx` :220-285（`.immediate`）：in-tx `custody2` :227 → A2 INSERT :237-242 → 挑战重读 :251-255 → 过期重验 :265-271 → 消费 :274 → 后置重读 :280-284。
- `kasia-console/src/api/identities.js` :263-289 `POST /api/identity/u1-register`：白名单六字段 :265-272；`createChallengeStore` 503 传导 :276-285；`registerIdentity` :287。
- `kasia-console/src/lib/u1-registration-pop.mjs`：`verifyMessageFn` 注入 :72-75；默认 `import('kaspa-wasm').verifyMessage` :123-125；throw ⇒ 拒 :132（S10 验证器照此形）。
- `XOnlyPublicKey.fromAddress(new Address(addr)).toString()` 先例：`kasia-console/src/api/feedback.js:18`、`bettor.js:1329/1549/1581`。
- `kasia-console/src/db/migrate.js`：最新 **v197** :5697-5730（`u1_identity_challenge`）；`docs/DATABASE.md` :846 "当前最新版本：v197"、:855 v197 条目形。⚠ CLAUDE.md 写的 "v55" 是陈旧值，以 migrate.js 为准。
- 既有 u1 测试（`kasia-console/src/lib/`）：`u1-registration.test.mjs`（真 migration 临时库 :12-18；夹具造 `relay_nodes` 行 :35-53、PoP 签 `signMessage({message: popMessageHashHex, privateKey})` :81；`verifyMessageFn` 钩子臂 :268-336）、`u1-wiring-behavior-nwt.test.mjs`、`u1-cas-concurrent.test.mjs`、`u1-registration.mutants.mjs`、`u1-v197-migration-acceptance.mjs`（真 migration 验收形）。
- golden vectors：`artifacts/2026-08-19-s10-golden-vectors-v1.json`（3 向量；锚 = `canonical_bytes_hex / canonical_sha256 / signed_message`；`example_signature_verifiable_not_reproducible` 只可 verify=true）。
- builder：`kasia-console/scripts/u1-build-submission.mjs`（`emit` 白名单 :37；密钥区 `KeyZoneError` :79，:100 解密）。
- 全仓零 `KANET-U1-IDENTITY` / `s10` 命中（新模块从零起）。

## 1 · 切片（每片：改动面 / 用例与向量 / 依赖 / 回滚 / NWT 审点）

| 片 | 改动面（file:行改动面） | 新增用例 + 确定性向量 | 依赖 | 回滚 | NWT 审点 |
|---|---|---|---|---|---|
| **C1 · S10 信封 + 纯验证器 + 向量** | **新** `src/lib/u1-s10-identity.mjs`（≈120 行）：`S10_DOMAIN='KANET-U1-IDENTITY'`、`S10_VERSION='1'`、`S10_OPERATIONS=Object.freeze(['register'])`、`S10_REJECT`（`MALFORMED/PUBKEY_NOT_CANONICAL/NETWORK_MISMATCH/OPERATION_NOT_ALLOWED/DOMAIN_VERSION_MISMATCH/SIGNATURE_INVALID`）、`canonicalBytes(fields)`（6 字段 `u32be(len)‖utf8` 固定序）、`s10Prefix()`（= domain‖"-v"‖version‖"\|"，派生非字面量）、`s10SignedMessage(fields)`（prefix‖network‖"\|"‖lowerhex(sha256(canonical))）、`assertCanonicalPubkey(hex)`（`/^[0-9a-f]{64}$/` ∧ `new XOnlyPublicKey(hex)` 可解析）、`verifyS10Envelope(env, { localNetwork, verifyMessageFn? })`：键白名单 7 个 → `env.domain===S10_DOMAIN ∧ env.version===S10_VERSION` → **`env.network===localNetwork`（本地权威，不收 payload）** → `operation ∈ S10_OPERATIONS`（`===` 白名单）→ L1 → `verifyMessage({message, signature, publicKey})` **try/catch ⇒ 拒**。零 DB/零 IPC/零 relay。**新** `src/lib/u1-s10-identity.test.mjs` | G1 三向量逐字节（`canonical_bytes_hex/canonical_sha256/signed_message`）+ `example_signature` verify=true；N1 改 network / N2 改 domain·version / N3 换 pubkey / N8 非规范 pubkey（63 位·大写·非曲线点）/ N9 跨网重放（`localNetwork='mainnet'` 收合法 testnet 声明 ⇒ `NETWORK_MISMATCH`）/ N10 `rotate`·未知 operation（合法签名）⇒ 拒 / N12 跨域（A2 PoP 64-hex 材料当 S10 ⇒ 拒；S10 消息当 A2 ⇒ 由 pop 层拒，在 C3 验）/ N13 外层 network 不存在（结构）/ throw-路径（垃圾签名 128 长度 ⇒ `verifyMessage` throw ⇒ 拒非 500）。**确定性向量文件** `src/lib/u1-s10-identity.vectors.json`（复用 golden 三向量 + 上述负例的固定输入；签名用 test-only priv=1 钥现签，只断言 verify 布尔）。运行：`node kasia-console/src/lib/u1-s10-identity.test.mjs` | 无（纯函数；kaspa-wasm 已在 deps） | `git revert` 单 commit；无表无接线，零影响面 | 纯函数零 DB/IPC；`localNetwork` 只能由调用方从本地配置注入；operation `===`；throw==false 同拒；canonical 与 golden 逐字节；`S10_REJECT` 冻结对象 |
| **C2 · v198 身份表 + DATABASE.md + 验收探针** | `src/db/migrate.js` 接 v197 之后 **v198**：`CREATE TABLE IF NOT EXISTS u1_relay_identity (relay_pubkey_xonly TEXT PRIMARY KEY CHECK(length(relay_pubkey_xonly)=64 AND relay_pubkey_xonly = lower(relay_pubkey_xonly) AND relay_pubkey_xonly NOT GLOB '*[^0-9a-f]*')  /* ⚠ 不是 GLOB '[0-9a-f]*'——那只约束首字符; NOT GLOB '*[^0-9a-f]*' 才是全串 hex; ④-3 用大写/非 hex 向量实测 */, network TEXT NOT NULL, operation TEXT NOT NULL CHECK(operation='register'), epoch TEXT NOT NULL UNIQUE, signature TEXT NOT NULL, registered_at TEXT NOT NULL DEFAULT (datetime('now')))`；**无 `local_relay_id` 列、无任何按 relay_id/ecdsa_pubkey_xonly 的索引**；additive。**新** `src/lib/u1-v198-migration-acceptance.mjs`（照 v197 探针形：临时库 + `runMigrations()` 真迁移）。`docs/DATABASE.md` :846 改 "当前最新版本：v198"，:855 前插 v198 条目（用途/字段/约束/写入方=`registerIdentity` 事务内（C3 起）/读取方/陷阱"无 relay_id 列，映射活算 fromAddress，别加回退列"）。CLAUDE.md 的 "v55" 顺手勘误为 "以 migrate.js 为准" | ④-1 表存在且列集精确；④-2 PK 冲突（同 pubkey 二次 INSERT ⇒ SQLITE_CONSTRAINT）；④-3 CHECK：大写/63 位/非 hex 拒、`operation='rotate'` 拒；④-4 `epoch` UNIQUE；④-5 幂等（`runMigrations()` 二次不报错）；④-6 **无** `local_relay_id` 列、`PRAGMA index_list` 只有 PK/UNIQUE 自动索引；④-7 今日零写入方（grep 全仓无 INSERT）。运行：`node kasia-console/src/lib/u1-v198-migration-acceptance.mjs` | C1 不依赖（可并行 NWT 审），但顺序上排 C1 后 | additive 表：revert commit 后已跑过迁移的临时/开发库残留空表无害；**live 库不在本轮迁移**（D-005）| PK 是 pubkey 非 relay_id；无 legacy 回退索引；无 `local_relay_id`；CHECK 与 L1 同口径；DATABASE.md 同步 |
| **C3 · A2 注册接线（fail-closed 默认）+ handler + 既有夹具 + 红臂** | `u1-registration.mjs`：(a) `REG_REJECT` 加 `RELAY_NOT_OWNED`、`S10_INVALID`（白名单式新值）；(b) `_registerIdentityImpl` 在 ②-b PoP 之后、③ 之前加 **S10 预筛**：`s.s10` 缺 ⇒ `RELAY_NOT_OWNED`；`verifyS10Envelope(s.s10, { localNetwork: KASPA_NETWORK 本地配置, verifyMessageFn })` 不过 ⇒ `S10_INVALID`；`s.s10.epoch !== s.challenge` ⇒ `S10_INVALID`；绑定 `s.s10.relayPubkeyXOnly === XOnlyPublicKey.fromAddress(new Address(relay_nodes[s.relayId].address)).toString()` 不等 ⇒ `RELAY_NOT_OWNED`（DB 读只做 L5 绑定，验签公钥仍取 payload）；(c) `runTx` 内、A2 INSERT :242 之后、挑战重读 :251 之前：**重做绑定**（同 `.immediate` 写锁下读 `relay_nodes.address`）+ 重验 S10（冗余）+ `INSERT INTO u1_relay_identity`（PK 冲突 ⇒ `_RegTxError(CONSTRAINT)` ⇒ 整笔回滚含 A2 INSERT，挑战不消费）；(d) `deriveCustody` 不动；成功返回加 `relayPubkeyXOnly`。`identities.js` :265-272 白名单加 `s10: pick7(b.s10)`（只取 7 键，不展开）；network 不收外层。既有夹具：`u1-registration.test.mjs` / `u1-wiring-behavior-nwt.test.mjs` / `u1-cas-concurrent.test.mjs` 加 `s10For(relayId, id, challenge)`（同叶钥签 S10），**期望不改**；`u1-registration.mutants.mjs` 加两格（去 (b) 绑定 / 去 operation 白名单）+ 一格（去 (c) in-tx 重验） | **R7 ⑦ 红臂**：攻击者新钥 X + `relayId=relay-B` + 活挑战，`s10` 由 X 签 ⇒ **必 `RELAY_NOT_OWNED`**（对照：B 自钥签 ⇒ PASS 且 `u1_relay_identity` 1 行 = fromAddress(B)）；N4 无 `s10` ⇒ `RELAY_NOT_OWNED`；N5 `relayPubkeyXOnly=B` 但 X 签 ⇒ `S10_INVALID`；N11 legacy 中毒（`ecdsa_pubkey_xonly` 填充、无合法 S10）⇒ 拒；E1 `epoch≠challenge` ⇒ `S10_INVALID`；T1 预筛后事务前换 `relay_nodes.address`（`verifyMessageFn` 钩子窗口同 :268 手法）⇒ in-tx 绑定拒、整笔回滚、身份表 0 行、挑战未消费；U1 同 pubkey 两次（不同 challenge）⇒ 第二次 `CONSTRAINT` 回滚；CAS 三判据（A2 :253 / C1 / A1 UNIQUE）**不变**；scratch `_u1_nonce_not_bound_selftest.mjs` 那条期望翻转为 R7。运行：`node src/lib/u1-registration.test.mjs`、`…wiring-behavior-nwt.test.mjs`、`…cas-concurrent.test.mjs`、`node src/lib/u1-registration.mutants.mjs` | **C1（验证器）+ C2（表）** | revert C3 ⇒ 回到无 S10 的 A2 路径（既有测试仍绿因夹具多给的 `s10` 被忽略——⚠ 白名单 handler 会丢弃未知键，模块层不读 ⇒ 无副作用）；表留空无害 | 绑定用 DB 地址、验签用 payload 钥分清；in-tx 重做存在且位置正确（A2 INSERT 后、挑战重读前）；`RELAY_NOT_OWNED` 为白名单新值；R7 红臂对照 scratch v1 前 PASS；三份夹具全补（规则 69）；handler 不展开 body、不收 network |
| **C4 · builder 产 `s10` + runbook v0.4** | `scripts/u1-build-submission.mjs`：密钥区内同一把叶钥再签 S10（`epoch=challenge`、`network` 取 `KASPA_NETWORK`），**`SUBMISSION_KEYS` :35 白名单加 `s10`（否则 `emit` :37/:139 静默丢键）**、`s10` 子键再白名单 7 个；`KeyZoneError`/`free()` 纪律不退。`docs/2026-08-27-j2-u1-operator-challenge-issuance-runbook.md` §4 步 2/4 + 验收 (a) 加 "`u1_relay_identity` 1 行且 = fromAddress(J2-tn 地址)（活算）" + ⑦ 红臂进 live 负测 | builder 离线自测：临时库 + 假 relay 行 ⇒ 输出含 `s10` 且 `verifyS10Envelope` 过、`relayPubkeyXOnly == fromAddress(address)`；NWT grep 零片段（3c876765 那套） | C1、C3 | revert C4 ⇒ builder 回六字段（此时 C3 会拒 `RELAY_NOT_OWNED` = 正确 fail-closed，不是半态）| 密钥区纪律；白名单只多 `s10`；R7 可上 live 负测 |
| **C5 · DECISIONS 注记 + ANTI-PATTERNS 一条** | `docs/DECISIONS.md` L158-159 ⑦ 注记（不改原话）："⑦ 关跨节点/远程抢注（远程签不出本机 relay-B 地址钥，v198 + `RELAY_NOT_OWNED`）；同机抢注仍在 loopback 内（同 (528)）；跨节点身份表 = `u1_relay_identity`；rotate/revoke 仍 OPEN"；`docs/ANTI-PATTERNS.md` 加一条"身份权威读只按 `relay_pubkey_xonly`，不按 relay_id/legacy 列回退" | 无（doc）| C3 | revert | 措辞与 NWT ① 一致 |

**依赖顺序**：C1 → C2 → C3 → C4 → C5（C1/C2 可并行审，C3 须两者 GREEN 后起）。**每片一 commit、pathspec、不推。**

## 2 · 每片自测证据（commit message 附）
- C1：`node kasia-console/src/lib/u1-s10-identity.test.mjs` ⇒ `N/N PASS`（含 G1 三向量逐字节）；`node scripts/lint-kanet.mjs <2 文件>` 0 errors。
- C2：`node kasia-console/src/lib/u1-v198-migration-acceptance.mjs` ⇒ ④-1..④-7 全过（临时库）；lint 0；`docs/DATABASE.md` 同 commit。
- C3：三份既有测试 + mutants + R7/N4/N5/N11/E1/T1/U1 全绿、mutants 全咬；lint 0（M0a 门：DB 走 `src/db/client.js`）。
- C4：builder 离线自测输出（脱密钥）+ NWT grep 零片段；lint 0。
- 🔴 **test-framework**：见 §4——承重在 standalone 离线层；框架层只加 `cases/identity/` 两条不落库的负向 http case（C3），证据 `logs/test-runs/<case>-latest.json`。

## 3 · 不做 / 风险（沿 v0.2 §6）
- rotate/revoke/迁移不含；P2 根治（7 个 `ecdsa_sign` 生产者域标签）不含、单独立项；同机信任模型不宣称关掉；地址派生不一致 ⇒ 绑定拒 = 正确拒（runbook 先跑 escape-hatch）；三份夹具漏一份 = "改路径没改观察量"（C3 审点专列）。
- ⚠ `kaspa-wasm` `XOnlyPublicKey` 构造对非曲线点是否 throw 须实测（C1 第一条向量先钉），不凭印象。

## 4 · test-framework 落法（按仓内约定实核 2026-08-27）
- **约定实核**：`scripts/test.mjs` 只自动发现 `test-framework/cases/<domain>/*.test.mjs`（`findCases` :22-44；`--case=` **不是名字查找，是相对 `process.cwd()` 的路径**，形如 `--case=test-framework/cases/<domain>/<file>.test.mjs`；`--domain=<目录名>`）；case = 默认导出的声明式对象 `{ id, description, domain, tags, steps[] }`，step 是 `http_post/…` 动作 + `expect.must{ http_status_equals, reply_contains, query_db{sql, expected_row} }`（README :25-70/:75-148/:166；runner 断言 `lib/runner.mjs:1467-1521`，执行 `runCase` :2281）⇒ 框架 case 是**对活 console 的 HTTP/DB 行为断言**，不是离线单元。既有 u1 全套（registration/wiring/cas/mutants/v197 探针）是 **standalone `node src/lib/*.test.mjs`**（临时库 + `runMigrations()` 真迁移，不经 test.mjs）。
- **§10 的分层（两层都加，各管各的）**：
  1. **离线确定性层（承重）** = C1/C2/C3 列的 standalone 用例 + 向量 JSON（golden 三向量 + 负例固定输入），与既有 u1 套同形，跑法 `node kasia-console/src/lib/<file>.test.mjs`，证据 = commit message 附输出（`N/N PASS` + mutants 咬合表）。**理由**：R7/T1/U1/CAS 这类需要注入时钟/verifyMessageFn/同事务夹具的臂，只有 standalone 能构造；框架的 http 动作构造不了"预筛后事务前换钥"。
  2. **框架层（薄壳，只做活 console 的行为回归）** = 新目录 `test-framework/cases/identity/`（domain=`identity`），C3 落两条 `*.test.mjs`：`u1_s10_register_negative_no_s10.test.mjs`（`http_post /api/identity/u1-register` 六字段合法夹具但**无 `s10`** ⇒ `http_status_equals: 400` ∧ `reply_contains: ['"code":"RELAY_NOT_OWNED"']` ∧ `query_db` 断言 `u1_relay_identity` 0 行、挑战 `used_at IS NULL`）与 `u1_s10_register_negative_bad_s10.test.mjs`（`s10` 由另一把钥签 ⇒ `S10_INVALID`/`RELAY_NOT_OWNED`），两条都是**不落库的负向**（活 console 安全，不产生真注册）；**正向注册不进框架层**（会真写 `u1_relay_identity`，属 runbook 的 operator 动作，C4 验收 (a)）。夹具挑战由 `u1-issue-challenge.mjs` 签发（(527) 裁 (i) 纯 nonce，不改）。跑法 `cd kasia-console && node scripts/test.mjs --case=test-framework/cases/identity/<file>.test.mjs`，证据落 `logs/test-runs/<case>-latest.json`（覆盖式，只留最后一次——CLAUDE.md 实测更正：无自动回归）。
- **不做**：不把 standalone 用例改写成框架 case（会丢注入能力）；不加"跑脚本"型动作到 runner（改 runner = 越界）。
- **规则 69 对照**：C3 三份既有 standalone 夹具补 `s10` 是必做；框架层两条负向是新增观察量，不替代前者。
