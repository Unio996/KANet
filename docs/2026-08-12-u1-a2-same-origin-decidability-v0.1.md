# u1 · A2 同源判定 设计稿 v0.3【DESIGN-ONLY · 零落码 · 待 Bettor 定 identities-per-account + NWT 复核转 GREEN】（文件名保留 v0.1，版本以本行与 Status 头为准）

> **Status**: CURRENT · v0.3（2026-08-12 · J2）—— v0.3 = 收 @NWT 红队 `9d7fc088` 的 PUSH-BACK：**§5-A 那句"R3 不受影响"撤销**，改成带前提的准确表述（缺口① = 我原论证是循环，复核成立）· 新增 **§5-A-ter**（NWT 已答 committee 抽样；我复核后给那个 22.2% **加上"stake 相等"这个条件**）· 新增 **V6**（残余风险进运行时输出）+ **§7-bis 缓解清单** · §8 补「形态 A 是一条新派生代码路径，须自带逃逸口审视」。
> **v0.2**: §5-A 那条 🟠 我自己实测坐实（带对照臂）· 自撤「被 R3 兜住」一半（第二 NWT 会话 18:50Z 抓）· threshold 由"引在册"改现查代码。
> **派工**: @Bettor 2026-08-11T18:38Z 频道 A2 裁定（`#p06qin.2/.3`）——「走 ① 公钥指纹落库；② 派生路径自陈登记否决」+ 硬要求一条：
> 「**登记根（xpub/指纹）与 relay 实际 pubkey 之间要有派生证明（验证方能从登记根推导 relay 子 pubkey 并比对），否则 ① 退化成换皮的 ②**」
> **上游**: 分配算法 spec `2026-08-11-u1-key-domain-allocation-spec-v0.1.md` §5-A2（"本 spec 交出去的最大缺口"）· 构造 runbook `2026-08-10-u1-key-isolation-construction-runbook-v0.1.md` §2 步骤 2/3
> **本文件不落码、不动开关、不碰链。** 所有读数均为本机现读，命令逐条附上，供一秒推翻。

---

## §0 一句话（先说结论，因为它改的是裁定的前提，不是裁定的选项）

**裁定选 ① 是对的，② 该否决也对。但那条硬要求——「登记根能派生出 relay 子 pubkey」——与 A2 要答的问题——「两个 relay 是不是同一份 mnemonic」——在本仓现行派生路径下【互斥】：能派生证明的那一层，恰恰是分不出同源的那一层；能分出同源的那一层，恰恰是派生证明不过去的那一层。**

⇒ 所以 A2 不是"选个格式然后写码"，**得先动一处派生路径，或者接受"同源只能正向证、不能反向证"**。两条我都写在 §5，带代价，**不自拍**。

---

## §1 地基事实（现读，不引转述）

```
relay_nodes 总行数                 = 32
is_oracle = 1                      = 10
ecdsa_pubkey_xonly 非空            = 0 / 32   （oracle 侧同为 0 / 10）
mnemonic_encrypted 非空            = 30       privkey_encrypted 非空 = 2
u1_domain_assignment               = 表在 live 库、0 行
address 重复组（同址两行）          = 0
mnemonic 密文去重                  = 30 / 30  （AES-256-GCM 带 IV ⇒ 密文不可比对，**不能**拿密文判同源）
```
```bash
node scratch/_j2_a2_probe_20260812.cjs          # 上表前 4 行
node scratch/_j2_a2_addrdup_20260812.cjs        # 重复 address / 密文去重
```

**现行派生路径**（`kasia-relay/src/lib/wallet.mjs:39-50`，console 侧 `kasia-console/src/services/wallet.js:28-34` 同形）：

```
m / 44' / 111111' / accountIndex' / 0 / 0
      ↑hardened  ↑hardened   ↑hardened    ↑非硬化 ↑非硬化
```

`accountIndex` 来自 `KASPA_ACCOUNT_INDEX`（`wallet.mjs:115`，默认 `'0'`）。
🔴 **全仓只有这一处【读】它，没有任何一处【写】它**：

```bash
grep -rn "KASPA_ACCOUNT_INDEX" --include=*.js --include=*.mjs --include=*.sh . | grep -v node_modules
# 唯一命中: kasia-relay/src/lib/wallet.mjs:115
```

⇒ **今天全部 relay 进程都走 accountIndex=0**（`relay-manager.js:85-87` 只传 `KASPA_PRIVKEY` / `KASPA_MNEMONIC` 两个之一，不传 account index）。

---

## §2 决定性实验：同一份 mnemonic 的两个身份长什么样

⚠ 全程用**当场随机生成**的助记词，**未解密任何在库真实密钥**。

```bash
node scratch/_j2_a2_derivation_probe_20260812.mjs
```
```
A 同一 mnemonic · acct 0 两次  : IDENTICAL
B 同一 mnemonic · acct 0 vs 1  : DIFFER      ← 同源, 但两个地址在公开数据上看不出任何关系
C 不同 mnemonic · acct 0       : DIFFER
```

**两条直接推论**：

- **(a) 现行部署下，"两个 relay 共享一份 mnemonic" 必然表现为【同一个 address】**（因为 accountIndex 恒 0 ⇒ 同 seed 同路径 ⇒ 同私钥）。
  ⇒ **A2 在"无人动 account index"的世界里，今天就是可判的，成本为零**：`GROUP BY address HAVING COUNT(*)>1`，现读 **0 组**。
- **(b) 而这条只在 `KASPA_ACCOUNT_INDEX` 保持不被设置时成立。** 它是**进程环境变量**，不在仓里 ⇒ **一个不配合的域运营方，在自己那台宿主上 `export KASPA_ACCOUNT_INDEX=1`，就能用同一份助记词造出第二个"看起来无关"的委员身份**，而 (a) 这道检查全程绿灯。
  🔴 **而它恰恰是 u1 唯一要挡的那个人。**

---

## §3 🔴 裁定的硬要求与 A2 的目标，在现行路径下互斥（本稿核心）

裁定要求：**验证方能从登记根推导出 relay 子 pubkey 并比对**。问"哪一层能当这个登记根"，答案只有一个，而它答不了同源。

**实测**（本仓 wasm，不是引 BIP32 教科书）：
```bash
node scratch/_j2_a2_hardened_gap_probe_20260812.mjs
```
```
① master XPub -> deriveChild(44, HARDENED)      : 抛错 "Bip32 -> Invalid child number"
② master XPub -> deriveChild(0,  NON-hardened)  : 成功        ← 对照臂: 这把 XPub 本身可用,
                                                                ① 的失败不是"对象坏了"
③ 账户层 XPub(m/44'/111111'/acct') -> 叶子 0/0   : 成功        ← 只有这一层推得到 relay pubkey
```

于是两层各自缺一半：

| 候选登记根 | 能派生证明到 relay pubkey？ | 能判两个 relay 同源？ |
|---|---|---|
| **master xpub / seed 指纹** | ❌ 中间三级全硬化，`deriveChild(_, true)` 在 XPub 上直接抛错（①） | ✅ 相等即同源 |
| **账户层 xpub** `m/44'/111111'/acct'` | ✅ 最后两级非硬化，推得到（③） | ❌ **同一 seed 的两个账户是硬化兄弟，彼此不可关联**（②/③ 合起来说明：从 XPub 跨不过硬化那一级） |

🔴 **⇒ 不存在一个既满足裁定硬要求、又能判同源的登记根。** 硬要求原样执行 ⇒ 登记根只能是账户层 xpub ⇒ **它证明的是"这个 relay 属于这个账户"，不是"这两个 relay 同源"** ⇒ A2 没被答上，而记分板会显示 ① 已落地。

🔨 **这一格值得单独记**：硬要求本身没写错——它防的"换皮的 ②"是真风险；**错的是它默认了"存在一个既可派生证明又能判同源的根"**。这个默认在硬化账户布局下不成立，而**光看裁定文字看不出来**，得去 wasm 上撞一次。

---

## §4 顺带回答裁定的另一半：存量 32 行 `ecdsa_pubkey_xonly` 全空的补齐路径

**它可以纯从 `address` 算出来，byte-exact 回推验证通过 4/4**：

```bash
node scratch/_j2_a2_addr_pubkey_bijection_20260812.mjs
# J2test / NWT / maker-1 / maker-2 : 回推 == 原地址 YES ×4
```

🔴 **⇒ 补齐这一列的信息量 = 0。** `address` 就是该 xonly pubkey 的编码（同 `coord-status-验签-SOP` 里推 Bettor 公钥用的同一条路），**不需要任何 relay 配合，也因此不带来任何新的可验证事实**。
⇒ **"把 `ecdsa_pubkey_xonly` 填起来" 不能算 A2 的进展**，它只是把已有信息换个列存。要不要填是**工程便利问题**（省得每次现算），**不是安全问题**——写进 spec 时必须这么标，否则下一个人会把"这列填满了"读成"同源判定有了"。

---

## §5 三条可行形态（带代价，不自拍）

### 形态 A · 改派生布局：让同一 seed 的委员身份共用一个【非硬化可推】的账户层
把 u1 委员身份从"每身份一个硬化账户"改为"**一个 seed = 一个硬化账户，身份沿最后一级非硬化 index 展开**"：
```
m / 44' / 111111' / 0' / 0 / i        ← i = 该 seed 下第 i 个委员身份
```
- ✅ **两个要求同时满足**：登记根 = 账户层 xpub（可派生证明到每个 relay pubkey，③ 已实测）；**同一 seed 的身份全部挂在同一个根下 ⇒ 同源变成"根相等"，可判**。
- 💰 **成本比看上去小**：runbook §2 步骤 2 本来就是**在目标宿主本地【新生成】委员密钥** ⇒ 新钥直接按新布局生成即可，不动存量。
- ⚠ **一条必须写明的代价 —— 已由我自己实测坐实（v0.2 升级：原为 🟠 未测，现 ✅ CONFIRMED，带对照臂）**：
  账户层 xpub 公开 + 该账户下**任一**非硬化子私钥泄露 ⇒ **可反推账户 xprv ⇒ 该账户全部兄弟私钥沦陷**。
```bash
node scratch/_j2_a2_xpub_leak_probe_20260812.mjs
```
```
反推出的账户私钥 == 真账户私钥 ? 🔴 YES — 反推成立
对照臂(乱 chainCode)           ? ✅ 推不出  ← 证明上一行【确实在用登记根】, 不是算式恒真
由反推私钥再算兄弟 i=1          ? 🔴 命中   ← 单把子钥泄露 ⇒ 该账户全部兄弟沦陷
```
  🔴 **⇒ 登记表把「泄露一把子私钥」放大成「泄露该账户全部身份」。这是形态 A 引入的新代价，不是原有风险的换个说法。**
  🔴 **v0.3 改写（原文写的是"影响面被 R1/R3 夹住 ⇒ R3 不受影响"——NWT 红队 `9d7fc088` PUSH-BACK，我复核成立，那句话撤销）**：

  **准确表述**：这条放大的**后果可控，仅在下列两条【同时】成立时**，而**这两条都不是形态 A 自己能保证的**：
  - **前提①：R1 未被违反**（同一 seed 的身份确实都在同一个域内）；
  - **前提②：私钥泄露达到整机/整域级别**（那时"多丢几把"确实不改变什么，因为攻击者已经在那台宿主上）。

  🔴 **而两条各自失效时会怎样，必须写出来**：
  - **前提① 失效（NWT 缺口①，我原论证的循环）**：我原来用"R1 保证同源必同域"去论证"违反 R1 的后果可控"——**而 R1 是否成立恰恰是 A2 存在来检验的那件事**。
    若一个运营方真的用同一 seed 在域 X、域 Y 各放一个"看起来无关"的身份（**正是 §0 画的那个攻击者**），
    则域 X 一把子私钥泄露 + 公开的账户 xpub ⇒ **同时拿到域 Y 的钥 ⇒ 放大【跨域】⇒ R3 被直接打破**。
    🔨 **我那段论证等于"在检验一条规则是否被遵守的机制里，假设了这条规则被遵守"。**
  - **前提② 失效**：见 §5-A-bis（窄通道单钥泄露不需要整机权限）。

  ⇒ **正确的定性**：形态 A **把 R1 从"分配规则之一"变成了【承重的安全前提】** —— 它一直是运维承诺，
  但在形态 A 之前，违反它的代价是"两个域各多一把钥"；形态 A 之后，违反它的代价是"**一次窄泄露同时打穿两个域**"。
  **A2 检查本身因此比它看起来更承重**（NWT 原话：这套防线里没有任何一层是密码学强制的）。
  ⚠ **随之而来的一条运维硬规矩**：`dev-coord-testnet` 是**链上明文**（在册）⇒ **账户层 xpub 绝不可粘进频道/任何公开面**，登记根只落库。粘一次 = 永久公开。
- ⚠ 存量 10 个 oracle 仍是旧布局 ⇒ 它们之间的同源仍只能靠 §2(a) 的 address 相等判（够用，因为它们的 account index 事实上都是 0）。

### 🔴 §5-A-bis · 我上面那段「被 R3 兜住」**不够**（v0.2 自撤一半 · 第二 NWT 会话 18:50Z 抓，我复核成立）

我写"放大面被 R1/R3 夹住"时，脑子里的攻击是**整机/整域级攻破**——那种情形下"多丢几把"确实不改变什么，因为攻击者已经在那台宿主上了。
🔴 **而它不覆盖【窄通道单钥泄露】**：签名实现的 nonce 重用、单个进程内的一次内存/日志泄露、一份误传的密钥文件——**这类泄露不需要整机权限**。

- **形态 A 之前**（每个身份自己一份 seed）：窄通道泄露一把 ⇒ **只丢那一把**。
- **形态 A 之后**（一 seed 一账户、多身份挂同一个**公开的**登记根）：窄通道泄露一把 ⇒ **该账户全部身份**（§5-A 实测已坐实，含对照臂）。
- ⇒ **每次窄通道泄露的"产出"从 1 把变成 identities-per-account 把（今按 R2 = 2）⇒ 攻击者凑到任意给定把数所需的独立泄露次数【减半】。**
  今 threshold = **4-of-5**（✅ 现查代码：`pool-p2sh-v06.mjs:96` / `pool-close-builder.mjs:49` / `bshard-auto-settler.mjs:33`，非引在册）。
  ⚠ **我没验的一格，留给 @NWT**：拿到的那几把是否**恰好落在目标市场被抽中的那 5 个委员槽**里（`pool-committee-sampler.mjs` 的抽样），决定"减半"是不是直接等于"两次窄泄露即可签"。**这一格没验之前，别把它写成"两次即可"。**

🔨 **而这条最该被记住的不是结论，是它暴露的结构**：
> **在形态 A 里，「同源可判」与「一泄全丢」是【同一个属性的两面】** ——
> 一个根覆盖多个身份，**正是**它能判同源的原因，**也正是**它一泄全丢的原因。BIP32 下这两者拿不开。
> ⇒ **`identities-per-account` 这个数字，就是"A2 判定力" ↔ "泄露放大面" 的旋钮**：调到 1，放大面消失、A2 也判不出任何东西；调到 R2=2，两者都拿到一半。

⇒ 🔴 **这个旋钮我不自拍**（它是安全取舍不是工作量取舍，且我上一条推荐已被采纳=更要自己先打）。**请 @Bettor 定 `identities-per-account`，@NWT 先判上面那格抽样问题。** 在这个数字定下来之前，**形态 A 的落码我不开工**——布局是烤进密钥生成的，事后改 = 重新生成全部委员钥。

### §5-A-ter · committee 抽样那一格：NWT 已答，我复核后**给它加一个条件**（v0.3）

**我 v0.2 留的问题**（"减半是否 = 两次窄泄露即可签某市场"）**@NWT 用 `pool-committee-sampler.mjs` 实代码答了，结论我接受**：
**不等于。** 单域按 R2 只占 oracle 池 10 个候选里的 2 个；5 委员无放回抽 ⇒ 该域两人**都**进同一场 ≈ **22.2%**（`C(8,3)/C(10,5)=56/252`，我独立重算一致），
**且即使都进也只有 2/5 票，而门槛是 4-of-5** ⇒ 离单方面签出结算差至少 2 个别域委员。**这一格不阻塞方向。**

🔴 **但那个 22.2% 是【均匀抽样】的数，而这个抽样器是【stake 加权】的**（`pool-committee-sampler.mjs:117-128`：`hit = rand % totalStake`，按累积 stake 命中；`:100` 对非正 stake 直接抛错 = fail-closed）。
⇒ **22.2% 只在"候选人 stake 相等"时精确；stake 不等时必须按实际权重重算** —— 一个持有超额 stake 的沦陷域，进场概率高于 22.2%。
⇒ 🔨 **所以正确的说法不是"22.2% 兜住了"，而是"兜住的程度 = stake 分布的函数"**，而 stake 分布是运维产物，不是常数。

⚠ **一条我【没有】据以下结论的读数，写出来免得别人误用**：`relay_nodes.oracle_stake_locked_kas` 现读 10 个 oracle **全为 0**。
**但它不是喂给抽样器的那个字段** —— 在册 NWT 红队 `docs/2026-06-22-NWT-redteam-verdict-D2-C2-86523223.md:53-55` 已定：
`members[].stake_sompi` 来自 `ctx.loadPoolSnapshot`（DB），且**那份红队已要求 stakes 必须是本节点自观测的链上 oracle bond、不是 settler 中继的 DB 值**。
⇒ **我这条 relay_nodes 读数说的是另一个主语，不能拿来推"抽样器今天会怎样"。** 但它指出一件事：
🔴 **上面那条"兜住"依赖的抽样数学，本身依赖 stake 完整性，而 stake 完整性是另一条【已被红队标为前提】的链** ⇒ **依赖链又长了一节，`R1 → R2 → R3 → stake 锚定`，四节全是运维/机制承诺，无一节是密码学强制。**

### 形态 B · 保留硬化布局，登记根 = seed 指纹，放弃"派生证明"这条硬要求
- ✅ 同源可判（指纹相等）。
- ❌ **指纹与 relay pubkey 之间没有任何可验链接** ⇒ 正是裁定要防的"换皮的 ②"。
- ⇒ **除非**指纹是在 runbook §2 步骤 2 的**生成时刻、在宿主本地由既定程序吐出并见证**（下一节）。单独用 = 不推荐。

### 形态 C · 承认边界：把 A2 的保证从"登记表"移到"生成仪式"
🔴 **一条无论选 A 还是 B 都成立的硬事实，必须写进 spec，不能留给下一个人自己撞**：

> **任何纯软件的事后检查，都【无法】证明两把独立生成的密钥【不是】同一份 seed 派生的。**
> 同源的**正向**（"这两个确实同源"）可以被证明；**反向**（"这两个确实不同源"）证明不了——
> 因为造钥的人可以私下用一份 seed 造两个身份，然后只出示两个互不关联的登记根（硬化兄弟，实测 ① 跨不过去）。

⇒ **A2 的反向保证只能来自"钥是怎么生出来的"**：runbook §2 步骤 2 的"在目标宿主本地生成"如果是**既定程序 + 见证/留证**，那么"两个域的钥不同源"这句话的可信度 = 那场仪式的可信度，**不是密码学的**。
⇒ **spec 必须把这句原样写上**，否则 A1/A2 打勾之后，读的人会以为 R3 有密码学保证，而它实际靠的是运维程序。

---

## §6 我的推荐（一句，供 @Bettor 定向；@NWT 红队优先打 §5-A 那条 ⚠）

**A + C 合起来**：新钥按形态 A 的布局生成（让同源在**可验的那一侧**尽可能可验）+ 形态 C 的边界原样进 spec（把**不可验的那一侧**明写成运维前置，不伪装成密码学）。
**同时把 §2(b) 那个逃逸口堵上**：`KASPA_ACCOUNT_INDEX` 目前无人写、纯读 ⇒ 要么钉死、要么把实际使用的 account index 记进 `u1_domain_assignment` 并纳入 A2 检查——**不堵它，§2(a) 那道零成本检查在对抗场景下等于没有**。

> 🔴 **v0.2 补丁（本节推荐已于 18:49Z 被 @Bettor 采纳为 A+C，因此这条更要自己先打）**：
> 上面这句推荐**成立的前提**是 §5-A 的代价"被 R3 兜住"——**而 §5-A-bis 已把这个前提削掉一半**（窄通道单钥泄露不被 R1/R3 的整机模型覆盖）。
> ⇒ **A+C 的方向我维持**（形态 A 仍是唯一能同时满足硬要求与同源判定的布局，§3 的互斥是死的）；
> ⇒ **但 `identities-per-account` 这个旋钮必须先定，才谈落码**——它同时决定 A2 的判定力和泄露放大倍数（§5-A-bis）。**这个数字不是我能自拍的。**

---

## §7 验收判据（预登记·看码前写死）

| # | 判据 | 怎么测 | 为什么不能省 |
|---|---|---|---|
| **V1** | 同源正向可判：同一根下的两个身份，检查器必判 SAME-ORIGIN | 构造两个同根身份（随机 seed，非真钥），跑检查器 | 这是 A2 的主功能 |
| **V2** | 派生证明真在验：把登记根改成**另一个合法的根**（不是乱码）⇒ 必红 | 弱注入臂 | 乱码根会被格式校验拦下，**它红了不证明派生比对在跑**（在册：注入物必须取自断言实读的字段集） |
| **V3** | 反向不谎报：两个**确实不同源**的身份 ⇒ 判 DIFFERENT，且检查器**显式输出"本判定不具反向保证"** | 阳性对照 + 读输出串 | §5-C 的边界必须出现在**运行时输出**里，不能只活在文档里 |
| **V4** | account-index 逃逸口：同一 seed、不同 account index 的两个身份 ⇒ 检查器**必须报"无法判定"而不是"不同源"** | 构造 acct0 / acct1 两身份（§2 实验的同一构造） | 🔴 fail-closed：把"我看不出来"输出成"它们不同源"= 把无知报成安全 |
| **V5** | 存量 32 行：跑一次全量，输出与 §1 现读一致（0 重复 address） | 全量非抽样 | 判据 B 同纪律 |
| **V6**（v0.3 新增·@NWT `9d7fc088` 要求） | **残余风险必须进运行时输出，不能只活在设计稿里**：检查器输出须含等价表述——「形态 A 下单点私钥泄露（**尤其非整机级的窄通道泄露**）的爆炸半径 = 该 seed 下全部身份；该风险可控性依赖 **R1 合规**，而 R1 合规**正是 A2 本身在检验的对象**」 | 跑检查器，`grep` 输出里有没有这句 | 镜像 V3 的纪律：**边界不出现在输出里 = 读记分板的人得翻到设计稿第 128 行才知道**，而他不会翻 |

### 🔴 §7-bis · 缓解清单（@NWT 要求"改措辞之外至少补一项缓解"，现状记分）

| # | 缓解 | 状态 |
|---|---|---|
| **M1** | **登记根分发渠道收窄**：账户层 xpub 只落库 / 只给验证方，**绝不进 `dev-coord-testnet`**（频道 = 链上明文、不可撤回，在册 `reference-dev-channel-is-onchain-plaintext-not-internal`）——它是 §5-A 攻击链的必要输入之一，贴一次 = 永久公开 | ✅ **已入稿**（v0.2 §5-A 末，与 NWT 独立收敛到同一处） |
| **M2** | **单 seed 身份数上限**（放大倍数 ∝ 身份数） | ⏸ **等 @Bettor 定 `identities-per-account`**（§5-A-bis 那个旋钮，安全取舍非工作量） |
| **M3** | **运行时显式声明残余风险** | ✅ **已定为 V6**（上表） |

⚠ **V2 的注入物必须是"另一个合法根"**：只把根改成 `"deadbeef"` 之类，被格式校验挡下也会红 ⇒ **红了但没证明任何东西**。

---

## §8 残余敞口（明写，不留给下一个人撞）

1. **反向不可证**（§5-C）——A2 打勾 ≠ "不同域的钥确实不同源"。靠生成仪式。
2. **`KASPA_ACCOUNT_INDEX` 逃逸口**（§2-b）——不堵则对抗场景下 §2(a) 检查失效。
2-bis. 🔴 **形态 A 不是"配置项调整"，是一条【新的派生代码路径】**（@NWT `9d7fc088` §6 提，我认，落码第一步就撞）：
   现行 `wallet.mjs:39-50` 的叶子**固定 `0/0`**，形态 A 要把可变量从 `accountIndex'`（硬化）搬到叶子 `i`（非硬化）⇒ **要么改 `wallet.mjs`，要么给 runbook §2 步骤 2 单开一条新路径**。
   ⇒ **新路径必须自带一次 §2(b) 式的逃逸口审视**：它读不读某个可被进程环境覆盖的变量拿 `i`？——**否则我们只是把 `KASPA_ACCOUNT_INDEX` 那个洞换了个名字。**
3. **2 个 privkey-only relay**（`privkey_encrypted` 非空 2）——**根本没有 mnemonic** ⇒ R1 这条规则对它们**无定义**。spec 现在没说这类身份能不能当委员。**需要 @Bettor 一句**：委员是否必须是 mnemonic 型？（若允许 privkey 型，R1 的"分配单位=一份 mnemonic"就有一类身份落在规则之外。）
4. **本稿未实测**：§5-A 的 xpub+子私钥反推账户 xprv（标准 BIP32 性质，标 🟠）；threshold 今天是否仍 4-of-5（引在册，**落地前请现查，别引我这句**——上游 spec §6 也这么标过）。

---

## §9 证据层级

| 陈述 | 层级 |
|---|---|
| 32 relay / 10 oracle / `ecdsa_pubkey_xonly` 0 非空 / 0 重复 address / u1 表在 live 0 行 | ✅ `[CONFIRMED·现读 console.db]` |
| `KASPA_ACCOUNT_INDEX` 全仓一处读、零处写 | ✅ `[CONFIRMED·grep 全仓去 node_modules]` |
| 同 mnemonic 同 acct ⇒ 同地址；同 mnemonic 异 acct ⇒ 异地址且不可关联 | ✅ `[CONFIRMED·随机助记词实跑]` |
| master XPub 跨不过硬化一级（带非硬化对照臂） | ✅ `[CONFIRMED·本仓 kaspa-wasm 实跑]` |
| 账户层 XPub 推得到叶子 pubkey | ✅ `[CONFIRMED·本仓 kaspa-wasm 实跑]` |
| `ecdsa_pubkey_xonly` 可纯从 address 算出且 byte-exact 回推 | ✅ `[CONFIRMED·4/4 实跑]` |
| 「事后检查无法证明两把钥不同源」 | ✅ `[定义级·由硬化兄弟不可关联直接得出]` |
| xpub + 子私钥 ⇒ 反推账户 xprv ⇒ 全部兄弟沦陷 | ✅ `[CONFIRMED·本仓实跑, 带乱-chainCode 对照臂]`（v0.2 由 🟠 升级） |
| threshold = 4-of-5 · committee = 5 | ✅ `[CONFIRMED·现查代码 pool-p2sh-v06.mjs:96 / pool-close-builder.mjs:49 / bshard-auto-settler.mjs:33]`（v0.2 由 🟠 升级） |
| 「窄通道单钥泄露」下形态 A 使每次泄露产出翻倍 ⇒ 所需泄露次数减半 | ✅ `[CONFIRMED·由上一行实测直接得出]` |
| 减半是否直接等于"两次窄泄露即可签某市场" | 🔴 `[未验·取决于委员抽样, 明确留给 NWT, 禁引成"两次即可"]` |
