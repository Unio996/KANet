# §6-3 门现状表（Codex MSG-267 (a)–(f) + 追加 (g)(h)）· 2026-08-27

> **Status**: SNAPSHOT 2026-08-27 · J2 · Bettor 派工 (15) · **只读汇总，不裁门**。每门一行：DRI / 最近证据 / Codex 最新裁定原句 / 剩余项 / 需不需要节点·Owner·J1。用途：下一条桥 MSG 与报 Owner 用一张表说清 §6-3 还差什么。
> **顶层不变**（Codex MSG-267 Final status 原句）：*"The same-chain Shape-B design-spec remains **CONDITIONALLY CLOSED** at design layer. … No implementation, covenant BUILD, deployment, DB mutation, signing/broadcast, settlement/refund, key movement, or production money-path action is authorized by this review."* ⇒ 下表所有"剩余项"都在这条之下：**没有任何一门给了 build 授权**。
> 🔴 **今晚的硬约束**：本机节点仍 IBD（`virtualDaaScore=0`）；J1 不在线；Codex MSG-271/272 未回。

## 一张表

| 门 | Codex 分类（MSG-267 原句） | DRI | 最近证据（commit / 桥 MSG） | Codex 最新裁定原句 | 剩余项 | 节点 | Owner | J1 |
|---|---|---|---|---|---|---|---|---|
| **(a)** LOCKED_F→O_AUTHORIZED 续链 buildability/provenance | *"pre-code buildability gate; must PASS before money-path implementation is allowed to rely on Shape B."* 五条证据：deployed-Toccata 源/可执行证据 · 目标 runtime 上真实最小续链 tx（`OpInputCovenantId(LOCKED_F)==locked_f_cid` ∧ 后继 `OpOutputCovenantId(O_AUTHORIZED)==locked_f_cid`）· 脚本/状态可变身份稳 · 阴性对照 REJECT · 钉到精确 runtime 版本 | **J1**（SS/链上原语域） | **无满足 267 五条的证据**。最近相关：J1 8/12 `docs/2026-08-12-j1-continuation-roundtrip-blocked-and-statestart-asymmetry-v0.1.md`（round-trip MUST-PASS 阳性对照"跑不成，原因不是没时间"）；离线侧 B-2 `u1-continuation-statestart.test.mjs` 4 PASS（ledger 7355，参数敏感性，**非共识证据**）；v0.15 §4 的 buildability 论证依赖 relay builder 注释 | *"A relay builder comment such as `covenant_id(outpoint, ...)` is supporting evidence only; it is not the authority for deployed consensus semantics."* | 五条全部 | **要**（真实续链 tx 上链 + 阴性对照）| 不要（pre-code；但 build 真 covenant 若涉钱路则要）| **要**。🔴 **J1 不在线 ⇒ 今晚谁都动不了** |
| **(b)** A2-whole receipt → 结算腿 | *"pre-registered implementation gate, not something that can be closed purely pre-code."* PASS = 一个真实结算 covenant/artifact（pinned toolchain）+ 两族预注册套件（8 字段各自 REJECT / threshold·member / 合约变异 / 交易级拆 weld / 零 inconclusive）| **J2**（acceptance design）+ **NWT**（变异矩阵）；真 covenant = J1 SS + Owner | 设计：`docs/2026-08-20-j2-a2-whole-receipt-binding-acceptance-design.md`（86c04c55 族 B 原子性负测；d969890a 族 B cutoff 排序配置级 + 三层）；NWT (b) 建其上（ledger (620)/(621)）| (621) Codex：*建在 J2 A2-whole acceptance design + typed-receipt schema 上… 可用 pre-registered target；但真 A2-whole receipt-verifying covenant 未有 ⇒ (b) OPEN*；267 原句：*"The minimal `checkSigFromStack` probe remains primitive evidence only and gives no A2-whole closure credit."* | 真 covenant（Owner-gated build）+ 套件机械执行 + 逐格拒因落盘 | **要**（E2E）| **要**（build 授权）| **要**（SS 编译/构造）|
| **(c)** cov_id 派生 / 续链 durable proof | *"pre-code buildability/provenance gate… (c) is the general capability/lineage identity assumption used by C and O."* 六条：精确 deployed 派生规则 pinned · 两个不同 genesis outpoint → 不同非零 cov_id · 只收 baked cid · reveal 续链恰一续出 · terminal 支零续出 · 四类变异各按正确理由失败 | **J1**（SS 域） | **无满足六条的 durable 证据**。同 (a)：v0.15 §4 依赖 relay 侧 `covenant_id(outpoint,…)` 注释；(h) v1.1 已把 `==1→>=1` / terminal 续出 / 假 O / 错 cid 落成 mutation-id（判据有了，**执行没有**）| 同 (a) 那句（builder 注释非权威）；P2 原句：*"These determine whether Shape B is actually buildable on the deployed Toccata path. If either fails, stop"* | 六条全部（第 1 条可先做：pin rusty-kaspa 派生源码位置与版本）| **要**（第 2–6 条上链）| 不要 | **要**。🔴 同 (a) 今晚动不了 |
| **(d)** 具名保守 `min_O / N_claim / N_margin` + reactive-liveness | *"split gate. The parameter semantics are design-layer and already part of the conditional safety claim; the numerical values/evidence are pre-deployment operational engineering, not protocol-proof by prose."* | **J2** | `docs/2026-08-27-j2-s63-gate-d-conservative-bounds-v0.1.md`（**e9c30104**，NWT 审中）：定义/单位域/四份实测/PROVISIONAL 值（`min_O` 1e7 sompi，`N_claim` 3,600，`N_margin` 57,600，和 61,200 DAA）/CFG-UNIT-DOMAIN 量级带/五项重采/fail-closed；Bettor 已点 NWT 打 "N_claim 用 149 s 单点 ×2, n=1" | 267 (d) 六条 PASS requires（其中 *"adversarial threshold test demonstrates the entitled claim can LAND/CONFIRM before recovery opens… merely broadcast/mempool-seen is insufficient"*）；P4：*"Finalize against the actual transaction shape produced by P3; fee/input/output counts and landing behavior must not be guessed before that shape is stable."* | ① 深确认级阈值测试（现零证据）② 同步后重采 lag/停滞/reorg ③ P3 真形状 mass ④ NWT 审 e9c30104 ⑤ 桥报 Codex 请收 | **要**（重采 + 阈值测试）| 不要（运营常量；活性成本 `M_observe` 若要降需包络裁定，可由 Bettor）| 间接（③ 依赖 P3 = (b)(h) 真 covenant）|
| **(e)** quorum 独立性 | *"hard pre-real-funds deployment gate, not a same-chain Shape-B design-closure gate."* 须基于真实委员选择实现 + 当前合格 stake/authority 人口，**用全局密码学身份而非 node-local `relay_id`**；部署时新鲜、可机械复核 | **Bettor**（部署闸）；测量实现 = J2/NWT | 无新鲜测量。ledger 9702：*"硬闸仍在: §7 quorum 独立性(真金前)"*；身份前提 = §10 pubkey 身份 v1（J2 计划 v0.2 **70761d33**，NWT PASS-WITH-NOTES，待 Owner 批方向）| *"Owner risk acceptance cannot convert a centralized quorum into cryptographic independence; it can only explicitly accept the weaker trust model."*；*"stale historical percentages are not deployment evidence."* | ① §10 身份落地（(e) 的"global cryptographic identity"前提）② 从 raw roster + selector 输入可复现的集中度测量脚本 ③ 部署时现跑 | 不要（DB roster 测量）；部署时要 | **要**（§10 方向 + 若接受较弱信任模型须显式接受）| 不要 |
| **(f)** 跨链 | *"separate future design track, **not** a blocker for the explicitly same-chain-only artifact provided scope enforcement is fail-closed."* PASS(当前 same-chain) = 实现**不可能**误把 O-lineage 实例化到异构链/网络 | **J2**（scope 机械化） | 无 artifact。判据方向已在两处：(d) 稿 §4 单位/量级带 + `network` 本地权威（§10 S10 信封同法：*network 取本地权威不收调用方*）| 267 原句如左；*"Cross-chain itself remains OPEN until a separately reviewed positive finalized-reveal proof/light-client/R1 design exists."* | ctor/装载处一条 fail-closed 的 `network ∈ {testnet-12}` 硬断言 + 负测（落码时与 CFG-UNIT-DOMAIN 同批）| 不要 | 不要 | 不要 |
| **(g)** toolchain / artifact provenance（P1，(a)(b)(c)(h) 的上游）| *"pre-code toolchain provenance gate and upstream dependency."* PASS 五条：durable 编译树/commit · exe SHA-256+源树+runtime 版本 · ctor 与产物 hash 入仓 · 独立环境字节级重建（或非确定性精确规范化）· **无 local-only 未推承重修复** | **J2** | 向量脚本 `P1G_SILVERC` sha 白名单（c9ad4fb5+7b5f32d7）；离线腿 `docs/2026-08-27-j2-p1g-offline-leg.md`（573bd154）；durable 证据包 `docs/provenance/2026-08-27-p1g-durable-evidence/`（59a5f3a7+15450427，14 文件 MANIFEST，A≡C≡8/20 上链 script 40B `21fa272f`）→ **桥 MSG-271 待回**；上链跑手落提交体 fc925044（NWT 审中）| MSG-269/270：*"P1(g) … logically eligible for closure by reading 甲, but NOT CLOSED on the currently durable repository evidence"*；*"So current state: **P1(g) = ONE NARROW EVIDENCE-DURABILITY ITEM OPEN.**"*；并独立核了 sha 白名单：*"an unregistered compiler is rejected before compile. This is a real improvement over post-hoc logging."* | ① Codex 收 MSG-271（读法甲）② 诚实缺口：8/20 REJECT **提交体**不可恢复 ⇒ 乙腿（同步后用 fc925044 跑手现跑并逐笔落盘）③ 🔴 铁律 0.5 的 OP_PICK 修复仍只在本地分支 8065184 未推上游——267 第五条 *"no local-only unpushed fix remains load-bearing"* 直指此处 | 甲：不要；乙：**要** | 不要（乙腿几 KAS 测试币，Bettor 批）；OP_PICK 上游推送 = Owner/J2 决 | 不要 |
| **(h)** Shape-B 全拓扑一致性 / 对抗变异套件 | *"pre-registered implementation acceptance gate. Freeze criteria now; PASS only after the real covenant set exists."* 七条（真实编译支集 = 五对象十支 · 机械化 WELD/EXCL/COUPLED · 交易级省略负测 · 配置级 · 每条承重 require 变异 · 矩阵从真实现再生 · N-way 场景套件）| **NWT** | v1 f0e6d211（17 语句级/3 交易组/2 配置）→ Codex H1/H2 → **v1.1 7899a94e**（H1 逐臂 7 条 TX-* / H2 `CFG-UNIT-DOMAIN`）→ **桥 MSG-272 待回**；CF-4 把 `N_claim/N_margin` 归 (d)（已由 e9c30104 接）| MSG-269/270：*"Gate (h): MATERIAL PROGRESS, still OPEN at design layer."*；*"After H1 and H2 are frozen, I am comfortable marking **(h) CLOSED AT DESIGN LAYER**"*，带 carry-forward：*"mechanical execution remains required for implementation acceptance, with zero skipped/inconclusive load-bearing cases"* | ① Codex 收 MSG-272 = closed-at-design-layer ② 机械执行 = 真 covenant 后（锚从 v0.15 §行换成 `.sil` file:line，矩阵从真实支集再生）| 设计层：不要；执行：**要** | 执行层 **要**（build 授权）| 执行层 **要**（SS）|

## 依赖链（267 P0–P5 原序）与"今晚能动什么"

| 阶段 | 门 | 状态一句 | 今晚可动？ |
|---|---|---|---|
| P0 冻结证据契约 | 门清单 + (b)/(h) 矩阵预注册 | ✅ 267 ACCEPTED；(b) 设计 d969890a、(h) v1.1 7899a94e | — |
| P1 | (g) | 一窄项 OPEN（durability → MSG-271 待回）；乙腿排同步后 | 等 Codex；节点同步后乙腿 |
| P2 | (c) + (a) | **零 durable 证据**；267：*"If either fails, stop"* | 🔴 **不能**：J1 域 + J1 不在线 + 节点 IBD。可做的只有 (c)-1（pin rusty-kaspa 派生源码坐标，J2 可读 `/d/rusty-kaspa` 但**不能替 J1 裁 SS 语义**）|
| P3 | (b) + (h) | 判据全冻；PASS 等真 covenant（Owner build 授权 + J1 SS + pinned (g)）| 不能（无 build 授权）|
| P4 | (d) | e9c30104 PROVISIONAL，NWT 审中；定稿依赖 P3 形状 + 同步后重采 | NWT 审；同步后重采 |
| P5 | (e) | 硬部署闸；身份前提 = §10（Owner 待批）| 等 Owner §10 方向 |
| 旁路 | (f) | 非阻塞；scope fail-closed 落码时与 CFG-UNIT-DOMAIN 同批 | 设计已够，落码等 build |

**一句话给 Owner**：§6-3 设计层已 CONDITIONALLY CLOSED（Codex e6a9eb55），pre-code 八门里 **(g)(h) 各剩一步等 Codex 回执、(d) 已出 PROVISIONAL 值待审、(f) 非阻塞**；真正卡住的是 **(a)(c)（J1 SS 域，零链上证据，J1 不在线+节点 IBD）** 和 **(b)(h) 执行层 + (e)（都要 Owner：build 授权 / §10 身份方向）**。没有节点同步，(a)(b)(c)(d)(g-乙) 五门的链上部分全部停在原地。

**引用锚**（可 grep）：`RESPONSE-20260822-MSG267-S6-3-PRECODE-GATES-CODEX-REVIEW.md`（各门 Class 句、P0–P5、Final status）；`RESPONSE-20260826-MSG269-MSG270-CODEX-REVIEW.md`（"ONE NARROW EVIDENCE-DURABILITY ITEM OPEN" / "MATERIAL PROGRESS, still OPEN at design layer"）；ledger (620)(621)(632)(633)(635)(637)、9702 "硬闸仍在: §7 quorum 独立性"、7355 B-2。
