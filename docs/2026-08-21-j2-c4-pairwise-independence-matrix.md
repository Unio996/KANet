# C4-FINALITY 同链构造：**两两独立性矩阵**（报备层 · 零生产码）

> **Status**: CURRENT（§0-A v7 = 对 v0.10 Shape B 重建；§0-0 及其下各节为历史，标 STALE 保留以示演进）

**作者** J2 · **日期** 2026-08-21 · **派工** Bettor 18:20（采纳 J2 提议）+ 22:11（据 Shape B 重建）

---

## §0-A v7（对 J1 v0.10 · Shape B 重建）：**5 对象 10 支**，逐支从实际 spend 条件导出

### 支表（每支注明导出自哪一节 + 「无其它支」断言）

| 记号 | 对象 | 支（**允许什么**，非"被谁用"） | 出处 | 无其它支 |
|---|---|---|---|---|
| C1 / C2 | `C` | reveal-claim / terminal-refund | §4-a,b | §4-a「只有两支…无第三支」 |
| R1 / R2 | `LOCKED_R` | transfer（揭 s，同笔消费 C **且** exact `LOCKED_F`）/ terminal-refund `>= T_cutoff_LOCKED_R` | §4-d | §4-d「恰两条互斥」 |
| **Fa / Fb** | `LOCKED_F` | **transition**（reveal 强制转 `O_AUTHORIZED`）/ **giveup-refund** `>= T_giveup_LOCKED_F` | §4-d | §4-d「两条互斥」 |
| **A1 / A2** | **`O_AUTHORIZED`**（新对象） | reactive-claim（**无下界**，O 作 co-input）/ recovery `>= OpTxInputDaaScore(O_AUTHORIZED)+N` | §4-c | §4-c「恰两支」 |
| O1 / O2 | `O` | pre-timeout（反向焊→`oauth_cid`）/ `T_O` 回收 | §4-e | §4-e「恰两支，均从实际 spend 条件写死」 |

⇒ **10 支 ⇒ C(10,2) = 45 对**（v6 时是 8 支 28 对；**Shape B 多了一个对象、两条支**）。

### 🔴🔴 §0-A-Fb 更正：**我提的 giveup 修法【无效】—— 它塌在我自己四小时前确立的限制上**

我建议 `T_giveup_LOCKED_F >= T_cutoff_LOCKED_R`，论证是
「giveup 只在 **reveal 窗关闭后**行使 ⇒ 退化成真 recovery，无早中止期权」。

🔴 **这句话默认了「`T_cutoff_LOCKED_R` = reveal 窗关闭」——而这个前提，
在同一份构造文档的 §4-f 里已经被写死推翻过**：
**`R1`（reveal）从来没有上界**；`T_cutoff_LOCKED_R` 只是 **`LOCKED_R` refund 的下界**，
**它不 disable reveal 支**。过了 cutoff，只要 `LOCKED_R` 还没被 refund 掉，**reveal 仍然 valid**。

⇒ **我的修法没有关掉 free-option，只是把它的起点从「随时」挪到了「cutoff 起」。**

### 🔨 而这是同一条限制的**第二次发作**，且第二次更隐蔽

| | 形态 | 为什么没被认出来 |
|---|---|---|
| 第一次 | 直接写 `current_daa < X` | 字面就是上界，被源码实读逮到 |
| **第二次（我这条）** | **假设某个下界 == 某个窗的关闭点** | **字面上没有任何 `<`** |

> 🔨 **判据要再收窄一次**：不只是「不要写上界」，而是
> **任何「某时刻之后就不能再 X」的推理，都要先问 `X` 有没有上界。**
> **没有上界的支，任何 cutoff 都关不掉它** —— 无论那个 cutoff 是谁的、写得多严。

🔴 **而我用来论证的那个前提，就写在我引用的同一份文件里、并且已被推翻。**
⇒ **引用一份文档做论证时，要检查这份文档【自己】有没有在别处推翻我用的那个前提。**

### ⇒ `Fb` 格改标（Bettor 23:38 派）

`Fb`（giveup）× 其余各支：**free-option 降低、非消除 · liveness-bound**

- **降低**：起点从「随时」推迟到 `T_cutoff_LOCKED_R`；
- 🔴 **未消除**：cutoff 之后首动方**仍可**在「reveal」与「giveup」之间自由选，
  只要对手方还没 refund 掉 `LOCKED_R`；
- ⇒ 剩余部分**落在 liveness 上**（对手方及时 refund 即关闭该窗），**不是结构性关闭**。

---

### 🔴 我漏的一格（NWT 独立核出）：**新支必须过【全部】已确立不变量，不是只过我当时想到的那条**

NWT 核 v0.10 §4-d：**`Fb`（giveup-refund）没有 `OpCovOutputCount == 0`** ——
而**同一文档里其它每一条 terminal 支都写了**（C-terminal / R2 / O2 / A2）。

🔴 **它违反的是【闸③】** ——「**每条非-reveal 支必须显式禁止续 lineage**」，
而闸③ **正是我参与确立、并写进我自己验收设计族 B 的必测格**。

🔴 **我错在哪**：我拿新支 `Fb` 只对了**"排序"**这一条（因为我当时正在想时序），
**没有拿【已确立的全部不变量】过一遍**。

> 🔨 **新增一支时，要拿【全部已确立的不变量】逐条过，而不是只想到哪条查哪条。**
> **我当时脑子里是什么问题，就只会查到什么问题。**

🔵 NWT 也算过：这条**目前推不出可兑现的坏处**（R1 要求 `O_AUTHORIZED` 输出**足额**承接 `LOCKED_F_value`，
而钱已被 giveup 拿走，dust 续链 output 里没有真钱）。
🔴 **但"目前推不出坏处"正是闸③存在要拒绝的那个理由** —— 闸③的立意就是**漏写是静默的**。
⇒ **该显式补 `OpCovOutputCount == 0`，不因"这次算出来没事"破例。**

### ⇒ 矩阵新增第二根轴：**支 × 不变量**（本表原来只有"支 × 支"）

两两矩阵只照**支与支之间**的关系；**单支是否满足既有不变量，它照不到**。
⇒ 每次拓扑变更，除重建 pair 矩阵外，**另出一张 branch × invariant 的合规表**：

| 不变量（已确立） | 适用范围 | 逐支核 |
|---|---|---|
| 闸③ `OpCovOutputCount == 0` | **每条 terminal / 非-reveal 支** | 🔴 `Fb` 缺（本次逮到） |
| 单位同域（DAA-score，`< 5e11`） | 每个时间常量 | 待逐支核 |
| 无 `TxTime <` / `current_daa <` 上界 | 全构造 | v0.8 已加静态 grep 检查 |
| 下界必有排序出处 | 每个 `>= X` 的 X | 🔴 `T_giveup` 缺（本次逮到） |

🔵 **本次两个洞（缺排序、缺终止条款）都在同一条新支 `Fb` 上** ——
**一条新支同时违反两条既有不变量，而两条是被两个人分别发现的。**

### 🔴🔴 新支带出的新对里，有一条**没有排序约束**：`Fb`（giveup）

**实读**：`T_giveup_LOCKED_F` 在 v0.10 **全文只出现一次** —— 它自己那行定义。
**没有任何一句把它与 `T_cutoff_LOCKED_R`（reveal 窗）排序。**

⇒ 若 **`T_giveup_LOCKED_F` < `T_cutoff_LOCKED_R`**：
首动方可以在**reveal 窗还开着的时候**先把自己本金 `LOCKED_F` 取回（Fb）。
Fb 一执行，`LOCKED_F` 没了 ⇒ **R1 要求同笔消费 exact `LOCKED_F`** ⇒ **reveal 被结构性封死**；
反应方的 `LOCKED_R` 仍锁到 `T_cutoff_LOCKED_R` 才退。

🔵 **这【不是】盗窃**（我核过：`LOCKED_F` 被 Fb 花掉后 R1 不可构造 ⇒ 首动方无法既 giveup 又 reveal）。
🔴 **但它是【单方免费期权】**：首动方可以观望到 `T_giveup`，再零成本中止，
**而对手方的资金在此期间一直被锁住** —— 原子交换里的经典 free-option 问题。

### ⇒ 建议（归 J1 裁，我不越界定构造）

加一条 baked 排序：**`T_giveup_LOCKED_F >= T_cutoff_LOCKED_R`**
⇒ giveup 只能在 **reveal 窗关闭之后**行使 ⇒ 它变回真正的「reveal 没发生 ⇒ 拿回本金」recovery，
**而不是一个可以随时行使的中止权**。
🔵 用的是已有的下界机制，无新原语。

🔨 **本条的方法论意义**：`Fb` 是 **Shape B 新增的支**，
**它一出现就带来了 9 个新对**（与其余 9 支各一），而**排序约束是在这些新对里才显形的**。
⇒ 这就是「**新增任一支，矩阵即失效、须同步重建**」那条义务的**第一次真实兑现**。

---

## §0-0 🔴 读之前必看：**本表整体作废，等 v1.0**

**Codex v0.9 verdict**：构造已迁 **Shape B** —— reveal 那笔**四路原子**把 `LOCKED_F`
**转成后继 `O_AUTHORIZED`**；反应方领的是 **`O_AUTHORIZED`**（不是 `LOCKED_F`），
且 recovery 下界锚在 **`O_AUTHORIZED` 自己的创建 DAA**（= 实际 reveal DAA）。

⇒ 🔴 **本表全文写的是 Shape A 拓扑**，其中至少这些说法**已经不成立**：

| 位置 | stale 说法 | 为什么错 |
|---|---|---|
| §0-quinquies、§0-ter(2) | 「`F1×O1` 双向 WELDED：花 O ⟺ 领 `LOCKED_F`」 | Shape B 里被领的是 `O_AUTHORIZED`，不是 `LOCKED_F` |
| §1 支表 `F1` 行 | 「`LOCKED_F` 的 reactive-claim 支」 | 该支已迁到 `O_AUTHORIZED` 上 |
| §2 矩阵 `F1×O1` 行 | 同上 | 同上 |
| 全表对象集 | 只有 C / `LOCKED_R` / `LOCKED_F` / O | **少了 `O_AUTHORIZED` 这个对象** |

### 🔴 为什么我现在**不**重建

Codex 已判 v0.9 是**半成品迁移**（两条 MUST-FIX：reveal 可完全不带 `LOCKED_F` / 拓扑未扫全）。
**此刻据它重建支集 = 把一个已知破的状态编进矩阵** —— 而这正是我先前栽过的形状
（按当时手上的版本画格，格画错了，穷举再完整也照不到）。

⇒ **等 J1 v1.0 把 Shape B 的支集定死后重建**，重建时仍照 §0-ter 那条义务：
**逐条对着实际 spend 条件导出，注明出处，记"允许什么"不记"被谁用"。**

🔨 **判据（本次学到的）**：**验拓扑迁移，要查【旧引用是不是都更新了】，不是只查【新代码加了没有】。**
本表就是那个"旧引用"——**新构造写对了，而指向它的表还停在上一个拓扑上。**

---

## §0-quinquies v6（对 J1 v0.8 重建）：**无上界世界 —— 每对标出【是谁的下界支】造出独占**

### 新原理（v0.8 §4-f）

covenant **只能 enforce 下界**。「你须 X 前行动」**不靠给你设上界**（不可 enforce），
靠给**对方**的 recovery 支设 `>= X` ⇒ **X 前对方那支被链上直接拒** ⇒ 你获得**可 enforce 的独占窗**。

⇒ 矩阵新增一列：**no-theft 来源 = 哪条【对方的下界支】造出这个独占窗**。

| 对 | 须先动方 | 独占窗 | **造独占的对方下界支** |
|---|---|---|---|
| R1 / R2 | 首动方 reveal | `[0, T_cutoff_LOCKED_R)` | **R2** `>= T_cutoff_LOCKED_R`（退反应方） |
| F1 / F2 | 反应方 claim | `[O_creation, T_refund_LOCKED_F)` | **F2** `>= T_refund_LOCKED_F`（退首动方） |
| O1 / O2 | 反应方 claim（同 F1 一笔） | 同上 | **O2** `>= OpTxInputDaaScore(O)+N` |

🔵 **F1×O1 仍 WELDED（双向）**：v0.6 反向焊未受本次重构影响（它焊的是"同笔"，不是"时刻"）。

### 🔵 而逐对推的时候掉出一条：**第 5 条硬假设比现在写的【弱】**

`reactive-liveness` 现在的表述是「**反应方**须在窗内广播 claim」。

🔵 但看 F1 的实际 require：`checkSigFromStack(A)` ∧ `blake2b(s)==h` ∧ O co-input ∧ **payout 焊死到反应方 baked 地址**。
**reveal 之后 `s` 与 `A` 都是公开的** ⇒ **任何第三方都能构造并广播 F1**，
而**钱只会进反应方那个烤死的地址** —— 第三方顶多替他付了手续费。

⇒ 🔨 **假设应弱化为**：**「窗内需要【有人】广播这笔 claim，而【任何人】都可以」**
**——不是「反应方本人必须在线」。**

🔵 这有实际后果：**watchtower / 任意第三方可代履行** ⇒ 这条假设的现实强度远低于字面。
🔴 **但我不声称它因此可以不列** —— 仍须有人动手，仍是活性假设，只是**门槛低得多**。
🟡 **未核**：第三方替广播时**手续费从哪出**（O 的 `min_O` 是否覆盖非反应方广播者的成本）——归 J1。

---

## §0-quater v5 🔴🔴 **撤回 v4 §(3) 那条"对齐不变量"—— 它机制上不可 enforce**

**Codex v0.6 verdict**：`T_refund_LOCKED_F` 在**锁定时 baked**，那一刻 **`O` 还不存在**
⇒ 它**无法**用 `OpTxInputDaaScore(O)` 定义。
⇒ 「`T_O` 锚点 == `T_refund_LOCKED_F` 锚点」这条等式 **建不出来**。

### 🔴 我错在哪 —— 比"判断错"更糟：**我给它配了测试，让它看起来更真**

v4 里我把它**升格**成"配置级不变量"，还写了两格负测（对齐⇒BUST / 错开⇒必须 LAND）。

🔴 **而那两格里"正确对齐"那一臂【根本不可构造】** —— 你没法把两个锚点设成相等。
⇒ **我不是漏检了一个约束，我是给一个【不存在的约束】发了合格证。**

> 🔨 **我检查了"这条约束有没有被满足"，没检查"这条约束【能不能存在】"。**
> **给约束配上负测，会让它看起来【更】被验证过 —— 而测试本身不会告诉你被测对象是虚构的。**

🔴 **而这条判据【本来就在我的记忆里】**：
`reference-a-verifiability-requirement-can-presuppose-an-object-that-does-not-exist`
（"可验要求预设不存在对象"）—— **我没有调用它。**

### 🔴 四人同错，形状值得记

J1 提需求 / NWT 确认 / **我升进矩阵并配负测** / Bettor 背书"三角 well-covered"
—— **四个人、三种角色，没有一个去问"`T_refund_LOCKED_F` baked 的那一刻，`O` 存在吗"**。

> **多人同意 ≠ 可 enforce。** 同意越多，越像已验证 —— 而我们共享的是**同一个盲点**。
⇒ 外部审查者（不在这个共识里的那个）逮出。

### ⇒ 处置

- v4 §(3) 那格及其两格负测 **作废**；
- 待 J1 v0.7（Shape A1：删假 equality、F1 加上界 guard、O 边界纯相对、equality 换 **ordering**）落定后**再重建该格**；
- 🔴 **重建时必须先问的第一个问题**：**这条约束在【它被 bake 的那一刻】，它引用的东西存在吗？**

---

## §0-ter v4（对 J1 v0.6 重建）：**支集这次从【实际 spend 条件】导出，逐支带出处**

### (1) 支表（v0.6 §4 显式定义；每支注明导出自哪一节）

| 记号 | 对象 | 支 | 导出自 | 「无其它支」断言 |
|---|---|---|---|---|
| C1 / C2 | `C` | reveal-claim / terminal-refund | §4-a、§4-b | §4-a：「只有两支…无第三支」 |
| R1 / R2 | `LOCKED_R` | transfer / terminal-refund | §4-d | §4-d：「恰两条互斥后继支」 |
| F1 / F2 | `LOCKED_F` | reactive-claim / terminal-refund | §4-c、§4-d | §4-d：「恰两条互斥后继支」 |
| **O1 / O2** | `O` | **pre-timeout（反向焊）/ `T_O` 回收** | **§4-e** | §4-e：「**恰两支，均从实际 spend 条件写死**」 |

🔵 **与 v3 的关键差别**：`O1` 不再记作「被 F1 消费」（那是用法），
而是记作 **`O` 自己 covenant 的 pre-timeout 支**（那是权限）。**记的是允许什么，不是被谁用。**

### (2) `F1×O1` 升回 **WELDED（双向）**

- 正向（§4-c）：领 `LOCKED_F` ⟹ 必带 O co-input；
- **反向（§4-e 支1，v0.6 新增）**：花 O ⟹ 必同笔领 `LOCKED_F` 且付反应方 baked payout。

⇒ **窗内两者只能一起花**，`s`/`A` 公开也无法独立毁掉 O。**单向蕴含已补成互焊。**

### (3) 🔴 我在 v0.6 里逮到的**新约束**：`O1` 窗与 `F2` 窗**必须对齐**

v0.6 §4-e 末尾写：`T_O` 锚点必须 **== `T_refund_LOCKED_F`** 锚点。
**这是一条【配置级不变量】，不是代码里的 require** —— 两个常量各自都合法，**错的只是它们的相对值**。

⇒ 若 `T_O` **晚于** `T_refund_LOCKED_F`：存在一段窗口 **O 仍可花、而 `LOCKED_F` 已 refund-eligible**
⇒ 首动方与反应方**抢跑**；反之则反应方窗口被提前掐断。

⇒ **必测（配置级，接族 B 第三层）**：
1. 两锚点相等 ⇒ 攻击 case **BUST**；
2. 🔴 **故意错开** ⇒ **同一攻击 case 必须 LAND** —— 否则这条对齐是**装饰**，将来会被顺手简化掉。

## §0-bis v3 🔴 **当前状态：已知不完整 —— v0.6 前不建议当覆盖证据用**

### (1) `F1×O1` 误标，已降级

v2 标 **WELDED**，依据是 v0.4 的 co-input 焊接。
🔴 **而那条焊接只证一个方向**：`claim LOCKED_F ⟹ 含真 O co-input`。
**逆命题未证**：`花真 O ⟹ 必同笔 claim LOCKED_F 且带精确 baked payout`。

⇒ 若 `O` 的 covenant 有自己的 pre-timeout spend 支且只验本地 witness，
**任何拿到公开 `s`/`A` 的人都能在独立 tx 里把真 O 花掉/烧掉** ⇒ 反应方永远领不到 `LOCKED_F`。

⇒ **本格改标 `IMPLICATION-ONLY（单向蕴含，非双向焊）`**，待 v0.6 的互反不变量补上再升回 WELDED。
（Codex 用本表 §4 caveat(a)「每格机制判断仍须对抗验证」逐格核出——**这正是 caveat 存在的用途**。）

### (2) 🔴🔴 更深的根因：**支路清单本身是从【意图】导出的，不是从【脚本】导出的**

我把 `O` 的支列成两条：**`O1` = "被 F1 作 co-input 消费"** · `O2` = `T_O` 回收。

🔴 **`O1` 这个写法【预设了答案】** —— 我列的不是"O **能**被怎么花"，是"O **应该**被怎么花"。
若 `O` 的 covenant 还允许别的 pre-timeout spend，**那是我从没列进表的第 9 支**。

> 🔨 **矩阵保证"没有格被跳过"，但【格是我画的】。**
> **画错格，穷举再完整也照不到那一块。**

### (3) ⇒ 新增产出义务（补给未来任何人重做本表）

1. **支路清单必须逐条对着 `.sil` 的 entrypoint / spend 分支核出来**，
   并在表头注明**导出自哪份源码的哪一段**；**不许从设计文档的叙述里抄**。
2. 每条支的记法必须是**"这条支允许什么"**，
   **不得写成"这条支被谁消费"** —— 后者把用法当成了约束。

🔴 §4 那条「新增一支即失效」要读作更狠的一句：**清单本身可能从一开始就不全。**

---

## §0 这张表要回答什么

四次红队逮到的洞**是同一个形状**：**两件各自合法的动作可以【各自独立发生】**。

① reveal-claim 与 LOCKED_R-transfer 未焊（只领钱不造 O）
② C 的其它支未禁（不走 reveal 也产合法 lineage）
③ 「同笔消费 C」未限定走哪条支（走 C 的 refund 支绕过焊接）
④ O 的生命期与被保护本金的退款窗未耦合（造真 O 却抢先 refund 自己本金）

⇒ **每焊住一对，下一对没焊的就成新洞。** 本表把「等下一次红队逮」换成「矩阵里有没有空格」。

🔴 **它不保证没有第五条**；它保证**第五条若存在，它是表里的某一格**，而不是"谁都没想到的地方"。

---

## §1 支路全集（8 支，取自 v0.4 §4）

| 记号 | 对象 | 支 |
|---|---|---|
| **C1** | capability `C` | reveal-claim（消费 C、造 O） |
| **C2** | `C` | terminal-refund |
| **R1** | `LOCKED_R`（反应方本金） | transfer 支（cutoff 前，首动方揭 s 领；焊接要求同笔消费 C） |
| **R2** | `LOCKED_R` | terminal-refund（cutoff 后退**反应方**） |
| **F1** | `LOCKED_F`（首动方本金） | reactive-claim 支（O 作 co-input，付反应方） |
| **F2** | `LOCKED_F` | terminal-refund（`T_refund_LOCKED_F` 后退**首动方**） |
| **O1** | `O` | 被 F1 作 co-input 消费 |
| **O2** | `O` | `T_O` 回收（退首动方） |

🔴 **本表只覆盖这 8 支。新增任何一支，本表即不完整** ⇒ **改构造时同步更新是义务，不是一次性附录。**

---

## §2 矩阵（每格：WELDED / INDEP-SAFE / 🔴 INDEP-SEAM）

**同对象两支**（UTXO once-spend 天然互斥）：

| 对 | 判 | 机制 |
|---|---|---|
| C1×C2 · R1×R2 · F1×F2 · O1×O2 | **WELDED** | 同一 UTXO 只能被花一次；且各配 cutoff 分窗 |

**跨对象**（承重的都在这里）：

| 对 | 判 | 机制 / 正证 |
|---|---|---|
| **R1×C1** | **WELDED** | v0.3 焊接：R1 require 同笔消费 C ⇒ C 被消费 ⇒ 其窗内唯一支 C1 ⇒ 强制造 O |
| **R1×C2** | **WELDED** | cutoff 排序不变量 `T_cutoff_LOCKED_R <= C_terminal_refund_cutoff` ⇒ R1 活窗内 C2 尚未开 |
| **F1×O1** | **WELDED** | v0.4：F1 require O 作 co-input ⇒ 花 O ⟺ 领 `LOCKED_F` |
| **F1×O2** | **WELDED** | `T_O >= OpTxInputDaaScore(O) + N_claim + N_margin` ⇒ 回收窗晚于反应方 claim 窗 |
| **R2×F1** | **WELDED（真空）** | 无 reveal ⇒ 无 C 消费 ⇒ 无 O ⇒ F1 不可构造 |
| **R2×O1/O2** | **WELDED（真空）** | 同上：无 O 可花/可回收 |
| **R2×F2** | **INDEP-SAFE** | 双方各自取回本金 = "什么都没发生"的正确结局，无人吃亏 |
| **C1×F2** | 见 R1×F2 | 同一时序问题 |
| 🟡 **R1×F2** | **INDEP-SAFE（有条件）** | 见 §3 —— **本表逼出的唯一一条需要显式记的假设** |

---

## §2-bis 🔴 v2 修正：加第四类 **COUPLED**，并补全 28 格（NWT 18:24 逮的标注精度问题）

**问题**：我 v1 只列了"值得说的对"，未列的默认落进"其余均 INDEP-安全"这类理解。
🔴 **而有些对【根本不是独立的】** —— 它们的可用时刻**由对方派生**：

| 对 | 为什么不是 INDEP | 安全性实际由谁扛 |
|---|---|---|
| **R1×F2** | `T_refund_LOCKED_F` **由** `T_cutoff_LOCKED_R`（R1 的截止点）算出 | F1×F2 的 once-spend + §3 的活性假设 |
| **C1×O2** | `T_O` 锚在 `OpTxInputDaaScore(O)`，即 **C1 造 O 那一刻** | F1×O2 的相对锚不等式 |

⇒ 新增第四类标注：

> **COUPLED**：**两支不独立**（一方的时刻参数由另一方派生），
> **但其安全性质由【另一格】的机制覆盖** —— 必须写出**是哪一格**。

🔴 **为什么这个区分不是文字游戏**：
标成 `INDEP-SAFE` 等于说"这一格自己站得住"。
**若它其实靠 X 格扛着，那么将来有人改了 X，这一格会【静默失去保护】而标注不变** ——
**矩阵会继续显示绿色。**
⇒ COUPLED 格必须**写明依赖哪一格**，这样改 X 的人能反查到谁依赖它。

### 补全：28 格的归类规则（保证无格被跳过）

8 支 ⇒ **C(8,2)=28 对**。逐类归：

1. **同对象 4 对**（C1C2 / R1R2 / F1F2 / O1O2）⇒ **EXCL**：同一 UTXO once-spend + cutoff 分窗。
2. **含 C2 或 R2 且需要 O 存在的对**（C2×F1、C2×O1、C2×O2、R2×F1、R2×O1、R2×O2）⇒ **EXCL（真空）**：
   无 reveal ⇒ C 未经 C1 消费 ⇒ **O 不存在** ⇒ 对面那支不可构造。
3. **R1×C1 / R1×C2** ⇒ **WELDED**：v0.3 同笔焊接 + cutoff 排序不变量（见 §2）。
4. **F1×O1** ⇒ **WELDED**：v0.4 co-input 焊接。
5. **F1×O2 / F2×O1** ⇒ **WELDED（时序）**：`T_O` 相对锚晚于反应方 claim 窗。
6. **R1×F2 / C1×O2** ⇒ 🔴 **COUPLED**（本节上表）。
7. **其余**（C1×R2、C1×F1、C1×F2、C2×R1 已含、C2×R2、C2×F2、R2×F2、F2×O2 …）⇒ **EXCL 或 INDEP-SAFE**，
   逐格理由：要么落进 1/2 的真空与互斥，要么是"双方各自取回本金"的正确终局（无人吃亏）。

🔴 **第 7 类我给的是【归类规则】而不是逐格文字** —— 这是本表现阶段的**已知弱处**：
**规则覆盖不等于逐格核过**。若要它达到与 1-6 同等强度，需要有人把这一类**展开逐格写**。
**我不声称第 7 类已被逐格验证。**

---

## §3 🔴 R1×F2：本表逼出的一条【尚未被列进硬假设】的依赖

**R1** = 首动方领走反应方本金（并被焊接强制造出 O）。
**F2** = 首动方在 `T_refund_LOCKED_F` 后取回自己本金。

**两者能否都发生？** 能 —— 只要**反应方在窗口内没有用 O 去领 `LOCKED_F`**。
此时首动方**两笔本金都拿到**。

v0.4 的不等式 `T_refund_LOCKED_F >= T_cutoff_LOCKED_R + N_claim + N_margin`
保证的是**反应方【有足够窗口】**，**不保证反应方【用了】那个窗口**。

⇒ 🔨 **判据**：
> **`T_refund_LOCKED_F` 那条不等式给的是【机会】，不是【结果】。**
> **no-theft 在这一格上条件于「反应方在窗口内主动 claim」——这是一条【活性假设】，不是结构保证。**

🔵 **我不认为这是洞**：任何 UTXO 系统里"自己不去领钱"都无法被合约救。
🔴 **但它必须被【显式列进硬假设清单】**，与 C4-ENTROPY / s-secrecy / finality-bound 并列 ——
否则「同链拿到**无条件**结构 Tier-2」这句话会被读大。
**目前的假设清单里没有它**（我核过 §17 的四条：C4-ENTROPY / s-secrecy / finality-bound / honest-reveal-timing）。

⇒ 建议记作第五条：**`reactive-liveness`（反应方须在其 claim 窗内行动，否则自负）**。

---

## §4 明列边界

- 🔴 本表**只保证没有格被跳过**，**不保证每格的机制判断是对的** —— 逐格机制仍须 J1（构造域）/ NWT（对抗）各自核。
- 🔴 只覆盖 §1 那 8 支；**新增支即失效**。
- 🟡 未覆盖**三方以上的联合时序**（本表是两两）。若存在需要三支同时发生才触发的缝，本表看不见。
  **我不声称两两穷举等于全穷举。**
