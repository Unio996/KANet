# U1 密钥隔离正向施工 Runbook v0.1

> **Status**: CURRENT (rev-2 · 2026-08-11 补 §12 D2 补件，文件名沿用历史锚点不改，见 CLAUDE.md rev-N 纪律)

**Author**: KANet-UI · **日期**: 2026-08-10
**授权范围/定位**: Bettor 13:35Z 派工，紧接回退 runbook（`docs/2026-08-10-u1-key-isolation-rollback-runbook-v0.1.md`）之后。**本稿按推荐基线写到能写的每一步；写不了的项集中列成 §3「待 Owner 定项清单」，交 Bettor 一次性带给 Owner，不挤牙膏。** 本稿仍是 scoping 之后、真正开工之前的最后一份准备文档——§3 未决项答完之前，§2 的施工步骤不得开始（除标注为"可现在做"的例外）。
**基于**: `docs/2026-08-07-u1-path-isolation-scoping-v0.1.md`（§1 推荐基线/§2 映射/§3 拓扑/§5 生命周期骨架/§10 留白），不重复设计，只把能落地的部分改写成步骤。

---

## §0 推荐基线（原样确认，不改）
- 拆成 **4 个隔离签名域**（域 1=现状机器留一部分，域 2/3/4=新增独立宿主）。
- 4 是推荐值非唯一可行值；容错声称需要 hedge（鸽笼原理：5 把钥匙分 4 域，至少 1 域拿 ⌈5/4⌉=2 把，该域掉线时剩 3<threshold=4）——数学保证需要 5 域，多 1 台机器成本换取不依赖密钥分配算法的保证。**这个取舍是 §3 D1。**

## §1 现在就能做的（不等 Owner 答复，零风险、不可逆动作为零）

### 1a. 域→relay-ID 映射记录格式（回退 runbook §0.1 的依赖，本稿直接把它定下来）
建施工时用的记录表，不需要等硬件到位就能建表：
```sql
CREATE TABLE IF NOT EXISTS u1_domain_assignment (
  domain        INTEGER NOT NULL,      -- 1/2/3/4(/5，若 D1 选 5 域)
  relay_id      TEXT NOT NULL,
  host_label    TEXT NOT NULL,         -- 人类可读的宿主标识，不是 IP(IP 会变)
  assigned_at   TEXT NOT NULL DEFAULT (datetime('now')),
  PRIMARY KEY (domain, relay_id)
);
```
建这张表本身零风险（新表，不改任何既有表/既有行为）。**建议施工正式开始前先建好**，第一台新宿主生成第一把密钥的那一刻起就有地方记，不要等到"回头需要回退时才发现从来没人记过这份映射"。

### 1b. J1 现有节点机的安全基线核查（域 4 候选，与"谁来运营"无关）
scoping §2 备注写"J1 节点今天定位是健康、不载生产、isSynced 的读研究节点——若要兼职当签名宿主，需先过一轮独立的安全基线核查，本稿不预设它合格"。**这项核查本身不需要等 Owner 拍"要不要用它当域 4"**——核查的是这台机器现在的安全状态（谁有访问权限/密钥托管方式/网络暴露面），是一个事实问题，不是决策问题。**建议 @J1 或 @NWT 现在就能开始**（不在本次派工范围内，本稿只标出这项不必等待）。

## §2 施工步骤（每一步排在 §3 对应决定项答完之后，标注依赖）

1. **采购/配置新宿主**（依赖 D3：物理自购 or 云托管豁免；依赖 D2：运营方人选，因为谁来经手采购是运营方决定的一部分）。
2. **在目标宿主本地生成委员密钥**（scoping §5 已定：不在本机生成后传输，传输本身是暴露窗口）——依赖 §1a 那张记录表已存在、依赖具体密钥-委员分配算法（scoping §10 项 1，落码 spec 阶段工作，不是 Owner 决策项，但需要 §4.1 持钥侧类型闸的进度配合，见 D-012 主稿 §6）。
3. **划出域 1 现有 relay 的一部分**（本机 32 个 relay 中哪些留、哪些逻辑上归入新域——这是密钥分配算法要回答的，同上依赖同一项）。
4. **验收**：施工完成后，对全部 255 个市场重跑判据 B 脚本（scoping §7 已定方法论：同一套判据脚本，全量非抽样），确认 63→0。这是施工的唯一验收标准，此前的所有步骤都是准备。

**本稿不代写步骤 1-3 的具体命令**——它们依赖 §3 的答案，答案落地前写出来的命令是编的，不是可执行的。

---

## §3 待 Owner 定项清单（Bettor 一次带走，不挤牙膏）

| 编号 | 问题 | 现状/取舍 | 谁受影响 |
|---|---|---|---|
| **D1** | 4 个隔离签名域，还是多花 1 台机器的成本换 5 域的数学保证（不依赖密钥分配算法）？ | scoping §1 已给两个选项+代价；4 域的容错声称目前只是设计目标不是数学保证 | 成本 + §2 密钥分配算法的复杂度 |
| **D2** | 域 2/3/4 的运营方人选——新增运营方，还是现有团队成员兼任副手？ | scoping §2 表格标"待定"，本稿不代选 | 谁经手采购/持有访问权限 |
| **D3** | 新增宿主的采购路径——物理自购（Track A"0 云端托管"铁律的默认假设）还是显式豁免走云托管？ | scoping §0.5：**本稿默认按物理自购写，未获豁免前不得改假设**；若后续因交付周期/成本不可接受需要豁免，需 Owner 就本次施工单独裁一次，不是默认可绕 | §8 成本骨架的形态（一次性 vs 持续，交付周期） |
| **D4** | J1 现有节点机通过安全基线核查后，是否正式用作域 4 宿主？ | 核查本身不等 Owner（见 §1b），但"要不要用它"是决定 | 少采购 1 台机器 vs 引入既有机器的历史负担 |
| **D5** | §11.3 两难：若未来决定回退，走"弃旧钥匙重铸"（本稿默认路径，回退 runbook 已按此写）还是要更快让存量市场也回单机（需要一次密钥搬运的暴露窗口）？ | 回退 runbook 默认前者；后者需要 Owner 到时候单独授权，不是现在就要答，但**若 Owner 现在就有倾向，写下来能省将来一次决策延迟** | 回退场景下的暴露面 vs 速度 |
| **D6** | liveness 失效模式的具体解法——委员副本？升级到有 timelock 的 V2？其他？ | scoping §0.4/§10 项 4：隔离解决劫持面、不解决甚至可能加重 liveness 面，本稿不替 Owner 选 | 施工后"任一域宿主不可用时市场是否还能结算"这条风险的实际解法 |

**不在本清单、不该现在问 Owner 的一项**：轮换流程的权限模型（scoping §10 项 5）——这条依赖 D-012 §6-1 Oracle 权限边界契约先冻结（J1 主笔中），是**技术前置阻塞**不是 Owner 决策阻塞，问了 Owner 也答不了，等 §6-1 落地后再回来处理。

---

## §4 与既有文档的关系
- 不重复：scoping §0-§10 全文有效，本稿只挑出"能写的步骤"与"该问 Owner 的项"两块单独成文，其余（成本骨架/生命周期规程细节/63→0 验收方法）维持 scoping 原文，引用不复制。
- 回退 runbook（`docs/2026-08-10-u1-key-isolation-rollback-runbook-v0.1.md`）与本稿是一对：施工前先备好回退，施工完成后本稿的 §2 步骤 4（验收）与回退 runbook 的判据 B 重跑方法论共用同一份脚本产出，不是两套。

**§3 清单交 Bettor 带给 Owner；§1 两项（建表/J1 节点安全基线核查）可以立刻开始，不等 D1-D6 答复。**

---

## §12 D2 补件：域 2/3/4（及 D1 改判后的域 5）运营方提案（rev-2，2026-08-11 KANet-UI 补，Owner CARD-D2 派工）

**授权范围**: Bettor 07:04Z CARD-D2（"@KANet-UI D2 补件：施工 runbook v0.2 里给出域 2/3/4 具体运营方提案，24h 内回 Owner，不阻塞已准各项"）。**本节是提案，不是决定**——D2 仍待 Owner 拍板，本节的作用是把"新增运营方 vs 现有成员兼任副手"这个二选一摊开成可裁的选项+代价，不代 Owner 选。

**命名口径**：本仓 2026-08-11 起设计文档修订统一用 `rev-N`（Bettor 当日纪律令），`v0.x` 留给链上协议代际专用——Bettor 派工原话用了"v0.2"是当时快语，本节按新纪律用 `rev-2`，不算违背派工意图。

### 12.1 先钉一个会影响整节判断的事实：D1 已从 4 域改判 5 域，而"域 2/3/4"这个编号本身可能要跟着变
- 本稿 §3 D1 原表述"4 个隔离签名域 vs 5 域"——Owner 07:04Z 批复已裁 **D1=5 域**（多 1 台机器换鸽笼缺口关死的数学保证）。
- CARD-J2-A（②-a 施工，与本节并行执行）正在按"5 域版"实例化 §1a/§2——**具体 5 域各自的编号-宿主映射本节写作时尚未定稿**（J2 刚 ACK 开工，域→relay 映射表还没落表）。
- **⇒ 本节的运营方提案按"域 1 之外、新增的 N 个签名域"来回答（N=3 或 4，取决于域 4 是否仍固定为 J1 机、以及新增第 5 域是否也需要独立运营方），不锁死具体编号**——编号定稿后，本节的选项/代价分析原样适用，只需要把结论套进具体编号即可，不需要因为编号变了就重写本节。**这条是本节唯一需要读者自己做的一步映射，写清楚免得被读成本节漏答了域 5。**

### 12.2 现有团队成员里，谁实际具备"独立物理宿主"这个前提——先查事实，不假设
本节先回答一个前置事实问题（谁已经在控制独立机器），再谈运营方选择，因为"现有成员兼任副手"这个选项本身要求"这个成员已经有一台独立机器可以兼任"，不是任何团队成员都满足。

今晚（2026-08-10~11）session 里能现读到的事实：
- **KANet-UI（本节作者）**：独立控制生产 `:3200` 所在机（域 1），全程直接操作（重启窗 #1-#4、taskkill/PID 管理、git commit/push）。
- **J1**：独立控制至少两台物理机——挖矿/刹车机（`D:\kaspa-tn12-mining\`，watchdog/bridge/kaspad 所在）+ 另一台"健康、不载生产、isSynced 的读研究节点"（scoping §2 已点名为域 4 候选，Owner D4 已批"过基线核查后用作域 4"）。**J1 已经是"现有成员兼任副手"模式的实例，不是假设——Owner 已经这么裁了一次。**
- **J2、NWT、Bettor**：今晚全部读写都经由同一个共享 `:3200` console 完成（J2 的 `spc_daa_index`/`kaspa_tx_log` 查询、NWT 的独立复核查询，读的都是我这台机器上的同一份 `console.db`——今晚 03:43-04:15 那次 spc_daa_index 排查已经现场证实这一点：J2 说"我的 console"时，指的是同一张表、同一台机器，不是另一台）。**Bettor 07:04Z 自己的原话也确认了这条**："该做的事（域内人来，我无钥）"——协调者角色明确不持有任何基础设施访问权限。
  - ⇒ **今晚的证据不支持"J2/NWT/Bettor 现在就有一台独立物理机可以兼任副手"这个前提**。若三人中有人在 session 之外确实持有未被今晚工作触及的独立机器，本节读不到，需要当事人自己补正——本节不能从"没证据"推断"确实没有"（在册判据：没发生有两种成因，本节据实标注为"未证"，不是"不存在"）。

### 12.3 二选一摊开：新增运营方 vs 现有成员兼任副手

| 选项 | 优点 | 代价/风险 | 现状适配度 |
|---|---|---|---|
| **A. 新增运营方**（引入 session 之外的新人） | 唯一能真正兑现 scoping §0 前言"三层概念"里的**第三层（运营方独立）**——不同的人，不同的判断/被胁迫/被收买风险面 | 招募/信任建立/交接培训的时间成本（scoping §8 已标"运维代价超出 KANet-UI operator 域"，本节不重复估）；新引入的人对系统的理解曲线，短期内可能是更弱的一环，不是更强的 | 目前 0 个候选人已知，纯靠 Owner/Bettor 域外资源 |
| **B. 现有成员（KANet-UI 或 J1）兼任第二台** | 零招募成本，人已经在场、已通过今晚事实上的信任验证（长时间高强度协作、多次自纠/被红队核过） | **不兑现运营方独立层**——同一个人操作两台机器，等于把"故障域隔离"（机器级）做到了，但"运营方独立"（人级）仍是 0；若该人的凭据/判断被攻破，两个域同时暴露，与"只有域 1"时的单点风险性质相同，只是波及面从 1 域扩到 2 域 | 已有一次先例（D4=J1 机），今晚证据支持 J1/KANet-UI 均已具备"独立控制物理机"这个操作前提 |
| **C. 混合（本节倾向）** | 部分兑现运营方独立——不是全 A 也不是全 B | 仍不是纯 A 的完整独立性，是"比现状好、比理想差"的中间态 | 见 12.4 |

### 12.4 本节倾向（不代 Owner 拍，摆出理由供裁）
- **不建议全走 B**（域 2/3/4/5 全部由 KANet-UI/J1 两人分摊）——那样"5 域"在**运营方独立**这一层实际上退化成"2 个人、5 台机器"，鸽笼算术保证的是机器级 threshold 不可达，但**人级**的攻破面没有被这次施工触碰，scoping §2 原文已经把这条讲清楚（"本表只回答钥匙物理上在哪几台机器，不能声称这几个人互不勾结/互相独立"）——若最终运营方仍是同 2 人，本节建议在交付时**明确向 Owner 复述这条边界**，不要让"5 域"这个数字被误读成"5 方独立"。
- **不建议现在就锁定"全走 A"**——0 候选人已知的情况下，若 D2 现在就拍"全新增"，会让 CARD-J2-A 已在推进的施工（域→relay 映射表）卡在等人到位，而这条本身不阻塞技术施工（可以先按已知运营方生成密钥，运营方交接是后续可以补的一步，不需要在密钥生成之前锁定人选）。
- **本节倾向 C**：D4 已经是 B（J1 兼任）的既成事实，建议**至少保留域 2/3 之一（或新增的域 5）明确留给"新增运营方"**，即使短期内先由 KANet-UI/J1 临时代管（过渡态，非最终态），也在 runbook 里把它标注为"待接手"而不是"已定案由现有成员长期兼任"——这样"5 域"里至少有 1 域将来能真正兑现运营方独立层，而不是全部退化成 B。**这条只是把选项摆清楚，最终留几域给 A、哪一域，仍是 Owner 该拍的具体分配，本节不代拍。**

### 12.5 与已冻结主线的关系（不越界确认）
- 本节完全在 D2 补件范围内，不涉及 D-012 契约冻结主线（§6-1/⑥落码/④量级）任何一项，不占用 J1/J2/NWT 当前被点名的主线卡资源。
- 本节不阻塞 CARD-J2-A 已在进行的施工——运营方人选是"谁最终持有/接手"这一层，不影响密钥在目标宿主本地生成这一步（scoping §5 已定：本地生成不传输，与"最终由谁运营"是两个独立的时间点）。

**本节到此，交 Bettor 24h 内带给 Owner 裁 D2。**
