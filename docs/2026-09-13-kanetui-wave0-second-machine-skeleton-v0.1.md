# 波 0「第二台机」骨架页 v0.1（2026-09-13 · KANet-UI · Bettor 派工，只做骨架不展开）

> **Status: 核心前提已被 D-017 取代（2026-09-13 补记，Bettor 派工时未预见此变化，非本页原有判断）**。`docs/DECISIONS.md` D-017 第 1 点原文列明取代对象包括**"9/7 评估 §5「不在同一台机跑第二个 kaspad」"**，第 2 点原文"**主网节点跑 da9 本机不租 VPS**"——这两句直接推翻本页 §1/§3 的核心前提（"波 0 机器 = 与 S-2 合一，找第二台物理机"）。主网节点位置这件事**已经不是待 Owner 拍的开放问题**，D-017 已经拍了：跑本机，不买/不租第二台机。
> **本页对 S-2（TN12 第二个前向节点，修"单前向 peer 会失联"的病）部分未被 D-017 显式提及**，但 TN12 已被 D-017 裁定退役（见 `docs/2026-09-13-kanetui-tn12-retire-mainnet-node-runbook-v0.1.md`）——继续为一个要退役的网络投入第二台前向节点，逻辑上已经没有意义，这是我的推断非 Owner 原话，供 Bettor/Owner 判断要不要正式收尾此页。
> **本页作为规格估算的历史记录保留**（§2 的数字来源仍有参考价值），但不再是"待细化的行动计划"。以下内容原样保留，仅 §2 按 Bettor 派工改为引用 `docs/2026-09-13-kanetui-tn12-retire-mainnet-node-runbook-v0.1.md` §4.5 的实测表，不再并列三套未调和的估算数字。

## 1. 用途（S-2 与波 0 合一，不是两台机）
一台第二台机同时承担两个角色：
- **TN12 前向节点 N2**（S-2 提案，ledger 998）——补上"全网只有一个前向 peer 且会失联"的病；
- **主网只读 pruned 节点**（评估 v0.1 §3 波 0）——转向主网的第一步，零资金、只读接入。
两者合一是评估文档 §3/§5 已定的口径（"不在同一台机上跑第二个 kaspad"指的是本机，不是禁止第二台物理机跑两个网络的节点）。

## 2. 规格 —— 改引用实测表，不再并列三套未调和的估算数字（2026-09-13 Bettor 派工·空档改）

下面三行是本页原有的**估算**（预留存档，说明数字曾经的来源），**权威口径现改为 `docs/2026-09-13-kanetui-tn12-retire-mainnet-node-runbook-v0.1.md` §4.5 的实测表**（D: 空闲/官方最低/TN12 现用/mainnet-data 旧内容/RAM 总量与空闲/TN12 kaspad 工作集——全部本机实测，非估算）。

~~| 来源 | RAM | 磁盘 | 备注 |~~
~~|---|---|---|---|~~
~~| ledger (998) S-2 | ≥32 GB | ≥250 GB | 只 TN12 单节点估法（S-2 提案，前提是找第二台机） |~~
~~| 评估 v0.1 §3 表格行 | ≥32 GB | ≥300 GB | 波 0 单条目"与 S-2 合一"（同上前提） |~~
~~| 评估 v0.1 §5 正文 | ≥32 GB | ≥1 TB SSD | "TN12+主网各一台"总量（同上前提） |~~

**为什么划掉不是删掉**：这三行的共同前提"找第二台机"已被 D-017 取代（见页首 Status），本页不再需要调和"到底该按哪行采购"这个问题——因为**不采购了**，主网节点在本机。三行原样保留只是不冒充"仍待用的口径"。
**现行口径（单机，非估算，来源 runbook §4.5）**：D: 空闲 758.7 GB（官方最低 640 GB）；TN12 datadir 现用 158 GB（实测，非早前 204 GB 估读）；`D:\kaspa-mainnet-data` 已存在陈旧内容 ≈37 GB（04-09 遗留，待判断复用/清空）；RAM 总量 61.6 GB / 现空闲 42.2 GB；TN12 kaspad 实测工作集 15–28 GB；主网节点工作集未知（新节点无实测，runbook §4.4/§4.5 标 TBD）。
上行带宽：998 给过 TN12 侧 ≥10 Mbps 的估法，本机口径下不再是采购判据，作废不再引用。

## 3. 候选来源（原样列自 998，未新增）
> 📌 状态注记（2026-09-13）：D-017 后**主网节点不走这三个候选**（跑本机）。以下三条只对"S-2 单独还要不要一个 TN12 第二前向节点"这个已很可能是空议题（TN12 退役中）的问题仍算数，原样保留不改。

1. **younio**（J1 现用机）——998 原话"若能加到 32 GB 即可"，即需先加内存，现状是否够未核。
2. **云 VPS**（同城，≥8 vCPU / 32 GB / 300 GB SSD 量级，998 给的是 TN12 单节点估，主网节点叠加后需求见上表未调和项）。费用 Owner 定。
3. **本机第二实例**——998 已明确**不推荐**（同一故障域、内存已紧），评估 v0.1 §5 同一口径。

## 4. 主网只读节点资源估（评估 v0.1 §2 第⑤条，仍是"待核"状态，未细化）
> 📌 状态注记（2026-09-13）：主网节点资源问题本身仍未解（本机 IBD 实测数仍缺），但"装在哪"已由 D-017 定（本机）。本节软件/端口信息不受影响，原样保留；本机磁盘/内存与 TN12 并存的预算表已移到 runbook §4.5，比这里的"待核"状态更新。

- 软件：上游 v2.0.1 官方 release（`kaspad --utxoindex`），非本机 1.1.1-toc.1 分支（其主网参数 `covenants_activation=never`，不能用于主网）。**本机已有一份经 sha256 核实与官方一致的 v2.0.1 win64 包**（`D:\rusty-kaspa-v201\`，见 runbook §4.1），不需要再找/下载。
- 硬件：评估原文写"Toccata 硬件规格上调（transient mass ×2 允许 ZK-STARK），pruned 磁盘/内存数字待 J2 按 elldeeone 报告核；本机 TN12 datadir 138 GB、kaspad WS 15–28 GB 可作下界"——**这条本身仍是待办**，runbook §4.4/§4.5 同样标 TBD，没有新增数据，等 J2 坐标。
- 端口：可与 TN12 节点共机分开（评估 v0.1 §5：主网 16111/17110 vs TN12 16311/17210），不冲突；runbook §4.3 用 `kaspad.exe --help` 核实过这两个端口号是官方默认值，非估读。

## 5. 装机路径（骨架，未细化步骤，等 GO 后再出可执行 runbook）
> 📌 状态注记（2026-09-13）：本节整段按"第二台机装两个节点"的前提写，D-017 后**主网这半已作废**——本机装主网节点的可执行步骤见 `docs/2026-09-13-kanetui-tn12-retire-mainnet-node-runbook-v0.1.md`（已出，非"等 GO 后再出"，GO 门仍在）。原文保留供 S-2（若还要做）参考。

沿用 998 落地路径骨架：候选机提权装 TN12 kaspad（同 exe 版本 + 参数待定）+ 主网 v2.0.1 官方 release 两套独立 datadir/端口 → 本机 `--addpeer` 接 N2 进 TN12 watchdog（需一次节点重启窗）→ console 加"网络=mainnet"只读配置分支（波 0 代码工作，J2 域）→ 验收：N2 isSynced、主网节点 isSynced、任一方失联另一方 ≤5 min 顶上（998 验收判据搬来 TN12 侧；主网侧验收判据待定）。

## 6. 待 Owner 拍的（不重复评估文档 §6，只点名与本页相关的两条）
> 📌 状态注记（2026-09-13）：第 1 条已被 D-017 answered（不起第二台机，跑本机）；第 2 条已被本次改动解决（改引用 runbook §4.5 实测表，不再需要"谁来定稿"）。以下原文保留存档。

1. ~~**波 0 GO**：是否现在起第二台机；来源选 younio 加内存 / 云 VPS / 其他。~~ → D-017 已拍：不起，跑本机。
2. ~~上表 §2 三个规格口径由谁在 GO 后定稿（建议 J2 出资源估、我按估法重算磁盘/内存/带宽，NWT 审）。~~ → 已改引用 runbook §4.5。

## 7. 未完成事项（本页故意留白，非遗漏）
- 主网节点精确资源数（J2 域，评估 v0.1 §2 已派待办，runbook §4.4/§4.5 同步标 TBD，仍未解）。
- 采购/云 VPS 报价——D-017 后大概率不再需要（不采购），未做也不会补做，除非 Owner 推翻 D-017。
- 装机 runbook 逐步命令——主网这半已出（`docs/2026-09-13-kanetui-tn12-retire-mainnet-node-runbook-v0.1.md`），S-2 那半（若仍要做）尚缺。
