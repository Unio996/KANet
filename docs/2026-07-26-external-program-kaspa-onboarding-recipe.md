# 外部程序接入 KANet — 实测记录(v0.1,**未完成,不可发布**)

> **Status**: CURRENT
> 🔴 **发送侧通了,接收侧没通。这份东西现在【不能发给外面的人】。**
> 一份「能上链但没人收得到」的说明书**比没有说明书更坏** —— 它把我们的缺陷伪装成使用者的错误。

| | |
|---|---|
| 实跑人 | J2(2026-07-26 21:15 / 21:19) |
| 环境 | 🔴 **全程本机**。未从另一台机器跑过 |
| 花费 | 一次性密钥 · faucet 领 10000 测试币 · 2 笔交易。未动生产身份 · 未动 50 KAS pilot |

---

## 🔴🔴 2026-07-27 晨:**"外部"这个词一直在滑动着用,而三层的门槛差一个数量级**

昨天这份记录写「一个外部程序完整走通了发送侧」。**那句只对最容易的那一层成立。**

| 「外部」是哪一层 | 状态 |
|---|---|
| **不用我们的身份/密钥** | ✅ 已实证(两个 txid) |
| **不在这台机器上** | ✅ 已实证(J1 经 tailnet 连通我们的 `3210` / `17210`) |
| 🔴 **不在我们这张网里的任何人** | 🔴 **这一层今天走不通** |

🔴 **⇒ J2 那两笔的 RPC 端点是 `ws://127.0.0.1:17210`(本机回环);J1 走的是 tailnet(`100.64.0.0/10` CGNAT 段)。两条都不是公网路径。**
🔨 **⇒ 落一条:凡写「外部 / 对外 / 外面的人」,必须同时写清【相对于什么的外部】。三者不能共用一个词。**

### ✅ 而「外面的人根本没有入口」这句也是错的(同晨修正)

- ✅ **实测**:我们的节点**此刻**连着**两个公网 IP** 的 TN12 对等节点,且是**发现来的**(配置零命中、命令行无 `--addpeer`),**并且都不是自己人**(J1 已核他的出口 IP 与两者皆不同)。
- 🔴 ⇒ **「TN12 上几乎不可能有别的节点」——确凿地假。**
- ⚠️ **而 `16311` 是 P2P 口,不是 RPC** ⇒ **「有别的公网节点」≠「外部程序能通过它们广播」**,后者**未验,且不去验**(那是对外主动连接)。
- ✅ **所以唯一站得住的是**:**外面的人连不到【我们这台】** —— 因为我们在公网上没有地址。**而他能不能通过别人的节点进来,不知道。**

### 🔴 而「要不要给这台机器一个公网入口」不是一次测量,是一个未做的决定

我们在 NAT 后面,没做过端口转发 ⇒ **不是被挡住,是没有入口**。
⚠️ **而两个我们用来测的地址(`192.168.1.101` / `100.99.147.101`)在公网上都不存在** ⇒ 拿它们做的任何"从外面连"的测试**必然失败,而失败原因与被测的那一层无关**。🔵 又一次:**一个失败,长得像答案。**

---

## 一、现在到哪一步

| 环节 | 状态 |
|---|---|
| 生成密钥 | ✅ 自己生成,不需要我们任何东西 |
| 领测试币 | 🔴 `POST /api/faucet/request` **存在**,而**外部口不可达**(实跑 `:3210` ⇒ **404**)⇒ 今天外部程序只能走人工网页 |
| 构造 + 签名 + 付费 + 上链 | ✅ **通**。`txid 089dd883d7068e01c400cad0a0e95a8af415db54562b3f168b9b35135cb7fc26`(fee 1213000 sompi) |
| 真实加密信封上链 | ✅ **通**。`txid a02efa4f23806bf5dfa7e5c778b9fcd79cdf8d7377f7c07e32d4a7434ce2df32`,明文 `hello from an external program — J2 recipe probe` |
| 🔴 **另一侧读到它** | 🔴 **没通。等 12 秒查 messages,按 `source_txid` 找不到。** |

🔵 **判据(2026-07-26 上午自己定的,晚上自己违反了一次)**:
> **上链成功不算数 —— 必须被另一侧实际读到,才叫接进来了。**

### 🔴🔴 21:26 更新:**不是格式问题。整条接收路径断,与「外部」无关**

> ⚠️ 本节推翻上面那句「正在查是格式哪一格不对」。**保留原句是为了让人看见判断是怎么变的。**

对照臂(NWT,一次只读 grep 就砍掉一半):
```
✅ 我们【自己】发的 comm 广播, 同样没有被收到 —— 只出现在发送侧那一行
⇒ ⇒ 与"外部程序"无关。整条接收路径在本机就是断的
🔴 若没跑这个对照臂, 我们会去比对信封字段 / x-only / 别名派生
   —— 因为"外部发的收不到"天然指向"外部那笔有什么不一样", 而那条路会走很久、沿途每步都合理
```

**分段定位(按证据强度标)**:

| 段 | 状态 |
|---|---|
| ① block-added 收块 | ✅ **排除**。块级日志 `11:09:37 → 21:25:54` 连续无断层 |
| ② classifyPayload 分类 | ⚠️ 有活动,而**未证明**那批里包含我们的消息 |
| ③ 分类之后 → RX 之前 | 🔴 **嫌疑段**。两处静默 `return`:`processComm:1015-1022`(前缀/alias 切分)· `:1029`(decrypt 失败) |

⚠️ **这是【范围推断】,不是定位。**

### 🔴 21:30–22:50 续:范围又变了两次,而 ③ 已被排除

```
❌ 撤:「223 个块里有候选」—— attempted 是【累计集合大小】且每 1000 块才打一次
     ⇒ 那 256 行是 8 个采样点 × 32 个 relay, 不是 223 个块
✅ 实情: 整轮运行【累计只 attempt 过 2 笔】(计数器被打印, 值就是 2)
❌ 撤:「断点在 ③」—— 该结论建立在上面那个 223 上, 前提没了, 结论跟着撤
```

**而 `_attempted.add` 之前有【三道出口】(J2 实读 `:841-851`),不止分类:**

| 出口 | 内容 | 状态 |
|---|---|---|
| **A** | `_seen` / `_attempted` 去重(`_seen` **落盘跨重启**) | ✅ **已排除** —— `loadSeen()` 的 `catch { return new Set(); }` ⇒ **每次启动 `_seen` 就是空的**,与文件内容无关;且四个 txid 实查都不在文件里 |
| **B** | payload 长度 | 🔴 **嫌疑** |
| **C** | `classifyPayload` | 🔴 **嫌疑** |

🔴 **而「那 2 笔是谁」用现有手段查不出**:`_attempted` 是**纯内存 `Set`**(`:102`,全文无 save/load),日志只打 `size`。要点名必须改码 + 重启。
✅ **NWT 不改码绕过了一半**:那个 `size` 每 1000 块打一次 ⇒ 取单个 relay 的采样序列 ⇒ 两次跳变各自的两分钟窗口 ⇒ **第一次跳变早于 J2 任何一笔 ⇒ 最多一笔是他的,另一笔是别人的。**

### ✅ 有效 / 无效证据清单(标死,免得明天重用无效的)

| | 强度 |
|---|---|
| `RX` 零命中 | ✅ **有效负证据** —— `rpc-listener.mjs:1037` 那行 `log('RX', ...)` **存在**且在 decrypt 成功**之后** ⇒ 零 = **从未成功解密过** |
| `_attempted` 累计 = 2 | ✅ **肯定证据**(计数器被打印) |
| `messages` 表按 `source_txid` / 明文 `LIKE` 命中 0 | ✅ **独立且有效** |
| ❌ 「txid 在日志里零命中 ⇒ 没走到解密」 | 🔴 **无效,已撤** —— live 路径在 `:851` 前三道出口上**一条日志都不打** ⇒ 「没被看见」与「被 continue 了」两种同一个观测 |

### 🔴 而那个 `seen.json` 是**症状,不是病因** —— 别把它当修复目标

文件解析不了(**非原子覆盖写**:新内容比旧的短时旧尾巴留下 ⇒ `]` 之后又接一段),`loadSeen()` 一行 `catch` 吞成合法空 `Set` ⇒ **不报错、不打日志、启动一切正常**。
🔴 **而 mtime = 2026-06-30,32 个 relay 天天在跑 ⇒ `saveSeen` 现在根本没在被调用** ⇒ 它更像是上游某件事的**下游痕迹**。
🔨 **⇒ 修好落盘不会让消息被收到。修完会以为方向错了。**
⚠️ 它自己**也是**实缺陷(非原子写 + 静默 catch + 32 个 relay 共享同一个路径),只是**不在这条因果链上**。两件事分开记。

### 📌 明天第一件,只有一件

> **给 J1 那台发一条加密信封,两边同时看。**
> - 他收到 ⇒ 对照臂成立 ⇒ 可以比"我们这台哪里不一样"
> - **他也没收到 ⇒ 不是「我们这台坏了」,是【两台都收不到】—— 问题性质完全不同**

✅ **对照臂前提已核**(两侧独立):J1 `HEAD=4ddba7b8`(behind origin 6),他与我们之间 **26 个 commit 的完整 delta 不碰 `kasia-relay/` · `shared/` · `vendor/`**;他逐字节核了盘上三文件 == 他 HEAD;两台 `kaspa_bg.wasm` 与 `kaspa.js` **逐字节相同**(`LICENSE` 差异 = **行尾**,同源)。
🔴 **仍未核**:两台都可能有**未重启的模块**(commit ≠ live)。
🔴 **而这是跨节点收发 ⇒ 需 Owner 点头**(他 7/26 早说过「跨节点稍微延迟」,而它现在是关键路径)。

### 🔴 而一件必须写进记录,免得明天有人误读今天

```
协调频道整天工作正常 —— 而它正常, 是因为所有参与者在同一台机器上读同一个库
⇒ 频道【没有】在行使链上往返
⇒ 今天所有"频道里核实过"的东西, 核的是【本地写入】
```
🔴 **⇒ 这不推翻今天任何结论**(它们本来就来自读码/读库/实跑),**而它推翻一件事:我们从来没有用频道本身证明过链路是通的。**

---

## 二、🔴🔴 四个坑 —— 报错**一字不差**,而其中一个外部**不可能自己发现**

**四种完全不同的写错,输出全部是:`RuntimeError: unreachable` + 一串纯地址栈,不提任何参数名。**

| # | 坑 | 正确写法 |
|---|---|---|
| 1 | 🔴 `networkId` 传我们**实际的网络名** `'testnet-12'` | **必须传 `'testnet-10'`**。【实读】`wallet.mjs:95-98` 有这个内部映射 |
| 2 | `payload` 传 hex 字符串或 `Buffer` | **必须 `Uint8Array`** |
| 3 | `outputs` / `changeAddress` 传字符串 | **必须 `PaymentOutput` / `Address` 对象** |
| 4 | 用 `createTransactions()` | **必须 `new Generator()`** |

🔴 **第 1 条是这份文档存在的核心理由**:一个外部的人**没有任何途径**能猜到"网络叫 testnet-12 但要填 testnet-10" —— 那是我们代码里的一个映射。而他填对了真名字,拿到的是一句什么都不说的报错。

🔵 **⇒ 这是「失败不产生失败,产生一个形状正确的答案」在最伤外面人的地方的实例**:四个不同原因,一个相同的、零信息的输出。**把这四格写下来,它们就不再是坎。**

---

## 三、④「广播」这一格的准确状态(我错过两次,两次方向相反)

```
❌ 我第一次说: "转发别人签好的 tx"这个能力不存在 ⇒ 要新写一个接口
   🔴 错法: 我 grep submitTransaction 七处全在 p2sh.mjs, 就断定能力不存在
           —— 我搜的是【我们怎么做】, 要答的是【外面的人能不能做】。外部程序根本不走我们的代码

❌ 我第二次说: "不缺 —— 节点本来就对外开着"
   🔴 又说过头: 那只在【本机】验过

✅ 准确: 【我们这边不缺一个广播接口】—— 成立, J2 用 kaspa-wasm 直连节点即成
        【外面的人有没有一个够得到的 TN12 节点】—— 🔴 未验
```

🔴 **而这一格比网关那格要命**:TN12 是我们自己搭的测试网 ⇒ **外面几乎不可能有第二个 TN12 节点** ⇒ 若我们这个节点外部连不上,**「外部程序自己上链」这条路不是慢,是不存在**。

---

## 四、外部可达性 —— 唯一填不了的一格,以及怎么问才问得出来

⚠️ 队里**没有第二台机器**(唯一有的那位已关节点)。从本机连自己**永远证不出**防火墙 / NAT 那一层。

🔴 **而我第一版的问法是【不可判读】的**(NWT 实测):
```
curl :17210 (口在监听, 但它是 gRPC 不是 HTTP) ⇒ code=000 · 0.001s
curl :19999 (什么都没有)                      ⇒ 完全相同的输出
```
⇒ **两种相反的结论指向同一个输出。** 而那个测试问了也白问,我还会照它下结论。

✅ **可判读的问法 —— 让人把浏览器报错的原话念出来**:

| 屏幕上写着 | 结论 |
|---|---|
| 「服务器没有发送任何数据」/ `ERR_EMPTY_RESPONSE` / `ERR_INVALID_HTTP_RESPONSE` | ✅ **连上了**(端口可达) |
| 「连接被拒绝」/ `ERR_CONNECTION_REFUSED` / 转很久才超时 | 🔴 **连不上** |

🔵 而 `:3210`(网关)本来就是真 HTTP,判据干净:**看到带 `txid` 的 JSON = 通**。

---

## 五、边界

- ✅【实跑 J2】上面两个 txid、四个坑、faucet 外部 404
- 🔴【未跑】从另一台机器连任何一个口
- 🔴【未通】接收侧
- ⚠️ J2 自标:**每一处跨过去的坎都靠内部知识**(读 `wallet.mjs` / `transaction.mjs` / 本机拿币)⇒ **外部人今天一个都跨不过去** —— 而把这些写下来,它们就不再是坎
- ⚠️ 🔴 **发布条件**:接收侧通 **且** 至少一次从外部机器验证过可达。**两条都满足之前,这份文档只对内。**
