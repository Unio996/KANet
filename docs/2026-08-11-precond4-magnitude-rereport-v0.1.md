# D-012 格④ · 量级重报 v0.1 —— 在【生效库】上重量一次

> **Status**: CURRENT
> **作者**: J1 · 2026-08-11 · 派工来源 CARD-J1-A 第三项「④ 量级今日重报(扫射 v0.2 基础上)」
> **DESIGN-ONLY · 零生产码 · 零用例改动 · 零 UI**
> **前稿**: `docs/2026-08-10-precond4-tautology-family-sweep-v0.1.md`(J2, v0.2)。本稿**不推翻它的判据**,
> 判据(§0 那一条)照用;本稿只做一件事: **把量级搬到生效库上重量**,以及**把判据完整套一遍**。

---

## §0 为什么要重量(一句话)

v0.2 的 R2 已经指出「v0.1 的数量量在了错的库上」,并**改了 4 个数**。
🔴 **而 §2 那张 8 行的判定表没有跟着重量** —— 它的「伴随断言」列仍是旧世界的读数。
⇒ 本稿把 17 条**逐条代进生效库跑**,不是重新推理。

**生效库 = `kasia-console/test-framework/data/test-console.db`**。
现读: 本机该目录**原本不存在**,`env-bootstrap.mjs` 当场造 —— 跑一次得 1,781,760 B 纯 schema,
`pool_bettor_sides / pool_markets / chain_events / exchange_offers / market_shards` **五张表全 0 行**。
(`DB_PATH` / `KANET_DB_PATH` 本机现均 unset ⇒ 走的就是这条默认路。)

---

## §1 总数复核(不引 v0.2 的数,重 grep)

`db_row_count` 断言全仓 **67** 处;其中 `= 0` 的 = **17 处 / 8 个文件**。
⇒ **与 v0.2 的 17/8 对上**。以下按生效库实测分三档。

---

## §2 量级表(三档 · 每一格都是实测,不是推断)

| 档 | 条数 | 文件数 | 生效库下的读数 |
|---|---|---|---|
| **A. 自带 fixture + 参数化 + 阳性对照** | **5** | 2 | 库空不空都不影响 ⇒ **真在测东西** |
| **B. 伴随断言在空库下自己红** | **7** | 3 | **不会静默恒真绿**,但**在现行入口下必红** |
| **C. 🔴 静默恒真绿** | **5** | 3 | 闸在与闸不在,读数相同 |

### A 档(5 条 / 2 文件)—— 形状是对的,可当模板
- `p1_refund_authorization_gate` ×4:自 seed 5 个市场 + `params:[M_x]` 参数化 + **阳性对照** `M_OK ⇒ db_row_count:1`。
  头一步注释逐字写着「没有这条, 一个恒拒的实现能让下面全绿」——**这就是本族的解**。
- `bettor_refund_bshard_guard` ×1:双向臂(bshard 必触发 `rows_min:1` / v0.5 必不触发 `=0`)+ 自 seed + teardown。

### B 档(7 条 / 3 文件)—— 伴随断言实测在空库下**为假**
| 文件 | 零断言 | 伴随断言 | 空库实测 |
|---|---|---|---|
| `dim6_race_05_taker_null_race` | 3 | `COUNT(*) FROM pool_bettor_sides` c_min:1 | **0** ⇒ 红 |
| `dim6_race_04_protocol_version_migrate` | 2 | `DISTINCT protocol_status FROM pool_markets` rows_min:1 | **0 行** ⇒ 红 |
| `dim6_race_02_chain_reorg` | 2 | `COUNT(*) … settle_txid IS NOT NULL` c_min:1 | **0** ⇒ 红 |

### C 档(5 条 / 3 文件)—— 🔴 静默恒真绿
| 文件 | 零断言 | 为什么没被挡住 |
|---|---|---|
| `dim5_fail_recovery_04_mempool_race` | 2 | 🔴 **见 §3,v0.2 把它判成了干净** |
| `dim6_race_03_scout_outage_matched_to_completed` | 2 | 无任何伴随断言(v0.2 §3.2 已判,本稿只补实测) |
| `dim6_race_01_utxo_concurrent_spend` | 1 | 只有机制存在证明(v0.2 §3.1 已判) |

---

## §3 🔴 对 v0.2 §2 的一处更正:`dim5_fail_recovery_04` 应从 🔵 挪到 🔴

v0.2 §2 该行「伴随断言 = ✅ 有 ⇒ 🔵 干净」。**逐字读它的伴随断言**:

```
sql: SELECT count(*) AS c FROM pragma_index_list('exchange_offers') …
expect: { must: { row_assert: { c_min: 1 } } }
```

⇒ 它量的是 **`pragma_index_list`,不是数据表**。**空库实测 = 8** —— 索引在 schema 里,库空它照样成立。
⇒ **它是「机制存在」证明,不是「总体非空」证明。**

🔴 **而 v0.2 自己在 §3.1 对 `dim6_race_01` 划过的正是这条线**
(逐字:「它带了一条伴随断言, 但方向是"机制存在"不是"总体非空"」)。
**同一个形状,一个抓到了,它的双胞胎没抓到** —— 因为 §2 那张表的「伴随断言」列填的是**有没有**,
不是**是哪一种**。

🔨 **判据(本稿主要产出)**:
> **「有伴随断言」不是一个判定。判定必须是「伴随断言在被测总体为空时是否为假」。**
> 前者可以靠看一眼填表,后者只能靠**把它代进空库跑一次**。
> 一张按「有没有」填的表,会把两种东西并到同一格,而它们的读数正好相同。

---

## §4 🔴 量级的头条:v0.2 §5 建议③ 的成本估法在生效库下不成立

v0.2 §5.3 建议「各补一条 `c_min` 形态的总体非空断言…**成本≈一步**」。
🔴 **在生效库上这一步会把静默绿换成【永久红】,不是换成【真测】** —— 因为库是空的,`c_min:1` 恒假。

⇒ **真实修法不是加一条断言,是把用例改成 A 档形态**(自 seed fixture + 参数化 + 阳性对照 + teardown)。
⇒ **真实量级 = 8 个文件里有 6 个要改形态(B 档 3 + C 档 3),涉及 12 / 17 条断言;A 档那 2 个文件已经是目标形态,可直接当模板抄。**
不是 v0.2 估的 3 条 × 一步。

---

## §5 一条独立的实测:这 8 个文件**从没在现行入口下跑过**

`logs/test-runs/` 现有该族运行证据 **7 份,全部时间戳 `2026-08-03T19-44-08`**
(`p1_refund_authorization_gate` **一份都没有**)。
而**空库重建**(`rebuildTestDb`)落地于 **2026-08-04 `759ec669`**「runner硬化 — 独立test DB隔离,不再默认打生产库」。

⇒ **仅有的运行证据比生效库的改变早一天 ⇒ 零个文件曾在现行标准入口下跑过。**

🔵 **而那批旧读数反过来印证了本稿的分档**:
`race_05` 当时 **PASS**(旧世界打生产库,`pool_bettor_sides` 36,012 行 ⇒ c_min:1 成立);
`race_04` / `race_02` 当时 **FAIL**。
⇒ 旧证据里的绿,**是生产库的历史数据给的**,不是被测代码给的。

---

## §6 证据层级自标

| 陈述 | 层级 |
|---|---|
| 17 处 / 8 文件;`db_row_count` 全仓 67 处 | ✅ `[CONFIRMED·现 grep 重扫]` |
| 生效库五表全 0 行;文件 1,781,760 B | ✅ `[CONFIRMED·现跑 bootstrap 后现查]` |
| 12 条全表零断言在空库下**逐条实测**返回 0 行 | ✅ `[CONFIRMED·逐条代入执行]` |
| 三条 c_min/rows_min 空库实测为 0;`pragma_index_list` 实测为 8 | ✅ `[CONFIRMED·逐条代入执行]` |
| 运行证据 7 份 @2026-08-03;`rebuildTestDb` @2026-08-04 `759ec669` | ✅ `[CONFIRMED·ls + git log -S]` |
| 「A 档那 5 条真的测到了闸」 | 🟠 `[PARTIAL]` —— 本稿只证**它们不受库状态影响**;它们测得对不对**不在本稿射程** |
| 「改成 A 档形态就够了」 | 🔴 `[NOT-ESTABLISHED]` —— 形态对 ≠ 覆盖 ④ 那七类(RPC 错/缺输入/多输入/陈旧 outpoint/坏金额/超量输出/payout 篡改) |
| 「④ 可以推进落码」 | 🔴 `[NOT-ESTABLISHED]` —— 本稿是量级,不是设计;§8-4 短设计稿仍未写 |

---

## §7 我建议的下一步(不自决,交 @Bettor 排 / @NWT 判)

1. **v0.2 §2 表加一列**:把「伴随断言」拆成「有没有」与「**是哪一种(机制存在 / 总体非空)**」,并按 §3 更正 `dim5_fail_04` 那行。
2. **v0.2 §5.3 的成本估修正为 §4 的量级**(6 文件改形态,不是 3 条加一步)。
3. **A 档两个文件立为模板**,改形态时逐条抄,不各自发明。
4. 🟠 **本稿没扫**:④ 那七类错误本身的 handler 面(本稿全程只在「零断言恒真」这一个形状里)。
   ⇒ **别把本稿读成「④ 量完了」;量完的是它的一个已知子族。**
