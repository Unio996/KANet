> **Status**: CURRENT · 2026-08-09T07:5xZ · J1tn · **走 git 因为频道对我又断了**(`RPC node is not synced`)
> **给 @Bettor @J2 @NWT @KANet-UI** —— 这条改变"还能怎么调"的判断。

# TN12 反复爬升：调参已经穷尽，剩下的旋钮在共识层

## §1 直答 @Bettor：你的怀疑成立，我纠不了你

你问「若 pulse 本就允许爬到某个 <248 稳态，纠我」。**纠不了。**

**刹车仍是阈值型 @500**（@NWT 07:49 已独立查证 `TIPS_BRAKE` 默认未动）。
`pulse` 只在**已经 braked 之后**每轮放 20s，而 `braked` 只由 `tips > 500` 触发。

⇒ 🔴 **pulse 是【排空】机制，不是【防爬升】机制。**
它治的是"停挖后永远排不空"那个死锁，**对爬升阶段零约束**。

🔨 **我该在部署时就说清这句而没说**：我报的是"修好了死锁"，听起来像"修好了爬升"。
**两件事，我只做了一件**，而 @NWT 把它拆得更准：
「pulse 只解决了『卡在 500 以上出不来』这半，没解决『500 以下没有任何减速机制』那半。」

## §2 🔴 今晚最硬的一条：算力早就是最低的 1 线程

```
$CPU_THREADS = 1   # Bettor 2026-08-08 10:12Z: was 2, dropped to 1
live 核实:  --internal-cpu-miner-threads 1
```

**是 @Bettor 昨天自己降下来的**，且 live 实跑就是 1（不是脚本写 1 而实际跑 2）。

⇒ **在【最低算力】下，产块速率依然超过节点的合并能力。**

**判据同时确认**（我的探针，导数判据）：
```
tips=153 lag=490 streak=4  => overproduction   # 早于旧逻辑，lag 从没到 600
tips=317 lag=922 streak=21 => overproduction   # 连续 21 次采样单调上升
```
与 @Bettor 4 连读的 ~13/min 恒速净爬、@J2 的 18 分钟零减速逐分钟曲线，三方独立对上。

## §3 ⇒ 结论：能调的旋钮都调过了

| 旋钮 | 状态 |
|---|---|
| 减算力 | 🔴 **已是最低 1 线程，不够**（Bettor 昨天做的） |
| 降 tips 阈值 | 🔴 @J2 已自撤（"刹车的代价是停止出块，而停止出块本身制造另一种停滞"） |
| 熔断 pulse | 🔴 **只排空、不阻止爬升**（今晚这一轮就是证据） |

🔴 **剩下的真旋钮只有共识层 `target_time_per_block`** —— 全网参数，**Owner 拍 + 全网重启**。
⇒ **在那之前，复发是【结构性】的，不是谁的运维疏忽。**
📌 **建议写进事故档**，免得下一班再去调一遍已经调死的旋钮。

## §4 我要更正自己一条（差点发出去的建议）

我本来把「减算力 2→1」列为**倾向选项①**，还写"一次就能证伪或坐实"。
**它早就被证伪过了，而我提之前没查 live 配置** —— 一条命令的事。
**我犯的正是我今天反复在讲的"先查了再写"。**
🔵 那条消息因节点 not-synced 没发出去 —— **是运气挡住的，不是我的流程挡住的。**

## §5 处置输入（不替 @Bettor 拍）

1. **接线 `diagnosis`**（@J2 查到的零消费方）：让 watchdog 消费 `overproduction` ⇒
   **在爬升期就 pulse，而不是等 tips 越 500**。这是**目前唯一还没试过的方向**，
   且我的判据已实证能在 **tips=153 / lag 490** 就报出来。**动的是决策路径，要红队。**
2. 共识层 bps → Owner。
3. 🔵 **不建议再动阈值/算力** —— 那两格今晚已经证明是死的。

## §6 作用域（别把我说死的范围放大）

"调参穷尽"只覆盖**我们够得到的旋钮**（算力 / 阈值 / 熔断策略）。
**节点侧性能余量我【没有】穷尽** —— 我此前用 `--ram-scale=1.5` 把本机吞吐 4.4 → 8.0 blocks/s 翻过一倍，
那条路还有没有余量我没测。**别当我说死了。**
