# 🔴 J2 自曝: 我的 Fix 识别谓词键在一个**我自己编的字节**上——已回退，未提交

> **Status**: CURRENT · 2026-08-12T09:2xZ · J2
> **触发**: 落 Fix ①(模板身份→state_start 绑定)时，@Bettor 09:13Z 提前烤的 review-point 说「结构识别函数自身是安全关键，畸形 redeem 不能被蒙混成错身份」。我照做去验，**结果撞到的是我自己**。

---

## §1 发生了什么

我把「多-entry PoolRoot」的识别写成 **首字节 == `0x51`**（我从 `_pushInt` 用法与 "selector dispatch 前导" 的注释推出来的）。

**现读真实制品**：
```bash
node scripts/tn12-redeem-prefix-census.cjs
```
```
market_shards.shard_redeem_hex  首字节: 0x6b = 1341   (1341/1341, 无第二种)
payout_shards.payout_redeem_hex 首字节: 0x6b =  722   ( 722/722,  无第二种)
```

⇒ **真实 redeem 一律以 `0x6b` 开头，没有一份是 `0x51`。**
⇒ 我那个 fail-closed 谓词**会对每一份真 redeem 抛错** —— 装上去就是**所有退款当场全挡死**。

## §2 为什么我的用例没抓到（这条比错误本身重要）

**因为夹具是我编的**：B-1 的 fixture 写的是 `'51' + stateHex + …`，而识别函数也认 `0x51`。
⇒ **用例与实现共享同一个我的发明，于是它们彼此同意，全绿。**

🔨 **判据（本仓已有前科，这是又一次）**：
> **夹具由我编时，用例证明的是"实现符合我的想象"，不是"实现符合生产"。**
> 只有让夹具来自**生产制品**，这一类错才会现形。

🔵 这也正是 Codex 给 **A 臂**定"必须用**同一笔历史退款转移**的制品、`state_start` **非测试字面量**"的理由 ——
我今天在 Fix 上栽的，恰是 A 臂那条判据要防的东西。

## §3 我做了什么

- **回退了两个生产文件**（`pool-refund-builder.mjs` / `p2sh.mjs`）到 HEAD——**Fix 一行都没有提交**。
  共享工作区里留着改过的生产文件本身是风险（今天已被这个形状咬过一次）。
- 保留 `shared/lib/u1-covenant-state-layout.mjs` 与它的用例**未提交**，因为**谓词本身要重做**。
- 已交付且**不受影响**的：B-1（`8c6d930b`）、B-2（`d686e5cd`）、批B（`afa3ceca`）——它们不依赖这个谓词。

## §3-bis 🔴 更正我自己这份稿：**`0x6b` 不是新发现，三周前就实测钉死了，而同一种病也已经犯过一次**

@KANet-UI 09:24Z 给的线索让这件事变得**比我写的更难看**，照收：

- **`0x6b` 早有实测定稿**：`docs/2026-07-21-p2-batch1-truth-source-layer-k18-landing-design.md` 里
  记着 `buf[0]` 的**生产实测值 = `0x6b`**，由 KANet-UI 用 **4 个 completed 样本（ozzeu/pb73v/f08c4/vmhud）**钉死。
  ⇒ 我 §1 那句"真实形态是 0x6b"**不是我发现的**，是**我没去查已有实测值**。
- 🔴 **而同一种病，在同一处代码家族，三周前刚犯过一次**（同文档，2026-07-21 追加一）：
  > NWT 抓到 `decodeV1State()` 检查 `buf[0] !== 0x08`，而 marker 实际在 `buf[1]`。
  > **根因（测试为什么没抓到）：20+ 单元断言里手搓的 V1 fixture 自己也把 `buf[0]` 设成 `0x08` ——
  > 跟代码里的（错误）假设自证自洽，循环论证，从没有一条走真实 dump 出来的字节验证过。**
  > 处置里就包括：把 fixture 的 `buf[0]` 显式改成**实测值 `0x6b`**，并加两条回归 case 堵死同类循环论证。

⇒ **所以本文件真正的价值不是"我发现了 0x6b"，而是：**
> **一次已经被诊断、已经写进文档、已经改过 fixture 的教训，三周后被另一个人在同一处代码家族原样重犯。**
> 说明**那次加固是"点状"的**（只硬化了 `bshard-payout-family-coherence` 那几条 fixture），
> 而**教训本身没有到达"下一个在这一带写字节谓词的人"**——我从 `p2sh.mjs` 的注释起步，
> 从没路过那份稿；**它躺在 docs/ 里，而我没有任何理由会打开它。**

🔨 **可执行的改法（不是"下次注意"）**：**凡是要写"某字节 == 某常量"的谓词，先跑首字节普查**
（`scripts/tn12-redeem-prefix-census.cjs`，20 秒，直接打脸）。**把"去查实测值"变成一条命令，而不是一条记性。**

## §4 重做时必须先回答的（我不猜）

1. ✅ **已答（@KANet-UI 09:24Z + 我现查）**：**PoolRoot redeem 落在 `payout_shards.payout_redeem_hex`**，
   写入方 `pool-shard-register.mjs` 的 `ensurePayoutShard/V2`，close/consolidate 流程 splice 更新（`DATABASE.md:642-646`）。
   🔵 **且 `buildRefundCommand` 现读【零活调用方】**（全仓 grep，除自身定义外无命中）⇒ 退款轨 runtime **尚未接线**，
   这也解释了为什么"命令里没有 state_start"至今没有任何现网症状。
2. ✅ **已答**：`0x6b` 是**已实测钉死**的 `buf[0]` 值（见 §3-bis），不必再猜。
   ⚠ **但它是不是"模板身份"的正确判据，仍未答** —— 实测值只告诉我"首字节是 0x6b"，
   **没告诉我"首字节 0x6b ⟺ 这是多-entry PoolRoot"**。这两件事差一个双向蕴含，而闸要的是后者。
3. 🔴 **判别符的"充分性"仍未答，而 @Bettor 09:26Z 把它磨得比我准，照收**：
   我写的是「首字节是 0x6b」≠「首字节 0x6b ⟺ 多-entry PoolRoot」；他指出更该改的是**这个谓词的性质**：
   > **(a)** 若退款路径**只跑 PoolRoot（多-entry）**、单-entry continuation 根本不流经此路，
   > 那"恒 start=1"是对的，**但判别符不是在判身份、是在【断言不变量】** ——
   > 应写成「**断言这份 redeem 属 PoolRoot 族（0x6b），否则 fail-closed**」，
   > 而**不是**「根据首字节**选** offset」：后者给人"能选 0"的假象，**却从来选不出**。
   > **(b)** 反之，若单-entry 也可能走这条 builder，那"恒 1"就是错的，判别符必须真的能分辨。
   ⇒ **所以先要答的是"这条路上会不会出现单-entry"，而不是"首字节怎么认"**。
   🔨 **这条值得单独记**：我把一个**断言**写成了**选择器** —— 形状写错会让人以为那里有分支覆盖，
   而实际上那条分支**永远不会被走到**，于是它既不会红、也不会被发现是死的。

4. **识别深度**：只看首字节是否足够？我用例里诚实标了"当前判别深度=一字节"，
   但既然首字节我都猜错了、性质也写错了，**深度问题要连同 ①②③ 一起重答**。

⇒ **在这三条有实证之前，Fix ① 不重写、不提交。** 方向（权威=机器可验的「模板身份→offset」关系）不变，
**变的是"身份怎么认"必须来自制品，而不是来自我对注释的推断。**
