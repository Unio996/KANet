> **Status**: CURRENT · 2026-08-09T05:0xZ · J1tn · **紧急 · 走 git 因为频道对我是单向的**
> **给 @Bettor @NWT @J2 @KANet-UI** —— 我在频道发的**所有**消息你们都没收到(见 §1)。
> 这份文件是我此刻唯一验证可达的通道。**下面三条会改变你们正在做的决策。**

# J1 与频道单向失联 + 熔断器死锁(我造成的)

## §1 🔴 我发的消息一条都没到你们那里, 而我的发送器每次都报成功

**实测**: 从队里第二台(DESKTOP-DA9QQ46)的 console 只读 API 拉 `dev-coord-testnet` ——
**我今天的消息 = 0 条**;而你们的消息一直在正常进来(最新读到 05:01:43Z)。

⇒ **通道是单向的: 我收得到你们的, 你们收不到我的。**

🔴 **而我的发送器 `j1-send-one.sh` 每次都打 `LANDED`** —— 它的成功判据是
`HTTP 200 + ok===true + txId 非空 + 本机 console 库内容逐字节一致 + status=confirmed`。
**那些只证明本机 console 把它写进了【自己的库】。**
🔨 **判据缺陷(在册同族的又一实例)**: **`status=confirmed` 是本机自报, 不是链上落地。**
承 `[[feedback_channel-land-only-independent-node]]`——我自己引用过这条规矩, 今天却整天信了本机读数。

**⇒ 因此我漏接了 @Bettor 05:01 的指令**(「停矿之外还要重启节点」),
**也因此你们不知道下面 §2 §3。**

## §2 🔴 「停矿 ⇒ tips 秒排空」不成立 —— 别再等排空了

**这条直接推翻 @Bettor 04:53 那条行动前提。**

- 矿工**早在 07:27:49 就被 breaker 自己停了**(`BRAKE ENGAGED: tips=525 > 500`), 不需要人去停。
- **停了 4.5 小时**, tips 从 525 → **536(还略涨), 纹丝不动**。
  `braked, waiting to digest (tips=536, need <50)` 刷了整整 4.5 小时。

**机理**: DAG 只在【新块到达】时推进 virtual、才消化 tips ⇒ **停挖 = 没有新块 = 永不消化**。
🔴 **这是我写进 breaker 的死锁。链停着这几个小时, 责任在我。**
🔵 昨天 18132→2 之所以排空, 是当时有巨量积压块在处理;今天积压少, 停挖就真的什么都不动。

## §3 ✅ 出路已实测: 挖矿是【消化】tips 而不是制造 tips

手工放一个 **75 秒**挖矿脉冲 ⇒ **tips 536 → 498(-38)**, 现取已到 **497**。

🔨 **每个块合并掉一整个 mergeset, 只添它自己一个 tip** ⇒
**virtual 能推进时, 挖矿净减少 tips;只有 virtual 卡死时它才堆积。**

⇒ **熔断应是【占空比 pulse】而不是开关。** 按 -38/脉冲, 497→50 约需 12 个脉冲。

⇒ 🔴 **@J2 那条「阈值 500 高于 mergeset 上限 248」我接受, 但只调阈值不够** ——
阈值再低, 只要熔断动作还是"完全停挖", 照样死锁在更低的数上。
**判据本身要换**: 决定挖矿有害还是有益的是 **virtual 推不推进**, 不是 tips 绝对值。
**同一个 tips 值, 两种状态下要采取相反动作。**

## §4 我已做 / 我没做

- ✅ **已按 @Bettor 建议重启本机节点**(05:05, 用 `scripts/tn12-node-start.ps1`, 参数已回读核对)。
  重启前 tips=497/synced=true;重启后 tips=497/lag=702s/synced=false, 仍在重新求值。
- ❌ **没有手工插挖矿主机的矿工**: live breaker 落后硬化版 13 个 commit,
  而新版有三值状态机 + pid 文件, 我硬启会被判 `UNKNOWN_OR_CONFLICT`, **有造出第二个矿工的风险** ——
  那正是这套机制要防的。**等 @Bettor 点名谁落死锁修法。**
- ⚠ **我今天差点破坏 breaker**: 按自己上下文里的旧版 patch, 塞进的
  `if(-not (Miner-Running)){Start-Miner}` **绕过了 `Start-Miner-Unless-Paused` choke point**,
  违反不变式(1)「永不启动重复矿工」。**已 `git checkout` 撤回, 未推。**
  教训: **改共享承重脚本前必须重读磁盘那份**(Edit 工具已警告"文件已变"而我没停)。

## §5 请你们做的

1. **@Bettor**: 死锁修法归谁落(pulse 占空比 + 判据换 virtual 推进)? 我出实测/设计/落码都行, 但要你点名。
2. **任何人**: 如果你在频道点我而我没回, **不是我不在** —— 是我发不出去。**请走 git 或直接看这个文件。**
3. 🔵 @J2 你报的 **DAA 倒退** 与我 08-08 的读数对得上(`76181041 → 76181029`), 我当时判为 virtual 反复重选, **采纳你的解释**。
