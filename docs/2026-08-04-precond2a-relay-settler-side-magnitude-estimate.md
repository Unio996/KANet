# 冻结前置②-a 结构改造 · relay/settler 侧量级估算(J2 半稿)

> **Status**: CURRENT · **DESIGN-ONLY · 零代码零表零开关** — 本稿是**量级估算**,按 Bettor 07:48「开工第一件是出量级估算,不是写码」。待与 J1 oracle 侧半稿(`9a788889`)合并成整稿 → NWT 红队。
> **范围(按 J1 精确化写死,不含糊)**:`②-a ⊂ 线二① 但 ②-a ≠ 线二①`。三权分离在子集② 要两块 —— (i) **签名权收窄 = ②-a**(本稿)+ (ii) **「规则解释」层新造**(D-012 §6-1 明写"需新造的"那块,**不在本稿**)。整卡范围 = ②-a ∪ 规则解释层,**不写成"②-a 顺便覆盖线二"**。

## §0 一句话结论

**relay 侧已经有一个位置正确的闸,但它的判别轴答的是另一个问题;而真正决定验收指标(63→0)的那条路,不是加闸能达到的。** 两条路的量级差一个数量级,选哪条取决于**验收指标认哪一个**。

## §1 事实底座(现读,file:line;不引用转述)

| # | 读数 | 出处 |
|---|---|---|
| 1 | relay 有**统一的命令授权闸位**,在 `validateCommandPayload` 之后、`switch (cmd.type)` **之前** | `kasia-relay/src/relay.mjs:362` `const authz = authorizeCommand(cmd);` |
| 2 | 该闸**当前 armed=off,inert 放行**(=现状,零新暴露面) | `kasia-relay/src/lib/authorize.mjs:70-75` `GATE_ARMED` 假时直接 `allow`,reason_code `GATE_INERT_ARMED_OFF` |
| 3 | 🔴 **它的判别轴是 `origin`,不是角色** | `authorize.mjs:66` `const origin = cmd?.__origin;` · origin 四值 = `internal` / `app` / `operator` / 缺失 |
| 4 | 拒绝时**已有结构化回执**(phase/reason_code),不必新造 | `relay.mjs:364-379`,`phase='authorization'` |
| 5 | 🔴 **relay 进程不知道自己是不是 oracle** —— 全文件无 `is_oracle`/role 概念;启动期身份只有 `RELAY_MODE`(默认 `indexer`) | `relay.mjs:9`;`grep is_oracle kasia-relay/src/relay.mjs` = 0 命中 |
| 6 | `is_oracle` 是 **console DB 的列**,不是 relay 自知的属性 | `relay_nodes.is_oracle`(console 侧) |
| 7 | 通用签名原语**只认 message**,零策略零域检查 | `commands.mjs:130` `[ECDSA_SIGN]: ['message']`;`:204` `{ message: 'string' }` |
| 8 | 结算签名原语只认 `tx_hex`+`input_index` | `commands.mjs:132/206` `[SIGN_INPUT_FOR_SETTLE]` |
| 9 | 调用面(J1 重数,本稿采用新数): `sign_input_for_settle` **11 处/6 文件**;`ecdsa_sign` **10 处/7 文件** | J1 `9a788889` |
| 10 | 量级基线(Bettor 亲跑,全量非抽样): **63 个市场本机单机可取 ≥threshold 签名,其中 41 个是 5/5** | Bettor `#ed8v6x` |

## §2 🔴 最要紧的一条:现成那个闸,armed 了也**不产生**②-a 要的性质

②-a 要的性质是「**oracle 角色够不到通用交易签名入口**」。而 `authorizeCommand` 判的是「**这条命令从哪来**」:

- driver(settler/自治 daemon)发出的命令,`origin` 正是 **`internal`** —— 而 internal 在设计里是**放行**的。
- ⇒ **把 M0c-1 闸 arm 起来,driver 照样能对每个本机 relay 要 `sign_input_for_settle`。** 那正是今天 (135) 记的"PB-S8-1 不覆盖 driver 路"的同一个洞。
- 🔨 **判词**:**闸位是对的(唯一、在 switch 之前、fail-closed 管道齐全),判别轴是错的。** 这不是"闸没 arm"的问题,arm 了也不解决 —— **别把 M0c-1 的 arming 记成 ②-a 的一部分进度**。

## §3 两条路,量级差一个数量级

### 路 A:签名入口按角色收权(在现有闸位上扩判别轴)

**要做的**:让判据从 `origin` 扩到 `(origin, 本 relay 的角色, 命令类)`,即 relay 必须**知道自己是什么角色**,并对不属于该角色的签名类命令拒绝。

**必须新造的东西(这是量级的主体,不是"改几行 if")**:
1. **relay 自知角色**:今天不存在(§1-5)。要给每个 relay 进程一个**启动期角色声明**(env/配置),且它必须**不可被调用方覆盖**(否则 caller 说自己是 oracle 就是 oracle,等于没做)。
2. **角色→命令类映射表**:哪些角色可以要哪类签名。需与 `commands.mjs` 的命令表对齐,并**随新命令自动收紧**(新命令默认拒,不是默认放)。
3. **信任来源问题(🔴 本路最难的一格,不是工程量是语义)**:「**谁说这个 relay 是 oracle**」。今天 `is_oracle` 在 console DB 里,而 console 正是调用方 —— **让调用方来定义"被调方是什么角色",在威胁模型上是空的**。⇒ 要么角色来自**进程外部的配置/密钥托管**,要么这条路只挡"手滑"不挡"作恶"。
4. 迁移面:11+10 = **21 个调用点**要按新语义分类(哪些走 attest 类、哪些走 settle 类),否则 arm 即全线拒。

**量级判断**:**中等偏大**。工程上是"扩一个已有闸 + 一张表 + 21 个调用点分类";但第 3 条把它从工程问题变成**信任边界设计问题**,而那部分与 J1 的 FactReceipt/typed 入口线**是同一块**(§7-2「不动旧原语、新增类型受限入口」)。**⇒ 建议:路 A 不单独做,并进 J1 那条线一起设计,否则会造出第二套授权语义。**

### 路 B:密钥隔离(让同机凑不齐 threshold)

**要做的**:把同一市场 committee 的多把私钥**分散到不同宿主**(或不同进程边界+独立密钥托管),使任何单机都拿不到 ≥threshold。

**量级判断**:**大,且主要不在代码里** —— 是部署拓扑、密钥托管、跨节点运维。代码侧改动可能很小(甚至为零),但**运维与信任模型改动最大**。

## §4 🔴 验收指标只绑其中一条 —— 这条必须让 Bettor/NWT 先裁

Bettor 给的验收指标:**改造后重跑判据 B 脚本,「本机可签 ≥threshold 的市场数」必须从 63 降到 0。**

- **路 B 能达到 0。**(钥匙真的不在同一台机上了。)
- 🔴 **路 A 达不到 0,而且永远达不到** —— 因为路 A **不移动钥匙**:63 个市场的 4~5 把委员私钥仍在同一台机上,判据 B 那个脚本(用生产谓词派生 pk 比对)读数**一个都不会变**。路 A 改变的是"一个**守规矩的**调用方能不能要到那些签名",不是"这台机器**有没有能力**产出那些签名"。
- ⇒ **两件事必须拆开说**,否则我们会做完路 A 然后发现验收指标一动不动,或者更糟——**改口径去迁就已经做完的工作**:
  - **能力面**(machine can produce ≥threshold sigs)⇒ 只有路 B 动得了 ⇒ **63→0 是路 B 的指标**。
  - **可达面**(a caller can obtain them through supported entries)⇒ 路 A 动得了 ⇒ 它需要**自己的指标**,例如「21 个调用点中,能在不声明角色的情况下取得 settle 类签名的调用点数 → 0」。
- 📌 **我的建议(不自行拍)**:**两个指标都写进卡,分别归属**;并且**不许**用路 A 的完成去宣称 63→0。

## §5 我明标做不到 / 未查的(不假装齐)

1. **路 B 的运维代价我估不了** —— 跨宿主密钥托管、节点数、谁运维,这些在 Owner/运维域,不在我域。本稿只说"代码侧可能很小,非代码侧最大"。
2. **`__origin` 今天的标注覆盖率我没测** —— `authorize.mjs:73` 有"未标 origin"的 warn 路径,说明存在未标调用方;arm 之前需要一次覆盖率盘点(属路 A 的前置)。
3. **21 个调用点的逐个分类我没做**(J1 只给了计数)。这是路 A 真正的工作量所在,必须逐个看,不能按文件名归类。
4. **规则解释层(线二①的另一半)完全不在本稿** —— 按 J1 精确化,它与 ②-a 并列,不是被覆盖。

## §6 建议的下一步(供合并整稿时用)

1. **先裁 §4 那两个指标的拆分**(不裁的话后面所有进度记账都会含混)。
2. 路 A 的设计**并进 J1 的 typed 受限入口线**,不另起。
3. 路 B 单独立卡,**它是唯一能满足 63→0 的那条**,且主要代价在运维 —— 需要 Owner 参与定夺,不是技术选型。

## §7 「复用既有闸」的具体形态 + 换判别轴会不会碰坏它现在挡的东西(答 Bettor 08:14)

> Bettor 要求写成"复用既有闸"而非"从零造闸",并附一句**换判别轴对现有拦截行为的影响** —— 即"别修一处碰坏另一处"。以下是实读 `authorize.mjs:77-110` 后的逐支回答。

### §7.1 形态:**加一根轴,不是换一根轴**

现闸的判据是 `origin` 的四值分派。②-a 要的不是把它换成角色,而是**在放行分支里再串一道角色判定**:

```
authorizeCommand(cmd):
    origin 判定(现状, 一个字不动)
      ├─ deny  ⇒ 直接拒(现状)
      └─ allow ⇒ 🆕 若 cmd.type ∈ 签名类 ⇒ 再问一句:本 relay 的角色允许这一类吗?
                     └─ 不允许 ⇒ deny(新增, reason_code 另立)
```
🔵 **这样写的两个好处**:① 现有四支分派的**行为逐字不变**(只在 allow 之后追加,不改任何一支的判定);② 新增拒绝有**自己的 reason_code**,与 origin 类拒绝在回执上分得开 —— 排障时"为什么被拒"不会两种原因糊成一个。

### §7.2 逐支回答:会不会碰坏它现在按 origin 挡的东西

| 现有分支 | 现在的行为 | 加角色轴后 | 会不会坏 |
|---|---|---|---|
| `origin=internal` | 放行(乙路 TCB) | 放行 → **再过角色判定** | 🔴 **这一支正是 ②-a 的靶心**:driver 就是 internal。**行为会变,而这是目的不是副作用** ⇒ 必须逐个核 11+10 个调用点,否则 arm 即全线拒 |
| `origin=operator` | 放行(operator-settle 端点白名单+auth+IP) | 放行 → 再过角色判定 | ⚠ **需要一次决定**:operator 专道是**人类运维**通道。若角色判定把它也挡了,会挡掉合法的人工结算。**建议 operator 走显式豁免**,但豁免要写死理由,不能默认 |
| `origin=app` | 走完整 grant/envelope 验证 | 不变 → 之后再过角色判定 | 🔵 **天然相容**:app 路本来就是最严的一条,再加一道只会更严;且 `READONLY_ALLOWLIST` 豁免的是只读命令,不含签名类 ⇒ **不受影响** |
| `origin=legacy-unmigrated` | 暂放行 + LOUD warn(迁移债 marker,**自陈非安全控制**) | 放行 → 再过角色判定 | 🔴 **这一支会被角色轴收紧,而这大概率是好事** —— 它自陈"非安全控制、目标 shrink 到零"。但**收紧它等于提前触发那批迁移债** ⇒ 量级要算进去,不能当免费 |
| origin 缺失/非法 | **fail-closed 拒** | 不变 | ✅ 不受影响 |

### §7.3 🔴 一条我实读时撞到、必须写进卡的既有假设

`authorize.mjs:85-92` 注释里有一条**单来源假设**,逐字:「`origin=operator` 的信任前提 = 全仓**唯一** `sendCommandAsync(...,'operator')` 调用点在 `operator-settle.js:72`……**若未来新增第二个 origin=operator 调用点,必须同步**……否则单来源假设被静默打破」。

⇒ **这与 ②-a 直接相关**:如果 §7.2 里 operator 走豁免,那么「谁能发 operator 命令」就成了角色收权的一个**缺口**,而它的守护今天**只是一条注释 + 一次 grep**。
🔨 **判据(与今天全队数了一整天的那族同形)**:**"唯一来源"是一个需要被持续验证的性质,而它现在的守护是"希望下一个人读到这条注释"。** ⇒ ②-a 若依赖 operator 豁免,**必须同时把这条单来源假设变成机制**(如 lint 规则:全仓 `origin: 'operator'` 调用点数 > 1 即报错),否则我们是在一个未被机制守护的假设上盖房子。
