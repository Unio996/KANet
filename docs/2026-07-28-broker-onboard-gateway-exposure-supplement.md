# broker onboard 上公网口 — 补充设计 v0.2(§③ 第二轮·NWT MUST-FIX 已吸收·零落码)

> **Status**: CURRENT — **§二 的分轴已被 §六 取代**(NWT 2026-07-28 MUST-FIX)。v0.1 原文保留在 §二,不抹掉。
> 依据:NWT 2026-07-28 §③ 红队打回(MUST-FIX B + note-1/2)· Bettor 批"出稿→NWT 审→我再批→才上公网口"
> owner KANet-UI · 审 NWT(它动**对外暴露面**)
> 🔴 **它是 ③ 现在【唯一】的闸**(Bettor 07:28:①②③④ PASS · ⑥ 部分不阻塞 · ⑤ 已剥给 ③ 自己)

## 一、先把这个端点【会做什么】写清楚 —— 这是 §③ 原稿最大的缺口

NWT 的原话:**「§③ 整节只回答了『别把另外 8 条带上去』,没有回答『那 1 条自己上去之后,外面的人拿到了什么』。」**
实读 `onboard`,**每一个匿名请求**会让我们:
```
① 写库: INSERT/UPDATE broker_onboarding
② 🔴 托管一份【第三方密钥】(bot_token 加密落库)——【仅当调用方提供了 token】
③ 🔴 对 api.telegram.org 发一次【出向请求】,每次最多占住一个连接 8 秒 —— 同样仅当提供了 token
④ 间接: 有 token 的记录会让 broker-bot-manager 【fork 一个进程】
```
🔵 **而 ①② 落地后有两格已经变好,要说准(不然会高估风险)**:
- ~~onboard 写 `identities … trust_level='recommended'`~~ ⇒ **已删**。NWT 那条"匿名自助提权"(recommended 在消息闸上 120/时 + 多两档 authority)**已断**,且他把它列为 MUST-FIX A 的理由 —— 该前置**已达成并验收**。
- `bot_token` 变可选 ⇒ **不带 token 的请求根本不触发 ②③④** ⇒ 攻击者要撬动那三样,**必须自己交一个 token 串**。
🔴 **而这不构成安全** —— 交一个字符串是零成本。**所以真正的控制是下面三样,不是"它需要 token"。**

## 二、三样资源控制(NWT note-1;而我按仓里已有形态选实现,不新加依赖)

### ② bodyLimit —— 最便宜的一条
```
【实读】external-gateway.mjs:141  Fastify({ logger:false, exposeHeadRoutes:false })
⇒ 未设 bodyLimit ⇒ 用 Fastify 默认 1 MiB
🔨 改: 在这个【独立实例】上设 bodyLimit = 8 KiB
🔵 它作用于该口全部路由 —— 而该口的路由本来就只有两条(读频道 + onboard), 请求体都是几百字节量级
🔴 判据不是"设了", 是: 超限请求必须收到 413 而不是被静默截断
```

### ① 每 IP 限频 —— 复用仓里已有的两套形态之一,不装插件
```
【实读】仓里没有 @fastify/rate-limit 依赖。而已有两套自造形态:
  · capability.js:62  SQLite 事务里滑动窗口(按 grantId)—— 持久, 但每请求一次 DB 写
  · mind-manager.js:220 RATE_LIMITS 内存表(按 relation)—— 零 IO
🔨 选【内存滑动窗口(按 IP)】, 理由: 这个口是防滥用不是防欺诈, 且它在【独立实例】上, 不共享主实例状态
🔴 而它的边界必须写进码: 进程重启即清零 ⇒ 它挡的是持续刷, 不是"总量配额"
🔨 建议初值: 每 IP 10 次/小时(与 mind 侧 stranger 同档 —— 同一个语义: 我们不认识的人)
🔴 超限回 429, 且【带 Retry-After】—— 否则集成方只能靠猜
```

### ③ 并发上限 —— 它针对的是"慢速占用"那一条
```
🔴 NWT 抓的那格: 每个带 token 的请求占住一个连接【最多 8 秒】(getMe 的 AbortSignal.timeout)
⇒ 无并发上限时它是一条现成的慢速占用路径
🔨 改: 该口维持一个 in-flight 计数, 超过 N(建议 4)立即回 503 + Retry-After, 不排队
🔵 不排队是刻意的: 排队会把"拒绝"变成"更长的占用", 那正是要防的东西
```

## 三、`why` 字符串订正(NWT MUST-FIX B —— 而它不是文案题)

```
【实读】external-gateway.mjs 白名单条目的 why:
  「读链上已公开的消息。不撮合·不托管·不定价·不审核参与者(无任何我们发放的凭证)。」
🔴 加了 onboard 之后: 这个口【开始托管密钥】(bot_token)、【开始接收参与者】
⇒ ⇒ 那句自述【当场变假】, 而全仓其它地方还在引它当这个口的定性
```
🔨 **订正方向(措辞归审,但它必须同批改,不是顺手)**:
- 该条目的 `why` 要**逐条对齐 `docs/KANet-Positioning.md:78` 那四条原文**说清:onboard **不撮合、不定价**;而**"不托管"这一条对它不成立** —— 它托管一份第三方通知渠道的密钥。
- 🔴 **而模块头那段 deny 清单也要跟着看一眼**:它把 `POST /api/agent/create 铸身份` 列为"不放上来"的例子。
  ✅ 而 onboard **在 ② 之后已经不写 `identities`** ⇒ 它不再是"铸身份"的一种形态 ⇒ **NWT 那条反对已被 ② 消解**,这一格要写进注释,否则下一个人会以为我们无视了它。

## 四、验收(含从 ② 剥过来的 ⑤,每条带对照臂)

```
⑤ 白名单只放行那一条:
   从公网口打 onboard ⇒ 通; 打同文件其余 8 条中任意一条 ⇒ 【连不上】
   🔵 对照臂(不许省): 打一条【已知在白名单里】的路由(读公开频道)⇒ 必须通
      ⇒ 否则"全都连不上"与"网关没起来"读数相同
   🔵 而填错不会静默: 白名单要求【排序后完全相等】⇒ 多出的 8 条会让网关起不来并逐条列出
      —— 这是【失败会喊】的少数例子, 与今天大多数相反
⑦ bodyLimit: 超限请求 ⇒ 413。对照臂: 正常大小请求 ⇒ 仍 200
⑧ 限频: 同一 IP 连打超限 ⇒ 429 + Retry-After。🔴 对照臂: 换一个 IP(或等窗口过)⇒ 必须放行
   ⇒ 否则"全都 429"与"这个口坏了"读数相同
⑨ 并发: 并发发起 N+2 个带 token 的请求 ⇒ 至少 2 个立刻 503(不是等 8 秒后超时)
   🔴 判据是【立刻】: 若它们也等了 8 秒, 说明是排队不是拒绝 —— 那正是要防的
```

## 五、我填不了 / 不打算填的格

- 🔴 **具体数值(10/时 · 8 KiB · 并发 4)是我拍的第一版,没有实测支撑** —— 它们该被审的是"量级对不对",而**我不假装它们是测出来的**。
- 🔴 **公网侧是否真的可达,今天仍未知**(暴露面盘存卡 §2 那一格)⇒ 本设计**不依赖**它:三样控制在内网同样生效。
- 🟡 **NWT 另抓的那条(现有那条公开路由的 `until`/`until_id` 未校验、喂给前置通配 LIKE)**:他已归给我那张暴露面盘存卡,**属"已存在的暴露"不是本次新增** ⇒ 不并进本稿,免得两件事混成一件。

---

## 六、v0.2 —— MUST-FIX 吸收:**我"别混成一件"的轴选错了**(取代 §二 的分轴)

### 🔴 我错在哪(精确,不含糊)

v0.1 §五 我写:NWT 抓的那条(现有那条公开路由的 `until`/`until_id` 未校验、喂给前置通配 `LIKE`)
**归暴露面盘存卡,不并进本稿,免得两件事混成一件。**
```
🔴 而"别混"这个直觉是对的, 我把它用在了【错的轴】上:
   我按【谁的缺陷 / 哪条路由】分 —— 那是【归属】轴
   而本稿在设计的是【这个口的资源天花板】—— 那是【资源】轴, 它按【口】走不按路由走
⇒ ⇒ 🔴 于是我给 onboard 的 8 秒占用加了并发上限, 而【同一个口上】另一条路由能触发
     不可索引的全表扫描、且没有任何上限 ⇒ 这个口的天花板【仍由没被保护的那条决定】
⇒ ⇒ onboard 的帽子挡不住它。我保护了一条路由, 而攻击面是一个口。
```
🔨 **正确的分轴(NWT 给的,我原样采用)**:**资源按【口】· 契约按【路由】。**
- **资源**(bodyLimit / 限频 / 并发)⇒ 做在**实例级**,覆盖该口**全部**路由 ⇒ 本稿管。
- **契约**(`until`/`until_id` 该不该校验、错了能不能从响应看出来)⇒ 按路由 ⇒ 仍归暴露面盘存卡。
🔵 **两件事仍然是两件事 —— 只是切线换了个方向。**

### 🔨 于是三样控制改成实例级(取代 §二)

| 控制 | 作用域 | 值 | 关键约束 |
|---|---|---|---|
| bodyLimit | **实例级**(Fastify 构造参数,本来就是) | 8 KiB | 超限必须 **413**,不是静默截断 |
| 每 IP 限频 | **实例级 preHandler**,覆盖全口 | **两档**:写入面(onboard)10/时 · 读频道面另给一档 | 🔴 两档都要 —— 只给 onboard 会让读频道吃 10/时,那对一个**本来就要给外面用**的读接口太紧 |
| 并发上限 | **实例级** in-flight 计数 | 4 | 超出**立即** 503 + Retry-After,**不排队** |

### 🔴 三个 note,全部要求写进【码】而不是只写在文档里

```
note-1 「每 IP」挂在一个环境事实上: 该实例【没有设 trustProxy】⇒ 对端就是真实来源
   🔴 而它依赖"这个口前面没有反代" ⇒ 这句要写在那个控制【旁边】
   🔨 且实现【显式读 request.socket.remoteAddress】, 不读 request.ip
      —— 后者能被一个配置开关悄悄改掉(同 NWT 回环断言那份的同一条理由)
note-2 内存计数【重启即重置】⇒ 而这台今天 07:19 才重启过, 且 supervisor 会在探针失败时拉它
   ⇒ 实际窗口是"自上次重启以来" ⇒ 额度可以靠【等一次重启】重置
   🔨 不改成持久化(那要每请求一次 DB 写, 而这个口就是为了便宜)
   🔴 但必须把这句写进码: 【它是抗持续刷的, 不是配额】
note-3 分布式流量下每 IP 限频无效 ⇒ 顶到并发上限那一层, 而那一层是共享的
   ⇒ 🔴 攻击持续时【正当注册者也会拿到 503】
   🔵 这是【刻意的取舍】: 对所有人 fail-closed 好过资源耗尽
   🔴 而它必须被写下来 —— 否则半年后有人看到"正当用户被 503"就去把并发上限拿掉,
     而那正是它存在的理由
```

### ✅ 验收补一条(因为作用域从路由变成了口)

```
⑩ 实例级生效验证: 对【读频道那条路由】也打超限/超体积 ⇒ 必须同样被挡
   🔴 对照臂: 正常大小、正常频率的读频道请求 ⇒ 必须仍 200
   ⇒ 否则"控制生效"与"这个口坏了"读数相同
```

---

## 七、落码时会咬人的两格(NWT 随 GREEN 附,**现在写比 diff 审时便宜**)

### 🔴 ① in-flight 计数器【泄漏】—— 而它的症状与"正在被攻击"读数完全相同

```
🔴 若只在【正常响应】时减计数: 客户端在那 8 秒里断开是【常态不是边角】
   ⇒ 几个中途断开的请求就【永久吃掉】那 4 个槽位
   ⇒ ⇒ 之后所有请求恒 503 —— 而【恒 503】与【正在被攻击】长得一模一样
🔨 实现: 减计数必须挂在【请求终结】的所有出口上(正常响应 / 错误 / 客户端断开),
   而不是只挂在成功那一支 —— Fastify 的 onResponse + 连接 close 都要覆盖
```
🔴 **验收补一臂(不只写进码,要进验收表)**:
```
⑪ 计数器不泄漏: 发 N 个请求并【在中途全部 abort】⇒ 等一会儿 ⇒ 再发一个正常请求【必须通】
   🔵 这一臂才是"没泄漏"的证明 —— 而只测"超出会 503"【测不到它】
   🔴 因为泄漏之后, "超出会 503"这一条【照样是绿的】
```

### 🟡 ② 钩子选 `onRequest` 而不是 `preHandler`(优化,非 MUST-FIX)

```
preHandler 在【路由匹配 + body 解析之后】才跑
🔵 今天无害: bodyLimit 8 KiB 已经把解析代价夹死了
🔨 而 onRequest 是最早的钩子, 同样免费 ⇒ 顺手就用它
🔵 理由: 限频与并发都属于【在花任何代价之前就该判】的那一类
```

---

## 八、风险 · 已知背景(Bettor 批 §③ 时补 · 本节正文由他 07:46 逐字给定为「定稿」)

> 🔵 **本节正文我逐字照录立卡人的定稿**,不改写、不精简。
> 而【它是怎么变成这一版的】另起 §8.1,**不混进他的文本** —— 免得两者被读成同一个人的同一句话。

```
### 风险 · 已知背景

✅ 该口与 console 是【同一个进程】(netstat 实测同 PID)
   ⇒ 独立 Fastify 实例给的是【路由隔离】, 不是【进程隔离】—— 连接 / 事件循环 / fd 全共享

✅ 该进程存在【已知的、至少持续 48 天的间歇性不可用】:
   · 已观测到两种表现, 🔴 而它们分别来自【不同的客户端形态】——
     慢到超时(探针: GET + 4s 短超时)· 连接被拒(发送器: POST)
     ⇒ 🔴 不可读成"服务端在两种模式间切换": 我们还不知道是不是这样
   · 失败时进程始终活着(PID 在 · 数秒后自行恢复)

🔴 病因【未定】。以下说法都不成立, 请勿引用:
   ❌「连接数逼近 backlog 天花板」—— established 与 backlog 是【两个不同的量】,
      且实测出现过 601 established 而系统未塌(若同限, 601 不该存在)
   ❌ 任何由单次观测算出的比例(如 483/511)——
      🔴 它骗人不只因为 n=1, 是因为【分子分母不是同一类东西】, 这个除法本身没有意义

🔴 对 §③ 的意义(两句都要, 少一句就会被读成极端):
   · 三样控制【不解决】它 · 也【不被】它否决(网关边际贡献被并发 4 × 最多 8 秒夹死)
🔴 上线后若有人看到公网口偶发连不上: 它可能是这个老毛病, 与"我们的限流生效了"【读数相同】
```

### 8.1 这一版是怎么来的(过程留档,不抹掉)

```
初稿(立卡人给, 我照写): 「此刻已经在【连接数天花板附近】间歇性失败」
  ① J2 三个样本 483 / 601 / 52 ⇒ 他与立卡人各自读成【相关性不够】= 证据不足
  ② 🔴 NWT 更上游: established 与 backlog 是【两个不同的量】⇒ 并排本身是【范畴错误】
     最干净的反证: 601 > 511 而系统没塌 —— 若同限, 601 不该存在
  ③ 立卡人收 NWT 那一档并自撤: 「不是相关性不够, 是那句话问错了问题」
  ④ J2 再收一格: 「错误类型至少部分是客户端形态的函数」这句是【限定词】不是并列项 ——
     另起一行 = 它会被单独引用 ⇒ 只读第一条的人会以为服务端在两种模式间切换
🔵 而两类的修法【相反】: 证据不足加样本可能翻案; 范畴错误【加多少样本都救不回来】
🔨 由此得的顺序(J2 拟, 立卡人升进判据清单):
   「看到一个比例, 先问【上下是不是同一把尺】, 再问【几个样本】。顺序是这个。」
   🔴 反过来问, 会先花力气去补样本 —— 而一个量错了对象, 加样本只会让错的结论更自信
🔴 而立卡人自记的那格: 三个人各错一次, 他排在最后 ——
   前两位撤的是【自己说过的话】, 他撤的是【已经进了稿、且他要求别人补进去的话】
   ⇒ 一句话进了文档就换了性质: 从「某人的判断」变成「下一个人的前提」
   ⇒ 所以【要求别人写进稿的话】, 强度门槛要比【自己说说】高一档
```

