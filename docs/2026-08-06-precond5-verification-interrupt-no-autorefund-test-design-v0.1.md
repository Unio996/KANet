# D-012 冻结前置⑤ · 「验证中断不会把市场路由进自动退款」用例设计稿 v0.2【DESIGN-ONLY·未落码】

> **Status**: DRAFT · 待 @NWT 红队 + @Bettor 裁 · **零代码 · 零 DB 写 · 零链上 · 无 money-path 授权含义**
> **作者**: J2(⑤ owner)· 2026-08-06
> **上游**: D-012 §2-bis 冻结前置⑤ 预注册原文 · 设计底座 `docs/2026-08-04-p1-cannot-verify-is-not-refund-authorization-design.md` §5(NWT 2026-08-04 19:14 设计层 GREEN)
> **P1 状态不受本稿影响**: **P1 OPEN · D4 BLOCKED · 无授权**。本稿造的是**证据**, 不是**授权**。
>
> ### 🔵 v0.2 变更(2026-08-06 · 均为落别人的裁定, 无我自己的新论点)
> - **§3-bis 场景映射表** —— @Bettor 对 N3/N4 两格的裁定(预注册原文四场景**不改**、稿内以映射表处理、**映射是否守住判别空间归 NWT 七条审终判**)
> - **§3-bis 第四列措辞** —— @NWT 必修项: N3 那格由"判别空间被承担住了"改为**未验证的已知缺口, 非已证等价**
> - **§3.4-bis 仪器阳性臂 I0**(本次新增) —— 我拿 NWT 七条正本自查时咬到**第一条**(工具先验证才能信结果), @Bettor 批
>
> ⚠ **文件名仍是 `-v0.1.md`**: 改名会撞 doc-lint 的"同名多路径"规则, 且会让今天所有引用过它的频道消息与 commit 里的路径失效。
> **⇒ 以本标题的版本号为准, 文件名只是路径。**(🔵 这一条我写出来, 是因为我半小时前刚在合稿上修过同族的版本漂移 ——
> 而这一份**我自己也漂了一次**: 标题升到 v0.2 时, 这里还留着"仍 v0.1"。**判据用在别人稿上很顺, 用在自己稿上又漏了一次。**)
>
> 🔴 **本稿有三个地方是【申请】不是【决定】**, 先列出来免得读者当成已定:
> ① §2 的形态偏离 —— **等价与否归 @NWT 判**, 我只给理由(@Bettor 2026-08-06 明纠我越界自裁)
> ② §6 的 allowlist 增项 —— 走「改 spec + NWT 审」, **我不就地加**
> ③ §4 里两条我认为**在本 harness 里做不成**的臂 —— 我给理由, 该不该因此改预注册原文, 不是我拍

---

## §1 预注册原文(逐字引, 不转述 —— 验收对照物就是它)

> 出处 `docs/2026-08-04-p1-cannot-verify-is-not-refund-authorization-design.md` §5:

```
**要证的命题**:**验证中断 ⇒ 市场不得自动进入退款广播路径。**

- **形态**:handler/服务级,**真 import 真调用**(照卡② pbs8-signreq-byzantine-handler.test.mjs
  的形状,不是 SQL 重放 —— Codex 打过这一条)。
- **断言的是"零调用"**:mock `dispatchRefund` 与广播 IPC,断言在下列每个场景里**调用次数 = 0**,
  且市场落在 `unresolved_needs_authorization`:
  1. quorum 永不可达(委员票缺)+ 超 grace
  2. `collecting_sigs` 超时 + `sigCount<4`
  3. RPC 全失败(PB-S8-2 锚点②查不到)导致持续拒签 + 超 grace
  4. 本地 ingest 缺失造成的**假**"签名不足"(签名实际存在但本机不知道)
- 🔴 **必须配阳性对照**:`bettors_absent` 与 `committee_affirmative_unjudgeable` 两个场景里,
  `dispatchRefund` **调用次数必须 = 1**。证明 mock 够得到它、"零调用"不是因为根本没接上。
- 🔴 **判别式**:把新守卫拆掉重跑,**红的必须是这几条用例自己**;红在别处 = 名字在说谎。
- **可跑性**:文件头写死完整命令;**并如实标注它在不在 runner 扫描面内**。
```

🔴 **(140) 已写死**: ⑤ 的完成判据 = **上面这段原文** + NWT 七条,**不许由"做过的东西"反推**。本稿全程按这条走。

---

## §2 🔴 与 §5 要求形态的偏离与理由(@Bettor 2026-08-06 要求单开此节)

### 2.1 偏离一处, 且只有一处

| | §5 原文要求 | 本稿提议 |
|---|---|---|
| 命题 | 验证中断 ⇒ 市场不得自动进入退款广播路径 | **一字未动** |
| 观察方式 | **mock** `dispatchRefund` 与广播 IPC ⇒ 断言**调用次数 = 0** | 驱动**真实** `poolSettlerTick` ⇒ 断言**落库痕迹为零** |
| 终态断言 | 市场落 `unresolved_needs_authorization` | **一字未动** |

### 2.2 为什么原形态在本仓做不成(结构性, 不是"我嫌麻烦")

```
dispatchRefund 是 export 的(pool-market-settler.js:2724)
🔴 但 settler 内部两个调用点(:1241 / :1384)调的是【模块内本地绑定】, 不是导出的那个;
   ESM 导出是只读 live binding —— 外部 import 改不动它, 内部调用也永远不看外部。
⇒ 只剩 loader hook(--experimental-loader)一条路。
🔴 而那与本仓测试框架的设计意图正面冲突, runner.mjs:737 逐字:
   「万能口等于给测试框架开一条绕过一切声明式约束的路, 且会把 cases/ 的信任级别
     提到与生产代码同级 —— 而 cases/ 的准入门槛远低于生产代码。」
```
🔵 **⇒ §5 那句"mock 一下"写于本仓有 `call_module_export` 之前, 没人验过它在本仓成不成立。今天验了: 不成立。**

### 2.3 我能给的理由 / 我不能下的判断

- **我给的理由**:「调用数 = 0」证的是**没调那个函数**;⑤ 要证的是**没被路由进自动退款**。落库痕迹为零与后一句的距离更近。
- 🔴 **我不下的判断**:「更近」是不是「**够格顶替预注册原文的形态**」——**归 @NWT**。
  **理由(我自己认的)**: 预注册判据的全部价值就在于**它不能被执行者事后重定义**;我若自评等价并静默替换, 那条判据就白写了。
- 📌 **处置(@Bettor 2026-08-06 定)**: 本节 + `p1-...-design.md` 开 **v0.5 变更块**同步修订**要求形态**, 标 **【要求形态修订·命题一字未动】**, **原文保留不删**。

---

## §3 断言表(六臂 · 全部 DB 可观察 · 零 mock)

**驱动方式**: 用例种 fixture → 调一次**真实** `poolSettlerTick()` → 读 DB 断言。
✅ **前提当场实测(非假设)**: `test-framework/data/test-console.db` 的 `pool_markets` = **0 行** ⇒ 一次 tick 只处理本用例种下的盘, 不会顺带碰别的。

### 3.1 四条阴性臂(验证中断 ⇒ 零退款痕迹)

| 臂 | fixture | 断言(全部必须成立) |
|---|---|---|
| **N1** quorum 永不可达 + 超 grace | `verifying` · deadline 已过 · 委员票不足 | ① `protocol_status == 'unresolved_needs_authorization'` ② `metadata.refund_tx_obj` 不存在 ③ `metadata.refund_dispatched_at` 不存在 ④ 该市场零条 `bettor_refund_available` chain_event ⑤ `metadata.evidence_gap` 存在(证明**是被冻结逻辑处置的**, 不是"根本没被处理") |
| **N2** `collecting_sigs` 超时 + `sigCount<4` | `collecting_sigs` · phase2 age 超阈 · `pool_oracle_tx_sig` 事件 < 4 | 同 N1 五条 |
| ~~**N3**~~ RPC 全失败 ⇒ 持续拒签 + 超 grace | —— | 🔴 **不设独立臂**, 降为**全臂共享的环境前提** —— 理由 §4.1 · 落位 **§3-bis 映射表**(@Bettor 2026-08-06 裁定) |
| ~~**N4**~~ 本地 ingest 缺失造成的**假**"签名不足" | 与 N2 **完全相同**的本机可见状态 | 🔴 **不设独立臂**, 降为 **N2 的附加断言 A4**(冻结 reason 必须带"本机可见"作用域限定)—— 理由 §4.2 · 落位 **§3-bis** |

### 3.2 两条阳性对照臂(证明"零痕迹"不是因为根本没接上)

| 臂 | fixture | 断言 | 🔴 强度标注 |
|---|---|---|---|
| **P1** `bettors_absent` | 0 注市场 · 过 deadline | ① **不**落 `unresolved_needs_authorization` ② 授权闸**放行**(`refund_authorization == 'bettors_absent'` 落库) | ⚠ **只能证"闸放行了", 证不了"退款真被构造出来"** —— 见 §4.3 |
| **P2** `committee_affirmative_unjudgeable` | 委员肯定但不可裁 | 同 P1 形状 | 同上 |

### 3.3 🔴 前提断言:不许只断"零行"(@Bettor 2026-08-06 硬要求)

在册判据:**「没有出现 X」只有在【产生 X 的语句在本窗口内会执行】时才有意义。**
⇒ 本用例**不靠事后想起来**补这一句, 而是**把它做成断言表里的一行**:

```
A0(前提断言, 与四条阴性臂同一次 tick 内):
    四条阴性臂落 unresolved_needs_authorization  ∧  两条阳性臂【不】落
⇒ 分岔本身就是阳性证据: 证明 tick 实跑到了判定点、且判定点确实按授权分流。
🔴 A0 不成立时, 四条阴性臂的"零痕迹"一律【不作数】(而不是记为通过)。
```
🔵 **为什么这条比"再加一个断言"重要**: 若 tick 因任何原因根本没跑到判定点(fixture 写错 / 提前 return / 异常被吞), **六臂会一起"零痕迹"**, 而那读起来与"全部正确挡住"**完全相同**。A0 是唯一能把这两者分开的东西。

### 🔴 3.4-bis 仪器阳性臂 I0 —— 验的是【读痕迹的那个查询】, 不是行为(v0.2 新增)

> **来源**: 我拿 NWT 七条正本自查本稿时咬到的**第一条**(工具先验证才能信结果)。**@Bettor 2026-08-06 批。**
> **为什么它必须单独存在**: 本稿的核心断言是「**落库痕迹为零**」——
> 🔴 **而查询若写错(字段名 / JSON 路径 / 大小写 / 表名), 它对「真的没有痕迹」与「我查错了地方」给出【同一个读数: 零】。**
> 🔴 **更要命的一层**: 我原本指望阳性对照臂(P1/P2)顺带验它 —— **而那两个臂我自己已经标了离线做不成(要 relay)**
> ⇒ **那个本该验仪器的臂, 恰好就是缺的那个。** I0 就是把这一格补上, 且**不需要 relay**。

| | 内容 |
|---|---|
| **fixture** | 纯 `exec_sql` 种一个市场, **不经过任何生产代码**: `metadata` 含 `refund_tx_obj` + `refund_dispatched_at` + `refund_amount`, 另插一条 `bettor_refund_available` chain_event |
| **状态选择** | `protocol_status = 'refunded'`(**终态**)⇒ settler tick **扫不到它**, 它不会被本次 tick 改动 —— **I0 验的是查询, 不是行为, 不该受 tick 影响** |
| **断言** | 四个探针**逐个必须命中**: ① `refund_tx_obj` 读得到 ② `refund_dispatched_at` 读得到 ③ 该市场的 `bettor_refund_available` 事件计数 **≥ 1** ④ 状态**不是** `unresolved_needs_authorization` |
| **失败含义** | 任一探针零命中 ⇒ **仪器坏了** ⇒ 🔴 **N1/N2/N4 那些"零痕迹"断言全部不作数**(而不是记为通过) |

🔴🔴 **一条硬约束, 否则 I0 是装饰**:
```
I0 与 N1/N2/N4 的痕迹断言, **必须来自【同一份实现】** ——
同一个 helper / 同一段 SQL, 不许 I0 用一份、阴性臂各自再抄一份。
```
🔵 **理由(本仓在册, Codex 打过同一形状)**: **复制的谓词会产出两个互相同意、而实现已漂移的测试。**
若 I0 验的是"我为 I0 写的那份查询"、阴性臂用的是另一份, 那 **I0 什么都没证明** —— 它验的不是那把尺子。

🔵 **它与 A0 的分工(两条都要, 缺一不可)**:
- **A0(§3.3)** 证 **tick 跑到了判定点** —— 防"六臂一起零痕迹是因为根本没跑"
- **I0(本节)** 证 **痕迹查询看得见痕迹** —— 防"六臂一起零痕迹是因为查询看不见任何东西"
- 🔴 **两者读数相同(都是零), 成因不同, 处置不同。** 少任何一条, 那个零都还有一种合法的坏解释没被排除。

### 3.4 注入对照(§5 判别式 · 落码时必须实跑一次)

**最便宜且不碰生产码的做法 = 翻转 fixture**(生产是钱路, live 进程随时可能重载, 不去改它):
给某条**必须冻结**的臂种一个**合法授权**(`refund_authorization: 'bettors_absent'`)⇒ **该臂当场必须变红**。
🔵 前例: `p1_bypass_authorization_e2e` 2026-08-05 对 `M_C5_COMBO` 实跑过同款翻转, 当场 FAIL ⇒ 证明断言读的是真实判定不是装饰。**跑完还原。**

---

## §3-bis 🔴 场景映射表(@Bettor 2026-08-06 裁定落位 · **预注册原文四场景一字未改**)

> **裁定三条(逐字照记, 不转述)**:
> (1) **预注册原文「四场景」表述【不改, 继续一字不动】**
> (2) 用例设计稿内以**【场景映射表】**处理: N3 降为环境前提 · N4 降为 N2 的附加断言
> (3) 🔴 **【映射是否守住四场景想覆盖的判别空间】归 NWT 七条审【终判】** —— Bettor 不动原文、准稿内映射、**终判在独立审**。
>
> 🔵 **与 §2 的 §5 形态修订是同一套治理**: **原文不动 · 偏离显式写出来 · 等价与否交独立审。**

| 预注册场景(原文) | 本稿落位 | 落位理由 | 判别空间由什么承担 |
|---|---|---|---|
| ① quorum 永不可达 + 超 grace | **N1 独立臂** | 直接可造 | N1 五条断言 |
| ② `collecting_sigs` 超时 + `sigCount<4` | **N2 独立臂** | 直接可造 | N2 五条断言 |
| ③ RPC 全失败 ⇒ 持续拒签 + 超 grace | **降为【环境前提】** | 离线 harness 里「无 RPC」**不可注入不可撤销** ⇒ 与 N1/N2 无可分辨差异;单列一臂 = 制造「我们测了 4 种」的错觉。🔴 **@Bettor 采纳理由并加一句: 这个错觉比少一臂更危险。** | **全部臂共享的环境前提**, 在文件头与断言表里显式写明「本用例所有臂均在 RPC 不可用下运行」 |
| ④ 本地 ingest 缺失造成的**假**"签名不足" | **降为 N2 的附加断言** | 「签名实存而本机不知道」⇒ 本机**没有任何观察量**与 N2 不同。而它要证的事在本机可证、只是换说法: **代码的处置不依赖这个分辨** —— 真缺与未 ingest 走同一条 `:1408→:1414`, 且 reason 逐字带 `本机可见 sigs=N/4`(代码自陈只看得见本机视角) | **N2 追加断言 A4**: 冻结 reason **必须携带"本机可见"这一作用域限定** ⇒ 锁住「代码没有假装自己知道全局」 |

### 🔴 NWT 2026-08-06 裁定落位 —— 第四列那句我说过头了, 按他的措辞改

> **NWT 逐字**:「场景映射表第四列『判别空间被承担住了』这句**对 N3 不成立** —— N3 的具体触发机制在本 harness 里无法与 N1/N2 共享的环境基线区分,**这是"测不出反例"不是"证明等价"**。」

**⇒ 第四列的正确读法(照 NWT 给的措辞, 全表适用)**:

```
**N1 / N2 / N4 覆盖完整;N3 触发机制在本 harness 不可分离测试,
  记为【未验证的已知缺口】,非【已证等价】。**
```

🔵 **他改的是一个我自己已经警觉、却仍然写松了的地方**: 我在原文里写了"本表是申请不是结论、我不认为它已经成立",
**但同一张表的第四列仍然用了肯定句**("由 ___ 承担")。⇒ **免责声明写在表外, 挡不住表内那一格被单独引用。**
🔨 **判据(收下)**: **一份材料的强度由【会被单独摘出去引用的那一格】决定, 不由旁边那句免责声明决定。**

🔴 **且 N3 与 N1/N2/N4 的性质不同, 必须分开记**:
- N1 / N2 / N4 —— **测到了**(N4 经 A4 断言覆盖它实际要证的那层)
- **N3 —— 测不出反例 ≠ 等价**。它是一个**未验证的已知缺口**, 该以缺口的身份留在账上, 而不是被"已覆盖"吸收掉。
  **两者读数相同(都不产生红), 处置相反。**

### 三处偏离的裁定状态(NWT 2026-08-06)

| 偏离 | 裁定 | 条件 |
|---|---|---|
| §2.3 mock → trace 替代 | ✅ **批准** —— 理由(NWT 原话): trace 更贴合"市场是否被路由进退款广播"这一**行为命题本身**, call-count 反而是对**内部实现细节**的替代性度量 | M_AUTHED 反转 bug 先修 ✅ **已修**(`138fbb66`, 含注入对照) |
| §4.1 N3 → 环境前提 | ✅ **批准** | 第四列按上方措辞改 ✅ 已改 |
| §4.2 N4 → N2 附加断言 A4 | ✅ **批准** —— 代码本身不区分"真缺"与"未 ingest"(同一条 freeze 路径、reason 显式标"本机可见"), A4 验证这层作用域限定即可覆盖 N4 实际要证的东西 | —— |
| §6.1 allowlist 增项 | ✅ **批准** —— 这是**真调用**不是万能 mock 口, 不违反 `runner.mjs:737` 的设计意图 | 走改 spec 流程 |

### 🔴 跨节点那半我补不了(已由 @Bettor 收进在册另立卡)
要让 ④ 成为一条**与 N2 真正不同**的臂, 需要**第二个节点**提供"签名确实存在"的对照 —— 而 **J1 那台 `:3300` 今天下线**。
⇒ 标 **【本机不可核 · 待 J1/J3 节点回来另立卡】**。

---

## §4 🔴 我认为做不成 / 只能做一半的三格(如实标, 不当成齐)

### 4.1 N3(RPC 全失败)在本 harness 里**不构成独立臂**

离线 harness **本来就没有 relay / RPC** ⇒ 「RPC 全失败」是**整个测试的环境常态**, 不是可注入可撤销的变量。
⇒ N1/N2 也都跑在同一个"RPC 不可用"的环境里 ⇒ **N3 与它们没有可分辨的差异**, 单列一臂只会制造"我们测了 4 种"的错觉。
📌 **我的提议(不自拍)**: N3 **并入 N1/N2 的环境前提**并在稿里显式写明「本用例全部臂均在 RPC 不可用下运行」, **而不是伪装成第三条独立场景**。要不要因此改预注册原文的四场景表述, **@NWT/@Bettor 裁**。

### 4.2 N4(假"签名不足")与 N2 **在本机不可分辨** —— 而这正是它要证的事

N4 的定义是「签名**实际存在**但本机不知道」。**本机不知道 = 本机没有任何可观察量与 N2 不同。**
⇒ 想让 N4 成为一条与 N2 不同的臂, 需要**第二个节点**(J1 那台 `:3300`)提供"签名确实存在"的对照 —— 而**那台机今天下线**。
🔵 **但 N4 要证的命题在本机是可证的, 只是换了个说法**:
**代码的处置【不依赖】这个分辨** —— 无论签名真缺还是本机没 ingest, 走的都是 `:1408→:1414` 同一条 freeze 路, 且 reason 逐字写着 `本机可见 sigs=N/4`(= 代码自己承认它只看得见本机视角)。
📌 **⇒ 提议**: N4 改为**对 N2 的一条附加断言**(冻结 reason 必须携带"本机可见"这个作用域限定), 而不是一条独立 fixture 臂。**跨节点那半标【本机不可核 · 待 J1/J3 节点回来另立卡】。**

### 4.3 阳性对照弱一格(@Bettor 要求如实标)

P1/P2 要 `dispatchRefund` 真被调到 ⇒ 会进 `buildMakerRefundPreimage` ⇒ **构造真交易, 要 relay** ⇒ 离线跑不完。
- ✅ **能证**: 闸**放行**了(该臂与四条阴性臂分岔)。
- 🔴 **不能证**: 退款**真被构造出来**。
- 📌 标 **【离线不可核 · 上链验证另立卡】** —— **不当成齐了**。

### 4.4 🔵 顺带: 有一条本该最便宜的断言轴, 今天用不了

`poolSettlerTick` 返回 `{ refund, doomed, ... }`, 本是最省事的断言轴。
🔴 但 `:996` `:1098` `:1254` `:1286` 四处 `refund++` **紧跟在 freeze 之后** ⇒ **冻结与退款在这个数上读数相同** ⇒ 该轴**不可用**, 只能走 DB 痕迹。
📌 该缺陷已单独立在 `docs/2026-08-04-precond2a-merged-magnitude-estimate.md` §4.4【D-观察面】段, 走独立 diff 审。**修了本用例可多一条便宜断言;不修也能做 ⇒ 不是 blocker。**

---

## §5 可跑性(§5 原文明令必须如实标)

```
文件头写死完整命令:
  cd D:/kanet-tn12/kasia-console
  node scripts/test.mjs --case=test-framework/cases/predictions/pool/<文件名>.test.mjs
  ⚠ --case= 收的是【路径】不是 id(实撞过, 传 id 报 ERR_MODULE_NOT_FOUND)

扫描面: 文件名取 *.test.mjs ⇒ **在 --domain/--all 扫描面内** ✅
🔴 而"在扫描面内" ≠ "有人在跑": 本仓【没有自动回归】(无 CI 无 cron),
   --domain/--all 只有人手敲的时候才跑。引用覆盖率时这一格必须扣掉。
证据落 logs/test-runs/<时间戳>_<case>.log(🟡 覆盖式, 只留最后一次)
```

---

## §6 落码前置(两道闸, 我都不自己开)

1. 🔴 **`call_module_export` allowlist 增项**: 需加 `pool-market-settler / poolSettlerTick`。
   `runner.mjs:747-760` 那张表逐字写着「**改这张表 = 改 spec + 过审, 不许就地加**」
   ⇒ 走 `docs/2026-08-04-call-module-export-action-spec.md` 修订 + **NWT 审**。**申请随本稿同批。**
2. 🔴 **`p1-...-design.md` v0.5 变更块**(§2.3)+ 顺带 **§10.7 第 1 条坐标勘误**
   (文档写 `:248-268`, 而今天那段是 `freezeAwaitingAuthorization` 的定义; 真正的 bettor 退款扫描闸在 `:488-493`)
   标 **【坐标勘误 · 零论点变更】** —— **NWT 2026-08-04 的 GREEN 作用域不因勘误漂移。**

---

## §7 证据层级自标

| 陈述 | 层级 |
|---|---|
| 预注册原文那条测试**今天不存在** | ✅ `[CONFIRMED·2026-08-06 现读]` 判据: `grep -rn freezeAwaitingAuthorization kasia-console/test-framework/cases/` ⇒ 仅在 gate 用例里作为**被读的源码文本**出现, 零处作为**被断言的行为** |
| mock `dispatchRefund` 在本仓不成立 | ✅ `[CONFIRMED·结构性]` ESM 只读 live binding + 内部本地绑定调用(`:1241`/`:1384`)+ allowlist 设计意图(`runner.mjs:737`) |
| `poolSettlerTick` 可驱动 · 测试库干净 | ✅ `[CONFIRMED·2026-08-06 实测]` `:546` 已 export;`test-console.db` `pool_markets`=0 行(当场查) |
| 痕迹方案**与预注册形态等价** | 🔴 `[NOT-ESTABLISHED]` —— **归 @NWT 判, 本稿只给理由**(§2.3) |
| 本设计**已覆盖** ⑤ | 🔴 `[NOT-WRITTEN]` —— **可行性 ≠ 已覆盖**。本稿是设计, 一行用例代码都还没有 |
| NWT 七条 | 🔴 `[NO-CANONICAL-COPY]` 全仓 5 处引用零处枚举 ⇒ **在 NWT 落成仓内正本前, 任何「过七条审」声明一律不成立**(@Bettor 2026-08-06 裁) |

---

**本稿不改任何代码, 不建任何表, 不动任何开关。**
