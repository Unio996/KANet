# 外部程序接入 KANet（TN12）— 照做指南

> **这份文档是什么**：一份**照着走**的说明书 —— 给一个**不在我们机器上、不用我们代码、不认识我们任何人**的开发者。
>
> **它覆盖**：生成身份 · 构造 KANet 加密信封 · 把它作为交易载荷广播上链。
> **它不覆盖**：编译合约(covenant) / 结算 / 预测市场。那一层另有边界，见本文末尾。
>
> 🔴 **照它能不能从零走通** —— **不能。到第 4 步为止能，第 4、5 步今天走不通**，而卡在**我们**这边不是你那边。
> 卡点写在第 4、5 节里，含「卡在谁那里」。**先读那两节再决定要不要开始。**
>
> 🔵 而**第 1–3 步今天完全可以走完并自证**（不需要节点、不需要币）——
> 那是这份文档现在唯一能兑现的东西，而它兑现得很实：你能拿到一串**字节正确**的载荷。

---

## 0 · 你需要什么

| | 要求 | 我们验过的 |
|---|---|---|
| 运行时 | Node.js ≥ 20（需要 ESM + 顶层 `await`） | **Node v24.14.1** |
| 系统 | 任意（纯 JS + 一个 wasm 模块） | **Windows 11** |
| 网络 | 第 1–3 步：**不需要网络**。第 4 步起需要能连到一个 TN12 节点 | — |
| 钱 | 第 1–3 步：**不需要**。第 5 步起需要少量 TN12 测试币 | — |

**没验过的**：Linux / macOS / 其它 Node 版本。它们大概率可以，**而我们没跑过** —— 若你在那上面撞到问题，那是这份文档的空白，不是你做错了。

---

## 1 · 装依赖

**唯一的依赖是 `kaspa-wasm`。**

| | |
|---|---|
| 包名 | `kaspa-wasm` |
| 版本 | 它的 `package.json` 写着 **1.1.0** —— 🔴 **而这个号不足以指认这份字节**，官方同版本号的产物与它逐字节不同，见下 |
| 上游 | `https://github.com/kaspanet/rusty-kaspa`（它是 rusty-kaspa 的 WASM 绑定） |
| 我们用的那一份从哪取 | 🔴 **`https://github.com/Unio996/KANet.git` 的 `shared/vendor/kaspa-wasm/`** —— 它是 **git 跟踪的**，仓库是公开的，所以你**取得到**（在默认分支 `master` 上，7 个文件齐全） |

### 🔴 不要用 `npm install kaspa-wasm` —— 它给不了你这一版

这不是偏好，是实测结论：

```
npm view kaspa-wasm versions  ⇒  只有 '0.1.2' 和 '0.13.0'，latest = 0.13.0（发布于一年多前）
npm pack kaspa-wasm@1.1.0     ⇒  ETARGET  No matching version found
```

⇒ **`kaspa-wasm` 这个包在 npm 上存在，而 `1.1.0` 不是它发布过的版本。**
（读数是可判读的：一个不存在的包名会返回 404，与上面的 ETARGET 不同。）

🔴 **所以 `npm i kaspa-wasm` 会给你 `0.13.0` —— 与我们用的差一个大版本，API 不同。**
🔴 **而更要紧的**：本文 §6 那几个坑是在 **1.1.0** 上测出来的，**对 `0.13.0` 未必成立** ——
你会撞到一组**这份文档里根本没写**的错，而它们长得和文档里写的那些一样（一句什么都不说的报错）。

✅ **⇒ 从我们的仓库把 `shared/vendor/kaspa-wasm/` 整个目录拷走。** 这样你和我们跑的是同一份字节。

```bash
git clone https://github.com/Unio996/KANet.git
cp -r KANet/shared/vendor/kaspa-wasm  ./kaspa-wasm
```

> 🔴 **而这份文档和 `send-comm.mjs` 本身，此刻【不在默认分支 `master` 上】** ——
> 它们在分支 `bshard-m3-deploy`。上面那条 `clone` 拿得到 `kaspa-wasm`（它在 `master` 上），
> **但拿不到这两个文件**。要连它们一起拿：
>
> ```bash
> git clone -b bshard-m3-deploy https://github.com/Unio996/KANet.git
> ```
>
> 🟡 这是**当前状态**，不是设计 —— 合并进 `master` 之后这一段就该删掉。
> 若你 clone 默认分支后找不到 `docs/examples/kanet-external/`，**不是你做错了**。

### 🔴 也不要去 GitHub release 拿「v1.1.0」—— 那一份**也不是**我们跑的字节

`rusty-kaspa` 的 release 页上**确实有**公开的 wasm 产物（28 个 release 带 `kaspa-wasm32-sdk-<tag>.zip`），
所以「外面拿不到」是错的 —— **拿得到**。而实测下来：

```
官方 v1.1.0 的 SDK zip ⇒ 内层 package.json version = 1.1.0  ✅ 版本号对得上
                       ⇒ 🔴 kaspa_bg.wasm 与 kaspa.js  逐字节【都不同】
                       ⇒ 🔴 该 zip 里全部 10 个变体扫过，【没有一个】与我们相同
尺寸：我们 11,795,029 bytes  vs  官方 nodejs/kaspa 10,631,293 bytes  ⇒ 我们大 11%
```

🔴 **⇒ `1.1.0` 这个版本号【不识别】我们在跑的这些字节。** 两边的 `package.json` 都写着 `1.1.0`，而内容不是同一份。
🟡 **它到底是什么** —— 我们不猜（可能是本地从源码构建的，rc 版本也没试过；没人查到底）。

✅ **⇒ 所以「从我们仓库拷」今天是【唯一正确的取法】，不是权宜之计。**
🔵 而这一格对你有实际后果：本文 §6 那几个坑、以及 `send-comm.mjs` 里的每一行，
都是在**这份字节**上验的 —— 换一份"同版本号"的产物，它们**未必还成立**。

**拷完之后，目录结构应当是这样**（`send-comm.mjs` 与 `kaspa-wasm/` 同级）：

```
你的工作目录/
├── send-comm.mjs
└── kaspa-wasm/
    ├── kaspa.js          ← send-comm.mjs 用相对路径 ./kaspa-wasm/kaspa.js import 它
    ├── kaspa_bg.wasm
    ├── kaspa.d.ts
    ├── kaspa_bg.wasm.d.ts
    ├── package.json
    ├── README.md
    └── LICENSE           ← 一共 7 个文件
```

⚠️ **不需要 `npm install`**，也不需要 `package.json` —— `send-comm.mjs` 用相对路径 `./kaspa-wasm/kaspa.js` 直接 import。

---

## 2 · 第 1 步：生成身份

你的身份就是**一把 32 字节私钥**。不需要向我们注册、不需要我们发给你任何东西。

```js
import * as crypto from 'node:crypto';
import * as kaspa from './kaspa-wasm/kaspa.js';
const { PrivateKey, NetworkType } = kaspa;

const privKeyHex = crypto.randomBytes(32).toString('hex');            // 64 位十六进制
const address = new PrivateKey(privKeyHex)
  .toKeypair()
  .toAddress(NetworkType.Testnet)      // ← TN12 用 Testnet
  .toString();
```

**样例输出**（拿它对照你自己的形状，不要用这把密钥）：

```
私钥  : 267d74f72ab0d743ecc354c1b4ae3f2f76d4e8efdc90e468450a2e8862f5cdc4
地址  : kaspatest:qz63yt3dndghh0kdznfx7xla5pxp8waw2lsmuc7ud8ncpqwup2h3xftd80w8c
```

⇒ 地址应当以 `kaspatest:` 开头，冒号后 **61 个字符**（实测 200 个随机地址，长度全部是 61）。

---

## 3 · 第 2 步：构造 KANet 加密信封 🔴 **这一节是全文最要命的一格**

### 3.1 为什么它最要命

一条格式不对的消息，**会被收信侧正确地、静默地丢弃**，而你**收不到任何错误** —— 交易照样上链、照样扣费、照样有 txid。你会以为发出去了。

这不是假设：**这份配方的第一版就是这么失败的**，缺的正是下面 3.3 那一段。

### 3.2 信封的字节布局

```
ECDH(secp256k1) → HKDF-SHA256(salt 空, info 空, 32 bytes) → ChaCha20-Poly1305

字节序：  nonce(12)  ‖  ephPub(33)  ‖  ciphertext(N)  ‖  mac(16)
                        ↑ 压缩式公钥，首字节 0x02 或 0x03（不是 32 字节 x-only）
                                        ↑ N == 明文的 UTF-8 字节数（流密码，不扩张）
总长 = 12 + 33 + N + 16 = N + 61
```

- **对方公钥从哪来**：从对方的 `kaspatest:` 地址里 bech32 解出 **32 字节 x-only** 公钥，前面补 `0x02` 凑成压缩式。
- **HKDF 的 salt 与 info 都是空**（零长度），不是省略、就是空。
- **加密给谁**：给**收信方**。

### 3.3 🔴 外层载荷格式 —— 少一段就全丢

```
ciph_msg:1:comm:<你的alias>:<base64( 上面那串信封字节 )>
                 ^^^^^^^^^^^ 🔴 这一段少不得
```

- `<alias>` 是**发送方**（你）的名字，**必须非空、且不能含冒号**。
  收信侧用 `ciph_msg:1:comm:` 之后的**第一个冒号**来切分 —— 没有那个冒号，整条消息直接丢弃。
- base64 用标准字母表（`+` `/` `=`）。
- 整个字符串按 **UTF-8** 编码成字节，那串字节就是交易的 payload。

### 3.4 一个可对照的完整样例

明文 `hello from an external program`（30 个 UTF-8 字节）：

```
信封总长      : 91 bytes            (= 12 + 33 + 30 + 16 ✓)
  nonce       : 5809b45d76341f8a5d77a3ea
  ephPub      : 03eb905f1842e7e1bc06ab2143a9909ab9e191f8bc57420074d6bba0eb53363e48
  ciphertext  : 30 bytes
  mac         : ae94bf9f1a3a044c305a948fe0659985

payload 字符串:
ciph_msg:1:comm:my-program:WAm0XXY0H4pdd6PqA+uQXxhC5+G8BqshQ6mQmrnhkfi8V0IAdNa7oOtTNj5IuZqFuLZsJenI3cto3HJXCjEG9JPh5xi7ZxqyXApHrpS/nxo6BEwwWpSP4GWZhQ==

payload hex（上链的就是它，开头这一段是固定的）:
636970685f6d73673a313a636f6d6d3a…
 └── "ciph_msg:1:comm:" 的 hex
```

> nonce 与 ephPub 每次都不同，所以你**不会**得到逐字相同的输出。
> **能对照的是形状**：总长 = 明文字节数 + 61 · ephPub 首字节是 `02` 或 `03` · payload hex 以 `636970685f6d73673a313a636f6d6d3a` 开头。

### 3.5 ✅ 现在就跑一次自检（不需要节点、不需要币）

```bash
node send-comm.mjs --self-check
```

它会打印上面那些字段，并跑**四条**自检：

| | 自检 | 它证明什么 |
|---|---|---|
| 1 | 信封能被解回原文 | 你的加密与解密是自洽的 |
| 2 | 总长 == 12+33+明文+16 | 你的字节布局对 |
| 3 | 🔴 **阴性对照**：少了 `<alias>:` 段的载荷会被丢弃 | 让你**亲眼看见** 3.3 那一段少不得 |
| 4 | 🔴 **阴性对照**：一个合约(P2SH)地址会被**拒绝** | 见下 |

🔴 **第 4 条为什么单列** —— 因为前三条**结构上抓不到它**：
它们全是「加密给自己」，走的永远是普通(P2PK)地址。
而**合约地址的脚本哈希也是 32 字节** ⇒ 一个只查长度的实现会让它整个通过，
于是你会把脚本哈希当公钥去加密，产出一个**谁都解不开的密文** —— 而它照样上链、照样扣费、照样有 txid、**零错误**。
⇒ 所以自检 4 用的那个 P2SH 地址是从 `redeem = OP_1` 算出来的，**谁都不拥有它**，你可以自己复算。

🔵 **四条都过 ⇒ 你的第 1–3 步是对的。** 这一句我们敢说死，因为：
① 这份例子里的密码学与本网络现役收信实现做过**逐字节交叉验证**（含一条改一个字节就失败的阴性对照）；
② 自检 4 在一个**把版本字节检查删掉的副本**上跑过，**报红**了 —— 所以它真的分得出对错，不是装饰；
③ 自检 3 的判别力是另一条路验的：把缺 `<alias>:` 段的载荷**喂给现役收信侧的解析逻辑**，它确实被丢弃，
　 而合格的那一份被读出原文 —— 两边读数不同。
🟡 而自检 1、2 **没有**做过这种"拆掉再跑"的验证 —— 它们只是断言，别把它们读成同等强度。

---

## 4 · 第 3 步：连一个 TN12 节点 🔴 **今天走不通 —— 卡在我们这边**

**你需要一个 TN12 的 kaspad RPC 端点（Borsh over WebSocket，默认 `17210`）。**

🔴 **而我们今天没有给外部提供一个。** 说清楚是哪一格不通：

| | 事实 |
|---|---|
| 我们的节点 | 在 NAT 后面，**没有做端口转发** ⇒ 不是"被防火墙挡住"，是**根本没有公网入口** |
| 你能不能连别人的 TN12 节点 | 🟡 **不知道**。TN12 上**确实还有别的节点**（我们这台此刻连着两个公网对等节点）—— 而那是 **P2P 口(16311)，不是 RPC 口**，两者不是一回事 |
| 你自己跑一个 TN12 节点 | 🟡 理论上可以，**而我们没写过怎么做**，也没有人从零试过 |

🔨 **卡在谁那里**：**给这台机器一个公网入口，是一个【没有做的决定】，不是一次没做的测量。** 它归本项目的 Owner 拍，不归写这份文档的人。

⇒ **在那之前，这份文档到第 3 步为止。** 我们**不**给你一个"也许能连"的地址让你去试 —— 因为连不上的时候，你分不清是地址错了、还是路不通、还是节点没起。

---

## 5 · 第 4 步：拿测试币 🔴 **今天也走不通 —— 同样卡在我们这边**

发一笔带载荷的交易需要一点 TN12 测试币付手续费（这份例子里的那条消息，费用预留约 `0.0035 KAS`）。

| | 事实 |
|---|---|
| 有没有 faucet 接口 | ✅ 有：`POST /api/faucet/request` |
| 外部够不够得到 | 🔴 **够不到**。跨机实测该端点返回 **404**（带阴性对照，两次独立测量） |
| 有没有网页版 | 🔴 **没有。** 这份文档此前写着"只能走人工网页" —— 那句**指向一个不存在的地方**，已删 |

🔨 **卡在谁那里**：与第 4 步同一格 —— **外部可达性是一个未做的决定**。

⇒ **今天外部程序拿不到 TN12 测试币。** 我们不绕过这句话。

---

## 6 · 第 5 步：广播（等第 4、5 步通了之后）

```bash
KANET_RPC_URL=ws://<KANET_HOST>:<RPC_PORT> \
KANET_PRIVKEY=<你的私钥> \
KANET_ALIAS=my-program \
node send-comm.mjs --to kaspatest:<对方地址>
```

> `<KANET_HOST>` / `<RPC_PORT>` 是占位符。**这份文档不写具体的主机和端口** ——
> 因为写在这里的任何一个地址，都会在环境变化时变成一个**指向空处的出路**（这份文档此前正是这样错过一次）。
> 这两个值应当由**给你端点的那个人**告诉你。

交易构造有四个坑，**四种完全不同的写错，报错一字不差**：`RuntimeError: unreachable` + 一串纯地址栈，不提任何参数名。

🔴 **坑 1（外部无途径可猜，这是这份文档存在的核心理由）**

```
networkId 要填 'testnet-10'，不是这个网络的真名字 'testnet-12'
```

网络**叫** `testnet-12`，RPC 连接也用 `testnet-12` —— 但 `Generator` 的 `networkId` **必须**填 `testnet-10`。
这是 kaspa-wasm 内部只认 `testnet-10 / testnet-11` 造成的。**你填对了真名字，会得到一句什么都不说的报错。**

其余三个坑已经写进 `send-comm.mjs` 里了（`payload` 必须 `Uint8Array` · `outputs`/`changeAddress` 必须 `PaymentOutput`/`Address` 对象 · 用 `new Generator()` 而不是 `createTransactions()`）——
**照那个文件写就不会撞到**，所以这里不再单列。

---

## 7 · 第 6 步：怎么知道对方真的收到了 🔴 **今天你没有办法自己验证**

我们自己定的判据是：

> **上链成功不算数 —— 必须被另一侧实际读到，才叫接进来了。**

✅ 这条判据对。🔴 **而今天你没有办法自己执行它。**

对方收没收到，证据在**我们的数据库里**（`messages` 表按 `source_txid` 查 `direction=inbound`）——
**而你查不了我们的数据库**，我们也没有开放任何一个"这条消息到了吗"的查询端点。

⇒ **今天你能拿到的只有一个 txid**，而 txid 只证明**交易上链了**，不证明**有人读到了**。
🔨 **卡在谁那里**：需要一个外部可查的回执端点。**没有这个东西，也没人在做。**

---

## 8 · 这份文档不覆盖的：合约与结算

本文覆盖到**发消息**为止。**编译合约(covenant) / 押注 / 结算**不在其中。

🔴 而在把这套东西扩到结算面之前，有一个**必须先答**的问题：
**用上游 `silverc` 编出来的合约，与本网络在用的，是不是同一串字节？** 这一格**尚未验证**。

字节不同 ⇒ blake2b ⇒ **P2SH 地址就是另一个地址**，不是"差一点"。而它**不报"字节不一致"这种错** —— 它报的是**查不到**，与"还没打钱"/"索引没跟上"/"地址填错了"读数完全相同。
⇒ 你按配方算出一个地址往里打钱，我们在**我们算出的地址**上等，两边都"按代码正确工作"，而钱在一个谁都看不见的地方。

**所以在那个验证完成之前，别把这份文档的方法往合约面推。**

---

## 附 · 这份文档的诚实边界

| | |
|---|---|
| ✅ 已实测 | 第 1–3 步（生成身份 / 构造信封 / 三条自检），与现役收信实现逐字节交叉验证过 |
| 🔴 走不通 | 第 4 步（连节点）· 第 5 步（拿币）· 第 7 节（自证送达）—— **三格都卡在我们这边** |
| 🟡 未验 | Linux / macOS · Node 20–23 · 我们这份 kaspa-wasm 究竟是从哪来的（不是官方 v1.1.0 release —— 已实测；而具体来源没人查到底） |
| 🔴 已测出 | **公开渠道都取不到我们跑的这份字节**：npm 上没有 1.1.0；官方 v1.1.0 release 的 10 个变体逐字节全不同 ⇒ 见 §1 |
| ⛔ 不覆盖 | 合约编译 / 结算 |

🔵 **而这份文档的前身是一份【实测记录】** —— 它记的是"我们走过之后回头看的坑"，不是"你该怎么做"。
一个真正的外部开发者照它走，**卡在第一步之前**。那次尝试的记录见 `docs/2026-07-26-external-program-kaspa-onboarding-recipe.md`。
