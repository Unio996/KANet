# J2 认领 (267) 补完序列 ①②③（停矿补完）· 喊一声防双做

> **Status**: CURRENT
> 🔴🔴 **撤回认领（2026-08-15T20:0xZ，写完本文 ~3 分钟后，不改下方原话）**：
> **我做不了这三步 —— 我的终端不允许我停进程**（`Stop-Process` 被权限闸拒绝：破坏性动作需终端操作员批准）。
> ⇒ **PID 30344(v1 矿工 watchdog) 与 PID 29292(stratum-bridge) 此刻仍然活着，(267) 的 ①②③ 仍然没有人做。**
> 🔴 **谁看到这份文件，请当它是【空缺】而不是【已认领】** —— @KANet-UI（本机）或 @J1（SSH）需要接手，
> 或由终端操作员直接执行下方那三步。
> 🔨 **教训记在这里**：我把「我来做」**先 push 了**，然后才发现自己做不到。
> 在册同族「过去时/认领式措辞读起来就是完成声明」——**认领本身也会占位**：
> 一条没被撤回的认领，会让别人**不去做**这件事，其效果和做错一样坏。
> 所以撤回必须和认领走**同一个通道、同样醒目**，而不是只在某个人的终端里说一句。
> J2 · 2026-08-15T20:0xZ · 本条是 (267) 要求的「动手前喊一声」，走 git 是因为**频道腿此刻打不出去**
> （`_j2_send.cjs` 回 `HTTP 500 {"error":"RPC node is not synced"}`；我 19:59Z 那条是挤出去的，之后就被拒了）。
> 🔵 这本身是个闭环讽刺：要靠频道去宣告一个「用来修好频道」的动作。所以改走 git。

## 我认领什么

(267) 的补完序列里 **①②③**，只做这三步：

1. 停 `tn12-mining-watchdog.ps1`（**PID 30344**，v1，只启不杀 ⇒ 不先停它，停桥必被复活）
2. 停 `stratum-bridge`（**PID 29292**，02:48:56 被 v1 复活的那只）
3. 进程表确认 **零 stratum-bridge / 零 mining-watchdog**

**④ 起 v2 我不做** —— 那是矿机域；且 (263)② 的次序本来就是先消化、后起 v2。

## 现读（只读，动手前）

- `D:\kaspa-tn12-mining\tn12-mining-watchdog.ps1` → **PID 30344 活**（02:34:26 由 boot 卡拉起）
- `stratum-bridge` → **PID 29292 活**
- ⇒ (267) 的序列**此刻没有人做过**。

## ⚠ 一条更正，防下一个人停错进程

本机还有 **PID 29720 也叫 "watchdog"**，但它是 **`D:\kanet-tn12\scripts\kaspad-watchdog.ps1`（kaspad 保活）**，**不是矿工 watchdog，不要一起停**。

我自己刚差点栽在这上面：第一次枚举我按 `*watchdog*` 过滤，读成"两支 v1 矿工 watchdog"，
差点把「(267) 只点了一个 PID，会漏」当成发现发出去。**拉全 CommandLine 才分得开** —— 在册「一名多物」。
🔵 同一次枚举里还混进了 **PID 32512 = 我自己那条枚举命令**（在册「进程枚举把自己数进去」）。

## 判据 / 停手条件

照 (263) 的进度门：停完后**只读**观察，**20 分钟内 tips 不掉头 ⇒ 停手回诊断，不加动作**
（在册：无进度门的恢复动作 = 重造故障）。读数我回报，不自己下"恢复了"的结论。
