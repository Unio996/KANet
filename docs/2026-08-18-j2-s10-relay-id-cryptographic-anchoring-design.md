# §10 `relay_id` 密码学锚定 — 设计报告（报备层 · 零生产改动）

> **Status**: CURRENT

**作者** J2 · **日期** 2026-08-18 · **派工** Bettor 06:54（报备层，只出设计，不碰生产码）
**审** NWT 红队 + Bettor · **上游** `docs/2026-08-17-j2-s61-live-wiring-design.md` §10（NWT 第二轮对抗性复核提出）

---

## §0 范围（先把这份报告**不做**什么写在最前面）

- **不改任何生产码**。本文只出设计与判据。
- **不阻** §6-1 wiring 落地（①②③⑤ 已完成并三独立审通过）。
- 本设计要处理的是 §10 记下的那条：**「`relay_id` 无密码学锚定 = 身份抢注面」**，
  它被归类为**北极星（开放测试网）之前必须处理**，而不是今天的阻塞。

🔴 **本文继承 Console 现有信任模型**（loopback 即可信 + 零 per-resource 授权），
与 `transfer` / `mnemonic-reveal` 同档。**这是前提，不是本设计新增的假设。**

---

## §1 攻击（复述 §10，不改判）

攻击者**不需要偷任何人的密钥**：自己生成整套 mnemonic / rootXpub / identityPubkey，
自己对自己签名，把 `relayId` 填成 `relay_nodes` 里任意一个已存在且 `custody=mnemonic` 的 id。

逐关都过（N4-bis 只判 custody 类型存在性 / 绑定证明对攻击者自己那套当然自洽 /
PoP 验的是**申报的那个 identity pubkey 本身**）⇒ INSERT 成功，
而 `relay_id` 是 **PRIMARY KEY** ⇒ **那个 relay 的身份槽被永久占死**。

PoP 挡不住是因为它防的是**镜像方向**（抄别人的根 + 用自己的 relay_id）；
本攻击是**自己的根 + 别人的 relay_id**。

---

## §2 可用原语（2026-08-18 实核，不是设计假设）

| 原语 | 实况 | 出处 |
|---|---|---|
| `get_pubkey` | 只读，返回 relay 自己的 `x_only_pubkey` + `address`，从 wallet 派生，**私钥不出 relay** | `kasia-relay/src/relay.mjs:654` |
| `ecdsa_sign(message)` | 用 relay 私钥对**任意字符串**签名（`signMessage`，与注册 PoP 同一原语），签完即忘 | `relay.mjs:638` |
| `relay_nodes.ecdsa_pubkey_xonly` | 列存在，**32 行里非空 0 行**，`u1-*` 链路零处引用 | §10 已记 |

✅ **好消息：不需要新增 IPC 命令**。两条都已存在，且**私钥不跨边界** ——
「relay 本人签一个挑战」这件事今天就做得到。

---

## §3 🔴 关键否定结果：**relay 签挑战挡不住 §1 那个攻击**

这一节是本报告最重要的部分，也是我一开始想错、核完代码才改过来的地方。

设想的修法是：注册时让 Console 经 IPC 叫 relay X 签挑战，验签过才准注册。
**在当前信任模型下，这挡不住 §1**：

> 发起 IPC 的是 **Console 自己**，不是提交方。
> 攻击者提交 `relayId = J2test`，**Console 就会去叫 J2test 签**，J2test 在本机、活着、照签。
> 验签通过 ⇒ **攻击原样成功。**

⇒ 「让 relay 签」证明的是**「relay X 存在于本机且受本 Console 支配」**，
**不是**「提交这份注册的人控制 relay X」。这两句差得很远。

🔴 而这不是实现细节能补的：**只要 Console 是 TCB 且任何能调 Console 的人都能驱动 IPC，
本机范围内的"控制权证明"就恒真。** NWT 那句「整个 console API 信任模型都得动」精确成立。

### §3.1 那它**买到**了什么（这才是它值得做的理由）

**它让注册记录第一次对【外部方】有意义。**

| | 今天 | 加了 relay attestation 之后 |
|---|---|---|
| 本机攻击者（能调 Console API） | 可抢注 | **仍可抢注**（§3） |
| **外部提交方**（开放测试网场景，relay 在别人机器上） | 可抢注任意 relay_id | **抢不了** —— 他没法让别人的 relay 签 |
| 外部**验证方**读到这条记录 | 记录不含任何密码学事实 | 可独立验：**relay X 的钥确实背书了这个 rootXpub** |

🔵 而北极星恰恰是**开放测试网**：那时提交来自外部、relay 在提交方自己机器上，
「让 relay 签」就从恒真变成**真闸**。⇒ 现在做，是为那一刻先把**记录的含义**立起来。

🔨 **判据（值得单记）：一个控制权证明的强度，取决于【谁能驱动那次证明】，不取决于签名算法。**
同一段代码在本机 TCB 模型下恒真、在跨机模型下是真闸 —— **闸的强度在调用点，不在闸里。**

---

## §4 设计：relay attestation 绑定

### §4.1 数据面

注册提交新增一个字段族（**与现有六字段白名单并列，不改既有六个的语义**）：

```
relayAttestation: {
  relayPubkeyXOnly : string  // 声明的 relay 公钥
  signature        : string  // relay 私钥对 §4.2 那条消息的签名
}
```

验证侧新增一步，位置**在 N4-bis 之后、identity PoP 之前**（贵活之前先拒）：

1. 经 IPC 向 `relayId` 取 `get_pubkey`，得 `livePubkey`；
2. `relayAttestation.relayPubkeyXOnly === livePubkey` 否则拒（**不接受提交方自报的公钥当权威**）；
3. 用 `livePubkey` 验 `relayAttestation.signature` 对 §4.2 那条消息 —— 不过即拒。

🔴 **第 2 步是承重的**：若省掉、直接用提交方给的公钥验签，
那就退化成「攻击者用自己的钥签自己的消息」= §1 原样重演，只是多绕了一层。
（同族：`u1-registration-pop.mjs` 已经在 identity 那一侧踩过并写死了这条。）

🔵 `relay_nodes.ecdsa_pubkey_xonly` 那一列**可以顺带回填**，但**不得当权威**：
它今天 0/32 非空，且它是一张普通表，能被改。**权威只能是 IPC 现取的 `livePubkey`。**

### §4.2 被签的消息（域分隔）

```
KANET-U1-RELAY-ATTEST-v1|<hex sha256(canonical)>
canonical = JSON.stringify({relay_id, root_fingerprint, identity_pubkey_xonly, challenge})
```

**为什么必须带前缀**：`ecdsa_sign` 是**任意串盲签**，同一把钥同时服务别的协议。
实核既有两个生产者签的都是 `JSON.stringify({...})`（`trade-protocol-filter.js:325` oracle enroll、
`:715` market publish）—— 即**都以 `{` 开头且都无域标签**。

- ✅ 因此本设计的前缀 `KANET-U1-RELAY-ATTEST-v1|` **今天与它们结构上不相撞**。
- 🔴 **但这只是"今天"的观察，不是保证**：那两个协议自己没有域标签，
  它们之间的互不相撞同样只是"字段集恰好不同"。
  ⇒ **建议（超出本设计范围，单独立项）**：所有 `ecdsa_sign` 生产者统一加域标签。
    不加的话，下一个协议接进来时没有任何机制会告诉他撞了。

---

## §5 承重前提，以及**每一条会怎么坏**

写成"前提 → 坏法"，而不是"注意事项"，因为前者能被审、后者只能被读。

| # | 前提 | 它坏掉时会发生什么 | 现在成不成立 |
|---|---|---|---|
| P1 | 验签用的公钥来自 **IPC 现取**，不是提交方给的、也不是 DB 列 | 退化成 §1 原样 | 设计如此；**实现时是最容易被"优化"掉的一步**（DB 读比 IPC 快） |
| P2 | 被签消息**域分隔**，与其他 `ecdsa_sign` 生产者不相撞 | 别处拿到的签名可被搬来当注册证明 | 前缀成立于**今天**（§4.2） |
| P3 | 挑战一次性、与 identity PoP **同一个** challenge 且**同一事务**消费 | 两次签名可被拆开重放 | 现有 challenge store 已是 CAS + 同事务（③ 已落） |
| P4 | relay 进程活着且可达 | 注册**拒**（fail-closed），不得降级成"跳过 attestation" | 🔴 **必须写死**：这是最可能被加成 `try/catch` 的地方 |
| P5 | 跨机场景下 Console 无法驱动对方 relay | 若将来给 Console 加了"代签"通道，§3.1 那一格立刻塌回本机档 | 今天成立；**是本设计全部价值的地基** |

🔴 **P4 单独说**：把 attestation 做成"取不到就跳过"，读起来像健壮性，实际是**把闸变成装饰**。
本仓已在册的同族：`catch 返回合法空值抹掉失败`、`不干活换安全`。

---

## §6 预注册验收判据（实现时用，事后不加项）

1. **公钥来源**：变异 —— 把验签公钥改成 `submission.relayAttestation.relayPubkeyXOnly`（提交方自报）
   ⇒ 用例必须**红**。（这一格直接对应 P1，也是 §1 攻击的正面复现。）
2. **抢注负例**：构造 §1 那个完整攻击（自己整套钥 + 别人的 relayId），
   在**跨机语义**下必须拒。🔴 **本机跑它会绿得没有意义**（§3）⇒ 用例必须用
   「relay 不可达 / 由另一把钥签」来**模拟外部方**，并在用例里写清它模拟的是什么。
3. **域分隔**：拿一条**既有生产者格式**（`JSON.stringify({...})`）的合法 relay 签名去当 attestation
   ⇒ 必须拒。
4. **P4 fail-closed**：relay 不可达时必须**拒且零写入**，不得降级放行。
5. **不碰既有六字段**：`registerIdentity` 生产签名与六字段白名单逐字未变（同 ①-1/①-8 的做法）。
6. **变异清单扩格**：以上每条闸各一格，且清单**首格是 no-op 探针**（§6.6 已实装，`cc41bcee`）。
7. 🔴 **一条否定判据**：报告/文案里**不得**把本设计写成"解决了身份抢注"——
   只能写成"外部提交方抢不了；本机 Console 调用方仍可以"。
   验收时**读一遍对外措辞**，这一条是 §3 的落点，不是文字洁癖。

---

## §7 明列的空白（不假装覆盖）

- 🔴 **本机 Console 调用方仍可抢注** —— 本设计**不解决**，也解决不了（§3）。
  真正的解法在 Console 侧信任模型（per-resource 授权），属另一条线。
- 🔴 **`/api/relay/:id/send-command` 对**所有**命令类型无条件打 `legacy-unmigrated`**
  （`kasia-console/src/api/relay.js:1783`，读码实核），而 `authorize.mjs` 对该 tag 无条件放行；
  只读白名单的豁免**只在 `origin==='app'` 分支里检查**，这条路径根本走不到。
  ⇒ 这条路由是一条**覆盖全部命令类型**的过渡通道，其中包括盲签 `ecdsa_sign`。
  🔵 它是**代码注释里已声明的迁移债**（"收敛类过渡暂放行·非安全控制"），不是新发现的秘密；
  但「收敛类」这个说法**低估了它的范围** —— 它不是几条路由，是一个入口下的全部命令类型。
  ⇒ 与本设计的关系：**P5 的本机那一半今天就是敞的**，这也是 §3 结论的机制来源。
- 🟡 **`legacyUnmigratedPassCount` 被诊断命令污染**：实测 `get_arm_status`（在只读白名单里）
  经该路由每调一次计数 **+1**（三连测 84→85→86，步长恰为我自己的探针数）。
  ⇒ 那个"目标 shrink 到零、长期非零应告警"的指标，**永远到不了零**，
  只要有人跑诊断。它现在度量的是"有没有人在探"，不是"还有多少未迁移业务流量"。
  🔨 同族在册：混合总体的指标会伪造趋势。**建议**：诊断命令走白名单时不计入该计数，或分开计。
- **relay 私钥轮换 / relay 重建**：attestation 绑的是**当时**那把钥。
  钥换了之后旧记录还成不成立、要不要重签，本报告**未设计**。
- **`ecdsa_sign` 全域标签化**：§4.2 建议，属另一条线。

---

## §8 我在这份设计里改过的判断（留痕，免得下一个人重走）

我一开始把方案写成「让 relay 签挑战 ⇒ 抢注被挡住」。
读完 `relay.js:1783` + `authorize.mjs` 才发现**发起 IPC 的是 Console 自己**，
于是"控制权证明"在本机范围内恒真 —— **方案没错，但我给它标的效果错了**。

🔨 判据：**先问"谁能驱动这次证明"，再问"这个证明证了什么"。**
顺序反过来，就会把一个恒真的检查当成闸写进报告，而它读起来完全像一道闸。
