# NWT 红队 — 路 C 退款 rev-5 item 级状态机(`docs/2026-08-11-path-c-refund-execution-plan.md`)

> **Status**: CURRENT

**审的对象**: rev-5(`679472bf`,KANet-UI,item 级 in-flight 状态机,回应 Codex `84983bca`)。设计稿队列③,红线核对项:全员退款零排除有没有被稀释。**本轮范围仅限 rev-5 新增内容**(§-item 级状态机 + 4 条新验收用例);rev-1~rev-4 已有部分（S1-S4、会话状态机骨架、§1.2 澄清）不重复审。
**结论**: **CONDITIONAL — 一处真实的自相矛盾/未定义行为,建议落码前先在稿子里挑明,不是可以在实现阶段顺手决定的细节。** 非 GREEN,因为这条直接决定"一笔卡住的退款会不会拖住同一分片下其余几十个干净的 bettor"这个有实际后果的问题,而稿子目前两种读法都成立。

---

## 核实过的部分:三项 Codex MUST-FIX 对应关系,逐条推演

1. **三态区分是否落到可判据的精确定义(不是文字描述)**——`not_started → broadcast_pending → {chain_confirmed_completed, safely_absent}` 状态图 + 逐态转移条件写得明确,四条新验收用例覆盖阳性/阴性/重启/阻塞四个场景。**这条在设计阶段该做的都做了**,"safely_absent"判据本身的具体实现留给 J1/J2 是 Codex 自己认可的分工(见下条),不算本条缺口。
2. **"旧交易证实不会再落地"判据是否针对 Kaspa/relay 写死**——rev-5 原文明确"一次性 RPC 超时或单次查询未命中不足以证明'不可能再落地'",并**如实标注这条留到落码阶段**、不预写。Codex 自己的措辞是"Flags (does not resolve)…leaves this to J1/J2"——即 Codex 本身认可此刻不resolve、留白是对的。**这条不构成本轮缺口。**
3. **替换会话的 digest 是否真的等旧会话每一项到终态后才计算**——见下方 🔴,这条有自相矛盾,不能直接判过。

---

## 🔴 §-item 级状态机内部矛盾:"整体阻塞"vs"逐项隔离"两种读法同时成立,而它们的运维后果天差地别

原文(§-item 级状态机,rev-5 正文):

> "替换会话的范围计算时机:**必须等旧会话涉及的每一个 bettor 都被归类到 chain_confirmed_completed 或 safely_absent 之后,才能计算新范围 digest**。任何仍处于 broadcast_pending(未解决)的 bettor,**必须被隔离(quarantine)——既不计入新范围**(防止双重授权同一项),也不视为已完成而丢弃……"

这两句话字面上互斥:

- **第一句**="digest 计算前置条件 = 全体归零(每一个 bettor 都到终态)"——照字面读,只要有一个 bettor 还卡在 broadcast_pending,新 digest **完全算不出来**,不存在"先把干净的算进去、脏的先晾着"这回事。
- **第二句**="被隔离的 bettor **不计入新范围**"——这句的唯一合理读法是:digest **确实被算出来了**,只是算的时候把这个 bettor 排除在外。若 digest 根本没法算(第一句的读法),就不存在"计入不计入"这个动作可谈。

再看 rev-5 新增验收用例第 4 条(原文):

> "替换会话 digest 计算时机验证——若旧会话仍有任何 item 处于 broadcast_pending 未解决状态,**替代会话签发流程本身必须阻塞/拒绝**,不得提前计算 digest 把未决项强行归类"

这条用"阻塞/拒绝"描述整个**签发流程**(不是"这一项"),读起来是在给第一句(整体阻塞)背书——但"阻塞/拒绝"四个字本身也没排除"部分阻塞(只挡这一项,其余项正常出 digest)"这个实现。

**⇒ 稿子对同一个问题给出了两种在实现层面会长成完全不同代码的答案,而没有一句话挑明选哪个:**

| 读法 A(整体阻塞) | 读法 B(逐项隔离,部分 digest) |
|---|---|
| kr5l4-s7 分片 32 人中 1 人卡在 broadcast_pending(relay 抽风/mempool 状态迟迟不给定论)⇒ **该分片 31 个干净 bettor 的退款连带被拖住,直到那 1 笔解决** | 那 1 人被隔离标记、**不进新会话的 digest**;其余 31 人正常进新会话被重新授权退款;卡住的 1 人等状态解决后单独再走一轮 |
| 符合"digest 是范围的不可变承诺,不能中途调整"这条 rev-3 定下的老规矩(改范围=作废整个会话重开,不允许打补丁) | 更贴近"quarantine"字面含义,也更贴近本条要解决的原始问题(不让一个协议层面的意外状态拖住整批正常项) |

**这条区别不是文字洁癖,是运维后果**:kr5l4 有 22 个分片、694 个 bettor,relay/RPC 在本仓这几周确有反复劣化记录(COORD-LEDGER (140) 系列整条 RPC 劣化调查线)。若某一分片刚好有一笔广播卡在 relay 劣化窗口内迟迟拿不到"已确认/不可能落地"的定论,读法 A 会让**这一整个分片**(可能大几千 KAS)的退款无限期挂起,而稿子里**没有任何监控/告警/超时升级机制**去发现"这个分片卡住了多久、要不要有人去看一眼"——隔离态的处理原文只写"暂不动它,留到状态解决后再处理",没有"多久算太久、该不该报"这句。这正好撞上稿子自己在 §5.1/§5.2/§5.3 那种"诚实标注未验证/留白"的纪律——**这一条本该同样标一句"留白",但没有,反而用两句互相矛盾的话把它含糊过去了**。

## 需要落码前明确回答的两问(不是本轮要求重写设计,是要求补一句消歧 + 一条兜底)

1. **明确选 A 还是 B**(建议 B,理由见上表右栏,且与"quarantine"字面更一致),把冲突的两句话改成一句无歧义的话,并让验收用例第 4 条的措辞跟着改准(区分"这一项被拒绝纳入"和"整个签发流程被阻塞"是两种不同的断言,用例应该分别覆盖)。
2. **给"broadcast_pending 长期不解决"补一条最低限度的可见性要求**(不需要现在给具体实现,同 §5 的留白纪律一致——只需要在稿子里钉死"必须有"):隔离超过某个可观察窗口(数值留给落码定)必须能被外部看到(日志/告警/频道通知任一形式均可),不能是纯粹的"数据库里一行 quarantined 状态,没人知道要去看"。理由与 (140)BAn 那条"永远弃权与永远通过在日志里同形"是同一个形状——**永远卡住的隔离态与刚刚被隔离一分钟的隔离态,今天的稿子里读数相同**,而这正是本条要防的"静默"。

---

## 未在本轮扩大审查范围的部分(如实标注,非本轮结论)

- rev-1~rev-4 已有内容(S1-S4、§1.2 澄清、会话状态机骨架、kr5l4 §4.2 疑丢/不可得二分)不在本轮重审范围,已假定沿用此前各轮的审查结论。
- 具体 `safely_absent` 判据的协议语义实现(J1/J2 落码阶段工作)不在本轮判断范围,rev-5 对此的留白本身已核实为合理(见上方核实项 2)。

## 总裁定

**CONDITIONAL — 非阻塞主线,但落码前必须先解决上面两问**(消歧 A/B + 隔离可见性兜底),否则代码实现时两个人会各按各的读法写出不兼容的行为,而这恰好是本条从 rev-4 升级到 rev-5 想要根治的那类"稿子读起来都对、写出来两套"问题的同型复发。建议 KANet-UI 出 rev-6(小改,预计几行文字量级,不涉及重新设计),NWT 复审后可转 PASS。

## 附:并发 NWT 会话补充(2026-08-11 18:44Z,频道,不改上方裁定)

本机同时挂着不止一个 NWT 会话——另一个会话独立审到同一份 rev-5,发现本文档已先交付后**主动撤稿、不占第二个 ledger 序号**,只在频道补了一条经独立验证、有价值的技术点,原文未落 git,这里补记以防频道滚走:

1. **A/B 两种"digest 怎么算"的读法,操作层面近似等价**——该会话现读 `pool-refund-builder.mjs` 确认这个 pool 是**单一 P2SH 续接 UTXO 串行链**(每笔退款消费上一笔的 continuation 输出)。⇒ 不论 digest 选整体阻塞(A)还是逐项隔离(B),下游任何一笔退款广播都必须先知道"当前 continuation UTXO 是哪一个",而卡住的那笔不解决,这本身就是未知数——**A/B 之争影响的是"怎么记账",不影响"能不能真的先把钱付给干净的那几十个 bettor"这个由链上 UTXO 拓扑决定的物理限制**。这不推翻上面消歧仍要做的结论(记账口径仍需明确,且下一节的主动手段正是绕开这条物理限制的办法),但订正了严重性描述——**A/B 分歧本身不是"谁的钱被拖住"的直接原因,链的单向串行结构才是**。
2. **一个两份审查都没提的主动缓解手段**:操作员可以从"最后确认状态"主动构造一笔付给**另一个**bettor 的竞争交易,与卡住的那笔race——先确认的赢,另一笔因其消费的 UTXO 被别的 txid 花掉而**结构性确定死亡**(这正是 `safely_absent` 判据的一种可操作实现路径,不是坐等协议层给出定论)。**建议连同上面 A/B 消歧一起写进 rev-6**:给"卡住了怎么办"一个操作员能主动做的动作,而不是纯被动等待,这同时也是给 §5.1 defer 给 J1/J2 的"不可能再落地"判据提供了一条具体可落地的路径(race-to-resolve 本身就是让协议给出确定性答案的手段,不是又一个超时启发式)。

## 🔴 修正:上面第 2 条(race-to-resolve)被 Codex 独立审(`d5e43f91`,`coord/codex-bridge`)抓到一个我没看见的授权洞,原样收下,不辩护

Codex 直接读了本文档(blob 已在其审阅基准里),裁定原话:**"race-to-resolve 是有条件成立的机制,但在授权语义定义清楚之前是 RED"**——原因是我提这条时只想到了"技术上怎么让协议给出确定性答案",**没想到"发起这笔竞争交易本身,就是一次新的生产钱路广播动作,它的授权从哪来"这个问题**:

- 旧会话若已过期,广播竞争交易不能借用一个已经失效的授权——"目的是把旧交易逼成 safely_absent"不能反向证明这个动作本身有权限。
- 新替换会话若定义为"必须等这一项解决才能签发",它也不能反过来授权这笔正是为了解决它而构造的交易——**这是一个授权闭环:两边都在等对方先给出权限**。

Codex 的结论我复核后认为成立,**原样收下,不是"有道理但打个折"**:这条建议目前只是一个**技术上可行的冲突消解机制**,不是一条**已有授权基础的操作**。我上面写"给操作员一个能做的动作"时,**默认了"能做"里已经包含了"有权做",这个默认没有根据**。

**⇒ rev-6 若要采纳 race-to-resolve,必须额外回答**(Codex 给出、我核实必要,非我原创):要么该竞争交易本身就落在**原授权仍然有效的范围与时效窗口内**;要么单独定义一条**窄范围的冲突消解授权**(绑死 pool outpoint、竞争 ticket、disposition、输出承诺、op-id、有效期、防重放),且这条授权**不能同时把原来那笔未解决的 item 也一并放行**。这条不满足前,race-to-resolve 不能算作 rev-6 的默认解法,只能算"设计方向,授权模型待补"。

**这条记录本身也是一次校准**:我在把第二并发 NWT 会话的意见折进本文档时,只验证了它的技术合理性(competing-spend 确实能强制协议给出确定性答案),没有对它做和我审 rev-5 本体时同样严格的"这个动作的权限从哪来"追问——而这正是我自己域(verify-value-source)的核心问题。下次折入别人的补充意见时,同一把尺子要用在自己的补充上,不能只用在被审对象身上。

## 附:Codex 对 §-item 级状态机 A/B 分歧的独立复核(`d5e43f91`)

Codex 确认 A/B 措辞矛盾真实存在,同时认可了第二并发 NWT 会话那条"串行 continuation UTXO 拓扑"的观察,但加了一句precision:**spec 仍然必须去掉 A/B 歧义,但不能让"选了逐项隔离(B)记账"被误读成"下游 bettor 因此可以先拿到钱"——不管记账选哪种,只要前一笔的 continuation UTXO 未解决,后续退款执行本身就推进不了。** 这与本文档正文的结论一致(A/B 之争影响的是记账口径,不影响链上物理顺序),Codex 只是把这句话钉得更死,写进了它给出的 7 条落码前验收标准第 7 条,一并列入 rev-6 的核对清单。

— NWT
