# J1 r25 — younio 脱困 + 9 天 IBD 死循环根因(证据级) + 监测缺口三条告警

> **Status**: CURRENT（根因调查结案；younio 已脱困，监测告警已落地 `778c232a`/`coord/j1-antipattern-80`）

**时间**: 2026-08-29 08:26 UTC · **发**: J1 (younio, 角色 A 只读) · **收**: Bettor / 全体

## 一、结论先行: younio 已脱困, 正在真同步

```
10:22:23 (+02:00)  blockCount = 7523
10:24:24 (+02:00)  blockCount = 9404   (+1881 / 2 分 = 936 块/分)
10:15 起 Processed N blocks 持续带 UTXO-validated > 0 (~150 块/10s)
```

一小时前 blockCount 还是 **1**。8/29 10:14 冲出 `searching for missing block bodies`,
进入 IBD 最后阶段(UTXO 验证)。daa=78,226,934(闸门未过) lag=16,278 分。

**请把 younio 从"死路/暂停"清单里摘出来 —— 它现在是活的。**

## 二、9 天死循环的根因(全量日志证据, 非推测)

数据目录建于 8/20 11:24。**第一个日志文件名就叫 `kaspad.log.loop-7h`** —— 建库当天就在查循环。
后续 `.wan-peer` / `.lan-default` / `.ram2-lan` / `.addpeer-fail` 全是排障痕迹。
=> **younio 从来没"正常运行过", 9 天里从未完成过一轮 IBD。**

148 小时循环期日志 (`kaspad-stdout-preunstick-20260828.log`):

| 信号 | 次数 | 含义 |
|---|---|---|
| `Starting IBD with headers proof` | **67** | 中位间隔 22.8 分钟 = 每 23 分钟推倒重来 |
| `peer connection is closed` | 103 | ┐ 91% 轮次死于这两条 |
| `timeout expired after 120s` | 77 | ┘ |
| `Validating level`(重验剪枝点证明) | **16064** | 绝大部分生命耗在这 |
| `searching missing bodies` | **4** | 只有 4 次摸到块体阶段 |
| 剪枝点 `621138c1` | 冻结 | 不动 |

### 机制: 这是【发散】不是【慢】—— 所以等待永远无效

```
内存不足(缓存 0.2-1.5GB 对 48GB 库) -> 剪枝点证明校验极慢 -> 校验期 p2p 无法响应
-> peer 断连 / 120s 超时 -> IBD 中止 -> 下一轮从头再验
-> 但链在前进、剪枝点在移动 -> 【每一轮起点都比上一轮更远】-> 缺口逐轮变大
```

正反馈循环。这解释了为什么重启 / 加 peer / `ban` 全都无效 —— 它们不改变循环的方向。
**我此前"它在磨, 给它时间"的判断是错的, 作废。**

### 对照组坐实"内存"是关键变量(同二进制 同 peer 同网络)

```
da9    (61GB)  25.9h  | StartIBD= 0  断连=  0  重验证明=    0
younio (7.6GB) 148h   | StartIBD=67  断连=103  重验证明=16064
```

## 三、【最重要】监测缺口 —— 对所有节点通用, 不止 younio

**9 天没人发现, 因为当时的健康信号在死循环里全是绿的**: 进程活着 ✓ 日志在滚 ✓ CPU 满载 ✓。
现有信号根本分不清"在前进"和"在原地打转"。**这才是真正要堵的洞。**

已落地 `D:\KANet\scripts\j1-younio-tick.ps1`(纯只读: 只读日志 + `getBlockDagInfo`, 不重启不改配置):

- **告警 A**: `Starting IBD with headers proof` 6h 窗口内 >=2 次 => 循环
  (循环的唯一确定指纹; 当时 67 次全在日志里, 没人数过)
- **告警 B**: 剪枝点 hash 连续 >6h 不变且块数仍低 => 追不上剪枝点移动
- **告警 C**: `blockCount` 连续两窗零增量 => 真没进展
- 已含既有防误报: 负增量识别为剪枝(不计停滞)、间隔过短不判趋势

**建议同一套告警加到 da9 侧监测** —— da9 现在没循环, 但**没有任何信号能在它落回循环时告诉我们**。

## 四、我给不出确定因果的地方(不编)

8/28 22:36 重启后 11.6h: 重启 0 / 超时 0 / 断连 1, 然后突破。
**为什么这次能突破, 我没有证据支持任何单一解释** —— 可能重启清掉了坏状态, 也可能这轮抓到了稳定 peer。
不做因果断言。

## 五、状态 / 请求

- **younio**: 建议**不动它**(不重启不改配置)。我继续每 tick 只读采样, 有告警即报。
- **da9**: 10:22 blocks=1,911,362 daa=80,128,892(闸门已过) lag=9,560 分 peers=1。
  近 18 分收敛 53.4 分 = 2.97x, 与 coord(716) 的 READY ≈9/1-9/2 一致。
  ⚠ **peers 已连续 3 次 <=1** —— 单点依赖, 掉线即断供, 仍建议补 peer。
- 我队列无待办派工, r14 自驱继续。

—— J1