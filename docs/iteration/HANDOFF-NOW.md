# 接位快照 —— 当前状态(这个文件只有一份,每次覆盖)

> 🔴 **这个文件是【覆盖式】的,不是追加式的。** 它永远只描述"此刻"。
> 🔵 **为什么单独一个文件**:`COORD-LEDGER.md` 是只追加的历史,把快照写进去 ⇒ 五分钟就会有一份更新的,
> 而**两份并存时必有一份陈**,读的人无法判断手上这份是不是最新。本仓在册通则:**同一事实存两份,必有一份会漂。**
> ⇒ 所以:**历史进 ledger,当前状态只进这里。**
>
> **更新时间**:2026-07-29 ~11:00 · **更新者**:Bettor
> 🔴 **改动这个文件时,把上面那行时间一起改** —— 否则它会变成"读起来是当前、其实不是"的那一类。

---

## ✅ 已闭合(不必再碰)

- **模块化 v194** —— 落库 + 受控重启 + 三条验收全过
- **外部接入配方** —— 🟢 `--to` 主路径**第一次真正走到底**(J1 在第二台机器实跑,txid `49b814e2…`)
  - 六条自检全绿;其中自检 5/6 用**假 UTXO** 把"选输入 + 造交易"两步变成**不需要节点、不需要币**就能跑
  - 🔵 J1 用 **UTXO 消费模式**从外部反推,证实"打印的 mass 描述的就是实际那笔交易" —— **不是让它自证**
  - 三格齐(NWT diff / Bettor 发布 / J1 实跑)⇒ **已合入 `bshard-m3-deploy`**(merge `89bf9496`,`--no-ff`)
  - ✅ **合入后字节逐字相同**(send-comm `8bb01743` · README `44cc1b9a`),**无搭车** —— J1、J2、Bettor 三方各自独立核过
  - 🔵 用 `--no-ff` 而不是 cherry-pick 的理由值得留:cherry-pick 会造出**新 hash**,而 J1 的验收绑在 `23858df7` 上
    ⇒ 那样"他验过的那一版"在主分支上就**不存在了**,只剩一个内容相同的副本。现在 `23858df7` 仍 checkout 得到

🔴 **而配方闭合的作用域,不许读大**:
```
✅ 覆盖: 第 1–3 步(生成身份 / 构造信封 / 自证字节)+ 广播
🔴 不覆盖: §4 连节点 · §5 拿币 · §7 自证送达 —— 三格全卡在我们这边
🔴 且【对方有没有读到】不在本次验收内 —— J1 只能证到"链上有这笔"
```

## 🔄 各线现状(有主,不必重派)

🔵 **配方线闭环后,产能已转向 Owner 目标②(模块化)** —— 因为外部接入剩下的三格全在 Owner 桌上,队里等不出结果。

🔴🔴 **而模块化的派工【已全部撤回前提】—— 原因见下方「路线图分叉」那一节。在它被裁定之前,不要据任何一份路线图派工。**

| 谁 | 手上 |
|---|---|
| **J1** | 🛑 批零【ZK D-number】**前提已撤**(我引的四句只存在于非权威那一份)<br/>✅ 仍成立的产出:他查出**两份路线图分叉**,并拿到了权威 v1.2 全文<br/>+ 顺手把 silverc 那张卡标成【当前不可执行 + 两条堵点】(两个前提他都实测缺) |
| **J2** | 🛑 批零【claim-complete】**前提已撤**(那一格 07-28 早已成文于 `docs/2026-07-28-batch-zero-j2-scope.md`)<br/>✅ 仍成立的产出:① 用**生产谓词** `_scanJudgeProposeCandidates` 复核 ⇒ 那 12 盘逐个仍 verifying ⇒ **claim-complete 仍挡第三、四段,这一条与版本之争无关**<br/>② 他往路线图加的批零状态表**未 commit**,已撤审(它加在了非权威那一份上)<br/>+ §4 文案(行2 结论"量不出来 ≠ 否")记下一版,不单开闸 |
| **NWT** | ①检测设计(数据源 = 链上答"烧多少" + `messages` 答"谁跟谁");diff 闸 |
| **KANet-UI** | supervisor 归拢(探针已常驻 · `/health/rpc` · nonce 回读留痕 · 发送器补回读)<br/>🔴 + catch-up 空转的修法待核:**为什么一个 DISABLED 的东西每分钟跑 31 遍** |

🔴 **eslint no-undef:裁定"现在不加"** —— J1 实测它对坏版本有判别力(命中 `best`),而 J2 已把该 bug 做成写不出来 + 自检可离线抓 ⇒ **用一个确定的新依赖成本,换一个推测的收益,现在不划算**。事实留档:哪天因别的理由带上 eslint,这一格顺手就有。

## 🔴🔴 路线图分叉 —— 同一路径下两份来源不同的文档(本班发现,未处置)

```
路径: docs/2026-07-25-kanet-trunk-roadmap-modularization-and-external-access.md
  A · c45acd37 · 2026-07-26 · 分支 coord/codex-bridge · 1531 行
      标题「KANet 主干执行路线图 v1.2 —— 开放经济路由与结算底座」
  B · 工作分支当前 · 319 行 · 最后改于 3609f10f(07-28,J2 的 settleMarketLive 订正)
      标题「KANet 主干路线图 —— 模块化与外部程序接入」
🔴 两者【没有共同历史】: merge-base 是一个与本文档不相干的 commit(6cab9393)
   ⇒ 不是"一份文档的两个版本", 是【两份来源不同的文档占了同一路径】
   ⇒ 🔴 于是不存在"合并"这个动作 —— 只能有人裁定【留哪一份】
```
🔴 **而 A 自己 §0 逐字写着**:「v1.2:当前唯一 `FROZEN-EXECUTING` 执行权威」·「**派工顺序**以本文件为准」·🔴「**禁止多个当前稿并存**」
⇒ **B 就是那个"不该存在的第二稿",而它已存在三天,队里两个人各自往它上面打过订正。**
🔴 **且 A §0 第 13 条直接改了 ZK D-number 的定性**(「恢复为 DECISIONS 决策条目问题,不再误写为协议层序号」)⇒ 据 B 派的那条工措辞已过期。
🔵 A §0 另有一条影响我们日常引用:「`coord/codex-bridge/` 视为**旧写法**;正式状态只以 `coordination/codex-bridge/` 的入库记录生效」。

🔨 **判据(本班立)**:**一份自称"唯一权威"的文档,它的唯一性不会自己维持** —— 要么有机制拦住第二稿(路径/lint/CI),要么它迟早变成"两份都在被人认真维护"。

## 🔴 在 Owner 桌上(四件,一件都没自行处置)

1. **agent 对外编造交易凭证**(6 条已核实,含教科书占位符 `0xabc123...`)
   ⇒ 判为**对外开门的前置条件**,不是并行缺陷。门一开它立刻是 live 的对外欺骗
2. **对外入口**(§4/§5)—— 口径是【**没能确定**外部有没有别的路】,不是"只能靠我们开口"
3. **autoreply 的【断】那半** —— 等①检测跑出数字再上报(**events 那条路答不了"烧多少",钱只在链上**)
4. 🔴 **路线图分叉留哪一份**(见上节)—— 涉及一份 **Owner 已签发的 FROZEN 文档**,不是 Bettor 能拍的

## 🟡 待验的预测(见 ledger (84))

RSS 到 4400MB 一线时,看有没有**带 `getWorkingRpc` 失败签名**的劣化。
```
10:00 → 1,427MB   ·   10:48 → 2,204MB   ⇒ 实测 ≈ 16MB/min(与 10:03 估的 15MB/min 对得上,速率在保持)
⇒ 窗口收窄到【约 13:05】(先前估 ~13:20)
```
🔴 **到那个窗口时要看的是【有没有带 `getWorkingRpc` 失败签名的劣化】,不是"有没有卡"** —— 卡每 4 分钟就有一次,那个读数没有判别力。
🔵 KANet-UI 已实证:**前三次劣化与 catch-up 卡是两个现象** ⇒ 该预测的对象未被推翻。

🔵 **而"发消息失败 ← catch-up 卡"这条因果现在是实测的**:Bettor 10:48 一次 SEND-FAILED,与 lag 心跳 10:48:48 的 `gap=13968ms` **对上同一刻**(两把独立的尺)。

## 🔨 本班新立、仍在生效的规矩

- **传输 ≠ 交付**:要给外机跑的版本推 `recipe-verify`,跑通了才合主分支(读者 clone 的正是主分支)
- **对外文档的每个数**:带来源(实测/算/抄),**且标明它会不会因人而异**
- **diff 里删掉或改名一个符号** ⇒ 必须 grep 它全文还有没有被用
- **打 GREEN 的人若改口** ⇒ 逐字写「前一条作废,以本条为准」;引 verdict 引**最后一条**
- **核已落码的东西有没有 bug** ⇒ 读**落地那份**,不读工作区(工作区天然领先于讨论中的对象)
