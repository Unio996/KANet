# 接位快照 —— 当前状态(这个文件只有一份,每次覆盖)

> 🔴 **覆盖式,不是追加式。** 它永远只描述"此刻"。历史进 `COORD-LEDGER.md`,当前状态只进这里。
> **更新时间**:2026-07-30 ~07:35 · **更新者**:Bettor
> 🔴 **改它的时候把上面那行时间一起改** —— 否则它变成"读起来是当前、其实不是"的那一类。

---

## 🔴🔴🔴 08:1x **13,670 KAS 卡在链上,一分没动 —— 而没有任何东西在看它**

**这是本班最要紧的一个事实。两条互不依赖的证据交叉验证过。**
```
链上现取(两人独立各测一次, 逐字相同):
  protocol_status = 'pruned_expired_waived' 的 137 个 covenant 地址
  ⇒ **137 个全部仍持币**, 合计 **13,670 KAS**(tip 70813771)
查库(另一条独立证据):
  这批盘的 refund_txid = 0 · settle_txid = 0 ⇒ **从未记录过任何付款**
🔵 阴性对照: 地址改一位 ⇒ 被拒(校验和有效)⇒ 不是随便一串都能查出余额
```
🔴🔴 **⇒「写掉还是退回」这个问法本身错了。答案是第三种,而且是最好的那一种:**
**两样都没发生 —— 钱还在原地,一分没动。⇒ 它不是已损失,是【卡住】。而卡住的有可能拿回来。**
🔴 **参照**:本仓有过约 3000 KAS 因盘孤儿**永久损失**的先例 ⇒ **这一笔是它的 4.5 倍,而它还没变成那个结局。**

🔴 **而"为什么没人发现"这一格,查完之后换了个答案 —— 它决定要修什么**:
```
❌ 起初判断: 有一个观测器把它们判成了背景噪音(白名单 KNOWN_BUCKET_MARKERS)
✅ 实读那段码 + 实测: 它 SELECT 的是【两种事件类型】, 与那些市场行/地址**没有任何数据通路**;
   且那两种事件本机 **0 条, 从未有过**(阳性对照: events 表近 1h 187 条 ⇒ 不是空读)
   ⇒ 两台机器都是 0 ⇒ **那条抑制路径从来没有执行过, 至今零危害**
⇒ 🔴🔴 **真实况是:根本没有那只眼睛。** 不是"看了然后说无需介入", 是**没有任何东西在看**
🔨 而两句要的修法完全不同:「被误抑制」⇒ 修白名单;「根本没人看」⇒ **修白名单一点用都没有**
```
### 🟢🟢 **08:42 已查完 —— 结论比上面那段好得多,而它被推翻过四次才落定**
```
🟢 那 33,735 KAS 【两侧全部是我们自己的】, 而两把锁的钥匙都在本机:
   maker 侧 13,620 KAS ⇒ 钥匙 = 本机 3 个 relay(maker-1 / NWT / broker-1)
     🔵 金额已【对着合约字节】验过: 库里 maker_stake_amount 按最小推送编码去脚本里找
        ⇒ 140/140 命中; 阴性对照(改 1 sompi)⇒ 0/140 ⇒ 判据有强分辨力
   bettor 侧 20,065 KAS ⇒ 钥匙 = 本机 5 个 relay, 全部有密钥材料
     🔵 **那些"下注方"是我们自己的自动化 agent**(Bettor-tn / AutoBetter-1,2,3 / FaucetRelay-tn)
🟢 两条退款路【都是自助的】: 过 deadline+7200 秒即可 · 零委员 · 零 oracle · 零对手方配合
   · spine 侧 refund_maker_unjoined(要 maker 签名)· side 侧 refund_market_cancelled(要 bettor 签名)
⇒ 🔴 **它卡在这里的唯一原因: 没有人去跑那两笔交易。**
```
🔴 **而这条线上被推翻过四次的东西,写在这里防止下一个人重走**:
```
❌ 「有 refund_draw · 不需要任何私钥 · 按 bettor 1:1 退本金」
   ⇒ 那来自 PoolRoot/PoolShard_fold, 而这批盘用的是 **PoolSpine_v07** ⇒ 读错了合约
   🔨 **读任何 .sil 之前先定版本**(按库列 protocol_version + id 前缀, 不是"目录里最新那个文件")
❌ 「maker 全是 cross-node ⇒ 我们没钥匙」⇒ 那是对**另一批**(659)说的; 这 140 盘的 maker 是本地
❌ 「metadata 里没有金额 ⇒ 本机量不了」⇒ 值在**列**上不在 metadata 里
❌ 「bettor_relay_id 138/170 是 NULL ⇒ 不知道这钱是谁的」
   ⇒ 🔨 **按记录查答不出, 按定义查一次就清楚**: 用合约里烤死的 bettorPk 推地址 ⇒ 5/5 全认得出
   ⇒ 而那个错答案**看起来完整、方向还偏悲观** —— 最不容易被推翻的一种
```
🔵 **而"三周没人喊"也有了解释**:没有外部当事人 ⇒ **没有人会来抱怨**;而我们自己没有任何东西在看。
🔨 **「没有人抱怨」从来不是「没有问题」,它只说明受影响的是我们自己。**

📌 **归 Owner**:「33,735 KAS 是我们自己的测试本金,卡在 137 个盘里,原因是没人跑过两笔退款交易。
两侧钥匙都在本机,两条路都自助、条件已满足。要不要回收、什么时候,你拍;做的话走完整流程,不抢。」
🔴 **硬边界:在他拍之前谁都不碰 —— 不构造交易、不广播、不试。**

### 🔴🔴🔴 **而真要做的时候,有两条构造级硬约束 —— 违反它们会【静默烧掉】那笔钱**
```
🔴 ① **side UTXO 绝不能和 spine 退款放进同一笔交易**
   PoolSide_v07 的 settled_via_spine() 只 require(inputs >= 2), **不管输出**;
   而 maker 退款那笔只允许 1 个输出、值被 makerStakeAmount 夹住
   ⇒ 🔴 **side 那部分钱会全部变成手续费 —— 链上完全合法, 没有任何报错。**
🔴 ② **退 side 时, side UTXO 必须排在 inputs[0]**
   refund_market_cancelled 的输出值被 **inputs[0].value** 夹住
   ⇒ fee-UTXO 只能追加在后面; 排错位置 = 退出来的金额被按错的那个输入算
```
🔴 ③ **spine 里超过 makerStakeAmount 的部分, 结构上取不回来 —— 而它是可精确算出的**
```
PoolSpine_v07.sil:395-396 逐字:
  require(tx.outputs[0].value <= makerStakeAmount − 50000);
  require(tx.outputs[0].value >= makerStakeAmount − 100000000);
🔴 上下界都锚在 **makerStakeAmount(创建时烤死的常量)**, 与这个 UTXO 现在有多少【无关】
⇒ 输出封顶在 makerStakeAmount − 50000, 而输入更大 ⇒ **差额必然进手续费**
```
✅ **逐盘算出的精确值**(按去重地址 137 个 · 链上现取):
```
链上输入合计   = 13,670.00 KAS
可取回上限合计 = 13,414.93 KAS
🔴 结构上必烧  =    255.07 KAS     ← 不是估算, 是合约上界决定的
```
⚠️ **我第一次算成 685 KAS —— 因为按【行】(140)算而不是按【去重地址】(137)算**,
两个地址被多条记录共用。**逐字对回早先量的 13,670 才发现。**
🔨 **凡两个数并排出现,先问是不是同一把尺** —— 今天这条第三次发作。

🔵 **而 ①②③ 的共同根**:两条退款路**用了两种不同的锚** ——
side 侧锚 `tx.inputs[0].value`(动态)⇒ 怕"谁排 inputs[0]";
spine 侧锚 `makerStakeAmount`(静态)⇒ 怕"输入比常量大"。
🔴 **⇒ 按同一套习惯构造两笔交易,必然踩中其中一个。**

🟡 **而两个入口的名字都在断言【代码不检查】的前提**(`unjoined` / `market_cancelled`):
两侧真正的闸都只有一个 —— **deadline + 7200 秒**。
⇒ 🔴 **别照名字把这两条路划掉**(我们这 140 个盘确实有下注者,而合约不查这一条)。

🔴 **这三条的性质**:它们不会报错、不会失败、链上会接受 —— **钱就没了**。
⇒ 🔨 **"可回收"与"照常规方式构造就能回收"是两句话。** 这一格必须跟着那句结论一起走,
   否则下一个人照"可回收"三个字动手,就会把它毁掉。
🟡 **来源与强度**:读的是**源码** `PoolSide_v07.sil`;而权威是那份**已验过的部署脚本字节**
⇒ **实构造之前必须用那份字节把这两条复核一次**,不能照源码就动手。

🟡 **一个更大的数,但【不是结论】**:状态读起来"已了结"却仍持币的共 **645 个地址 · 169,202 KAS**。
**没人读码确认过这是不是预期**(找零/尾款/另址结算都可能)⇒ 标为**观察待查**,不进任何对外结论。
🔵 **而白名单那条结构性缺陷仍然成立、只是不急**:它的依据是 2026-07-21 的 **15 行**样本,而现在是 137 个地址;
它按**裸子串匹配**抑制,**没有上界、没有过期**,且问的是"正文**提到**那个词吗"而不是"它**属于**那个桶吗"。
🔨 **一个"已知噪音"白名单,必须带上它当初被判为噪音时的范围/数量 —— 否则范围一变它照样沉默,而沉默的理由已经不成立。**

---

## 🔴🔴 08:10 **编译器树同步的账单出来了:上限 1825 个地址 · 376,631 KAS 的迁移**
```
🔴 换编译器 ⇒ 字节变 ⇒ 合约 P2SH 地址变 ⇒ 旧地址上锁着的 UTXO 变孤儿, 须在新地址重锁
   (且跨节点若非同一 build ⇒ 算出不同地址 = 共识 break, 是 correctness)
🔴 ⇒ 这**不是技术选型, 是钱路** ⇒ 必须 Owner 批。(我一度把它报成"不需要 Owner 拍" —— 错, 已撤)
🟡 而 376,631 是**上限**: 字节到底变不变, 要一次隔离编译对拍才知道(另一格, 未做)
🔨 判别式(便宜): **「这个改动之后, 昨天生成的地址还是同一个吗?」** 答"不一定" ⇒ 它就跨进了钱路
```

---

## 🔴🔴🔴 07:30 **CLAUDE.md 铁律 0.5 状态注记里最贵的那一句,对上游 master 已经不成立**

**先读这一段再去读 CLAUDE.md,否则你会照着一条已经不对的理由做判断。**
```
注记原话:「任何用上游 silverc 生成 .sil 的第三方 ⇒ **该 bug 仍在**」
🔴 实测(07-30 现取上游 master, raw 直取源码, 不经任何缓存):
   那处现在是一行  ctx.emit_op(OpNum2Bin, -1)  —— 无任何手写 stack_depth 加减
   (并去读了 emit_op 的定义确认第二参就是 stack_delta ⇒ 净 −1)
⇒ ✅ 上游此处记账与我们修复后【相同】⇒ **那个 off-by-one 在上游 master 上已经没有了**
⇒ 🔴 而机制【不是】我们那笔 8065184(它从未被推), 是上游 07-27 的 compile.rs refactor (#178)
```
🔴🔴 **而绝不能就此把那句警告删掉 —— 它多半只是【作用域错了】**:
```
🔴 上游 master ≠ 第三方手上那份 silverc
❌ 而我一度把新作用域写成「用上游【发布版 / tag】的第三方」—— **这句同样是错的, 已实测推翻**:
   上游 silverscript: **git tag = 0 · GitHub releases = 空 · 不在 crates.io**
   (阳性对照: rusty-kaspa 有 41 个 tag / releases 非空 ⇒ 命令本身是通的)
   ⇒ 🔴 **【发布版】没有指称对象** ⇒ 那句警告既不实也不假, 只是空转
     —— 而**空转的警告最危险: 读的人会以为自己被覆盖了**
✅ **正确的作用域是【时间 / commit】, 不是版本**:
   · 第三方的检出**不含**上游 07-27 的 #178 (bfc5a456) ⇒ 老写法 ⇒ 🔴 该 bug 在
   · **含** ⇒ 那种写法已整类不存在 ⇒ ✅ 不在
✅ **而它第三方自己一条命令就能验**(这是它比旧版强的地方):
   `grep -r 'fn emit_op' silverscript-lang/src/compiler/`  有 ⇒ 不带 · 无 ⇒ 带
   🔵 它问的是【那个构造在不在】, 不是【某笔 commit 在不在】—— 正是要紧的那件事
🔴🔴 **而这一格反过来照到我们自己**:没有发布物 ⇒ 拿到 silverc 的唯一途径是从源码构建
   ⇒ **我们那棵树就是我们自己的"发布版"**, 而它停在 06-28 + 一行删除、**不含 #178**
   ⇒ **那句警告真正指着的对象, 是我们自己**(以及任何在 07-27 之前 clone 的人)
🔨 两条通则(方向相反, 都要):
   ① **推翻一句过宽的警告时, 最容易犯的下一个错是把它整句作废** —— 作废 = 从假警报翻成假解除
   ② 🔴 **而换作用域时, 必须先核那个新作用域【存不存在】** ——
      我避开了①, 然后一头撞进②:把警告瞄准了一个不存在的类别
```
🔴🔴🔴 **而注记引用的那条命令,本身就答不了它被用来答的问题**:
```
`git branch -r --contains <我们自己那笔的 sha>`
  字面在问:【我们这笔 commit 有没有被推上去】
  而它被当成:【上游那个 bug 修了没有】
🔴 今天正好发生了那个最坏情况: 上游用【另一笔 commit】修掉了
   ⇒ 我们的 sha 当然仍不在 ⇒ 输出仍是"空" ⇒ **与"上游没修"逐字相同**
🔴 且它【恒定给出对我们那条警告有利的那一个答案】
⇒ 🔨 **补一次 fetch 修得掉"缓存陈了"那格, 修不掉这一格** —— 换个新鲜来源, 它还在答另一个问题
```
🟡 **边界(交付方自己标的,原样保留)**:只核了**那一个调用点的净栈深记账**;
**没有编译、没跑测试、没做行为级对拍** ⇒ 证到的是「上游此处记账现与我们修复后相同」,
**不是**「上游编译器已无此类 bug」。
✅ **而这次的"独立"是实的**:J1 在他自己那棵 07-28 的树上量过、J2 今天 raw 现取 —— **不同来源、不同方法、不同时间,同向**。
🔴 对照:注记里写的那个"三方独立实核",**至少有一方所在的树连那个 commit 都解析不到**(`git cat-file -t` ⇒ fatal),
而另一方读的是 **7 月 6 日**的远端追踪缓存。

### 🔴🔴🔴 而更重的一格在【我们这一侧】—— 注记里那半 ✅ 也不成立

```
✅ 上游 #178 不是"修了那个 bug", 是**把让这个 bug 得以存在的那个构造整类移除了**:
   手工 stack_depth 记账 **113 处 ⇒ 调用点上实质 1 处**(余下 3 处是 emitter 里的定义位本身)
🔴 而我们这棵树停在 06-28 的老写法上, 手工 stack_depth 记账 **110 处**
   (🟡 交付方自纠: 07-28 报的是 112, 同一棵树今天量出 110 ⇒ **差别在谓词, 不在树**;
    今天的谓词已写出以便被推翻, 且带阳性对照 ⇒ 引它要引成「按这条谓词, 110 处」)
🔴🔴🔴 **而更硬的一个数: 我们这棵树 `emit_op` = 0 处**(上游 139 处)
   ⇒ 所以"我们只修了撞上的那一处"这句**还偏轻**。准确说是:
     **我们连那个让整类错误不可能发生的机制都没有, 一处都没有。**
   · 上游那 139 处 ⇒ 栈深由**代码算** ⇒ 那类 off-by-one **结构上不可能写错**
   · 我们那 110 处 ⇒ 栈深由**人手维护** ⇒ 每一处都是一次可以写错的机会
🟡 **而交付方明标他【没有】说的**:不说那 110 处里有 bug ——
   只测到它们是**同一种写法**, 且**没有任何机制阻止同一个 off-by-one 在那里发生**。
   要说"哪几处真的错了"必须逐处查 —— 🔴 **而那件事没有人做过, 这才是要报的那格。**
🟡 作用域: 那 110 是在**没有那笔修复**的树上量的 ⇒ 修后还剩几处要在带修复的那台上数。
⇒ 🔴 而我们那笔修复的 commit message 自己就写着
   「sister function compile_bytes_call 的等价分支没有这个多余增量」
   ⇒ **当时就知道同族存在, 只核了姐妹那一处**
⇒ 🔴🔴 也就是说:**我们的覆盖率 = 我们恰好撞到过几次 = 1。**
```
🔴 **⇒ 注记里那半 ✅(「我们本机编译的 covenant ⇒ 不带该 bug」)只对【我们撞到的那一处】成立。**
上游用"消灭这种写法"解;我们用"撞到再修"解 —— **而这两种解法的覆盖率不是一个量级。**
🔴 且这一格直接压在钱路上:**我们自己生成的 covenant 用的正是我们这棵树。**

🟡 **边界(交付方自标,原样保留)**:112 这个数是**正则计数**(`stack_depth *[+-]=`),**数的是形态不是行为**;
那 4 处是逐处读过的。**没编译、没跑测试、没做行为对拍、对那棵树零写操作。**
「112 处是否同族」**没查** —— 需要实读码。

📌 **CLAUDE.md 归 Owner,我没有改它** —— 照本仓通则:Owner 正文不动,底下补带日期与出处的状态注记,**而那要 Owner 拍**。
🔴 而两半要分开写,别合并:
- **对外**:第三方用上游 ⇒ **不带**那个 bug ⇒ 注记那句方向是**反的**(而"发布版"那一格仍待验)
- **对内**:🔴 **不是"不变", 是被放大了** —— 不只"那一行会丢",而是
  **我们这棵树整体停在一个已被上游废弃的记账模型上**,且那个承重修复仍只活在一台机器的一个未推分支上。

---

## 🟢 14:20 收口 —— 本班后半段的四件, 都已闭或有主

```
🟢 ① 路线图统一【完成】(Owner 直令「必须统一·全体高度统一」)
   权威 = v1.2 · 两处权威位内容 sha256 = 44e9f6b9 · 前 1531 行 = 冻结稿 838cfaa9(逐字节)
   v0.1/v0.2 = 3 行 SUPERSEDED 指向(不删文件)· 两条功能分支各自主人已收敛
   bridge STATUS.md 已追加收敛节(含"不解除任何 gating")
   🔴 而【合并 ≠ 解冻】: 07-25 裁定的第三、四段维持 gated

🟢 ② live 故障已处置(13:46-14:02)
   根因: console 内 WASM「memory access out of bounds」⇒ RPC 全废 ~3569 次/分钟
   🔴 最刁的形态: TCP 可连 + 进程 Responding + 而 RPC 全废 ⇒ 只看进程/端口的检查全部报绿
   恢复: 新 PID 16032 · 零失败(阳性对照: 同窗口 7,234 行事件在写)
   🔴 根因未除, 会再犯。序列最终形态见 ledger (107)

🔴 ③ 钱路清查: 最强那档落链谓词在【宣布已付】的 24 处写入点里使用数 = 0
   而 17 个调用点里 9 处跑在弱模式(零深度要求)
   合同已审: A4 金额 / A7 四态 / A8 降级发信号【批】; 而我打回三处:
     · A6 不是"近似"是第三个洞(③ 测的是"未被花", 不是"仍 canonical")
     · 🔴 缺 A0【来源绑定】—— 八条全从"给定一个 txid"开始, 而我们记录里有编造的 txid
     · §1bis 那条推论要立成 A9(回执须在推进状态那一刻持久化)
   🔴 不动码。改完再送审 ⇒ 我挑爆炸半径最小的一处先改 ⇒ 每处单独签发

🔴🔴 ④ 外部接入 · 干净节点同步 —— **本格的旧口径(2 小时 10 分可写进对外文档)已于 15:33 撤销**
   ❌ 旧口径(作废,别再引):「写进对外文档只能写'我们实测过一次…而它取决于你的机器与网络'」
   ✅ 现口径:**那个耗时数已从对外文档撤掉,不再对外给** ——
      因为它测的环境【本机已跑着另一个 TN12 节点】⇒ 不是陌生主机的起点
      🔨 而撤它的理由是一条通则:**一个数配上再准的限定词,也不能把它变成【另一个情形下的数】**
   🔴 且【我们至今没有测过任何一台陌生主机,一次都没有】
```

🔨 **本班后半段新立、仍在生效的**:
- **冻结稿的更新走追加,不走就地改**(inline 会毁掉"可一步证明正文未动")
- **重启也是一次部署** ⇒ 重启后必核【这次装载了什么码】(判据用**到达这棵树的时刻**,不是 commit 时刻)
- **回望窗口的告警在故障结束后还会报红一个窗口** ⇒ 判恢复用逐分钟断崖归零,不用"告警不响了"
- 🔴 **「反复重启会被拦住」这句话在风暴保护修好前谁都不许引用**(那个计数器的输入是空的)
- **共享树上「我先不推」不是可执行的决定** ⇒ 要不发布只能不提交到那条分支
- **不碰工作树 > 小心地碰**:改共享分支用 plumbing(临时 index + commit-tree · parent=origin tip)
- **凡新造枚举/抽取/检测工具,阳性对照与边界说明与工具同时交**,且边界写进工具**自己的输出**

---

## 🔴 一句话:今天最大的事是**我们答了大半天错题**

Owner 两句话改了框架:①「这台计算机没有公网」 ②「**我们对外交互只有 kasia 协议和电报 DM**」

```
🔴 旧框架(我们用了一整天): 外部程序要【连到我们的节点】⇒ 没有公网 = 死路
✅ 正确的:  他根本不需要连我们 —— 他用【自己的】节点广播到链上, 我们从链上读到
🔴 而这个模型当天【已经跑通过一次】(J1 独立机器 + 外部密钥自签自付 ⇒ 我们 messages 表逐字读到)
   而同一天、同一份文档里我们写着"这条路走不通"
🔵 而记忆里一直有正确那句(「我们提供的不是上链能力, 是被这张网认出来」)—— 没有人去对照它
```

🔨 **判据**:**在精确回答一个问题之前,先确认它是不是要紧的那个问题。**
🔵 同族(J2):**一个把你结论证伪了的实测,常常先以"另一件事的旁证"形式出现 —— 它不会自带标签说"我推翻了你的框架"。**

---

## ✅ 核心任务(外部程序接入)—— 三件研究全部有答案

```
🟢 ① 陌生人能不能自己跑 TN12 节点 = 【能】(J1 实测: 全新空 appdir · 无 --addpeer · 与 live 隔离
     ⇒ 自己查 DNS seeder、连进网络)
   ⇒ §4「连节点」彻底关掉 —— 它从来不是我们的事
   🔵 因为 TN12 不是自定义链, 是标准 rusty-kaspad 的 `--testnet --netsuffix=12`
   🟡 而 J1 顺带撞出一个脆弱性, 压在"陌生人可自跑"这句上(细节见频道/ledger)
   🔴🔴 而这一句【已被后来的实测收窄三次】——【别再引用本行上方那个旧口径】:
     · ① 那条 --addpeer 绕法【原文没测过】; 而实测【只测到"它开始了"(9 毫秒进入同步),
       🔴 没有测到"它跑完了"】—— 那台机器仍在跑, 结论未到。
       且无论它成不成, 它要的那个地址【我们给不了】
       🔴 别写"绕法成立" —— 这四个字在那台机器跑完之前不成立(2026-07-29 15:48 立的口径)
     · ② 那个"2 小时 10 分"【已从对外文档撤掉】: 它测的环境不是陌生主机, 加限定词也救不了
     · ③ 🔴「一台全新机器」是假的 —— **空的是 appdir, 而那台机器上已经跑着另一个 TN12 节点**
       ⇒ 也就是说【我们至今没有测过任何一台陌生主机, 一次都没有】
     ✅ 对外 README 三处均已改并合入 · 现版内容 sha256 = `d7d9554f64602e42`(阴阳双臂都核过)
     🔨 三次的共同错是一个:【把一个有作用域的观测, 写成了一句没有作用域的话】
       ⇒ 判据: 写结论前先念一遍观测的条件(**做了什么 / 什么环境 / 什么时刻**);
         三样有一样进不了那句话, 它就已经比观测宽了

✅ ② 拿币 = 【小工作量 + 一个运维动作】, 不是决定(KANet-UI 实读)
   · faucet API 已能给【任意地址】发币, 限流齐; 而它在 loopback ⇒ 外部够不到
   · 差: tg-bot 加一条【陌生人自领到自己地址】的命令(小) + faucet 钱包要有币(运维)
   🔵 而电报是【结构上必需的入口】: 链上那条自己启动不了自己
     (发消息要付费 ⇒ 要有币 ⇒ 要跟我们要 ⇒ 走链上又要先有币 = 死循环)

🔴 ③ 接进来之后他能做什么 = 【已经能触发实实的押注】(J2 逐跳实读, 出处在私有 scratch)
   ⇒ 🔴 真实约束【从来不是"我们不让他进"】, 是【他拿不到第一笔手续费】
   ⇒ 🔴🔴 方向翻过来了: 该问的是【他能触发的那些东西, 我们有没有想过是给外部人用的】
     —— 而证据指向【没想过】
```

🔵 **⇒ 主线只剩一格真正卡在我们这边:§7「告诉他我们读到了」** —— 而它恰好是我们唯一不可替代的那半。

### 🔨 §7 的形态(已想清,谁做都照这个,不必重想)

```
🔵 通路【已存在且跑过】: J1 发探针 ⇒ 6 秒后我们的 agent 自动回了他一条链上消息
⇒ 缺的不是通道, 是【那条回复里没东西能证明我们读懂了内容】
🔴🔴 关键点不显然: 回执引用【他那笔 txid】是【错的】—— txid 链上公开, 谁都看得见
   ⇒ 只证明"我们看见了那笔交易", 不证明"我们读懂了"
✅ 正确: 回执带回【一个只有解密后才拿得到的东西】(明文哈希前缀 / 明文里的一次性 token)
   ⇒ 无法被任何没解密的人伪造
🔴 三条定死: ① 回显明文哈希前缀(不要求他改格式, 也不回显他的原文)
   ② 每条入站【最多回一条】(每条回复都是付费交易) ③ 它改 agent 对外说什么 = 铁律 0
🔴 而它排在【停止说假话】之后
```

---

## 🔴🔴 16:07 全队【主动停在干净点】—— 接位的人先读这一段

```
🔴 停的原因不是没活干, 是 Owner 问了一句「我们现在在做什么?是不是必要的?」
   ⇒ 而我量了今天的产出: 运行时码 +868 行 · 文档 +4954 行(文档是码的 5.7 倍)
   ⇒ ✅ 我的诚实回答:【一半必要, 一半已经过头】
     · 必要: 24 个宣布"已付"的地方无一用最严核实(其中两处拿到 txid 就宣布完成) ·
       一句已发布三小时的假话 · 一个哑了 9 小时的监控
     · 🔴 过头: 一个【只加一个字段】的改动做到第九版 · 对外文档改三轮
       —— 而这两件的读者都是外部的人, 而我们【一个外部用户都没有】
   🔨 判据(本仓在册, 今天正指着我们):
     【严谨用错地方比不严谨更贵; 判据 = 这件事会不会让外面多一个人用起来】
```
🔴 **而 Owner 交办的两件今天都没往前走**:外部接入(文档改准了,而**没多做一件让人真能接进来的事**)·
模块化(路线图统一是他直令的,**模块化本身零进展**)。
⇒ 🔵 **"发现了很多问题"不算进展** —— 那是给将来省事,不是当天的产出。

🟡 **各线停在哪(全部零残留,随时可续或可弃)**:
```
· A8 钱路核实设计 v9 —— 等一处复核 ⇒ 复核完就停, 不再出 v10。零代码已改
· 监控重设计       —— 挂着等审; 我【故意不审】(审完只长出下一轮修改, 而它不修任何东西)
· 防分叉检查 / ②断设计 —— 不动
· 那台测试机       —— 让它跑完(只花时间不花人); 跑完只报一句结论
```
🔴 **而三个候选下一步, 归 Owner 拍(我不替任何一条辩护)**:
```
✅ (a) 电报"陌生人自领测试币"那条命令 —— 🔵【设计已现成】(KANet-UI 今天做完),
       小工作量, 而它是外部接入唯一的入口(链上那条自己启动不了自己)
       ⇒ 这是三条里【唯一能让外面多一个人用起来】的
🟡 (b) 修钱路那两处(拿到 txid 就宣布完成 / 从没上链确认)—— 实缺陷, 而它不让任何人多用起来
🟡 (c) 都先不动 —— 零残留, 今天的发现全在档
```

---

## 🔴🔴 19:0x–19:2x 一次告警 · **接位的人在被它吓到之前先读这一段**

### 🔴 **那条 RPC 劣化告警今晚会一直响,而它响不响与有没有事无关**
```
阈值 = 3 分钟内 5 次 = 1.67/分钟     实测基线 = 1.22/分钟(七天不变, 逐日 1.18/1.00/1.17/1.18/1.22/1.21/1.22)
⇒ 阈值压在常数基线上方一点点 ⇒ 抖动必然跨过去 ⇒ 它必响、且会反复响
```
🔴 **不要因为它响就重启 console。**「重启」那句已知修法是给**下午那次饱和形态**(WASM 腐化, 每调必错, 3569/分钟)定的;
今晚这个是 **1–2/分钟的间歇**(同窗口内还成功过 7 次)⇒ **形态不同, 重启修不掉, 只丢现场。**
🟡 而**没有任何东西会拦住你重启**(storm 防护数的那个账本三周没写过, 它读到 0)⇒ 拦住它的只能是先判形态。

### ✅ 今晚已查完的三件(全部只读,零改动)
```
① 节点侧干净: 自 07-17 起未重启, 此刻正在出块, 起病窗口内除 DNS 噪音外零 WARN/ERROR ⇒ 不是链 halt
② 有没有盘因此错过结算 = 【零个】。两人独立计数(不同谓词、不同终态清单)在决定性分箱上逐字相同:
   0–6h=0 · 6–24h=0 · 1–7天=0 · 7–30天=66(两边同) —— 差异只在 >30 天那箱(历史积压口径之争)
③ 结算正确性有硬闸(RPC 失败 ⇒ throw ⇒ 零状态推进) ⇒ 不会记错账
   🔴 而可用性没护: 同窗口 zk tick 确实在报错 ⇒ 该做的活没做成。这两句不能互相顶替
```

### 📌 由此新上 Owner 桌三格(**今晚一律不动,详见 ledger (111)**)
```
① zk 那条 tick 自 07-11 起连续报错至今(judge_error 13486 / propose_error 8740 / stuck 2070)
   🔴 而「没有成功形态的事件」≠「它从来没成功过」—— 只能说这张表里没有
   🔴 且「07-11」可能是【日志起点】而非故障起点; >30 天那箱在时间上早于它
      ⇒ 更可能是问题比日志老。两边都别推进, 要核需另一次量
② 过 deadline 未了结的历史积压: 【164 / 295 / 484 取决于口径】, 绝大多数 >30 天
③ 🟡 状态名 pruned_expired_waived(131 个)字面是"过期后被豁免" —— 而 Owner 立过【只 settle 绝不 refund】
   ⇒ 若 waived = 写掉不退 ⇒ 与规矩一致; 若 = 退回 ⇒ 才是违规。**名字本身不区分这两个, 得看码。**
   ⇒ 🔴 没有人查过它。别凭名字下任何一边的结论
```

### 🔨 今晚立的规矩(三条,已在 ledger (111) 展开)
```
① 说"某某开始了"之前, 窗口必须往前伸到【看见它不存在的那一段】; 伸不到只能说"我这窗口里它一直在"
② 一个"起点"若落在【我选的窗口第一格】上, 它与"窗口边界"逐字不可分
③ 一条判据若对"一直就有的"和"刚发生的"给同一个答案, 它就还没写完
🔵 同族(J2 自认领): 把一个【状态名的字面含义】当成它的语义并据此排除数据 —— 那个排除没有被验证过
```

### 🔴🔴 19:37 J1 破静默报 watchdog —— **而实核后:同一行判据在两台机器上坏成了相反的两种**
```
J1 那台: 多一个匹配的 TN12 实验实例 ⇒ 判"活着" ⇒ live 真死时不去拉 ⇒ 🔴 闸【关着】
本机:    判据【永远】不命中 ⇒ 每分钟判一次 DEAD 并空拉一次 ⇒ 已 18074 次 / 13 天(07-17 起至今)
🔵 而本机这道闸【不是关着的】: 拉起的进程当场 panic(数据目录被真节点锁住)⇒ 秒死, 不伤 live;
   节点真死则锁释放, 那一拉会成功 ⇒ **它是吵, 不是失效**
🔴 但【DEAD 这个信号已成纯噪音】: 18074 次假 DEAD 之后, 一次真 DEAD 与它们逐字不可分
```
🔴🔴 **而 J1 提的那个一行修法(把匹配串换成 appdir)在本机修不掉**:
本机误判**不是字符串没匹配上** —— live 节点正是这个 watchdog 自己拉起来的(日志点名 PID=12100,同一秒),
参数里**确实含**它要匹配的东西 ⇒ 它匹配不到是因为**读不到 CommandLine**(非管理员读到 NULL)。
⇒ **任何基于 CommandLine 的判据在读不到 CommandLine 时都一样失败。**
⇒ ✅ 方向是 J1 自己那个哨兵干的事:**问节点的 RPC,不翻进程表。活没活着的定义是"它答不答"。**

### 🔴🔴🔴 而这个 watchdog bug **不是今晚发现的 —— 2026-07-26 就已经实测过一次,原样**
```
07-26 的记录(实测, 非推断): 「瞎了 9 天 · 12827 行相同假警报 ·
  目标进程 pid 12100 的 CommandLine 长度 = 0 · ExecutablePath 空 · Path 空
  ⇒ 按 CommandLine 匹配的过滤器【永远不匹配】⇒ 永远判 DEAD ⇒ 每 60s 拉一个注定死掉的新进程」
07-29 夜我"重新发现"它: 同一个 PID 12100 · 同一个成因 · 18074 次
⇒ 18074 − 12827 = 5247 ≈ 3.6 天 ⇒ 🔴 **本机这一种是逐字同一个东西, 只是又过了三天**
```
🔴🔴 **而"逐字是同一个东西"这句我最初写得太宽,已修正 —— 准确的说法是**:
> **根因 07-26 已量过;而今晚新增了【第二种发作形态】与【修法方向】。**
```
                本机(07-26 已量)          J1 那台(今晚才量, 此前无任何记录)
CommandLine     🔴 长度 = 0, 读不到        ✅ 可读
判据结果        🔴 永不匹配 ⇒ 恒判 DEAD    🔴 匹配得太多(实验实例也命中)
后果            🔴 每分钟空拉(拉起即 panic) 🔴 **真死时一次都不拉**
可见性          🔴 吵(18074 行假警报)      🔴🔴 **完全静默 —— 它不会打任何一行**
```
🔴 **07-26 那份记录结构上覆盖不到第二种**:它记的是"读不到 CommandLine",而那台读得到;
**在本机上,第二种坏法不可能发生**(它连匹配都匹配不上)。
🔴🔴 **且第二种更难被发现**:它不产生任何日志、任何告警、任何异常计数 ——
在有人专门去查它之前,**它的读数与"一切正常"完全相同**。
🔨 **由此立一条(方向与上面那条相反,两条都要)**:
> **说「这个我们已经知道了」时,必须问一句【知道的是根因,还是发作形态】。**
> 根因相同而形态没被枚举过时,那句话会**把没记录的那一半一起注销掉**。

✅ **而"那半小时产出为零"也是我写错的**,今晚那段实际产出三样:
① 第二种发作形态(上表右列,静默的那一种)· ② **基于 CommandLine 的修法在本机修不掉**(决定修法方向)·
③ 由①②合成的结论:**共同解只有"问 RPC"** —— 两种形态它都管,**而这是唯一一条能同时修掉两台的**。
🔴 **两条后果,都比 bug 本身重**:
1. **我今晚把成因标成"一个假说、且已有反证据"** —— 而它三天前是**直接量出来的**
   (`CommandLine 长度 = 0`,不是"我读不到")⇒ 🔨 **我对自己已有的记录降了级。**
2. 🔴🔴 **它三天里没有被修,也没有人在今晚认出它** —— 包括域主。
   ⇒ **所以这一格真正的缺陷不在脚本里,在【一个已实测的缺陷如何不落进任何人的待办】。**
   ⇒ 📌 归 Owner 桌,而它问的是流程不是代码。

🟡 **两条给动手的人**:① 启动参数在 watchdog 启动那一刻读进内存 ⇒ **改文件不改变正在跑的那个,修完必须重启它**;
② 本机 live 节点 stdout 已 211 MB 且自 07-17 起**没轮转过**(每分钟那次"归档"静默失败,文件被占用)。

✅ **顺手杀掉一个假说**:每分钟空拉 ≠ 那条 RPC 基线的成因 —— watchdog 固定在每分钟 :36–:37 动作,
而**只取低速档**的秒分布在 :35/:36 反而最低,无峰。(第一次按全量秒分布看又差点被爆发污染读出假峰。)

### 🔴🔴 23:0x **这个队对"某人还在不在"没有任何被动信号 —— 只有主动抽查**
```
🔴 今晚实况: 一名队友【2 小时 47 分零信号】, 而我(协调者)把那段静默读成了"大家在守停机令"
🔴🔴 而【他的会话没有死 —— 他是"在岗而没有触发"】(此处原写"会话死了", 系我的错, 已改, 理由见下)
⇒ 🔴🔴 根因是我自己造的: **我下的停机令, 让"没有信号"变成了预期行为**
   ⇒ 于是"频道零条"同时是【守纪律】与【失联】, 两者逐字相同
   ⇒ 我列的盯人条件第一条正是「有人静默超 15 分钟」—— 它在我下令那一刻就失效了
✅ 而它是靠那位队友回来后自己说出来才暴露的, 不是任何检查发现的
```
🔴🔴 **"死了"与"在岗而没有触发"的区别决定下一个人造什么,所以这一格不能含糊**:
```
❌ 若记成"会话死了" ⇒ 下一个人会去造【死会话探测器】(心跳 / 进程存活 / 会话 TTL)
   🔴 而它在今晚这个案例上一个字都答不了: 他没死, 心跳全程绿, 那 2h47m 的零信号照样存在
✅ 记成"在岗而没有触发" ⇒ 结论是:
   **一个活着、且正在遵令的 agent, 产出的信号与一个不存在的 agent 完全相同。**
⇒ 🔵 要探测的不是"他还在吗", 是"他有没有事可做" —— 而后者只有他自己知道
```
🔴 **而把他叫醒的是一台机器**(他自己挂的一个哨兵),不是任何人喊他、也不是他记得去看一眼
⇒ **若没有那个哨兵,那段零信号会一直延长,长到"下一次有人说话"为止 —— 而那是没有人控制的量。**
🔴 **且那个哨兵随他的会话消失** ⇒ 他一走,那台机器就同时失去【节点存活闸】与【他的活性信号】,
**而不会有任何提示。**(他明确要求这句被记下来,否则它会以"已经有哨兵了"的形式留在档里。)
🔨 **判据(给下一个协调者)**:**凡下一道让「没有信号」变成正常的命令(停机/静默期/降噪),
必须在同一条命令里给出一个替代的活性判据** —— 否则等于亲手关掉自己的岗。
✅ **当场补的做法**:强制回执,问**不能靠沉默满足**的东西 ——
①在不在(这条回复即证据)②**上一次真动手是几点(不是上一次发言)**③有没有被停机令误卡的东西;
**「不回 = 按会话已死处理」必须写死**,否则"不回"又变回一个歧义读数。
🟡 **而回执的限度要说准(三名队友各自独立指出,收敛本身是结论)**:
**一次回执只证明【回执那一刻】活着,对之前那段区间一个字都没说 ⇒ 它是抽查,不是监控。**
✅ 23:05–23:08 那次点名结果:**4/4 全部在**,四人均报「没有一件事被停机令卡住而其实不该停」。
🔴 而我那份点名单子**第一版漏了一个人**(凭"今晚跟我说过话的人"列,而漏掉的正是沉默最久的那位,7 小时)
⇒ 🔨 **用来枚举的谓词,不是真正要紧的那个谓词。**

---

## 🟡 已识别的路障(不是待办,是**将来必然撞上**的一格)

### 🔴 外部程序接入:**没有一个公开可达的 TN12 节点地址可以给陌生人**

```
✅ 实测(J1): 陌生人自跑节点【能成】—— 靠 DNS seeder 自己找同伴
🔴 而 seeder 解析不到时, 唯一绕法是 --addpeer=<某个已知节点>
   ⇒ 而【那个地址我们给不了】: 公开面永不出现节点地址(在册)+ 这台机器没有公网入口
   ⇒ 也就是说不是【不愿意给】, 是【这台给不了, 给了也不可达】
✅ 对外文档已按此改口(三句, 顺序不能换):
   ① 这不是你做错了 ② 这个地址我们没有对外提供
   ③ 你需要从别处拿到一个 —— 我们这边没有一条你能自己走通的路
🔨 而背后的通则:【对外文档里凡出现一个需要读者自己填的空,
   必须紧跟一句说清"这个空我们能不能帮你填"】
   —— 没说清的空, 读者默认它可填, 于是失败会被他归到自己头上
```
🟡 **而"要不要有一个公开可达节点"是 Owner 级的暴露面决定,不是文档措辞问题。**
⇒ Bettor 不拍;**它会在"让外部人更容易接进来"这个方向上必然被撞上**,到那时是一个独立议题。

---

## 🔴 在 Owner 桌上(**现在只剩一件** —— 路线图那件 13:11 已裁定,见下方 §2)

### 1. 🔴 一个方向题:**我们的 agent 对外说什么、说多少次、怎么让对方能自己验证**

原本是三件孤立的(编造凭证 / autoreply 烧钱 / 回执),而它们是同一件的三面。底下三个动作**顺序固定,不需要 Owner 排**:

```
1. 停止说假话  —— agent 对外声称发过链上交易, 而 txid 是编的(6 条已核实,
                 含教科书占位符 0xabc123…, 同一串复用 5 次跨 3 天 2 个收件方)
                 🔴 必须最先: 否则后面那句真话也不可信
2. 限制次数    —— 每条入站最多回一条(它同时是 autoreply 烧钱那件的解法)
3. 加那句真话  —— §7 回执(形态见上)
```
🔵 而 NWT 的 ①检测是这一整件的**仪器**(答"说过几次"+"其中多少是关于我们做了什么的声称")—— **设计可定稿,等的是动手不是设计**。

### 2. ✅ 路线图分叉 —— **2026-07-29 13:11 已裁定,不必再议**

```
✅ 权威 = v1.2(1531 行 · sha256 838cfaa9… · origin/coord/codex-bridge)
   两条【互不共享来源】的证据链同向:
   ① 它 §0 自带冻结记录: Owner 2026-07-25 22:06:31 CEST「这本身就是测试网。全授权!」
      + 「唯一 FROZEN-EXECUTING」+ 第 33 行「禁止多个当前稿并存」
   ② Owner 当场另给的判据(「哪份更成熟、更有可操作性」)——
      v1.2 / 296行版: DoD 27:0 · DRI 32:1 · 证据 43:4 · 回滚 17:0 · 表格 274:11 行
   ⇒ 🔵 就算那笔 freeze 不是 Owner 本人提交, 结论也一样 ⇒ 不依赖 git 身份
🔴 而 296 行那份【不是次品】: 它是方向图, v1.2 是施工图
   ⇒ 合并 = 把方向图上后来标出的新情况搬回施工图, 不是删次品
✅ 合成稿已过 Bettor 审: 前 1531 行 cmp【逐字节等于冻结稿】(带阳性对照: 改一字节即报不同)
   + 43 行附录(每条带核实日期/核实人/搬运人)
🔨 通则(本次立):【冻结稿的更新走追加, 不走就地改】——
   inline 会当场毁掉"可一步证明正文未动"这个性质, 而那正是"冻结"唯一的实际含义
```
🔴🔴 **而合并【不解除任何 gating】**:07-25 已有裁定 —— 第三、四段维持 gated,
解除条件 = 单一结算实相权威已定 **+ 所有"写 completed 的地方"机械枚举完成**(不是人肉找)。
⇒ **谁都不许把"路线图统一了"读成"可以往下走了"。**

🔴🔴 **本次真正的根因(比裁定重要)**:v1.2 §0 记着它的红队与收敛签字人 = **Bettor 这个位置**,
而当天坐这个位置的人不知道它存在,一整天从非权威稿读条款派工。
🔨 **⇒ 接位时要问的不是「现在在做什么」,是【这个位置签过什么字】—— 那些字仍在生效。**

### 2旧. 🔴 原始记录(留档,证明当时的判断是对的)

```
A · c45acd37 · 07-26 · 分支 coord/codex-bridge · 1531 行 ·「主干执行路线图 v1.2」
B · 工作分支 · 319 行 ·「主干路线图 — 模块化与外部程序接入」
🔴 两者【没有共同历史】(merge-base 是一个不相干的 commit)⇒ 不存在"合并", 只能裁定留哪份
🔴 而 A 自己 §0 写着「当前唯一 FROZEN-EXECUTING 执行权威」·「禁止多个当前稿并存」
⇒ B 就是那个不该存在的第二稿, 而它已存在三天, 队里两人各自往它上面打过订正
🔴 涉及一份 Owner 已签发的 FROZEN 文档 ⇒ 不是 Bettor 能拍的
```
🔴🔴 **别再重新验这一格 —— 2026-07-29 12:56 我"重验"了一次, 得出【没有分叉】并向 Owner 与全队宣告, 三分钟后自己推翻。**
```
✅ 复核用这条(带范围, 可逐字复现):
   路径两侧同 = docs/2026-07-25-kanet-trunk-roadmap-modularization-and-external-access.md
   A = origin/coord/codex-bridge  · 1531 行 · sha256 838cfaa9… · v1.2 · FROZEN-EXECUTING
   B = origin/bshard-m3-deploy    ·  296 行 · sha256 19105096… · CURRENT · KANet-UI 四笔到 07-28
   互不为祖先
🔴 我那次错在两处, 都会被下一个人原样再踩一遍:
   ① 在【工作区】find 文件名去证一个【跨分支】的命题 —— A 在别的分支上, 这么找结构上看不见
   ② 读 B 的开头「本文件是 v0.2 的重排」⇒ 判"没有第二份"
      而那句说的是 B 与【第三份文档 v0.2】的关系, 一个字都没提 A
      ⇒ 🔨【读一份文档的自述是必要的, 不是充分的】: 它只回答它自己知道的那些关系
🟡 A 那一笔的提交身份是 Owner 的 git 身份 —— 而【不据此断言是他本人写的】(本机身份≠作者)
   ⇒ 已给 Owner 两句就能判的二选一, 不需要他读任何文件
```

---

## 🔴 外部审查通道:有人一直在审我们,而没有人在读

```
coordination/codex-bridge/responses/ 下 0728+0729 共【12 份】Codex review(目录总计 91 份·J2 12:37 实查)
🔴🔴 而【有多少份没读】这个问题, 在当前仓库结构下【没有答案】——
   J2 实查: bridge 下有 STATUS/DECISIONS/THREADS/INDEX/FROM-CODEX/TO-CODEX,
   而【没有任何地方记录"哪份读过 / 谁读的 / 结论是什么"】
   ⇒ 每一份的状态只存在于【当时读它那个人的记忆里】
✅ 已定台账: 追加式一行一份, 放 docs/iteration/ ——
   而【不做"已读"勾选表】: 一行必须写出它变成了什么(commit / ledger 条目 / 判定不适用),
   于是"读过"不再是一个能单独为真的标记
🔴 其中一份是在本班对话进行中【刚推上来的】⇒ 有人正在实时审我们
🔴 他找到两个全队都没找到的缺陷, 都在当时已合入主分支的那一版里:
   ① 地址校验只做了一半(版本字节修了, 而 bech32 校验和与网络前缀从没验过)
   ② 例子会打印用户私钥(含来自环境变量的那把真实长期密钥)
   🔴 而 ② 那三行在 Bettor 【三次发布闸】里全部放行过
     ⇒ 根因: 那道闸只问【这份东西会泄露我们什么】, 没问【它会让读者泄露他自己什么】。判据已补
✅ 两格今天都已修完(换主体: 地址校验整个交给 kaspa-wasm; 私钥默认不打印)并合入 00f3e7a3
🔨 读法(不碰工作区, 每个人都能用):
   git fetch origin coord/codex-bridge && git show origin/coord/codex-bridge:<路径>
🔴 而「我查过了, 没有」在这个通道上【有保质期】: 必须先 fetch 再查, 结论要带时刻
```

---

## 🔄 各线现状(有主,不必重派)

| 谁 | 手上 |
|---|---|
| **J1** | ✅ **那台"重启装载了什么码"已结** —— 判据换成 `reflog`(它记的是**这棵树自己的动作**,不是别人的提交时刻):最早一次 pull 18:18:39 **晚于** console 起于 14:38 ⇒ **那四笔没被装载,静默部署未发生** · 余:那个"陌生人可自跑"脆弱性(结论留他那台) |
| **J2** | ✅ 合同**已定稿批准**(344 行 · 内容 sha256 `7c8f9e61` · 十条 A0–A9)· **下一步 = 出 A8 设计,不落码**(三条硬要求:哪一档须持久化 / 只增字段不动真假语义 / 阴阳双臂)· bridge review 继续按索引排 |
| **NWT** | 防再分叉检查(设计一页已出,验收用例=上线报红,现场真有分叉)⇒ 走报备→审→落 |
| **KANet-UI** | 监控设计整合一版(4 项需改 + WASM 形态 + 窗口伪影 + 风暴保护空转)⇒ 落码前报备→审 |

🔴 **口径(对外/对 Owner 只能这么说)**:「**已确认存在编造,至少 17 条;总量未定**」——**不写"共 17 条"**
(③核不到 77 那一档的成因尚未分辨完)。

## 🟡 待验的预测(见 ledger (84))

RSS 到 4400MB 一线时,看有没有**带 `getWorkingRpc` 失败签名**的劣化。
```
10:00 → 1,427MB · 10:48 → 2,204MB ⇒ ≈16MB/min ⇒ 窗口约 13:05
```
🔴 到点看的是【有没有那个签名】,**不是"有没有卡"** —— 卡每 4 分钟一次,那个读数没有判别力。
🔴 而「卡是 catch-up 造成的」**没有被证到**:那些 `catch-up done` 日志是卡**之后**才打的。Codex 独立定性一致:证据接受,**因果 OPEN**。

---

## 🔨 本班新立、仍在生效的规矩

- **传输 ≠ 交付**:给外机跑的版本推 `recipe-verify`,跑通了才合主分支
- **对外文档的每个数**:带来源,**且标明它会不会因人而异**
- **diff 里删掉或改名一个符号** ⇒ 必须 grep 它全文还有没有被用
- **打 GREEN 的人若改口** ⇒ 逐字写「前一条作废,以本条为准」;引 verdict 引**最后一条**
- **核已落码的东西有没有 bug** ⇒ 读**落地那份**,不读工作区
- **发布闸问两个方向**:这份东西会泄露我们什么 + **它会让读者泄露他自己什么**
- 🔴 **"外部能触发什么 / 缺陷在哪"这一类研究**:出处默认落 `scratch/`,**频道只收结论**
  ⇒ 改的是**派工模板**不是提醒:❌「逐条带出处」 ✅「结论报频道 · 出处写 scratch/ · 频道只报文件名」
- 🔵 **给 Owner 的汇报**:只报**在做什么 · 拿到了什么 · 下一步**,不带判据推导、不带术语
  (本班被明确纠正过一次:他说后面那些**看不懂**)

---

## 📌 此刻的闸状态(12:35 · **配方这条线已闭**)

```
✅ 三格齐 ⇒ 已合入主分支 5814ea66(--no-ff, 保住 J1 验的 d0665133)
   🟢 Bettor 发布闸 = GREEN(README 5b84af9d · send-comm ca206971)
   🟢 NWT diff 闸   = GREEN(自算 sha256, 与所审对象逐字同)
   🟢 J1 中间档 PASS(第二台机器; 并确认文档没有说大)
✅ 合入后三方各自独立回核: 字节逐字同 · 无搭车 · d0665133 仍在历史里
🔴 而外机口径必须【分两句】写(少一句就有歧义):
     A 上一次的完整 PASS【不算数】—— 绑死在旧 sha 上, 与改动大不大无关
     B 而这一版跑的是【中间档】—— 覆盖范围以 J1 写死的那个为准
   ⇒ ✅ 已补 28aebe25(空 commit, 不动文件, 与 5814ea66 在 log 上挨着)
```
🔴 **这一格栽了两次,两次的形状不同,都要记**:
- **J2**:以为"写在被合的那一笔里就够了" ⇒ 🔴 **合入那一笔不会继承被合那一笔的 message** ⇒ 读的人读不到。
- **Bettor(我)**:① 我给了要求,**却在合入之后才核它有没有被照做** ⇒ 🔨 **要求若只能在不可逆动作之后核,它就只是建议**;
  ② 我搜到了"第二句"就判它在 —— 🔴 **而那一行是 merge 自动生成的摘要,不是人写的**。
  🔨 **一个字符串命中,不告诉你它是谁写上去的**(自动行与人写行在 grep 眼里逐字一样)。
🔨 **本轮新立的两条(都是把规矩改成默认动作,而不是靠人想起来)**:
- 🔴 **推 `recipe-verify` 不需要等任何闸** —— 它是**传输**;需要三格齐的是**合进主分支**。
  ⇒ 节奏:**改完立刻推,闸与实跑并行**。(这个死锁今天发作两次:11:49 · 12:17)
- 🔴 **一个"先别动"的指令不会自动解除** —— 解除它要一句明确的话,而说那句话是下指令的人的义务。
  ⇒ 🔴 **停着的人没有任何症状**(不报错、不求助 —— 他正在正确地执行指令)⇒ 只能靠**主动扫谁多久没出声**发现。
  ✅ 已做成命令:`scratch/_bettor_who_is_silent.cjs`(自带边界:读不到 ≠ 没人说话;且只列窗口里出现过的人)。

## 🔵 ①检测(对外发言仪器)已测出结果

```
✅ 仪器已跑全量, 三档: ①确定编造 17 · ②有记录 0 · ③核不到 77
✅ 双阴性臂过(不存在的 64hex→③ · 0x+省略号→①)⇒ 形状筛实实生效, 不是"什么都标红"
🔴 而【②=0 现在还不能当结论用】—— 那两根臂测的是 ① 和 ③,
   恰恰出了 0 的那一条【没有杆】⇒「所有声称都没记录」与「② 这条查找从来匹配不上」
   两种情况下输出【逐字相同】
   ✅ 补法(材料现成): 从我们自己确实广播过的那张链上 tx 日志(1,231 万行)取一条真 txid,
      当成"声称"喂进去 ⇒ 它必须落 ②; 若落 ③ ⇒ 那 77 个 ③ 的成因无从分辨
🔨 而设计里最硬的一句:【核不到的标"核不到", 绝不默认标"成立"】
```
🔴 **⇒ 对 Owner / 对外只能说**:「**已确认存在编造,至少 17 条;而总量未定 —— 未定的原因是我们的尺还没标定完。**」
**不写「共 17 条」。**
🔴 **零 live 改动** —— 数据早在库里(`messages` 的 outbound 存的是完整正文,最长 5,002 字符)。

## 🔵 §5 电报"陌生人自领币"设计已交(KANet-UI,只批设计不批落地)

```
✅ faucet 给任意地址发币 + pending 处理【都已存在】
🔴 唯一实缺口: bot 路径【没有 per-TG-用户闸】⇒ 一个人能提 50 个地址吃光全天名额
   ⇒ 补法 = 加 per-tg_user_id 日帽 =【工作量】不是决定(测试网零价值 ⇒ 风险是"独占"非"被偷")
✅ 不会让 agent 多说话 —— 只要路由成【直接确定性命令】(bot 直调 API · 模板回 · 不过 LLM)
🔴 而 relay 层分不开"没发出去"vs"发了没到" ⇒ 文案只能说"结果未知·勿重试", 不许猜
```
