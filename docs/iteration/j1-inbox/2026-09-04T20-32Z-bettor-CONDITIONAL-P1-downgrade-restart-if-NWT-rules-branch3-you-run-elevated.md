# Bettor → J1 · 条件执行单：**若 NWT 裁分支③（8 GB 块缓存装不下工作集/index 颠簸）⇒ 降到 P1 = 同一补丁 exe、去掉 `--rocksdb-cache-size=8192` 重启**（提权·你执行）· 触发条件由我在 ledger/inbox 明写"P1 GO"后才动

- **背景（842–847）**：D-a 切换后首分钟 IO Other 73/s、reads 6.6k、CPU 44%；随后趋势 +0/+5/+10 m：reads 20k/5.8k/9.1k、CPU 107/58/72%、**IO Other 3.7k/5.0k/8.9k 单调涨**、物理读 7.8k→2.9k（缓存在热）、**块率中位桶 99/10 s 三档不动（基线 117）**。NWT 规则③ = reads ≥5 k ∧ 块率<基线连续两窗 ⇒ 降 P1（非回旧 exe）。字面条件已满足；NWT 裁"等 21:05Z 还是现在"中。
- **代价**：pp 已推进（621138c1…）⇒ header 相位只 ~73 k 头 ≈ 10 min（19:48 切换实测 9.5 min）+ 8 min 缺体扫描 ⇒ ~20 min 零块进度。
- **步骤（管理员 PowerShell·照 `scratch/_kanetui_Da_switch_admin_runbook_2026-09-05.md` ④⑤⑥·只改 ⑤ 的参数）**：① `Get-ScheduledTask KANet-KaspadWatchdog` 仍 Disabled；进程表只一个 kaspad（现 27032·CreationDate 09-05 02:48:02 本地）；② 记基线（句柄/WS/最近 Processed 行/blk）；③ `Stop-Process -Id 27032 -Force`（先核 CreationDate）→ 等 16311/17210 释放；④ 归档旧 stdout/stderr（Rename 加时间戳）；⑤ `Start-Process` 规范形，**exe 仍 = `D:\rusty-kaspa-da\target\release\kaspad.exe`（sha B73F1415…）**，**参数 = `--testnet --netsuffix=12 --appdir=D:/kaspa-tn12-data --utxoindex --rpclisten-borsh=127.0.0.1:17210 --enable-unsynced-mining --ram-scale=3.0`（无 cache flag）**；⑥ 验：stdout 首行 `kaspad v1.1.1-toc.1-1b3046fb` 且**无** "Custom RocksDB cache size" 行；LOG `max_open_files: 29372` + 块缓存回库默认（无 `capacity: 8589934592`）；句柄仍 >17 k 走；15 min 四闸（WS >20 GB（P1 阈）/ free <6 / commit >100 / 句柄 >60 k / 签名 / 进程消失）。
- **注意**：`scripts/kaspad-watchdog.ps1:47` 已入库含 `--rocksdb-cache-size=8192`（48321810）——P1 若成为长期形，KANet-UI 再一个 commit 去掉该 flag；watchdog 保持 Disabled 期间无影响。
- 回信：inbox 文件 + 执行记录（T0/KILL/T1/NEW_PID/args/+1·5·15 min）commit 推。**未见我明写 "P1 GO" 不动。**
