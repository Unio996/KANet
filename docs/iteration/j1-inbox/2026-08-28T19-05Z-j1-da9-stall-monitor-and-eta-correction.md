# J1 → Bettor：da9 静默卡死探测器已上线（跑在 younio，只读）+ 我自己修正 44h 那个数

- 提交人：J1（younio）
- 时间：2026-08-28 19:05Z
- 性质：**只读**。**没有在 da9 上安装任何东西**、没改它任何状态、没 stop/start、没写它磁盘、没推分支。

## 1. 探测器已上线并跑通 —— younio 本机，`466c720f`

我上一份报告建议给 da9 加卡死监控、并说"归你裁，不擅自在 da9 上装东西"。我按这个口径做了个**不需要动 da9** 的版本：进程跑在 younio，每 300 秒经 SSH 跑一次**已经在 da9 上的只读探针** `scratch/j1-remote/dagstate.mjs`，只读计数器。

**判据**：`blockCount` 连续两窗增量为 0 ⇒ 告警。
理由：重块区只会让速率**变慢**，不会让它**归零** —— 这条线把"静默卡死"和"慢"分开。

**运行证据**（不是声明，是日志）：

```
20:56:52 === da9 IBD 探测器启动 (间隔 300s, 判据: blockCount 连续两窗零增量) ===
20:56:55 #1 ♥ blocks=1611682 daa=79364475 lag=10673.5分 peers=4 (首轮, 无增量可比)
21:01:58 #2 ♥ blocks=1615543 +3861 (772 块/分) daa=79368336 lag=10660.1分 peers=4
```

两个细节是刻意的：
- **首轮明说"无增量可比"**，不拿单点冒充速率
- **采样失败不计入卡死判定**（拿不到数 ≠ 没长），但必留痕 —— 修的正是我 watcher v1 那个"只在有变化时才写日志、冻住三小时没人发现"的病

顺带一条好消息：**da9 的 peers 从 1 涨到 4** 了，我上一份报告担心的"唯一 peer 掉线"暴露面自然缓解了不少。

## 2. 🔴 我修正自己上一轮给的 ETA：44h → 44~67h，coord 的 77h 站得住

上一轮我用 6 分钟窗口测出链上时间 **5.07× 实时**，据此给了 **44h 下界**。这次第二个独立窗口（5.05 分钟）测出来是 **3.65× 实时**：

| 窗口 | 块速率 | 链上时间倍率 | 净追赶 | 推出的 ETA |
|---|---|---|---|---|
| 18:37–18:43Z | 841 块/分 | 5.07× | 4.07× | ~44 h |
| 20:56–21:02Z | 772 块/分 | 3.65× | 2.65× | **~67 h** |

⇒ **速率本身在波动，我上一轮那个 44h 是快时刻的瞬时值，不该当下界用。**
⇒ 修正口径：**当前实测区间 44–67h**，而 coord(707) 的 **~77h 落在慢端的延长线上，是站得住的**。我上一轮说"77h 可能偏保守"这个语气收回。

结构性结论**不变**且仍然重要：**闸门的约束项是 `lagMinutes<10`，不是 daa** —— daa 会先约 15h 达标而 `isSynced` 仍为 false，只盯 daa 会误判 READY。

## 3. younio 仍卡死，等你口径

`blockCount=1`、`headerCount` 冻在 5,930,693 不动、peers 掉到 3。根因见 `2026-08-28T19-00Z-j1-younio-ibd-stall-rootcause.md`。
解卡脚本 `scratch/_j1_younio_ibd_unstick.mjs` 写好了（ban 卡住的 IBD peer 逼重选，不重启、可 unban 还原、内置 4 分钟观察窗+失败自动还原），**但被 younio 本机的权限闸拦下未执行**，我也认为该先报你。

## 4. 一条给全员的踩坑（我今天第二次撞）

**PS 5.1 会把无 BOM 的 UTF-8 当 ANSI 读**，中文注释足以让 `.ps1` 语法崩掉，而且**完全静默** —— `Win32_Process.Create` 返回 `ReturnValue=0` 给了个 PID，进程却起不来、不报错、不留日志，看起来就像"启动了但什么都没干"。两台机器都中招（远端我 8/28 早些时候撞过一次）。**写 `.ps1` 必须带 BOM。**
建议进 ANTI-PATTERNS（要不要收归你定，我不擅自改共享文档）。

## 5. 本轮没做的事

- 没碰 da9 任何状态、没在其上安装任何东西
- 没执行 younio 解卡（等口径）
- 没推分支（`466c720f` 在 younio 本地；`4e0e0f00`/`9ef7a564`/`409ab9d4` 在 da9 本地）
- 没改播报文案、runbook、`/d/silverscript`

## 6. 附一条我自己的隐患（免得日后害人）

younio 的 `D:\KANet` 克隆**落后 origin/bshard-m3-deploy 2318 个 commit**（本地领先 16）。pre-commit hook 已经在吼这条。含义：**我不能在 younio 这个树上做任何代码审阅/grep 结论** —— 会读到 STALE 版本、假阴性。我审码一律去 da9 的 `D:\kanet-tn12`（当前树）。在此备案，免得我哪天顺手在本地 grep 完给出错结论。

---
复核：younio `scripts/j1-watch-da9-ibd.ps1`、`logs/j1-da9-ibd-watch.log`；告警会落 `logs/j1-da9-ibd-ALARM.txt`。
