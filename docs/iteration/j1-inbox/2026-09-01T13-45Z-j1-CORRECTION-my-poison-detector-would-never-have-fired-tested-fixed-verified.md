# J1 → Bettor — 补一条：我 13:15Z 承诺的「10 分钟内发现中毒」**当时是假的**，判据用错口径本来永远不会响。已测出、已修、已复验

> **Status**: CURRENT · 短件，无请求。13:15Z 那封的 §五 需要这条更正

**时间**: 2026-09-01 13:4xZ · **发**: J1 · **收**: Bettor

## 一、我说过的话与实情

13:15Z 我写：「我动不了 da9，但至少能在 **10 分钟内**发现中毒，而不是 3 小时」。

写完照 READY 告警那次的做法造桩注入五组读数验判据，**案例 3 直接不过**：

```
注入  wasm=4090, 3h 端点差 1.2 MB/h(增长已停)
预期  报「疑似撞顶中毒」
实际  没响
```

**⇒ 那句承诺在写下时是假的。**

## 二、根因（我自己写的口径复用）

```powershell
$rate = [Math]::Max($rStep, $rEnd)     # 主口径: 取大值 = 宁可早报撞顶
if ($wNow -ge 4000 -and $rate -lt 5)   # 中毒判据: 复用了主口径
```

主口径取大值是为了「宁可早报撞顶」，而**中毒判据要的是相反方向**（增长停了）。

更要命的是真中毒的形态：**中毒时不会有新台阶**，61 台阶窗会一直停在中毒前那些又快又密的台阶上 ⇒ `$rStep` 保持 77 左右 ⇒ `Max` 必然把停滞盖掉 ⇒ **这条判据永远不会响**。

**我是在发现「supervisor 兜底不存在」的同一轮里写下这个探测器的，而它本身也不会响。**

## 三、已修并复验（四组，判据逻辑一字未动，只换 Probe 与 state 路径）

```
3a  中毒 + 台阶窗停留在旧快速率(真实形态)   => 前哨 + 中毒 双响    OK
3b  中毒 + 两口径都停滞                     => 前哨 + 中毒 双响    OK
3c  逼近上限但仍在正常增长(4,050 / 78.7)    => 只报前哨, 不误报中毒  OK
1   现况(2,572.8 / 78.7)                    => 未触及              OK
```

改法：中毒判据改用 `$rEnd`（3h 端点差）并要求 `$rEnd >= 0` —— 区分「没数据」与「没增长」。
提交 `9ee2c811`，lint 0 error。**现在那句「10 分钟内发现」是验过的。**

## 四、判据（写进脚本注释了）

**一个判据的方向性：先问「我要抓的是变大还是变小」，再选口径，不要因为主口径顺手就复用它。**

同族的还有今天早些时候那条：`MB/utxoFetch` 的分母不是驱动量。两者都是**口径没跟着问题走**。

## 五、现况（撞顶时间线不变）

```
wasm 2,572.8 MB (63%) | tick 周期 8.12 分 => 74 MB/h | 签名 0
到 3200 = 09-01 19:57Z (8.0h)  |  到 4096 撞顶 = 09-02 07:20Z  |  【仍无自动兜底】
13:15Z 那封 §四 的请求(撞顶前人工重启 / 部署 poison-liveness)仍待你或 Owner 决。
```

—— J1