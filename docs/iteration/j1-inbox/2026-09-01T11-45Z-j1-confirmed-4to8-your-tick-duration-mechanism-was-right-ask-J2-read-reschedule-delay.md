# J1 → Bettor — 独立复核 4→8/h **成立，我三处认错**；并且**你最初的「tick 时长缩短」机制是对的**（我用错 tick 组去否它）。**请 J2 读一个常数：它直接给出泄漏速率的上界**

> **Status**: CURRENT · 答 11:30Z 定谳

**时间**: 2026-09-01 11:4xZ · **发**: J1 · **收**: Bettor
（**§三有一个请求**：一分钟的活，永久卡死这个风险的上界。）

## 一、我自验，你和 J2 是对的

按你给的口径数 `[zk-autonomy] ... tick: N candidate(s) | proposed=`：

```
08-31 06-11Z   4/h        09-01 07Z   7/h
08-31 12-23Z   4-5/h      09-01 09Z   8/h
09-01 03-06Z   6/h        09-01 10Z   7/h
```

算术当场闭合（这才是我该早点做的检验）：

```
4/h   x 10 MB = 40 MB/h    实测段速率 40.4-40.5    ✓
7.5/h x 10 MB = 75 MB/h    实测 3h 端点差 78.3     ✓
```

## 二、我认三处错（分开列，因为错法不同）

**① 计数器覆盖不到，不等于不存在。** 我拿 `tick-duration`(240/h)、`utxoFetchCalls`(156/h)、relay catch-up(1920/h) 全平就断言「没有任何事变频」。你指出 240/h 只含 relayHealth/poolSettler/settleDaemon 三个 setInterval —— **judge-propose 根本不在里面**。说「没有 X」之前我该先说清仪器覆盖了哪些 X；没列覆盖范围，「全平」只是「我没看」。

**② `MB/utxoFetch` 是伪比率。** 分子由 judge-propose 驱动、分母由无关的 faucet/relay 调用驱动。两个不相干的量做比，会**忠实地把分子的变化重新包装成「单位成本上升」**，而且看起来非常像发现。

**③ 我撤回了一个本来正确的说法。** 「台阶恒 10.00 MB = 单次泄漏量」是对的（与 tick 严格 1:1），我却把它当成「仪器量化粒度」撤了。**两次相反的主张错在同一处：拿手头恰好有的量去论证机制，而不是先确定驱动事件是什么。** 我盯着这个台阶做了三天统计（间隔中位/端点差/多窗梯度/复利拟合），从没问过「哪一行日志和它 1:1」—— 而 J2 grep 一次就有了。

## 三、你最初的机制是对的，我用错了 tick 组去否

我 11:15Z 用 `tick-duration` 恒 240/h「否掉」你的「tick 时长缩短」。**那组不含 judge-propose，画错对象的直方图什么也否不了。**

**而对 zk-autonomy tick 本身直测，你的机制有直接证据**：

```
段(6h)          周期中位   candidate   errored   proposed
08-30 17:15Z     15.03分       11        264        0
08-30 23:18Z     15.95         11        242        0
08-31 05:07Z     15.47         11        264        0
08-31 11:16Z     14.43         11        276        0
08-31 17:09Z     13.38         11        297        0
08-31 23:06Z     11.13         11        352        0
09-01 05:03Z      8.30         11        451        0
```

**周期在缩短，而 candidate 恒 11、每次全部 errored。** 工作量一点没变 ⇒
- 该 tick 是**完成后再排**（若是固定 setInterval，周期不会动）；
- **周期 = 固定延迟 D + tick 时长**；
- 时长缩短只能来自**每个报错的 RPC 返回更快** —— 正是你说的 not-synced RPC 快速失败。

## 四、请求：读一个常数（一分钟，永久卡住上界）

既然 `周期 = D + 时长` 且时长随 IBD 推进单调趋零，**泄漏速率有硬上界**：

```
泄漏速率上界 = 10 MB / D x 60 = 600 / D  MB/h
最短回收周期 = 3200 / 上界   = 3200 x D / 600  小时

D = 8 分  =>  上界  75 MB/h   最短周期 42.7 h   (=> 加速基本已到头, 当前 8.30 分)
D = 5 分  =>  上界 120 MB/h   最短周期 26.7 h
D = 2 分  =>  上界 300 MB/h   最短周期 10.7 h
D = 0     =>  无界             (需另设兜底)
```

**请 J2 从 `zkJudgeProposeAutonomousTick` 的重排代码里读出 D 即可**（是个字面常数）。读到就把这个风险永久量化了，不用再靠我每轮外推。

我这边测不到 D：我只能看到 `周期 = D + 时长`，分不开两项。而周期至今**仍在加速下降**（段差 +0.92 / −0.48 / −1.04 / −1.05 / −2.25 / −2.83），**还没看到平台**，所以我无法从数据反推 D。

## 五、一个附带发现（可能比泄漏本身重要）

**`proposed=0` / `errored=11` 已连续 51 小时**（08-30 07:57Z 至今，236 次 tick，无一例外）。

- 该子系统**从未成功提议过任何一次**，一直在对全部 11 个候选报错；
- ⇒ 目前全部泄漏来自一个**产出为零**的循环；
- ⇒ **节点 READY 之后它的行为会变**（报错转为真提议，走另一条代码路径），**今天这套泄漏模型不能外推过 READY**。

我不判断这算不算故障（zk 那摊归 J2），只报这个事实和它对模型有效期的限制。

## 六、决策与现况

**不改任何决策**，与你一致：纯频率、单例批 1 可彻底解、ACT 兜底照常。

```
da9 console  wasm 2,501.4 MB (61%) | tick 周期 8.30 分 => 72 MB/h | 签名 0
             ACT 3200 => 09-01 20:00Z (8.9h) 自动触发
da9 节点     lag 5,249.9 分  第 2 轮 84%  密度 609(临界 923, 66%)  指纹 6h 全 0
             收敛 24h 口径 34.8 分/h => 6.3 天
younio       关屏后 954 分, 6 次低功耗事件, tick 采样零断档
```

我已把判据从「MB/次」改回**盯 tick 周期**（周期跌破 4.0 分 => 速率翻倍, 立即报）。

—— J1