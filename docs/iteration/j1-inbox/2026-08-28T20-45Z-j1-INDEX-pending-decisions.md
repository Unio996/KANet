# J1 → Bettor：**待裁决清单（修订版，覆盖全部 22 份）**

- 首版 2026-08-28 20:45Z（只覆盖前 15 份，**已过期**）→ **本次修订 2026-08-29 00:55Z，覆盖全部 22 份**
- 用途：你一醒来面对的是 22 份未推报备。这份是**唯一入口**，按「谁来拍」分组，每条写清拍什么、不拍的后果。
- **本文件不含新内容**，全是已提交件的索引。

---

## A. 🔴 需要你尽快拍的

| # | 事 | 出处 | 不拍的后果 |
|---|---|---|---|
| A1 | ~~younio 解卡~~ | ~~`409ab9d4`~~ | **已撤销** —— 根因判断是错的，见 `3cec2481`。它不是卡，是内存不足下的慢。**不需要你拍了** |
| A2 | **维护窗两个缺口**（`stopAll` 调用面 / console 启动器与 env 清单） | `4426ab27` | 维护窗当天我照 runbook 走会卡在这两步 |
| A3 | bundle 缺 HEAD ref 的修法：(a) 重生成带 HEAD / **(b) README 取用命令加 `-b`（我倾向）** | `4e0e0f00` | 接手人照直觉 `git clone` 得到 **0 文件工作树** |
| A4 | 🆕 **`57fde30f` 优先推** —— 22 份里**唯一的代码 fix**（console 启动期吞异常 → 僵尸增殖根治），已压 4 小时 | `57fde30f` | 它防的正是 8/23 把 da9 内存吃爆的病根 |
| A5 | 🆕 **da9 下次重启会丢掉 `--ram-scale=3.0`**（按你 `04a7b6ef` §2 裁定）—— 而它今晚正是靠 7.9 GB 缓存 8.3 分钟走完块体扫描 | `1c383e89` §4 | 重启后若需重做该阶段，可能显著变慢。**我不主张任何一边**，只把取舍摆出来 |

## B. 归 Owner 域（请你精炼后单点上报）

| # | 事 | 出处 |
|---|---|---|
| B1 | **8065184 推到哪个我们控制的 remote**（或先只归档不外推）—— 涉发布面 | `4e0e0f00` |
| B2 | 🆕 **要不要做一次真实链上 ZK 验证** —— 换我们自己的 vk → 打小额 → 用 signature script 花掉 → 看是否 accept。成功即实证 `covenants_enabled` 在 TN12 为真 | `db7fcd67` §4 / `319bb9bf` §5 |
| B3 | 🆕 **younio 内存** —— 那台机器提交量 16.9 GB / 物理 7.6 GB（超配 2.2×），关掉 33 个浏览器进程是唯一零风险的实质提速手段 | `3cec2481` §5 |

## C. 要你排期（不需 Owner 批，但要定谁做/何时）

| # | 事 | 出处 |
|---|---|---|
| C1 | **给 silverc 加 `OpZkPrecompile` builtin** —— 接口签名、栈布局、目标字节序列、设计约束**我已全部备好并经上游测试核实**。三件先定：变参怎么表达 / 上限要不要编译期挡(>240) / fee 怎么算(非常数) | `d541bd61` `b745ce20` `db7fcd67` |
| C2 | 🆕 **C1 可能不是必经之路** —— 纯 ZK 门禁用 `ScriptBuilder` 就能造（我已离线造出可用地址）；只有「ZK **叠加** covenant 内省」才必须走 silverc。**先定目标形态，再定要不要投编译器** | `db7fcd67` §3 |
| C3 | 要不要编 `silverc-d25bd342` 对照，把 OP_PICK 见证从「形状吻合」升级到「完全隔离」 | `f82fc22d` |
| C4 | 产物旁写 `.provenance.json`（记编译二进制 sha256+commit）—— 现 `compiler_version` 恒为 `"0.1.0"`，产物无法追溯 | `f82fc22d` §4 |

## D. 建议收进共享文档（收不收你定，我不擅自改）

| # | 事 | 出处 |
|---|---|---|
| D1 | **块速率是误导性进度指标** —— 唯一可信是 `lagMinutes` 收敛。**今晚拿到两个独立理由**：① 重块区（速率涨 30% 而进度为负）② 剪枝（blockCount 一次掉 45 万而 lag 反而改善） | `559b3b9a` `a8895477` |
| D2 | **PS 5.1 把无 BOM 的 UTF-8 当 ANSI 读**，中文注释足以让 `.ps1` 崩且**完全静默** | `ad59088f` §4 |
| D3 | **查「某能力在不在」，grep 必须覆盖 `Cargo.toml`/`Cargo.lock`/全部 crate + `~/.cargo/git/checkouts/`**（git 依赖源码不在 `registry/src`）。同病我犯两次 | `48535c41` §6 / `d541bd61` §9 |
| D4 | 🆕 **`isScriptPayToScriptHash(ScriptPublicKey 对象)` 静默返回 false** —— 传错类型不抛错，给假阴性。必须传裸脚本 `spk.script` | `275b0a9b` §2 |
| D5 | 🆕 **`getMetrics` RPC 比刮日志准** —— 直接给 `nodeBodiesProcessedCount` / 磁盘 IO / 活跃对端。建议维护窗与播报统一改用它 | `a8895477` §4 |
| D6 | 🆕 **`searching for missing block bodies` 是一次性步骤**，da9 基准 **8.3 分钟**（以前没人量过）；写操作数是它的过关信号 | `1c383e89` |

## E. 只需知会

- `778f869a` P0 transition probe 编译 + 五确认
- `0108e17f` 四件欠项全部交付
- `9ef7a564` watcher hash `280317d8` 交付 + ETA 分歧实测解释
- `67a83e0a` **两台节点跑逐字节相同的 kaspad，且确实编进 ZK**
- `48535c41` `d541bd61` `b745ce20` `db7fcd67` `319bb9bf` `275b0a9b` — **ZK 六连**：缺件 → 接口 → 约束 → P2SH 构造 → 离线造出可用地址 → 三重验证
- `a8895477` `1c383e89` `3cec2481` — 节点侧三件：剪枝失真 / 缓存敏感 / **我的根因更正**

## F. 我这一轮的自纠汇总（7 处，免得你逐份翻）

1. **44h ETA** —— 先收回语气，后连「下界」这个词一起收回（存在净进度为负的区段）
2. **「younio IBD 卡死」根因判错** —— 它一直在磨；我只凭计数器冻结就下结论，没量 CPU/IO。**代价：一次不必要的重启，35 分钟 + 15 GB 重复写入**
3. **「探测器上线跑通」** —— 首轮确实通，但**它没活下来**；我因命令行截断误判成「实例正常」
4. **两次窄 grep 当成「不存在」** —— 只扫 `silverscript-lang/src`、只搜 `registry/src`
5. **「header 已全量」** —— 当时并未全量
6. **杀掉自己的 shell** —— 枚举条件含脚本名字面量
7. **「在磨」≠「在推进」** —— 我先前混为一谈；官方计数器显示后者**无证据**

---
全部 22 份原件在 `docs/iteration/j1-inbox/`，按时间戳排序即为上述顺序。
