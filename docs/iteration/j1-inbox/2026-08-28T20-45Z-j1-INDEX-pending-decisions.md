# J1 → Bettor：**待裁决清单**（15 份报备的索引，按"谁来拍"分组）

- 时间：2026-08-28 20:45Z
- 用途：我这一轮积了 15 份未推报备，你一醒来面对的是一堵墙。这份把它变成**可执行队列** —— 你只需按下面的顺序看，每条都写清了「谁拍/拍什么/不拍的后果」。
- **本文件不含新内容**，全是已提交件的索引。

## A. 🔴 需要你尽快拍的（挡着活）

| # | 事 | 出处 | 不拍的后果 |
|---|---|---|---|
| A1 | **younio 解卡**：ban 掉状态卡住的 IBD peer 逼重选。不重启/不动 DB/可 unban 还原/脚本内置 4 分钟观察窗+失败自动还原 | `409ab9d4` | younio 已卡 **2 小时**且证明不自愈，第二 vantage 一直上不来 |
| A2 | **维护窗两个缺口**（stopAll 调用面 / console 启动器与 env 清单） | `4426ab27` | 维护窗当天我照 runbook 走会卡在这两步 |
| A3 | bundle 缺 HEAD ref 的修法：(a) 重生成带 HEAD / **(b) README 取用命令加 `-b`（我倾向）** | `4e0e0f00` | 接手人照直觉 `git clone` 得到 **0 文件工作树**，会以为产物是坏的 |

## B. 归 Owner 域（请你精炼后单点上报，别让我直戳）

| # | 事 | 出处 |
|---|---|---|
| B1 | **8065184 推到哪个我们控制的 remote**（或先只归档不外推）—— 涉发布面/暴露 | `4e0e0f00` |
| B2 | 要不要做**一次真实链上 ZK 验证**来实证 `covenants_enabled` 与语义（要花钱、要动链） | `b745ce20` §5 |

## C. 要你排期的（不需 Owner 批，但要定谁做/何时）

| # | 事 | 出处 |
|---|---|---|
| C1 | **给 silverc 加 `OpZkPrecompile` builtin** —— 三件先定：变参怎么表达 / 上限要不要编译期挡(>240 必失败) / fee 怎么算(非常数)。接口签名、栈布局、目标字节序列**我已全部备好** | `d541bd61` `b745ce20` |
| C2 | 要不要让 J2 编一份 `silverc-d25bd342.exe` 对照，把 OP_PICK 见证从"形状吻合"升级到"完全隔离" | `f82fc22d` |
| C3 | 产物旁写 `.provenance.json`（记编译二进制 sha256+commit）—— 现在 `compiler_version` 恒为 `"0.1.0"`，产物**无法追溯是哪个二进制编的** | `f82fc22d` §4 |

## D. 建议收进共享文档的（收不收你定，我不擅自改）

| # | 事 | 出处 |
|---|---|---|
| D1 | **块速率是误导性进度指标** —— 实测同期块速率涨 30% 而实际进度为负。播报 READY 唯一可信度量是 `lagMinutes` 是否收敛 | `559b3b9a` |
| D2 | **PS 5.1 把无 BOM 的 UTF-8 当 ANSI 读**，中文注释足以让 `.ps1` 崩且**完全静默**（`Win32_Process.Create` 回 0 给了 PID，进程却起不来）。两台机器我各撞一次 | `ad59088f` §4 |
| D3 | **查"某能力在不在"，grep 必须覆盖 `Cargo.toml`/`Cargo.lock`/全部 crate，以及 `~/.cargo/git/checkouts/`**（git 依赖源码不在 `registry/src`）。同一个病我犯了两次 | `48535c41` §6 / `d541bd61` §9 |

## E. 只需你知会、不用拍的

- `57fde30f` console 启动期吞异常 → 僵尸增殖**根治**（这是唯一一份**代码 fix**，压在队列里两小时了，建议优先推）
- `778f869a` P0 transition probe 编译 + 五确认（已交付）
- `0108e17f` 四件欠项全部交付
- `9ef7a564` watcher hash `280317d8` 交付 + ETA 分歧的实测解释
- `67a83e0a` 线上节点二进制确含 ZK，**两台跑的是逐字节相同的 kaspad**
- `48535c41` `d541bd61` `b745ce20` ZK 三连（缺件 → 接口 → 约束单）
- `559b3b9a` 重块区量化证据 + **我收回"44h 下界"这个说法**

younio 本地另有 6 个 commit（`9558b5f5` `7e1ad13e` `466c720f` `280317d8` `5b1db917` `8b465e78`），都是我这台机器的运维/工具，不进主线也行。

## F. 我这一轮的自纠汇总（免得你逐份找）

1. **44h ETA**：先收回语气，后连"下界"这个词一起收回 —— 存在净进度为负的区段
2. **"探测器上线并跑通"**：首轮确实通，但**它没活下来**；我因命令行截断误判成"实例正常"
3. **两次窄 grep 当成"不存在"**：只扫 `silverscript-lang/src`、只搜 `registry/src`
4. **"header 已全量"**：当时并未全量，仍在以 3863/分推进
5. **杀掉自己的 shell**：枚举条件含脚本名字面量 → 匹配到枚举者自身

---
全部 15 份原件在 `docs/iteration/j1-inbox/`，按时间戳排序即为上述顺序。