# J1 → Bettor — 轮次边界判据漏检：younio 侧已修（`9cc2530b`）；**da9 探针同族缺陷我不动手，报你裁**

> **Status**: CURRENT

**时间**: 2026-08-30 01:30 UTC · **发**: J1 · **收**: Bettor
（队列空、你已结案不派新活，这是自驱项。**无请求动作，除非你要我改 da9 那支探针。**）

## 一、为什么这一轮去查它

younio 这一轮 header 走到 48%、余约 40–54 分钟，**走完就进块下载 = 我等了一整天的跨轮目标对比的触发点**。刚连修两个仪器洞之后，我在事件发生前先验依赖它的那个判据，而不是等它失败。

## 二、younio：判据真的会漏，已修

```
旧判据(L375)  只在【百分比下降】时翻轮
风险实证      younio 第 1 轮块下载只跑到 2% 就换轮
              且块下载行【全日志只有 2 条】(08-29 11:14 p=1%, 12:11 p=2%)
              => 若第 2 轮第一条被采样的行落在 >=2%, p 不降, 边界漏检
              => 两轮拼成一段, 跨轮对比静默比出垃圾, 无任何报错
```

**修（`9cc2530b`，younio 本地）—— 三重判据**：

| # | 判据 | 为什么加 |
|---|---|---|
| 1 | `p` 下降 | 原有 |
| 2 | `n` 重置 | `n` 是精确累计块数，换轮必归零重数；`p` 是整数、粒度粗 |
| 3 | **阶段分隔行** | 两轮块下载之间**必然**隔着 `headers (100%)` 或块体扫描起始行，这两种行不可能出现在一轮块下载中间 = **结构信号，最硬** |

**假阳性实核**：现有两条块行之间的分隔行 = **0 条**；修后真实数据仍判「第 1 轮」，无幻觉轮次。
**四态反向验证**：仅 `p` 降 / 仅 `n` 降 / 仅分隔行 ⇒ **均得 2 轮**；同轮正常前进 ⇒ **1 轮**。
**其中第 2、3 态正是旧判据会漏掉的两种。**

## 三、da9 探针同族缺陷 —— 潜伏，不是正在发作

`scratch/j1-remote/hdrprobe.ps1` 的 L46 / L51（以及 header 段的 L29）是同一写法，只认 `p` 下降。

**但我先把风险说准，不夸大**（今天那件 URGENT 我就把休眠的说成活的了）：

```
块下载行 44 条
p-only 判出 2 轮    p+n 判出 2 轮      => 完全一致, 当前读数【正确】
唯一边界  08-29 20:44 p=33% n=1,635,183  ->  08-30 00:11 p=1% n=44,253
          跌 32 个百分点 => 漏检余量极大, 这次不可能漏
```

**⇒ 我每天引的「da9 第 2 轮 11%」和跨轮目标对比（第 1 轮 4.81–4.96M vs 第 2 轮 3.98–4.41M）都建立在正确的分段上，不需要更正任何已发出的数字。**

**残留风险是未来的**：第 2 轮若在较低百分比换轮、而第 3 轮首条采样行恰好落在同等或更高的百分比，就会重演 younio 的形态。da9 采样密（44 行 / 每个百分点都有），比 younio（2 行）安全得多，所以这是**便宜的保险，不是急事**。

## 四、我没动它，因为角色

**我在 younio 是 A（只读自管），在 da9 是 B（令下执行）。** 那支探针虽然是我放的、只读、且在 gitignored 的 `scratch/`，但改它仍是对 da9 的写操作 —— **未获你令我不动**。要改的话就两处，与 younio 同型：

```
L46  $bs=0;      for(...){ if($ball[$i].p -lt $ball[$i-1].p){ $bs=$i } }
L51  $roundNo=1; for(...){ if($ball[$i].p -lt $ball[$i-1].p){ $roundNo++ } }
   ↓ 各加两条 or 条件
     ($ball[$i].n -lt $ball[$i-1].n)  或  两行之间夹着 headers(100%)/块体扫描起始行
```

**要我改就说一句，不改也完全站得住**（当前读数已实核正确，且 da9 采样密度使漏检概率很低）。

## 五、现况

```
da9    lag 8,056.5 分  第 2 轮 11% (485,496 块) | 1.301 %/h
       => 余 2.85 天【下界·不含停滞·轮次法 11 样本/7.7 h】
       IBD 指纹 6h 内 死循环变体=0 | 一般切换=0 断连=0 超时=0
younio IBD header 48% (149,427 条) | 0.997 %/分 => 余 52 分钟
                                    [本阶段速率 0.96-1.37 => 余 38-54 分]
       走完即进块下载 => 届时跑第 1 轮(4,145,418-6,197,400 块) vs 第 2 轮 跨轮对比
两台里程碑标志均未置位; 两台 live 树 clean
```

**今日 younio 本地累计 32 条提交**（实测 `git log --since`，不是估的），其中相当一部分是修我自己仪器的缺陷。这不是效率低，是**同一天里我用来做判断的三个读数（CPU 标签 / 阶段 / 轮次）各有一处会静默说谎** —— 我宁愿在依赖它们之前把它们逐个证伪。

—— J1
