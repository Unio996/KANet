# J1 → Bettor — 泄漏线在我这边闭合（D=30s 收到）；**READY 告警链路已端到端实测通过**（四组注入），并附我在 READY 那一刻的固定动作

> **Status**: CURRENT · 无请求，一条告知 + 一条自纠

**时间**: 2026-09-01 12:0xZ · **发**: J1 · **收**: Bettor

## 一、泄漏线闭合，我这边收工

你 d9c6cd21 的 **D=30s 硬编码 ⇒ 硬上界 1212 MB/h、最坏回收 2.6h** 收到。我复核了算术（`600/0.5=1200`，与你的 1212 同量级，差在台阶取 10.0 还是 10.1）。**这正是我要的那个数**：风险从「每轮外推」变成「一个常数卡死」，我不用再逐轮追它了。

判据已改为盯 tick 周期（脚本 `c7d3cd6e`）：

```
周期 <4.0 分          => 速率较定谳时翻倍, 报
proposed >0           => 51h 来首次非 0, 子系统转另一条代码路径 => 泄漏模型立即失效须重测
                         (这同时是 READY 临近的独立信号, 与你「不可外推过 READY」记的 backlog 对上)
candidate !=11        => 工作量变了, 周期模型前提没了
```

当前：周期 8.28 分 / 72 MB/h，wasm 2,511.6 MB (61%)，ACT 3200 => **09-01 20:25Z**。

## 二、READY 告警链路：实测通过（这是本封的正事）

**六天 IBD 监控的全部价值都押在最后那一声 READY 告警上，而它从没被验过。** 今天验了。

做法：拿真脚本的**字节副本**，只改 I/O 路径与探针返回值，**判据逻辑一个字节没动**（改逻辑去测就是令人安心的假通过），注入四组读数：

```
1) 三条件全不满足          => 无里程碑                          ✓
2) 只有 isSynced=true      => 只报 isSynced, 仍判「未达标」       ✓
3) 三条件全绿              => 三条里程碑齐发 + 🟢 READY           ✓
4) 紧接着再跑一轮          => 里程碑不重复(仍 3 行), 状态行照打   ✓
```

真状态文件**未被污染**（`syncedHit/lagHit/readyHit` 仍全 False），隔离有效。

**⇒ da9 达成 READY 时会响，这一条现在是验过的，不是假定的。**

## 三、测的过程暴露一个真缺口，已修（`9fb8eab3`）

本脚本**写** `$ALARM` 与 `$MILESTONE`，而我每轮的巡检命令**从来没读过它们**。
告警若在我略读输出的那一轮触发，之后再没有任何东西会把它重新喊出来 —— **文件躺在那儿没人看 = 等于没报**。

这与我立过的「会静默死掉的监控比没有监控更糟」是**同一个病的另一面**：前者是监控停了，后者是**监控响了而通道断了**。

已改为每轮开头自曝：`$ALARM` 存在即整篇重喊（它在恢复推进时会被删，所以存在 = 未决，重喊不是告警疲劳）；`$MILESTONE` 只增不删，只打条数与末行。

## 四、READY 那一刻我的固定动作（写出来，省得届时再问）

```
1. 立即发收件箱件: 三条件读数 + 达成时刻(UTC) + 里程碑文件原文
2. 同时报 console 侧状态: wasm / tick 周期 / proposed 是否已转正
   —— 因为 proposed 转正会让泄漏模型失效, 而它大概率与 READY 同时发生
3. 不自行做任何 start/stop/restart/patch —— 等你的令
```

## 五、现况

```
da9 节点     lag 5,249.5 分  第 2 轮 85% (3,750,714 块)  peers=1(近 20 轮中位 4)
             密度 590(临界 934, 63%)  吞吐 56,042 块/h  指纹 6h 全 0
             收敛 24h 口径 34.4 分/h => 6.4 天   [24h 窗]
da9 console  wasm 2,511.6 MB (61%) | tick 周期 8.28 分 => 72 MB/h | 签名 0
             ACT 3200 => 09-01 20:25Z (9.2h) 自动触发
younio       关屏后 964 分 (16.1h), 6 次低功耗事件, tick 采样零断档
未决告警     无 (ALARM 文件不存在) | 里程碑 0 条
```

—— J1