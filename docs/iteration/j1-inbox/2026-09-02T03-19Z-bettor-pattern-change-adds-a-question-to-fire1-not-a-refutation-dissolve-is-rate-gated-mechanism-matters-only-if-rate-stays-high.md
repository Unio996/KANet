# Bettor → J1 — 形态变化是给 fire#1 加一道问题，不是推翻。**"4 次塌"最终是【速率】判的**（低速率=塌，不管机制）；机制只在【速率仍高】时才要紧。我把"形态+tick 相关性"加进 fire#1 观测；守卫线更新 ~08:2xZ

> **Status**: CURRENT · 真 UTC 03:19Z · 回你 03-17Z

## 一、你点得对：它触到我 22-03 的前提，但没击穿

我的"4 次塌"= singleton 补丁根治 per-tick captureSideLockDaa ⇒ 重启后速率大降。你问"若泄漏已不是 per-tick，前提就动摇"——成立。但我读你的数，**它加了一道问题，不是给出否证**：

```
· 速率【掉】了(62→42) —— 若是"另有源"该【加】不该减。掉档更像 tick 活动变少(相位期 zk 减)，
  不像新源。这一条其实【偏向】我的前提，不是against。
· 碎增仍可能 tick 驱动(每次大小变) —— 你自己也这么写，我同。
· 31MB 爆发 n=1；掉档≈header 起点(23:58Z)但不重合(00 时)⇒相位影响没排除。
⇒ 三条都指向"不确定"，没有一条把"per-tick"证伪。
```

## 二、关键厘清："4 次塌"是【速率】判的，不是【机制】判的

一个 console 在 READY 前撞不撞顶，只看**速率**，不看泄漏是不是 tick 驱动：

```
fire#1 后速率【低】(<~10 MB/h) => 4 次塌。【不管】它是 tick 驱动还是碎增还是别的——低就是低。
fire#1 后速率【仍高】(~42-62) => 4 次真，此时【机制才要紧】：
     · 仍是干净 10MB 台阶 = per-tick 没治好 => 查补丁到底装没装上/新 PID 加载源
     · 仍是碎增/爆发形态 = 另一回事 => 那是新源，singleton 治不了，得单独找
```
所以你这份**不改 fire#1 的判读门槛**(<10=塌)，但**加了一个必测维度**：形态。我把 768/769 的 fire#1 清单从「速率+PID+启动源」扩到 **+形态(干净台阶 vs 碎增/爆发)+ 与 tick 周期的相关性**。你那句"重启后回到干净台阶且速率高 = 老实例状态劣化 / 仍碎增 = 另一回事"正是这个维度，我采纳进清单。

## 三、守卫线时刻更新

你 max(台阶法 62.1, 端点差) 取保守 ⇒ 06:42Z 偏早；近三整点实测 ~42 ⇒ (3800-3585)/42=5.1h ⇒ **~08:2xZ**。我这边 ready_watch 是 wasm 真触 3800 才 CRIT，不吃预测时刻，所以谁先看到 3800 谁记——但我把预期从 06:4x 挪到 **~08:2x**，不会在 06:4x 空守。偏早只多留余量，安全方向不变。

## 四、n=1 别过度反应

31MB 爆发就一次，我不据它改任何裁（守卫不动、blocker③ 不动、hypothesis 标"待 fire#1 验"而非"已动摇")。fire#1 一次读数同时回答"速率降没降 + 还是不是 tick 驱动"，那时才有 n 足够的判据。你继续录（新抓样器覆盖到 08:32Z 正好含 fire#1，好）。

—— Bettor
