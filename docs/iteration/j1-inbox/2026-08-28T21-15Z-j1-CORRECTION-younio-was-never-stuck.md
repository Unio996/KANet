# 🔴 J1 更正：`409ab9d4` 的根因判断是错的 —— younio 从来没卡死，它一直在磨

- 时间：2026-08-28 21:15Z
- 更正对象：`409ab9d4`「younio IBD 卡死根因 + da9 同类暴露面预警」，以及建立在其上的 `ad59088f` §5、`5beafc09` A1
- 性质：只读实测 + 一次**不必要的**节点重启（我做的，younio，Owner 直授权限范围内）

## 1. 我错了，先说结论

我在 `409ab9d4` 里断定：younio 的 IBD 任务随连接 reset 而死、`is_ibd_peer` 仍为 true、**「没有任何东西会把它救回来」**。

**这是错的。** 实测（CIM 差分，30 秒窗口）：

```
日志      18 秒前仍在增长
CPU       48.3%  (占单核; 本机 12 核)
读操作    +90,014 次 / 349.3 MB   ← 约 3,000 次/秒随机读
写操作    +30 次
工作集    0.38 GB
```

**它一直在干活。**「searching for missing block bodies」那一步是**本地扫描** —— 在 600 万 header 里找出约 160 万个缺失的块体；而 `--ram-scale=0.4` 只给了 0.38 GB 工作集，于是几乎每次查找都打到磁盘。**慢，但没停。**

## 2. 我的错误在哪（这是重点）

**我只凭"计数器冻结"就断定卡死，全程没量过 CPU 和磁盘。**

- `headerCount` 冻结 2h13m + `blockCount` 恒为 1 → 我读成"死了"
- 而**长时间本地扫描在计数器上的表现与卡死完全一样** —— 两者只能靠 CPU/IO 区分
- 我甚至在同一天写过 ANTI-PATTERNS 规则 75（CPU 测量要用 CIM 差分），却没想到把它用在这里

**这与 8/28 的 ram-scale 事故是同一类错误**：证据不足就下结论，然后动手。

## 3. 代价与残值

**代价**：一次不必要的 kaspad 重启，白赔约 35 分钟的 header 重跑（重启后从 13% 重跑到 96%）。
（重启本身是干净的：`taskkill` 不带 `/F`，5 秒内正常退出，未走 8/23 那条 OOM 硬杀致 RocksDB 损坏的路。）

**残值**（不是给自己找补，是列清楚哪些结论仍成立）：
- `--unsaferpc` 已加进 watchdog，`ban`/`unban` 实测可用 —— 这个能力本身有价值，留着
- 实测确认 ban 一个 peer **压不住它**：3 分钟后它重连并再次成为 IBD peer。这条是新知识
- 重启后它**又走到同一步**并继续磨 —— 反过来印证了"这一步本来就慢"，不是偶发故障

## 4. 连带更正两处

**(a) `409ab9d4` §5 的 da9 预警**：我说"这个卡死状态日志证明不自愈，da9 有同类暴露面"。
**前提不成立** —— 那个"卡死状态"根本不存在。da9 的单 peer 风险作为**一般性担忧**仍然合理，但**不能引用 younio 作为先例**。请按此下调该预警的分量。

**(b) 我那个监控的判据是错的**：「`blockCount` 连续两窗零增量 ⇒ 卡死」——
**长时间本地扫描会稳定触发这条误报**。已修（younio 本地）：报警前先跑 CPU/IO 证伪探针，**在忙就只记录不报警，双静才报**。
已实测验证：da9 当前 `CPU=52.4% 单核 / 20 秒读操作 +261,488` ⇒ 若此刻零增量，新逻辑会正确判为"在磨"而不报警。

## 5. younio 现在需要什么：时间，不是操作

- 它在以约 3,000 次/秒随机读扫 160 万缺失块体
- `ram-scale=0.4` 是 kaspad 自己文档对 4–8GB 机器的**推荐值**（本机 7.6GB）
- **我不会调高它。** 调高 = 违背该 RAM 规模的官方建议，且正是 8/23 把 da9 内存吃爆→panic→DB 损坏的那条路。**宁可慢。**
- 处置：**不再重启、不再 ban、不再动参数**。只观测 `blockCount` 何时离开 1。

## 6. 请把 `5beafc09` 的 A1 划掉

裁决清单里的 **A1「younio 解卡」不再需要你拍** —— 它不是需要解的卡，是需要等的活。我已自行处置完毕并更正。

---
复核：`scratch/_j1_hung_or_grinding.ps1`（younio，CIM 差分实测）、`scratch/j1-remote/busycheck.ps1`（da9 证伪探针，只读）。
