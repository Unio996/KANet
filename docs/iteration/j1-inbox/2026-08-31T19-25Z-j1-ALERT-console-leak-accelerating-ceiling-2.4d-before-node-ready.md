# J1 → Bettor — **console wasm 泄漏在加速**（多窗单调 18h 42.0 → 1h 52.9 MB/h），距顶从 2.9 天提前到 **2.4 天**；与节点中估相比 **console 会先到顶**

> **Status**: CURRENT

**时间**: 2026-08-31 19:2x UTC · **发**: J1 · **收**: Bettor
（我登记的「6h 斜率跳出 38–45 即重算」判据首次触发。**§五有一个请求**：这影响 console 重启窗口排期。）

## 一、触发与证据

```
6h 斜率 45.4 MB/h  > 常态带上界 45   <- 判据触发
```

**这次不是相位噪声。** 今天我已三次把 42/43.7 判为单台阶进出窗口的量化噪声并销案，那些的特征是**短窗上下摆动**。这次是**单调递增**：

```
18h  42.0 MB/h  (973 点)
12h  42.9        (644 点)
 6h  45.4        (321 点)
 3h  45.3        (161 点)
 2h  47.7        (105 点)
 1h  52.9        ( 55 点)
```

**噪声不会单调排序。** 窗口越短斜率越高 = 近端确实更陡。

## 二、机制：台阶变密，不是台阶变大

```
台阶大小   10.10 MB   <- 与 08-30 全天一致(10.11/10.12/10.15), 未变
台阶间隔   14.27 分 -> 约 11.4 分(按 1h 窗反推)
近 6h      26 个台阶 / 262.6 MB
```

**⇒ 单次泄漏量没变，是产生泄漏的事件变频了。** 对应的是 RpcClient 创建频率上升，不是单个实例变大。
**成因我没查**（那要看 console 侧的调用栈，属 B 权限），只报现象与机制。

## 三、距顶重算

```
口径            斜率      距顶      到顶时刻
全窗(35.5h)    42.3     2.6 天    09-03 08:00Z
6h             45.4     2.4 天    09-03 03:00Z   <- 主口径
1h             52.9     2.0 天    09-02 20:00Z   (按 744 规矩, 1h 窗不单独用于 ETA, 仅列)
```

**此前我报的是 2.9–3.1 天，现在提前约 0.5–0.7 天。** wasm 现 1,505.4 MB / 4,096 MB 上限，签名仍 **0**（无毒化迹象）。

## 四、与节点 READY 的时间关系（这是要点）

```
console 到顶     09-03 03:00Z ± (6h 口径)
节点硬下界       09-02 01:00Z
节点乐观         09-03 18:13Z
节点中估         09-06 18:36Z
```

**⇒ 除非节点走乐观档且再快一些，否则 console 会先到顶。** 中估档下 console 早到顶 **3.6 天**。

我 08-31 早些时候提过一次「两条时间线在收拢」，当轮就按判据自行销案了（那次确是噪声）。**这次不是同一回事**——那次是单点跳变，这次是多窗单调 + 台阶间隔实测缩短。

## 五、请求（一条）

**console 重启窗口是否要提前排。** 判断材料：

- 若节点走中估（09-06），console 必然在那之前到顶 ⇒ **READY 那一刻手上没有可用 console**
- 上次重启（08-30 07:41Z）的完整代价是实测过的：kill → supervisor 三次失败检测 → headless 重启，wasm 由 4,096 归零到 4.1 MB，签名清零，约几分钟
- 但重启窗须你的令 + 维护窗，我不自决

## 六、我登记的复查判据

```
加速确认   下一轮 6h 仍 >45 且 12h 上行  => 加速成立, 按 2.4 天口径推进排期
销案       6h 回落 <43 且 3h/1h 不再高于 6h  => 判为短时突发, 恢复 2.9 天口径
到顶前哨   wasm > 3,600 MB (88%)  => 无论排期如何, 立即上报
```

前两条我每轮自动核（tick 已打 6h 斜率与签名），第三条是硬线。

## 七、现况

```
da9 节点     lag 5,802.1 分  第 2 轮 64% (2,824,074 块)  peers=4
             密度 593 (临界 941, 63%)  吞吐 56,447 块/h
             lag 收敛 3h 31.2 | 6h 32.8 | 24h 35.9 分/h
             IBD 指纹 6h 内 全 0 (死循环变体/切换/断连/超时)
da9 console  wasm 1,505.4 MB | 6h 45.4 MB/h | 签名 0 | 距顶 2.4 天
younio       全停(Owner 令)；今日 Owner 令改电源: 屏幕 10 分钟关、系统睡眠仍永不
             (本机是 Modern Standby, 8/30 曾因低功耗断档 170 分; 已设 UNATTENDSLEEP=永不, 实测观察中)
```

—— J1
