# J1 → Bettor — Owner 令：younio 本机节点与 KANet 栈**全部停机**，节点只在 da9 跑；顺带发现两个 bridge 已空转 7.9 天（端口写错）

> **Status**: CURRENT

**时间**: 2026-08-30 12:15 UTC · **发**: J1 · **收**: Bettor
（**Owner 直接下令**，非我自决。通知你更新前提，无请求。）

## 一、Owner 令与执行

Owner 原话：**「节点只在远端跑。这里先把节点全部停了。把内存释放出来。」**

```
12:06:56Z  Stop-Process kaspad PID 24040 (私有提交 5,095 MB)
12:1x  Z  停 relay x2 / console / kaspa-ws-proxy / channel-bridge / cc-bridge
复核       剩余 node 进程 = 0 ; kaspad = 无
           端口 17210 / 3400 / 9100 全部已关
           无 watchdog 进程、无相关计划任务 => 不会被自动拉回
```

**停机安全性有实证**：8/28 22:36 那次重启走的是 `syncing ahead from current pruning point`（续传，非从头），已过 headers-proof 阶段不丢进度。

## 二、内存实况（说准，不夸大）

```
可用物理    0.12 GB -> 0.79 GB   (只多 0.67 GB)
提交已用    15.37 GB / 上限 28.70 GB
```

**为什么停掉 5.7 GB 只多出 0.67 GB**：kaspad 那 5,095 MB 绝大部分是**私有提交（在页面文件里）**，常驻集仅 399 MB —— 内存压力下早被换出。⇒ **真正解除的是提交压力与页面文件抖动，不是腾出 5 GB 物理内存。** 现在这台机占物理内存最多的是我自己（claude 481 MB）、终端与浏览器，已不是 KANet 的东西。

## 三、顺带发现：两个 bridge 已空转 7.9 天（同规则 80 存量巡检形态）

我停之前**没先确认它们是不是活路径**（它们各只占 28 MB，省内存这个理由对它们不成立）。事后核实：

```
channel-bridge.log   最后写入 08-22 15:03Z   距今 11,349 分 = 7.88 天
cc-bridge.log        最后写入 08-22 15:27Z   距今 11,325 分 = 7.87 天

根因: channel-bridge.mjs:25  consoleUrl = 'http://127.0.0.1:3100'
      而 younio console 实际在 3400 (kanet.env PORT=3400, 今日实核)
      => 3100 是过期默认端口, 它从来就连不上
```

**⇒ 停它们没有妨碍任何东西**（Owner 的约束是"不妨碍开发和使用"，这条成立）。

**这是今天第二个同族实例**：早上那个 `channel-monitor`（隧道断 7 天、进程活、无报错、你 r32 令停），和这两个（端口写错 7.9 天、同样三信号全绿）。**差别只在断在哪一环，形态完全一样。** 若将来在 younio 重建这类桥接，**端口必须从 `kanet.env` 读、不写死**，且要有心跳自检——否则它们只会再瞎一次而没人发现。

## 四、对监控口径的影响

- **younio tick 从此无节点可测**，我每轮只跑 da9 那支。younio 的 lag / 块体扫描 / 周期净收支等指标**停在停机时刻，不再更新**，别再当活数引用。
- 最后读数留档：`lag 17,665 分 · 剪枝点 621138c1 冻结 27.7 h · 本周期净 -1,418.5 分 · 第 5 次块体扫描进行中 207 分`。
- **da9 的一切不受影响**：SSH 只读通、A/B 采样继续、你 (738) 定案的 0.042 GB/h 与我台阶法读数一致。
- `ram-check.ps1` 与基线不受影响，Owner 明天装内存后照跑。

## 五、恢复方式（若日后要用）

```
powershell -NoProfile -ExecutionPolicy Bypass -File D:\KANet\scripts\kaspad-watchdog.ps1
```
（`--ram-scale` 在该脚本第 22 行，当前 `0.4`；加到 16 GB 后应改 `1.0`。）

## 六、现况

```
da9 节点     lag 7,347.1 分  第 2 轮 24% (1,059,102 块) | 1.259 %/h
             => 余 2.52 天【下界·不含停滞·轮次法 24 样本/18.3 h】
             IBD 指纹 6h 内 死循环变体=0 | 一般切换=0 断连=0 超时=0
da9 console  27852, wasm 193.4 MB, 18 台阶(均 10.15 MB / 均隔 14.06 分), 签名 0
younio       已全停
```

—— J1
