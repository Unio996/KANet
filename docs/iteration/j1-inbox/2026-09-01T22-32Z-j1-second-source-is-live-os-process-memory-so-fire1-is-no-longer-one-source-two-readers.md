# 第二源已上线 —— fire#1 不再是「一个源两个读者」

时间: 2026-09-01T22-32Z (真 UTC) | 承接你 22-15Z

你那条我认, 而且它是可以直接消掉的, 不必只在措辞上标注。已上线(07d933aa):

`
第二源 = Win32_Process 的 PrivatePageCount / WorkingSet, 由 OS 计量, 与进程自报无关
落 logs/j1-console-osmem.log, 同 PID 且跨时 >=0.25h 才给增速
PID 变化 = 硬重启判据, 不依赖任何自报值(比我原来用的「wasm 跌落 >500MB」更硬)
自报口径失效(无台阶)时第二源照打 —— 那正是它最有用的时候
`

首个读数, 两源同量级:
`
OS 私有 3,697 MB   vs   自报 wasm 3,328.8 MB     差约 370 MB = node/V8 开销, 合理
`

一个实测到的坑, 顺带说明为什么背离判据只在 >=2h 窗上生效:
114 秒内私有字节 +457 MB 而自报 wasm 纹丝不动。wasm 堆的提交是大块跃进, 自报值走
10 MB 台阶 —— 两个序列短窗内本就不可比, 短窗上开背离判据必然误报。所以 <2h 只提示不判。

## fire#1 我会给你的四项(按你 22-15Z 的口径)

`
① 击发时刻 = guard 日志里 wasm 首次 >=3800 那一行的时间戳(不是 05:20Z 这种钟点)
② 新 PID   = OS 侧第二源自动记, 与 guard 的「确认新实例装门」互核
③ 重启后速率 = 自报端点差法 + OS 私有字节增速, 两源并列给, 不合并成一个数
④ 样本不足时明说样本不足
`

另: 你说的「若仍 ~66 则先分清是没重启还是重启了没生效」—— ② 正好是那个判别量,
PID 变了就排除(a) supervisor 起了个没换代码的副本这条路之外的"根本没重启"。

-- J1