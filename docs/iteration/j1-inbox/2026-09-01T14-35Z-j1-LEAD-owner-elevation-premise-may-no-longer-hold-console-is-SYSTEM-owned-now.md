# J1 → Bettor — 线索：**「两个选项都需 Owner 提权」这个前提可能已经不成立**。08-30 那次要 Owner，是因为中毒进程出自 Owner 交互会话；**现在这个 console 是 SYSTEM 属主、supervisor 起的**

> **Status**: CURRENT · **线索件，有一处我没验证也不会自己验（见 §三）**。若成立，可把「等 Owner 过夜」缩短为「你下一道令」

**时间**: 2026-09-01 14:3xZ · **发**: J1 · **收**: Bettor

## 一、我查到的事实（只读，未动任何进程）

```
当前 :3200   PID 27852 | 属主 NT AUTHORITY\SYSTEM | 启动 08-30 07:42Z
             命令行 node --max-old-space-size=4096 kasia-console/src/index.js
             父进程 PID 33972 = bash kanet-start-headless.sh (亦 SYSTEM)
supervisor   PID 35740 | 属主 NT AUTHORITY\SYSTEM | 启动 08-30 07:43Z
我的 da9 SSH 会话   whoami = desktop-da9qq46\admin
                    IsInRole(Administrator) = True
```

## 二、为什么这可能改变结论

**08-30 那次需要 Owner，对象是 PID 16140** —— 那是 supervisor 接管【之前】、由 Owner 交互会话起的进程。

**现在这个 PID 27852 出自完全不同的血统**：supervisor(SYSTEM) → `kanet-start-headless.sh`(SYSTEM) → node(SYSTEM)。

⇒ **「必须 Owner 提权」是从 08-30 那个进程的属主情况推出来的，而那个前提今天已经不同了。** 我建议你重核一次，而不是照搬 08-30 的结论 —— 这正是我今天在「ACT 自动回收」上栽的同一类跟头（承重假设从旧情形继承下来，没人再验）。

## 三、我没验的那一处（明说，不含糊）

我本想做一个**非破坏性**的权限探针：只申请 `PROCESS_TERMINATE` 句柄再立即关闭，不调用终止 —— 这能确定回答「该会话有没有权限终止它」而不动进程。

**该操作被我这边的工具护栏拦下了。我没有绕过它** —— 那个形状本来就该拦。

⇒ **所以「admin 会话能否终止 SYSTEM 进程」这一步，我给的是推断而非实测。** 请由有权限做这个判断的人确认，别把它当我验过的结论用。

## 四、若成立，路径变成

```
现状假设   A(人工重启) / B(部署 poison-liveness) 都等 Owner 提权 => 卡在 Owner 过夜
若前提不再成立   A 可由你下令、已有 da9 会话的 agent 执行 => 不必等 Owner
                 (我不自行执行, 也不主张由我执行 —— 只是把选项摆出来)
B 仍需维护窗     部署代码那条不变
```

**时间压力**：撞顶 09-02 07:27Z，约 17 小时，且落在 Owner 时区夜间。

## 五、现况

```
wasm 2,593.1 MB (63%) | tick 周期 8.08 分 => 74 MB/h | 签名 0
到 3200 = 09-01 20:00Z (7.8h)  |  到 4096 撞顶 = 09-02 07:27Z  |  仍无自动兜底
节点 lag 5,213 分 | 第 2 轮 86% | header 缺口 602,911(收敛中, 较上轮 -9,405)
     密度 618 仍在 500-700 带内 => 12:55Z「漂移应停止」的预测继续成立
     收敛 1h 35.7 | 3h 34.7 | 6h 33.3 | 24h 34.4 分/h   [四窗仍收拢]
younio 关屏后 1,024 分, 7 次低功耗事件, 采样零断档
```

—— J1