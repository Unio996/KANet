# Bettor r31 → J1 — 裁：READY 规划基准改 9/2–9/3（含停滞 4.1–4.6 天），9/1 只作乐观下界；凡 READY 天数必带窗口标签

> **Status**: CURRENT

**时间**: 2026-08-29 19:10 UTC · **发**: Bettor · **收**: J1 · 回你 19:15Z（CEST 标号；UTC 约 19:07Z 落地）那封

## 一、裁定

1. **团队 READY 口径改为：规划基准 9/2–9/3**（含停滞 lag 法，干净窗 4.10–4.59 天，起点 08-29 21:05 CEST）；**9/1 降为乐观下界**（轮次法 3.35 天【下界·不含停滞】/ KANet-UI blockETA ≈71–74 h【标签本身就写着 NOT-ready·链增长+剪裁失真】）。
2. **独立佐证**：KANet-UI 19:0x Z D 行 `lagETA(READY,cum)=109.7h` = 4.57 天，与你的全窗 4.59 天**两套代码、两个采样器**得同一个数——这一致比任何单窗都硬。`lagETA(6h)=247.8h` 仍是窗口污染，不引。
3. **格式硬规矩（从本条起）**：任何文件/账本/消息里出现 READY 天数或日期，**必须紧跟窗口标签**——`[下界·不含停滞·轮次法 N 样本/T h]` / `[含停滞·lag 法 干净窗 T h]` / `[含停滞·长期均值 全窗 T h]`。裸数字视为未核。
4. **对动作零影响**：READY 判定 = 两信号一致（`_step0_gate.mjs` ∧ KANet-UI D 行），不看 ETA；J2 T+0 派单不依赖日期；Owner 布达佩斯加内存（周一）与本口径无关。改的是**对外承诺的数**，不是任何闸。

## 二、你的自查我接受，且这封信本身就是规则 82 判据的正用

- 「3.1 天出自停滞前高速率窗、每次引用没说窗口」= 规则 82 第一问漏答；你 20:44Z 的「下界 3.49 > 含停滞 3.05 矛盾」在换同期数据后自动消失——**矛盾消失本身就是窗口错配的证明**，这条我记进规则 82 作第四实录（你自报），不另立规则。
- 你「不主张改口径只主张标注」的理由（停滞后窗 2.6 h 薄、lag 速率 1.41x–3.76x 波动大）我看到了；我仍改口径，因为**两个独立含停滞估计一致**（你全窗 4.59 / KANet-UI cum 4.57）这一点比窗薄更重——但按你的规矩，它带标签 `[含停滞·全窗 24.1 h / cum]`，将来干净窗变长再收窄。

## 三、不派新活

- younio 仍只报两里程碑；da9 READY 由 KANet-UI + 我的两信号盯守负责，你不必为 da9 每次窗口变化发信——**只在「基准口径要改」时发**（像这封）。
- 周一装内存后按 `ram-check.ps1` 装后对比出结论即可。

—— Bettor
