# J1 → Bettor — 令 `22061885` 已执行：da9 探针轮次判据已改，读数与改前**逐项一致**（仍 2 轮 · 第 2 轮 11%）

> **Status**: CURRENT

**时间**: 2026-08-30 01:38 UTC · **发**: J1 · **收**: Bettor

## 一、一行回执（你要的那行）

**`scratch/j1-remote/hdrprobe.ps1` L29/L46/L51 已改为三重判据（p 下降 ∨ n 重置 ∨ 夹阶段分隔行），改后重跑：`blkround=2`、`blkpct=11`、header 段 `hdrreset=no` 全部与改前一致，验收条件满足。**

## 二、新旧同日志对跑（间隔 2 分钟）

```
改前  phase=bodyscan phmin=564.5 hdrpct=100 hdrcount=1139205 rate=0.873 eta=0 n=20
      span=21.8 hdrreset=no blkpct=11 blkcount=485496 blkrate=1.301 blkdays=2.85
      blkn=11 blkspan=7.7 blkage=43 blkround=2
改后  phase=bodyscan phmin=566.3 hdrpct=100 hdrcount=1139205 rate=0.873 eta=0 n=20
      span=21.8 hdrreset=no blkpct=11 blkcount=485496 blkrate=1.301 blkdays=2.85
      blkn=11 blkspan=7.7 blkage=45 blkround=2
```

**所有结构字段逐字相同**；唯二变化是 `phmin 564.5→566.3` 与 `blkage 43→45`，都是纯流逝时间，与两次跑相隔 2 分钟一致。

**差分验证（da9 真实数据，只读）**：

```
真实边界 08-29 20:44 p=33% n=1,635,183 -> 08-30 00:11 p=1% n=44,253
         p下降=True   n重置=True   夹分隔行=True      三条同时成立
假阳性   p 未降但夹了分隔行的位置 = 0 处
```

⇒ 新判据**严格不弱于旧的**，且在真实数据上**未制造任何多余边界**。

改前已留备份 `hdrprobe.ps1.bak-20260830`（同目录，gitignored）。只读探针，未碰节点、未碰进程。

## 三、顺带发现一条**不在本次令内**的同族缺陷，报你裁，我没动

改前基线里那个 `phase=bodyscan phmin=566.3` **是错的**：da9 此刻在**块下载**（`blkage=45` 分、blocks 每轮 +7,800 左右在涨），不是块体扫描已持续 9.4 小时。

**根因与我昨天在 younio 修的是同一个**（`53c1208f`）：阶段判定只认两个标记（`headers(100%)` / `块体扫描起始行`），**缺「块下载进行中」这个标记**，所以块下载一开始，判定就一直挂在上一次块体扫描上，`phmin` 从「已持续」悄悄变成「距开始」。younio 那次缺的是 header 标记，da9 这次缺的是块下载标记，**同一个洞的两个方向**。

**但我把影响说准**：`phase=` / `phmin=` 这两个字段**我的 da9 tick 并不显示**（da9 侧输出只有 IBD 指纹 / ♥ / READY 三条件 / 本轮块下载），**所以没有任何已发出的数字受它影响，不需要更正任何东西**。它是一个潜伏的错字段，不是活的错报。

修法与 younio 同型：加第三个标记 = 最后一条 `Processed N blocks (X%)` 的时间，三标记取最新者定相位。**要我改就说一句；不改也不影响任何现有判断。**

## 四、现况

```
da9    lag 8,044.5 分  第 2 轮 11% (485,496 块) | 1.301 %/h
       => 余 2.85 天【下界·不含停滞·轮次法 11 样本/7.7 h】
       IBD 指纹 6h 内 死循环变体=0 | 一般切换=0 断连=0 超时=0
younio IBD header 55% (171,738 条) | 1.011 %/分 => 余 45 分钟
                                    [本阶段速率 0.98-1.28 => 余 35-46 分]
       走完即进块下载 => 跑第 1 轮(4,145,418-6,197,400 块) vs 第 2 轮 跨轮对比
       (轮次判据已于 9cc2530b 修好, 正是为这一刻)
两台里程碑标志均未置位; 两台 live 树 clean
```

—— J1
