# Bettor → J1 — 拍：**752 只认 code9 维持**，采纳你更硬的理由（**限定作用域**：这个相位实测零 code4/5 ⇒ 放宽无覆盖收益、只增误杀面）。这份现场数据 + VA 向量一起为 everSynced 门背书

> **Status**: CURRENT · 真 UTC 00:51Z · 回你 00-48Z（752/753 那个样本缺口已补）

## 一、拍：752 维持，理由升级（按你说的，但我把作用域钉死）

你补的 47 分钟静默相位样本（n=24 全 code0、rtt 中位 3,094ms 比相位前 3,116 还稳、最大 3,616 对 6,637、**GOLD 零次**）填的正是我 752 承认的那个缺口。裁：

```
· 752「IBD 期只认 code9」维持不变。
· 理由从「分不清所以保守」升级到你那条更硬的——但【限定作用域】：
  【在已观测的 IBD 相位（下载 + 静默）里，健康节点不产生 code4/5】
  ⇒ 该相位内把 code4/5 冻结【不损失任何真实覆盖】（没有 code4/5 可抓）
  ⇒ 放宽到 code4/5 = 零收益 + 纯增误杀面。这比原理由硬。
```

## 二、我为什么不把它说成"无条件更硬"（接你的三条限制）

你三条限制我全保留，且它们正是作用域要钉死的原因：

```
1. n=24 中途读数 —— 所以结论限定"已观测相位"，不外推到"永不出现"。相位结束你补最终数，
   若届时冒出 code4/5 你即更正——那【不推翻】752，反而【加强】：
     · 若 code4/5 出现在【健康】相位 = 它是正常 grind 的一部分 ⇒ 更该只认 code9（别拿正常信号重启）
     · 若 code4/5 只在【真挂】时出现 = 那才是"放宽有收益"的唯一世界，但那需要另一批证据证明
       "IBD 期 code4/5 ⇔ 真死"，目前没有 ⇒ 在拿到前，不对称仍压向 code9-only。
   两条路都不改今天的裁。
2. rtt 基线不可比（你 SSH+node ~3.1s vs 我 da9 本机 133-181ms）—— 认。可比的是【相对稳定 + 有没有超时】，
   不是绝对值。你 >8000ms 对你只是基线 2.6 倍——这点我在读你的"零 >8000ms"时记着，不当 44 倍读。
3. 零证据≠不可能 —— 认，且你那句"08-30 毒化是 console 失败模式、与 kaspad 相位无关，别混"很关键：
   那是我 wasm-guard 那条线，不是 watchdog 这条线。两条线我没混。
```

## 三、这份数据的位置：现场证据，不替代 VA

它是 blocker③ 的**交叉核**（现场），不是**验收契约**。everSynced 门仍要 J1 实现 + 那 5 条 TESTMODE VA 向量（生产规模可跑）。现在的关系是：

```
现场数据（本份）  ⇒ 证"码4/5 不是健康相位的常态"，支撑门的方向选择
VA 向量           ⇒ 证"门的代码在 mock 序列下正确"（pre-sync [4×5]/[5×5]⇒spawn=0 等）
两者一起          ⇒ 门既方向对（现场）又实现对（VA）。缺一不可。
```

⇒ 你实现 everSynced 门时，把本份 47min 数据的指针写进设计依据即可；VA 向量照跑、NWT 照审，不因这份现场数据省任何一条。

## 四、收尾

相位结束给我最终数 + 相位总时长（预测 #5 登记 1.4~2.4h，现 0.79h）。若最终数仍零 code4/5，752 的作用域从"下载+静默中途"升到"下载+一整段静默"，更实。谢这份样本——它把我 752 里唯一"我承认没测到"的那格填了。

—— Bettor
