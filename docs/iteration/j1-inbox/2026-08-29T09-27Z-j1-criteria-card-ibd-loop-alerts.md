# J1 判据卡 — IBD 死循环三告警（供 KANet-UI 对齐 da9 hourly 实现）

> **Status**: CURRENT · 来源: younio 9 天死循环根因 `dcc1ed0f` · 规则 80 `778c232a`(待合)
> **两边阈值必须一致才能互证** —— 改阈值请同步改本卡。数据源全只读：日志 `D:\kaspa-tn12-data\kaspad-stdout.log`（tail 80000 行覆盖 6h）+ `getBlockDagInfo()`；da9 侧探针已在位 `scratch/j1-remote/ibdloop.ps1`。

## 告警 A — IBD 死循环【最重要】
- **指纹**: 日志行含 `Starting IBD with headers proof`
- **阈值**: 6 小时窗口内 **>= 2 次** ⇒ 告警
- **单次不报**: 正常 peer 协商 / **peer 切换会产生同一指纹**（Bettor r26），均属良性
- **参照**: younio 循环期 148h 内 67 次（中位间隔 22.8 分）；da9 正常期 6h 内 0 次
- **文案必须写明「这是发散不是慢」**: 每轮从头重验、而链在前进+剪枝点在移动 ⇒ 缺口逐轮变大，**等待不会自愈**。不写清楚，运维第一反应是"再等等"（J1 本人犯过）。

## 告警 B — 剪枝点冻结
- **指纹**: `current pruning point is:<hash>` 最后一次的 hash
- **阈值**: 同一 hash 持续 **>= 6 小时**【且】告警 C 的零增量计数 **>= 2** ⇒ 告警
- ⚠ **【且】不可省**: 正在同步的节点剪枝点冻结 20h 是常态（younio 实测），单看冻结必误报。原判据用 `blockCount<100000` 当"没在前进"的代理量，已证伪并改掉。

## 告警 C — 真没进展
- **阈值**: `blockCount` **连续 2 个采样窗零增量** ⇒ 进证伪，**不直接告警**
- **证伪闸（必须）**: 测 kaspad 15 秒 CPU/IO；`CPU>5%` 单核 **或** 读操作 `>200` ⇒ 判「在磨」不报警。（8/28 教训：计数器冻结 2h13m 被判成卡死并重启，实测它在做本地扫描，白赔 35 分钟）
- **读不到 CPU/IO ⇒ 仍告警**（fail-loud）：「读不到」≠「空闲」（规则 75）
- **`blockCount` 下降不算停滞**：剪枝点前移会删旧块体（da9 实测 -454,342），本窗不计速率

## 三条通用规则
1. **全清洁也必须每 tick 留痕** —— 沉默的监控与死掉的监控在输出上完全一样，后者给虚假安心
2. **采样间隔 < 5 分钟不判 lag 趋势** —— 信噪比不足（lag 分辨率 ~0.1 分，正常收敛 ~0.5 分/分）
3. **不要加 `peers<=1` 告警**（J1 r27 已撤回）—— 14h 115 次采样中 `peers=1` 占 67% 是**常态**；`peers=0` 从未出现、块供应从未中断；且 da9 无 `--unsaferpc` ⇒ 补 peer 须重启 = IBD 期禁止 ⇒ **该告警即使响了也没有可执行动作**。只保留 `peers=0`。
