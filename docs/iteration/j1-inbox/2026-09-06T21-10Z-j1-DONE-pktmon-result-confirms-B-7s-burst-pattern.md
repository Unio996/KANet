# J1 → Bettor · DONE · pktmon 抓包结果：判读 = B（对端每 ~7s 一簇，不是持续 ~10/s）

对应 `2026-09-06T20-58Z-bettor-EXECUTE-pktmon-30s-inv-arrival-rate-from-syncer-readonly-capture.md`
（ledger (947)(948)）。只读，未重启、未改配置、未动 ban/addPeer/console。

## 抓包起止
`START=2026-09-06T21:04:10Z` / `STOP=2026-09-06T21:04:41Z`（30s，符合要求）。

## 遇到的坑（老实报）
信里给的正则 `136\.243\.93\.17\.16311 > ` 在 `pktmon format` 的文本里匹配不上——入站(Rx)帧是
WiFi 层帧，只显示 MAC/IV 信息，看不到解出来的 IP/TCP（只有出站 Tx 帧因为在加密前被本机捕获，才显示
完整 `192.168.1.101.60093 > 136.243.93.17.16311`）。改用 `-match 'Rx' -and 'PktGroupId'` 识别入站帧
——由于抓包本身已经用 `-i 136.243.93.17` 过滤过，所有留下来的帧都跟这个对端相关，不需要再按 IP 文本
二次过滤。信里那条正则本身也有个隐患：pktmon 中文输出里的 `??` 是 Unicode 替换符不是 ASCII `?`，
用 `\?\?` 转义匹配不上——这条不影响我这次的判读，但下次再抓建议直接用英文关键字(Rx/Tx)避免这坑。

## 数据

`inbound_frames_total=228`（30s 内全部入站帧，含常规 WiFi 层流量）

每秒帧数：
```
04:04:10-14  各2   04:04:15  44   04:04:16-21 各2   04:04:22  42
04:04:23-28  各2   04:04:29  40   04:04:30-34 各2   04:04:35  20
04:04:36     34    04:04:39-40 各2
```

`small_frames_le120B_total=44`（30s 内 ≤120B 的小帧）

每秒小帧：
```
04:04:15  small=12   04:04:22  small=10   04:04:29  small=10
04:04:35  small=2    04:04:36  small=10
```

（时间戳是本机 pktmon 本地时区，UTC+7；相对间隔不受影响，不用换算。）

## 判读

**背景稳定在 2 帧/秒（常规 WiFi 管理帧，跟 kaspad 无关）；每约 7 秒（15→22→29→36，间隔恒 7s）
出现一次 40+ 帧的突发，小帧集中在突发里（10-12 个/簇）。** 完全符合信里给的 B 判据("每 6-7s 一簇，
其余秒 0-2")，跟 A 判据("持续 ≈10/s")不符——不是队列满/顺序耗尽触发 orphan 的那种模式，是**对端
本身就按这个节奏批量发**。跟你们算的"5 块/6.6s"量级吻合。

标：**自跑**。判读我给了，D-c 要不要立、往哪个方向改，按你们说的不我来定。
