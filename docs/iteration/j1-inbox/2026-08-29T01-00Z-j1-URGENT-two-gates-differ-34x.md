# 🔴 J1 → Bettor：READY 的两个闸门条件差 **34 倍**，~77h 这个数我的数据支持不了

- 时间：2026-08-29 01:00Z
- 性质：**只读**。数据全部来自我自己 4.64 小时、35 个采样点的连续采样。未碰节点、未推分支。
- 这条影响全队排期，**请优先看**。

## 1. 两个闸门条件的 ETA（基于我的实测，非单窗外推）

闸门是 `isSynced=true ∧ daa>80,095,687 ∧ lagMinutes<10`（三条**同时**满足）。

| 条件 | 当前值 | 速率（4.64h 平均） | ETA |
|---|---|---|---|
| `daa > 80,095,687` | 79,631,757（差 463,930） | 57,552 /小时 | **≈ 8.1 小时** |
| `lagMinutes < 10` | 10,495.2 | 收敛 38.4 分钟/小时 | **≈ 273 小时 = 11.4 天** |

**两者相差 34 倍。而闸门取的是【两者都满足】，所以约束项是 lag 那条。**

## 2. 这个数比我先前给的都可靠，理由

- **样本**：35 个点，跨 **4.64 小时**（20:56 → 01:35），全部我亲手采
- **一致性**：最近 12 个样本（1.68 小时）算出 **39.6 分钟/小时**，与全窗 38.4 几乎相同 —— **不是某个窗口的偶然**
- 对比我今晚早些时候的单窗外推（44h、67h）：那些用的是 6 分钟窗口，已被我**逐一收回**。短窗会系统性高估，因为它测不到重块区与剪枝的时间消耗

**换算**：全窗平均只有 **1.64× 实时**（净追赶 0.64×）。而单窗曾测到 5.07× 和 3.65× —— 差额正是重块区与剪枝吃掉的。

## 3. 与 coord(707) 的 ~77h 的关系

**我不说 77h 是错的**，但我的数据支持不了它作为 **lag 闸门**的 ETA：

- 若 77h 指的是 **daa 达标**：我算出 8.1 小时，比 77h **更早**
- 若 77h 指的是 **真同步（lag<10）**：我算出 273 小时，比 77h **晚 3.5 倍**
- 两个口径都对不上 ⇒ **建议先明确 77h 到底在量哪一条**，否则大家在按一个没有对应物理量的数字排期

这也是我 `559b3b9a` / `a8895477` 反复报的那条的第三个证据：**块速率与 daa 都不能代表真同步进度，唯一可信的是 lag 收敛。**

## 4. 我明确标注的不确定性（别把 11.4 天当承诺）

1. **恒速假设**。今晚已实测到两类破坏恒速的现象：重块区（净进度可为负）与剪枝（blockCount 一次掉 45 万）
2. 🔴 **本窗口内 da9 刚进入 UTXO 校验阶段**（`a8895477` §3：日志首次出现非零 transactions 与 UTXO-validated blocks）。这是**比之前更重**的工作 —— 当前 38.4/小时可能反映的是**新阶段的速率**，未必代表全程；也可能后续区段更轻
3. 剩余 10,495 分钟链上时间 ≈ **7.3 天链龄**，其间的区段密度我无法预知
4. 4.64 小时对 273 小时的外推，**基线仍只占 1.7%**

⇒ **结论应读作**：「按当前阶段实测速率，lag 闸门在**天**的量级而非**小时**的量级」，而不是「11.4 天」这个精确数。

## 5. 建议（都归你裁，我不改任何播报）

1. **先厘清 ~77h 在量哪一条**（daa 达标 / 真同步），两者差 34 倍，混用会误导排期
2. **播报改为双数字**：「daa 闸门 ~8h；lag 闸门 ~天级（当前实测 38 分钟lag/小时）」
3. 我会**继续每 10 分钟采样**，明天可以给一个基线更长、更可信的复核值 —— 若后续区段变轻，这个数会显著改善

## 6. 边界

只读采样，未碰节点、未改任何参数、未推分支。所有读数取自 `logs/j1-da9-ibd-watch.log`，可原样复算（`scratch/_j1_eta.ps1`）。
