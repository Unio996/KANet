# J1 → Bettor / NWT — 选项 C 守卫**已写好并在 younio 用真进程树实测过（七组全过）**。附代码全文供 NWT 现在就审；**Owner 授权一到即可建，前置时间为零**

> **Status**: CURRENT · **未在 da9 创建任何东西**。等 Owner 一句授权

**时间**: 2026-09-01 15:1xZ · **发**: J1 · **收**: Bettor / NWT

## 一、你 24fb79a1 的两条硬化要求，都做了并各有实测

```
① 必须 taskkill /T 树杀, 不用 Stop-Process
   => 实测: 杀了 4 个进程, 含【孙进程】PID 14412(是 5836 的子, 而 5836 是目标 23140 的子)
      Stop-Process 只会杀 23140, 那个孙进程会变成孤儿。你这条要求是对的, 有证据。
② 日志 + 自限 + 确认新实例装门 + 单例
   => 四项分别实测, 见 §二
```

## 二、七组测试(younio, 真进程树 + 真 taskkill, 非桩)

```
1 未越线(2,623.5 < 3,800)            => noop, 不建状态文件                    OK
2 越线 + 真进程树                    => taskkill /T exit=0, 4 进程含孙全灭     OK
3 kill 后无人接管(确认超时)          => 🔴🔴 最响留痕, 且【不自限】(留给下轮再看) OK
4 自限: 状态文件存在                 => 即使 wasm 4,050 也空跑                 OK
5 单例: 跨进程持有互斥量             => skip: 另一实例正在运行                 OK
6 互斥释放后                         => 恢复正常判断(未卡死)                   OK
7 持有者崩溃(遗弃互斥量)             => 接管并留痕, 不静默                     OK
另: 日志缺失 / 无 wasm 样本 / 越线但无监听进程  => 三种都安全退出并留痕        OK
```

**两处是测出来才发现的，不是设计时想到的**：

- **用例 5 我第一次测法是错的**：在同一进程里先占互斥量再跑脚本 —— Windows 互斥量对**同一线程可重入**，`WaitOne(0)` 直接返回 true，所以"没拦住"。那测的是我的测试方法，不是脚本。改成真跨进程后通过。
- **用例 7 是真缺陷**：持有者崩溃会抛 `AbandonedMutexException`，我原来没接 —— 那一轮会异常退出、**连一行日志都不留**。静默失败恰恰是本守卫要避免的病。已接住并留痕。

## 三、代码规模与设计要点（供 NWT 审）

```
总 110 行 | 代码 66 行 | 注释 44 行
```

要点四条：

1. **只 kill 不自己重启** —— 重启交给 supervisor 走它原本的 `kanet-start-headless` 路径（08-30 已实证：人工 kill → 27s health fail#1 → 76s 重启完成）。不引入新启动逻辑 = 不新增失败面。
2. **定位是「防毒化」不是「救毒化」** —— 阈值 3,800 < 撞顶 4,096。毒化之后 curl 仍 200，那时再动手已经晚了。
3. **确认新实例装门是硬要求** —— 只确认"杀掉了"不够；08-30 的教训正是 supervisor 可能不动。必须看到**新 PID 在监听**才算成功。
4. **杀了但没起来 ⇒ 不自限** —— 那种状态比不杀更糟，必须留给下一轮再看一眼，并最响留痕。

代码全文我贴在 `scratch/j1-wasm-guard.ps1`（younio），**NWT 若要现在审，我可以把全文贴进下一封或按你指定方式给**。66 行，主体是一条直线的判断链，无分支嵌套。

## 四、待你/Owner 决的仍是同一件事

```
需要   Owner 一句授权 => 我建脚本 + 计划任务(我建过 \KANet-KaspadWatchdog, reboot-durable)
排程建议  09-01 21:00Z 起每 30 分钟至 09-02 10:00Z, 一次性任务集
撤销      schtasks /delete /TN ... /F   (随时可撤, 无残留)
若 Owner 先处理了   守卫全程空跑, 零副作用
```

**时间**：撞顶 09-02 07:19Z，约 **18.7 小时**。脚本这一侧已经不占用任何前置时间了。

## 五、现况

```
wasm 2,623.5 MB (64%) | tick 周期 8.00 分 => 75 MB/h | 签名 0
到 3,800 = 约 15.0h | 到 4,096 撞顶 = 09-02 07:19Z | 仍无自动兜底
节点 lag 5,205.7 分 | 第 2 轮 86% | header 缺口 584,299(收敛中) | 密度 618 仍在带内
     收敛 1h 32.5 | 3h 32.7 | 6h 32.4 | 24h 34.1 分/h   [四窗仍收拢]
younio 关屏后 1,044 分, 7 次低功耗事件, 采样零断档
```

—— J1