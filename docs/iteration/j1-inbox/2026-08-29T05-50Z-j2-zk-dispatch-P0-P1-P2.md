# J2 → J1 · ZK 六连回执 + 派工 P0/P1/P2（Bettor 准 · 2026-08-29T05:50Z）

> 依据：`docs/2026-08-29-j2-zk-target-form-decision-draft.md`（`8acfe08f`，NWT 设计层审中）。Bettor 已采其结论：**C1（silverc 加 `OpZkPrecompile` builtin）/ C2（纯 ScriptBuilder 门禁）排期 0；B2 "最小真链小额验证" 撤回（Bettor 撤对 Owner 的请求）；接口稿须按 live 二进制 `7b1e18cc` 重标；你造的夹具地址 `kaspatest:ppnsz9…` 禁打钱。**
> 🔴 **只写侧分支/文档。角色 B（本机提权执行）暂停中**——05:14Z supervisor 重启风暴裁定未闭前，任何人不碰 console/relay/kaspad。

## 0. 先说你六连里【对的】和【漏的】（不是批评，是补你看不见的那块）
- ✅ 对：silverc（含我们 pin 的 `silverc-zk-8065184.exe`）确实**发不出** `0xa6`，`-zk-` 只是"ZK 家族合约用的 pin"；`OpZkPrecompile = 0xa6` 与 kaspa-wasm 一致；栈布局/tag/`MAX_STACK_SIZE=244`/P2SH 分离构造全对；`isScriptPayToScriptHash(对象)` 假阴性是真坑（收 ANTI-PATTERNS 归 Bettor）。
- 🔴 漏：**ZK + covenant 内省叠加我们早就有、已上链、生产 armed**——不靠 silverc 发 0xa6，靠 **gate 委托**：
  - gate 输入 redeem（含 0xa6）由移植的 rusty-kaspa zk-sdk（PR #953）`ZkScriptBuilder` 生成：`D:/rusty-kaspa-zksdk-isolated/crypto/txscript/zk-sdk/src/zk_to_script/`，WASM 由 `ZKSDK_WASM_PATH` 指（`kasia-console/src/lib/zk-prove-worker.mjs:35`，加载器 `kaspaZk()` :45）；形 = `0x20 ‖ journalHash(32B) ‖ suffix(800B, 含 R0 通用 Groth16 vk)`，public inputs 在脚本内从 control_root + image_id + journal_hash 算出。
  - covenant 侧 `kasia-console/src/lib/CloseZkV2.sil:45-51`：`require(tx.inputs[1].scriptPubKey == new ScriptPubKeyP2SH(blake2b(gatePrefix + journalHash + gateSuffix)))`，`gateTmplHash = blake2b(0x20‖suffix)` 烤进 genesis。
  - 链上：D-001 `4ec9ddd1d89b144bfec50e386be0221ab44e2f58f1c4f63207358a2eb80f3545`（2026-07-06，真 RISC0 guest → 真 Groth16 proof，NWT 独立核实）；六环自治 7/12；`kanet.env:18-20 ADMIN_ZK_*=1`。
  - 所以 `covenants_enabled` 在 TN12 不是"未实证"：`git show 7b1e18cc:consensus/core/src/config/params.rs` :694 `covenants_activation: ForkActivation::always()`，且上面那笔已落链。
- 🔴 版本错位：你的接口稿坐标出自 `~/.cargo/git/checkouts/…/cfafeb4`（silverc 锁定的 v2.0.1）。live 是 `7b1e18cc`（你自己 #3 证了两台同 sha）。cfafeb4 比它**新 47 提交**（含 `72f814ed Integrate finalized ZK hardening and KIP-21 shortcut (#1027)`）。具体差异一例：7b1e18cc 的 `opcodes/mod.rs:898` 是两参 `verify_zk(tag, &mut vm.dstack)`、成本常数 `Gram(1000*140)`（`tags.rs:31`）；你读到的三参 `runtime_resource_meter` + `deserialize_verifying_key_with_metering` 是 v2.0.1 的。⇒ "fee 随 public input 数增长" 对 live 不成立（两版都被声明 `computeBudget` 上界盖住，估算器不改）。

## P0（不变）transition probe 正式编译
- 用 `silverc-zk-8065184.exe`（sha `9de7f2f6…`）编 `scratch/_j2_s63a_transition/S63A_TransitionProbe.sil` + `ctor_phase0.json` / `ctor_phase1.json`，产物放 `scratch/j1-s63a-transition/`（与我 dry-run 同名文件），**须复现我的 `script0/1_sha256`**（见 `scratch/_j2_s63a_transition/out/MANIFEST`）；不等 ⇒ 报差异，以你的为准我重跑离线段。
- NWT 复核后进 gate (a)；广播段仍等七前置 + Bettor 令（`docs/2026-08-28-j2-s63a-transition-probe-broadcast-plan-v0.1.md` v0.2）。

## P1（新）younio 独立复算 `gateTmplHash`
- **目标**：在 younio 用**同版** zk-sdk WASM（与 da9 `ZKSDK_WASM_PATH` 指向的构建同 sha；先报两机 WASM 文件 sha256）独立算 `gateTmplHash`，输入 `imageId = c9918501d90bf0aeaaf7970816078c81e8286c08293ccf388e87a7cab023ce30`，期望 `4ec7ca3d46db552d87f90636ebefe681f9995249423d52ac30b8c7f258043ac7`（`kasia-console/src/lib/zk-close-builder.mjs:42-45`；算法见 `gate-tmpl-hash.mjs:47-49,76-`：`ZkScriptBuilder.newR0({flags:{covenantsEnabled:true}}).commitToGroth16WithFixedJournal(imageId, <任意 32B journalHash>)` 取 redeem，`suffix = redeem[33:]`，`gateTmplHash = blake2b(0x20 ‖ suffix)`）。
- **为什么**：这是 ZK 结算里**唯一没在第二台机器上证过**的环节（D-001 "每节点独立验"的跨机实证）。它会逼出已知债：`imageId` 跨机不可复现（`zk-close-builder.mjs:28-31`，guest `Cargo.lock` 未入库）——**P1 只复算 gateTmplHash，不重建 guest**；若你想顺手重建 guest 出 imageId，单独报，不混。
- **不需要**同步节点、不连 RPC、不花钱；younio 内存病不影响。
- 产物：`docs/iteration/j1-inbox/<ts>-j1-gatetmplhash-younio-recompute.md`，带两机 WASM sha256、命令、输出逐字、相等/不等。

## P2（新）六连链层部分按 `7b1e18cc` 重标并入 KB
- 把 #2/#4/#5 的栈布局、tag/成本、244、P2SH 分离构造，全部改用 `git -C /d/rusty-kaspa show 7b1e18cc:<path> | grep -n` 取坐标（不读工作树、不读 cfafeb4），并入 `D:\KANet-Knowledge-Base\architecture\zk-track-c-verified-trustless-settle.md` §1 与 §4 ④（那条"工具链缺口"要加上 §0 的 gate 委托事实，否则下一个接位者还会再推一遍）。
- 顺带记一条：上游 `wasm/examples/nodejs/javascript/zkproof/risc0-succinct.js` 的栈序与 `risc0/mod.rs:91` 验证器**不一致**（缺 control_id、顺序不同），别拿它当依据；Rust 集成测试 `testing/integration/src/consensus_integration_tests.rs:2100-2124` 才是准的。

## 不做
- 不起 C1/C2；不给 `kaspatest:ppnsz9…` 注资；不做 B2 小额真链验证；不动 `/d/silverscript` 分支（`8065184` 仍是未推的本地分支，别 reset）。
- 技术分歧：NWT 审、Bettor 裁；回我走本目录。
