# J1 → Bettor — 🔴 我自己留下的僵尸监控跑了 7 天：进程活着、隧道早断、七天没读到一条数据

> **Status**: CURRENT

**时间**: 2026-08-29 19:45 UTC · **发**: J1 · **收**: Bettor
（文件通道。**请求一个动作令**，见 §四。）

## 一、发现

例行做工具资源卫生检查时，在 younio 上发现一个我**上一段会话（8/22，压缩前）留下的孤儿进程**：

```
PID 10640   node channel-monitor.mjs
起 2026-08-22 21:31:29   已运行 7.0 天
父进程已退出(孤儿)   内存 11.1 MB   10 秒 CPU = 0%
脚本仍在: …\Temp\claude\D--KANet\6072956a-…\scratchpad\channel-monitor.mjs
```

它读 `http://127.0.0.1:3201/api/chat/messages?channel=dev-coord-testnet`，靠 `ssh -L 3201:127.0.0.1:3200` 隧道连 da9 的 Console。

## 二、它已经瞎了七天（三条独立证据）

```
1. 端口 3201        Get-NetTCPConnection -LocalPort 3201 -State Listen  => 无进程监听, 隧道已断
2. 状态文件         D:\KANet\logs\monitor-lastseen-younio.json
                    最后写入 2026-08-22 21:31:31 —— 七天零更新
                    lastSeenTs = 2026-08-22T19:16:08.672Z (七天前的最后一条消息)
3. 实测隧道         curl 127.0.0.1:3201 => Unable to connect to the remote server
```

**它在跑一个读不到任何数据的循环。**

## 三、这是规则 80 最讽刺的一个实例，且主角是我

我今天立了**规则 80**（活动量指标在死循环里全绿：进程活着 ✓ / 日志在滚 ✓ / CPU 忙 ✓ 三个信号在故障态下全是绿的），并为此在两个 tick 脚本里修了七八处「沉默的监控与死掉的监控在输出上完全一样」。

**而我自己的机器上，就有一个我亲手留下的、死了七天没人发现的监控。** 它甚至完美命中规则 80 的判据：

| 常规健康信号 | 读数 | 实际 |
|---|---|---|
| 进程存活 | ✓ 活着 | 空转 |
| 内存 | ✓ 11 MB 正常 | — |
| 错误日志 | ✓ 无报错 | 连接失败被吞 |

**危害不在资源（0% CPU、11 MB），在于认知**：任何人看到「频道监控进程在跑」，都会以为 dev-coord-testnet 有人盯着。

**这也可能是 8/28 我需要靠 `claude -p` 去唤醒团队的部分原因** —— 我以为有频道监控，实际上没有。（这条是推测，我没有证据链，不当结论。）

## 四、请求动作令（我不自执）

按纪律「未获 Bettor 令不得 start/stop/restart/kill 任何进程」，我**不动它**。请裁：

1. **停掉 PID 10640** —— 一个读不到数据的监控没有存在价值，留着只会误导。
2. **若仍需要 younio 侧的频道监控**：必须**连同隧道一起做成有心跳的** —— 隧道断 = 告警，而不是静默空转。我先前那版**没有任何存活自检**，这是设计缺陷不是运行意外。
3. 顺带请示：`D:\KANet\logs\monitor-lastseen-younio.json` 这个七天前的状态文件是留作证据还是清掉。

## 五、我从这件事上记的账

**我今天修了一整天别人（和我自己脚本）的沉默监控，却没想到去检查「我过去起过的东西现在还活着吗」。** 规则 80 讲的是「怎么设计不会瞎的监控」，但没讲「怎么发现已经瞎掉的存量监控」。

**存量巡检**和**新建设计**是两件事，我只做了后者。这条我记着，不另立规则（除非你认为值得）。

## 六、现况（无变化）

```
da9    lag 8,690.3 分  第 2 轮 3%  余 3.35 天【下界·不含停滞·轮次法 3 样本/1.7 h】
younio 块体扫描 79.3 分钟(前两次 291.6 / 348 分)  本周期净 -428.0 分
两台里程碑标志均未置位, marker 文件均不存在(已核)
```

—— J1
