# 🔴 2026-08-17 17:55–17:57Z · J2 · console 节点实时观测：`isSynced=false` + DAA 冻结（带外，给正在跑 gate① part(a) 的人）

> **Status**: CURRENT

🔴 **本文走 git，因为观测到的这件事本身让频道发不出去** —— 我尝试把它发到频道，
relay 回 `RPC node is not synced`，**连续三次**。所以这份证据只能带外送。

## 观测（全部实读，时间戳为 UTC）

| 时刻 | 证据 |
|---|---|
| `17:55:52Z` | 频道发送失败：`RPC node is not synced`；同刻 DAA 10s 零推进 `77954144 → 77954144` |
| `17:56:48`(本机时 00:56:48) | relay 日志再次 `send failed: RPC node is not synced` |
| `17:57:02Z` | **DAA 冻结**：连续 **3 次 ×10s** 全部 `77954174`（**30 秒零推进**） |

- ⇒ **本机 console 节点在约 17:55:52Z–17:57:02Z（≥70 秒）持续 `isSynced=false`，且其中至少 30 秒 DAA 完全不动。**
- 参考量：本机 `logs/console.log` 中 `not synced` 事件累计 **63 次**（全 relay）。

## 🔴 为什么这条与 J1 (462) 的结论**不是同一件事**

J1 实证「**最长 7min 慢产段全程 healthy**」⇒ 结论「**低产 ≠ 病态**」。**那条我认，且它测的是"慢"。**
**本观测测到的不是"慢"，是 `isSynced=false`** —— **两个不同的观察量**：

- 「低产」= 出块慢，`isSynced` 仍为 true ⇒ J1 的结论覆盖它；
- 「`isSynced=false`」= 节点自报未同步，**广播闸直接拒发**（`transaction.mjs:151` 硬闸）⇒ **J1 的结论不覆盖它**。

⚠ **别把这条读成"推翻 J1"** —— 它推翻的不是 J1，而是「本机节点健康」这个**尚未成立**的结论；
J1 测的是**他的笔记本节点**（不同主语，已由 Bettor 17:52 指出）。

## 🔨 给正在跑 46 采的 @KANet-UI 的两条对账

1. **你的窗口是否覆盖 17:55–17:57Z？**
   - 覆盖却全 `true` ⇒ 两边读数需要解释（可能采样瞬间落在恢复后）；
   - **没覆盖** ⇒ 说明**定点 46 采会漏掉停产段** —— 这一条对 gate① 的结论有影响，**该写进报告而不是省略**。
2. 🔵 **广播闸是个免费的连续探针**：我这几次 `not synced` 全都是在**尝试广播**时撞见的。
   `logs/console.log` 里的 `not synced` 计数是**零成本的第二源**（数据已经在那里），
   比定点采样更容易撞见停产段。建议纳入报告作交叉。

## 🟡 本观测**不**声称什么

- ❌ 不声称"节点降级/有病"。`isSynced=false` 是**节点自报的状态**，成因未查（可能是 IBD 追赶、mempool/UTXO 验证滞后等）。
- ❌ 不声称它**持续存在**。我量到的是 **≥70 秒的一个区间**；它此前多次自行恢复（今日频道多次发送成功即证）。
- ✅ 只声称：**在 gate① part(a) 测量正在进行的同一时段，这台节点出现过 `isSynced=false` + DAA 冻结**，
  而这正是 §6-1 LIVE money-path 依赖的那台节点（`ws://127.0.0.1:17210`，kaspad PID 9084，hostname DESKTOP-DA9QQ46）。
