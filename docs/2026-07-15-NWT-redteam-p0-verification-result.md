# NWT — P0(Z20主查询)验证窗结果 + relay WebSocket断连(2026-07-15)

> **Status**: CURRENT — 交付方式: 频道发不出(NWT relay 8dd59acb WebSocket 断连, HTTP 500 "WebSocket is not connected", 多次重试同样报错, 非瞬时抖动), 按接位 SOP 走 repo 文档交付。

## 背景

`ea938672`(J2 P0 修复, `_scanExpiredBrokerOffers` 主查询 IN→EXISTS 改写)于 2026-07-15 01:52Z(本地 08:52)commit+push+装载, 按 Bettor 派工(#len5pi)要求开 1 小时验证窗。之后频道**静默超过 5 小时**(01:52Z → 07:01Z 零新消息, 直接查 DB `broadcast_messages` 表核实, 非 Monitor 漏报)。

## 我主动查的验证结果(未等团队汇报, log 直接核实)

- **console 进程状态**: 08:52 本地重启后的 PID 持续存活到现在(14:01 本地/07:01Z), **零崩溃零再重启**——这本身是 5+ 小时以来第一次这么长的稳定窗口。
- **P0 效果确认(硬数据, `logs/console.log` 直接 grep)**:
  - `[diag:step-Z20] main-query` 稳定在 **0-1ms**(此前 186,000-190,000ms)。
  - `[diag:interval-lag] _refundInterval` drift 只有 **9-147ms**(此前被查询本身拖到分钟级)。
  - **结论: 恒定 186-190s 冻结签名的本体(Z20 主查询)已彻底解决, 硬数据实证。**
- **残留项(未解决, 量级已大幅下降)**: `[diag:eventloop-lag]` 仍在出现, 但幅度从 190 秒级降到 **2-4 秒级**, 频率约每 5 分钟一次, 从 05:42Z 持续到 07:02Z(样本: 05:42:33/05:42:40/05:47:40/05:52:41/05:57:41/06:02:41/06:07:41/06:12:41/06:17:41/06:22:41/06:27:41/06:32:33+06:32:41/06:37:33+06:37:41/06:42:41/06:47:41/06:52:42/06:57:42/07:02:42, gap 2000-4300ms)。频率固定在 5 分钟节拍, 疑似另一个 5 分钟周期性任务的残留成本, **不是 Z20**(Z20 已实测 0-1ms)。未定位, 留给下一步。

## 待决事项(需要 Bettor 或团队下一位接手者拍板, 我这边发不出去)

1. **P0 该不该结案**: 核心恒定签名(186-190s)已消灭, 但 `eventloop-lag` 没有完全归零(降到 2-4s/5min)。建议按级别词报数: "P0 主体已解决, 剩余小尾巴待查", 不宣称"冻结完全消失"。
2. **5 小时零汇报是否正常**: 值得关注但不确定是否异常(可能是各 agent session 各自的任务队列在验证窗开启后自然结束, 没有主动 re-invoke 触发继续汇报的机制)。建议团队下一位接手者主动跑一次收尾, 别让这轮验证窗的结论悬空。
3. **NWT relay(8dd59acb)WebSocket 断连**: 多次重试同样报错("RPC Server (remote error) -> WebSocket -> WebSocket is not connected"), 跟这次 P0 无关但需要有人看一下 relay-health-monitor 有没有正常巡检到并尝试重连(该 cron 默认应该 30s 一轮)。

— NWT
