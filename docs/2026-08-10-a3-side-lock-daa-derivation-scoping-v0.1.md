# A-3 · `side_lock_daa` 取值出路 scoping【SCOPING-ONLY · 零落码 · 待 NWT 红队】

> **Status**: CURRENT · **rev-2**(2026-08-11 02:2xZ · J2)
> ⚠ **文件名仍是 `-v0.1`**:照 Bettor 2026-08-11 纪律令③「存量文档不改历史文件名(改名会断链接),只在下一次修订时把 Status 头切到 rev-N」。
> **本文的修订号是 `rev-N`;`v0.x` 一律指链上协议代际,与本文无关。**
> **上游**: `docs/2026-08-10-canary2-layer3-side-lock-daa-recovery-plan-v0.1.md`(第三层执行记录)
> **本文件不改任何代码、不动开关、不碰链。**

## rev-1 → rev-2 改了什么(三处,都是**结论级**改动)

| # | rev-1 | rev-2 | 为什么 |
|---|---|---|---|
| 1 | 受影响面 = j34vb 8 行(+ 转述的"6 盘 426 KAS") | **9 盘 / 627 行 / 30,533 KAS**(整盘 34,892 KAS) | rev-1 **转述**了别人的数;rev-2 **自己量**(§1) |
| 2 | 链上派生"本机不可得" | **永久不可得**(Owner 2026-08-10 终裁 + NWT 逐层独立复核) | 三类候选节点全排除、explorer 亲查无块 ⇒ 从"未测"升"已证" |
| 3 | 「路 B 授权层级 = D-012 授权边界,须 Owner」 | 🔴 **这句两处都错**,见 §4.0 | 我**凭记忆引了 D-012**,而 D-012 是「总纲 reframe / Oracle 权限边界契约 / Broker 属 Track B」,**与授权层级无关**;真正管这件事的是 **D-011**,而它的结论**相反** |

🔨 rev-1 那处错误正是 @NWT 预先点名要挡的三项之一(「D-012 授权层级标注不准」)。**它没被红队抓到,是我自己在 rev-2 复查在册原文时抓到的** —— 记 J2 账上:**在册条目的编号不能凭记忆引**。

---

## §0 这份稿回答什么 / 不回答什么

**问题不是「能不能从 `block_time` 算出一个 DAA」** —— 那当然能算。
**问题是「算出来的那个值,拿去喂给下游每一个消费点,够不够格」。**

⚠ **不回答**:那些行到底能不能从链上恢复(**已由 Owner 终裁判死**,见 §2);也不回答「该不该做」(§4 只把代价与授权层级摆全,拍板归 Owner/Bettor 按 D-011 口径)。

---

## §1 受影响面(**实测**,2026-08-11 02:1xZ,只读)

**判据**(四条与,缺一不算):母盘 `resolution_rule_spec.zk_native === true` · `settle_txid IS NULL` · `deadline_daa < 剪裁点 75,508,341` · 且该盘确有 `side_lock_daa IS NULL` 的行。

| market | status | deadline_daa | 行/NULL | NULL 行本金 | 整盘 |
|---|---|---:|---:|---:|---:|
| `…-7jy3s` | verifying | 55,369,479 | 1/1 | 1.50 | 1.50 |
| `…-ldtyn` | verifying | 55,608,486 | 3/3 | 127.78 | 127.78 |
| `…-8xykm` | verifying | 55,609,834 | 5/5 | 120.42 | 120.42 |
| `…-cswib` | verifying | 55,610,250 | 5/5 | 102.55 | 102.55 |
| `…-s6zwj` | verifying | 56,831,240 | 70/11 | 11.00 | 70.00 |
| `…-9ez2u` | verifying | 60,665,153 | 1/1 | 500.00 | 500.00 |
| `…-9jaty` | verifying | 61,452,455 | 4/4 | 8,500.00 | 8,500.00 |
| **`…-kr5l4`** | verifying | 60,722,281 | **694/589** | **20,875.00** | 25,075.00 |
| `…-j34vb` | verifying | 61,421,827 | 10/8 | 295.00 | 395.00 |
| **合计** | | | **793/627** | **30,533.25** | **34,892.25** |

🔴 **j34vb 是这堆里第二小的一份。** 最大的是 **kr5l4(589 行 / 20,875 KAS)** —— 它在册的标签是「25K / 694 人 leaf 疑丢」,**而它同时也撞在这堵墙上**;这两件事此前没被并到一起看过。

### 🙋 §1-bis 我第一版量错了,**留在这里当反例**

第一版口径是「母盘可结算 + 已剪 + 有 NULL 行」⇒ **81 盘 / 63,363 KAS**,差点就这么发出去。

🔴 **打掉它的是一个判别查询**:去看**已经 completed 的盘** ——

```
母盘状态                盘    行数    NULL   NULL%
completed              267    9990    9598   96.1%   ← 它们结成了
pruned_expired_waived  140   10800   10630   98.4%
verifying               92    4911    3308   67.4%
attested_v2              9      22       0    0.0%   ← 真正要用它的那条路, 一个不缺
全库                        36012   33149   92.0%
```

⇒ **`side_lock_daa` 为 NULL 本身【不】挡结算。全库 92% 的 side 行都是 NULL,它是常态不是异常。**
**挡的是 V2/ZK 那条 close 路**(`bshard-close-transport.mjs:369-370` 遇任一 NULL 即 throw;`canonicalBetOrder` 缺值 throw)。
🔨 **判据**:81 盘那个口径**没有区分「缺值」与「缺值且这条路要用它」** —— 在册「一个缺失有两种成因」那族,这次是我犯。**先量到的那个大数是错的,不留着当上限用。**

### 🔵 §1-bis-2 与 NWT 独立复算的差,已对平(**不是分歧**)

NWT 2026-08-11 用自己写的查询独立重跑:**NULL 行 627、NULL 本金 30,533.25 KAS —— 分毫不差**。
两把尺在**次要**数字上差 38 行 / 38.00 KAS(他 755 行 / 34,854.25;我 793 行 / 34,892.25)。

**差因已现查坐实**:他按 **shard 粒度**(只收「自己有 NULL 行的分片」),我按**逻辑盘粒度**(整盘所有分片)。
⇒ 差的正是**那 2 个「零 NULL」的分片:38 行 / 38.00 KAS**,逐位对上。
🔨 **两个口径各自都对,但用途不同**:「整盘 KAS」这一列问的是**这盘一共有多少钱卡着**,**必须**含零 NULL 的分片 ⇒ **该列用逻辑盘粒度**;「有 NULL 的分片数」问的是**要修几处** ⇒ 用 shard 粒度。**别把其中一个"改正"成另一个。**

### 🔴 §1-quater kr5l4 并案 —— 「疑丢」与「不可得」是**两种定性**,别合并

@Bettor 2026-08-11 提出:kr5l4 那桩 25 天悬案里「缺的那个高检出率历史源」,与今天钉死的剪裁墙**大概率是同一件事**。**我同意方向,但这两个词必须分开写**,否则处置会走错:

| | 问的是 | kr5l4 的状态 |
|---|---|---|
| **leaf 疑丢** | **钱/叶子在不在**(链上状态的**存在性**) | 🟠 `[未定案]` |
| **派生不可得** | **值取不取得到**(我们**读不读得到**已剪历史) | ✅ `[CONFIRMED]` 589/694 行缺 `side_lock_daa`,deadline 在剪裁点下 **18.4 天** |

🔴 **为什么必须分开**:
- **「不可得」是我们这侧的能力问题** —— 钱在链上原地没动,**只是我们重建不出委员要签的那个序**。
- **「疑丢」是链上状态问题** —— 若叶子真丢了,那是另一类损失,**换个归档节点也救不回来**。
- ⇒ **两者的修法完全不同**,而**它们今天看起来一样(都表现为"结不出来")**。在册「一个缺失有两种成因」族。

🔨 **⇒ 一条可执行的判别**:kr5l4 的 deadline ≈ **2026-07-13**(见 §1-quinquies 的换算),而那桩悬案是 **25 天前**开的 —— **开案那天,它的块很可能【已经】在墙外了**。
⇒ **若属实,那 25 天的"查不到高检出率历史源"从头到尾就是这堵墙**,不是索引器缺陷、不是采集漏。**这条可证**:取当时的剪裁点读数(若有留档)与 60,722,281 比即可。`[待查·白班]`

### 🔴 §1-quinquies 这堵墙有多深 —— **实测:1,480,722 DAA ≈ 4.05 天**

> 🙋 **本节 02:5xZ 整节重写,撤回前一版的两个数(9.5 天 / 「3 盘 3.6 天内落墙」)。撤回过程见 §1-sexies —— 那一节比这个数本身更该读。**

**用真实块索引 `spc_daa_index`(1,626,970 行真实块的 `daa_score` + `timestamp_ms`)**:

```
剪裁点 75,508,341 → 真实时刻 2026-08-06 17:56Z
   (夹在 75,498,599 @ 17:39Z 与 75,508,627 @ 17:56Z 之间 —— 相邻真实块, 17 分钟跨度)
最新块       76,989,063 @ 2026-08-10 19:02Z
⇒ 剪裁窗深 = 1,480,722 DAA ≈ 4.05 天
```

🔵 **引用时请带上 DAA 那个数**:**不变量是 1,480,722 DAA**(协议深度);「4.05 天」是按这段时间的实际出块节奏(≈9.51 DAA/秒,取自剪裁点附近两个相邻真实块)换算的。**链一慢一停,同样深度对应的天数就变长。**

🔴 **⇒ 窗口只有 ~4 天,而在册说法是 ~30 天 —— 差约 7 倍,方向是【更急】。**
⇒ **任何盘只要 deadline 后 ~4 天内没结完,链上派生就永久失效。**

**受影响 9 盘的 deadline(用真实块索引换算,不用 `deadline_daa` 估值)**:

| | deadline 真实时刻 | 在剪裁点**之前** |
|---|---|---|
| `…-7jy3s` `…-ldtyn` `…-8xykm` `…-cswib` | 2026-07-10 | 27.0 – 27.1 天 |
| `…-s6zwj` | 2026-07-10 | 27.6 天 |
| `…-9ez2u` / `kr5l4` | 2026-07-15 | 22.1 天 |
| `…-9jaty` / `j34vb` | 2026-07-16 | 21.0 天 |

🔨 **⇒ 窗口 ~4 天,而管线停摆 20 天 ⇒ 这批钱是【被停摆推过墙的】,不是"恰好过期"。修结算本身就是止损。**
**窗越窄,这句因果越强,不是越弱。**

**前瞻敞口(时间结构与前一版完全不同)**:

- **此刻正在往外滑的盘 = 0** —— 没有任何未结算盘的 `deadline_daa` 落在剪裁点与 tip 之间。
- 🔴 **而 32 盘 / 5,155.28 KAS 的 deadline 尚未到达** —— **每一盘在 deadline 到达那一刻起一根 ~4 天的引信**。
- ⇒ **任何超过 ~4 天的结算中断,都会把当时到期的那部分直接转成永久损失。**
  **归档节点那条建议照旧成立且更急** —— 理由从「正在滑出」换成**「窗口只有 4 天,而我们刚证明自己能停 20 天」**。

### 🙋 §1-sexies 上一版那两个数是怎么错的(**方法教训,比数值重要**)

**错因一句话**:我拿 `pool_markets.deadline_daa` 去拟合 DAA↔时间。
🔴 **那个字段是建盘当时【估】出来的,不是链上读数** ⇒ **拟合出来的是我们自己那个估算器,不是链。**
而**真尺一直在同一个库里**(`spc_daa_index`,160 万行真实块)。

🔨 **本可以自己抓到,判据留下**:那个拟合**同时给出两个互相矛盾的预测** —— 它说剪裁点在 08-01,而 tip 读数是 08-10 且两者只差 148 万 DAA(≈1.8 天)。**「08-01」与「只差 1.8 天」不可能同时成立。**
⇒ **一个拟合若能推出两个数,就必须让它们互相对一次。我只用了其中一个。**

🔴🔴 **而这次的独立复核【没有拦住它】,原因值得单独记**:
NWT 独立写了自己的回归(**3,615 点,不是我的 2,288 点**,斜率 9.915 vs 我的 9.276),**结论与我一致**。
⇒ **两次独立回归、不同点数、不同代码,一起错** —— 因为**它们读的是同一个坏源**(`deadline_daa`)。
🔨 **在册判据的现场实例**:**共享源上的"独立验证"是空的,哪怕两边逻辑真的独立。** 独立性必须落在**取数的那一端**,不在算法这一端。
(点数差 2,288 vs 3,615 的来源:我取 `DISTINCT (deadline, deadline_daa)` 并滤掉了 `deadline_daa > 1e9` 的远期估值行。**这个筛选口径不重要了 —— 两边用的都是坏尺。**)

**第二个数整条作废**:「3 盘 / 80 KAS 在 ~3.6 天内落墙」——
那 3 盘(`349ih`/`bq4gr`/`fim6c`)的 `deadline_daa` = **78,426,9xx,高于当前 tip 76,989,063** ⇒ **deadline 还没到**。
**是那把坏尺把未来当成了过去。**

### ⚠ §1-ter 三条边界(请照这个强度引)

1. **9 盘里实测过的只有 j34vb**(A2 探针逐行跑过)。另外 8 盘是**同代码路径推断** —— 强,但标 `[推断]`。逐盘坐实需再跑探针(白班可做)。
2. **用 `deadline_daa` 当剪裁代理,方向是安全的**:下注块必然**早于** deadline ⇒ deadline 已过墙 ⇒ 注的块更早、更确定没了。**反向不成立** ⇒ 「墙内那 6 盘还能救」是**乐观**的,不是保证。
3. **剪裁点只会前移** ⇒ 这个集合**只会变大**。**这就是「现在就起归档节点」那条结构性建议的量化理由**:今天不封顶,明天这张表多几行。

---

## §2 链上派生 = **永久不可得**(rev-2 升级:从「本机不可得」到「已证」)

| 层 | 谁做的 | 结论 |
|---|---|---|
| 本机探针逐行跑 | J2 2026-08-10 | 7 盘的块在剪裁点下 12.1M–20.1M DAA ⇒ 本机读不出 |
| `--archival` / `retention_period_days` 语义对源码 | NWT | 二者都是「**从今起不删我的**」,**不是**「把网络已删的取回来」⇒ 新起节点救不了 |
| UTXO 条目自带 `blockDaaScore` 这条路 | J1 提 · J2 实测 | **对 `spine_p2sh` 成立,对 `side_p2sh` 不成立**,见 §5 |
| 三类候选节点(早期归档 / 当时已配保留期 / 未剪索引器) | Owner 终裁 | **TN12 无归档节点;explorer 亲查无那个块** |

⇒ **二元问题关死在否定侧。** 后面三条出路都是在「真值取不回来」这个前提下选的。

---

## §3 双轴 —— 而**不是**「排序键宽松 / 共识输入严格」那个二分

> 🙋 原框法是「作排序键 vs 作共识承诺输入是两个精度需求」。**方向反了**:
> **那个排序键【本身】就是共识承诺输入。这里没有「宽松的排序用途」这一档。**

| 轴 | 问的是 | 谁吃这一轴 | 近似派生过得了吗 |
|---|---|---|---|
| **精度** | 派生值离真值多远 | ①② 的阈值比较 —— bet 离 deadline 越远越不敏感 | 🟢 大概率能(需实测误差包络) |
| **确定性** | 各节点是否得出**逐位相同**的值 | ②(防 fork)③(排序→根) —— **差 1 都不行** | 🔴 **结构上不能** |

### 消费点逐条(现读,排除测试与 DDL)

| # | 位置 | 怎么用 | 失败模式 |
|---|---|---|---|
| ① | `bshard-close-enforce.mjs:416-417` | C1 链锚:`null ⇒ fail`;`> deadline_daa ⇒ fail` | 阈值比较 |
| ② | `bshard-close-enforce.mjs:748-749` | committee-exclude:`null ⇒ throw`(注释原话「**fail-loud 防 cross-node fork**」) | 阈值 **+ 跨节点必须一致** |
| ③ | `pool-payout-root.mjs:66-76` `canonicalBetOrder` | **排序键** `side_lock_daa ASC`,tiebreak `side_lock_tx` 字典序;缺值 `throw` | **排序 ⇒ 根** |
| ④ | `bshard-close-transport.mjs:369-370` | 前置:任一 NULL 即 `throw` | 存在性 |

### 🔴 ③ 的承重链(**已实读,非注释推断**)

`bshard-close-enforce.mjs:30` 的 import 行注释原文:`W2: committee 独立重算 betsRoot/refundRoot`。
⇒ **排序键 → `canonicalBetOrder` → betsRoot,而这个根由【每个委员各自独立重算】。**
⇒ **两个委员若派生出不同的 `side_lock_daa` ⇒ 不同排序 ⇒ 不同根 ⇒ 签名对不上。**
🔨 **这不是「精度不够导致数值略偏」,是【共识分叉】。**

---

## §4 三条出路 × 代价 × 授权层级 × 撞哪条在册判据

### §4.0 🔴 先纠正 rev-1 的授权层级(两处都错)

rev-1 写「路 B 授权层级 = D-012 授权边界,须 Owner/NWT,不是数据修复」。**逐条查在册原文后**:

- **D-012 不是授权边界条目**。原文是「KANet 总纲 reframe —— Agent 应用与角色经济;Oracle Skill 抽象立项;Broker 开放入口属 Track B」。**引错了。**
- **真正管这件事的是 D-011**(2026-07-21 Owner 频道直令),而它的结论**与 rev-1 相反**:
  > 涉钱路/covenant/结算的改动,**不再要求 Owner 对每一项逐笔点头才能上线**;「什么时候技术上具备上线条件」由 Bettor 协调团队自行判定并驱动执行。
  > **不变的硬约束**:内部双审纪律照走不降 —— NWT 独立红队 / J1·J2 互审 / design-first→红队→实现→验落链。

⇒ **修正后的授权映射**:

| 出路 | 授权层级(按 D-011 口径) |
|---|---|
| 路 A / 路 B(改结算路的技术实现) | **内部审核链**:design-first → NWT 红队 → J1·J2 互审 → 实现 → 验落链。**不需要 Owner 逐项点头。** |
| 路 C(承认 34,892 KAS 永久不可结) | 🔴 **Owner** —— 这不是技术实现层,是 D-011 明确留给 Owner 的「**方向 / 结果**」层。在册有同族先例(「只 settle 不 refund、孤儿盘永久损失」)。 |
| 若路 B 需动 guest circuit / imageId | 另撞 **D-009 变更冻结门**。🟠 `[推断·未验]` 我判断**不需要**动电路(见 §4.2 末),但这一条得由动手的人现读电路输入坐实。 |

### §4.1 路 A · 近似派生 + **各节点独立重算**

- **代价**:低(纯计算)。
- 🔴 **过不了 §3 确定性轴** ⇒ **对 ②③ 不可用**。`block_time` 取自 `kaspa_tx_log`(**本机索引器记录,不是共识值**),各节点未必逐位相同 ⇒ 各自派生 ⇒ 各自不同 ⇒ **正是 ② 那句「防 cross-node fork」要防的东西**。
- 🟡 **可用射程**:仅 ①(及纯本地排序/展示)。**而 ① 单独通不过结算** —— ②③ 仍会挡。
- **⇒ 净评价:收益近零。** 它不解决问题,它**制造分叉**。
- 🟠 `[推断·未实测]` 「各节点 `block_time` 不逐位相同」有在册支撑(`kaspa_tx_log` 完整性缺口在册),**但我没有跨节点实测过两台的 block_time 差异**。

### §4.2 路 B · 派生一次 + **承诺/共享**,委员验自洽而非各自重算

- **代价**:中(需要一个所有节点读同一份的载体:市场 metadata / close request 载荷)。
- **授权层级**:**内部审核链**(D-011),**不是** Owner 逐项 —— 但 §4.2-c 那条后果决定了红队标准要顶格。

#### §4.2-a 撞在册 `verify-value-source`

在册要害:**checker 必须读到【决策时刻真正生效的值】;caller 喂进来的值可控、可伪。**
⇒ 路 B 恰恰把 `side_lock_daa` 从「委员自己去链上取的事实」变成「载荷里带来的值」。
⇒ **委员从此验的不是「这个 bet 何时上链」,而是「有人声称它何时上链,且这份声称内部自洽」。**

#### §4.2-b 撞 **D-008** —— 这不是类比,是**同一形状的实战先例**

D-008 在册原文(NWT 源码级定论):
> CloseZkV2 `zk_close` 对 `guestPayoutRoot` **零链上校验 vs 委员值** = by-design;委员 attest 只锁 winner/**betsRoot**/refundRoot/atMs,payout 分配的 binding authority 在 guest circuit。

**而实战后果(2026-07-08 pxvml)**:propose 侧与 prove 侧用了**两套不同的 fee 政策**,同一市场两棵树三处三说法 —— **该架构决定了这种不一致【永远不会被自动拦下】**,直到有人做了一次真正独立的第二路盲算才暴露。

⇒ 🔴 **路 B 的风险不是「可能被伪造」,而是【不一致会变得结构上不可检测】。** 这正是 D-008 那个案子的教训本体,也是它末尾那句方法论沉淀(「重跑 N 次一致只证确定性不证正确性」)指的同一件事。

⇒ 🔴🔴 **且注意方向**:D-008 里委员**明确锁 betsRoot**。**而 `side_lock_daa` 正是 betsRoot 的排序输入。** 路 B 等于把「委员唯一还在独立重算的那一格」的**输入**也变成喂进来的 ⇒ **委员 attest 从「独立重算」退化成「对同一份输入做同一次计算」。**

#### §4.2-c 🔴 单点伪造面(@NWT 点名要,逐条摆开)

**谁能伪造**:**构造那份载荷的一方**(propose/close 发起者)。

**伪造能换来什么**(不是抽象风险,是三个具体后果):
1. **把一个真实 bettor 的 `side_lock_daa` 抬到 `> deadline_daa`** ⇒ 消费点 ① 判越界 / ② 进 exclude 名单 ⇒ **该 bettor 被排除出结算**。
2. 🔴 **反方向(NWT 2026-08-11 红队 MUST-FIX 补入)——把一笔【实实迟到】的注往下压到 `deadline_daa` 之下** ⇒ **这笔本该被排除的迟到注混进结算池** ⇒ **稀释所有合法投注人的分成**。
   **判据在代码里是单向的**:`bshard-close-enforce.mjs:416-417` 的 C1 链锚判的是 `side_lock_daa > deadline_daa ⇒ fail` —— **它只挡"看起来太晚"的,不挡"看起来不太晚、实则是压下来的"。**
   🔨 **⇒ 这一向比第 1 向更该警惕**:第 1 向**损害一个人**、当事人会喊;第 2 向**稀释所有人**、**没有任何一方会发现自己被拿走了什么**,而且同样不产生异常信号 —— 五个委员照样签得整整齐齐。
   ⇒ **任何路 B 的缓解方案必须【同时防两个方向】**;只堵"把合法人踢出去"那一半,等于没堵。
3. **改任意一行的值** ⇒ `canonicalBetOrder` 排序变 ⇒ **betsRoot 变 / merkle_index 变** ⇒ 分配对不上。

**什么拦得住**:目前设计里是 **4-of-5 委员签名**。
🔴 **而这正是路 B 最贵的一格**:**委员的独立性,就是那个「4-of-5」的全部含义。** 若五个委员读的是**同一份喂来的值**、各自只验自洽,那么**五个都会一致地接受同一份伪造** ——
> **⇒ 路 B 在这个字段上把 4-of-5 门槛静默降成 1-of-1(构造者)。**
> **它不会表现为「签名不够」,而是表现为「五个签名整整齐齐」。**

#### §4.2-d 「委员验自洽」到底验得了什么 / 验不了什么

| 能验 | 验不了 |
|---|---|
| 载荷内部字段互相一致(daa 序与 tx 序不矛盾) | **这个 daa 是不是那笔 bet 真实的接受块 daa** |
| 所有委员拿到的是**同一份**载荷(哈希一致) | **那份载荷本身对不对**(一致 ≠ 正确) |
| `side_lock_daa <= deadline_daa` 成立 | **越界 bet 被喂一个合规假 daa 从而混进 exclude 名单** |
| 派生法可复现(同输入同输出) | **输入(`block_time`)本身是否可信** —— 本机索引记录,非共识值 |

🔴 **一句话**:自洽验的是「大家看到的是同一份、且这份不自相矛盾」;**它验不了「这份是真的」**。
而 ②③ 要的恰恰是后者 —— ② 注释里那个「**链锚**」二字就是在说「不许信 DB,要信链」。

🟠 **电路边界**:betsRoot 由委员链下重算、`guest circuit` 绑的是 payout 分配 ⇒ **我判断路 B 不需要改电路/imageId、不触发 D-009**。`[推断·未验]` —— 动手前须现读电路输入坐实。

#### 🔵 §4.2-e 留给设计阶段的种子(NWT 2026-08-11 提 · **不是要求,不阻塞本稿**)

若日后仍要保留「承诺值」这条路的可能性:**让多个委员各自独立取 `block_time` 派生一次,做中位数/多数投票**,而不是信单一 proposer 的载荷 ——
⇒ 至少把 §4.2-c 那个 **1-of-1 恢复成某种 N-of-M**。
⚠ **但它仍解决不了 §3 的确定性要求**:各节点 `block_time` 本身就不保证逐位相同,**投票能压住恶意,压不住漂移**。
🔨 ⇒ 这条种子的价值在于**它把两个问题分开了**:「防伪造」可以投票解决,「逐位相同」不能。**先分清要治哪一个,再选药。**

### §4.3 路 C · 不派生,承认链上派生不可得

- **技术代价 0**;**实际代价 = §1 那张表**:**9 盘 · 627 行 · 30,533 KAS(整盘 34,892 KAS)永久不可结**。
- **授权层级**:🔴 **Owner**(D-011 的「方向/结果」层)。在册同族先例:「只 settle 不 refund、孤儿盘永久损失」。
- 🔴 **红线(在册,写死,rev-2 原样保留)**:**绝不照 shard-9 那个「标 `manual_recovery_refunded` 排除」的修法办** —— 那会把真实 bettor 排除出结算,**是实实在在的损失,不是记账动作**。
- 🔵 **注意本仓已有一个同族状态名**:`pruned_expired_waived`(140 盘 / 10,800 行)。**路 C 若被选,它大概率就是落到这个状态。** ⇒ 请**先查清那 140 盘当初是怎么裁的、谁裁的、bettor 有没有被通知** —— 否则等于在不知情的情况下沿用一个先例。`[未查·白班可做]`

---

## §5 路 D · UTXO 条目自带 `blockDaaScore` —— **已实测,对 side 侧不成立**

> 来源:J1 2026-08-10 源码实核(`utxo_validation.rs:147` `pov_daa_score` → `utxo_diff.rs:234` `UtxoEntry::new(value, spk, block_daa_score, …)`)。
> **想法本身是对的**:UTXO 集是剪裁**不删**的那部分,条目里的 daa 是**接受块 DAA、共识值、被剪裁点 MuHash 承诺覆盖** ⇒ **在 §3 确定性轴上结构性合格**。J1 在 `spine_p2sh` 上实测三盘全读得出(块本身已 `cannot find header`)。

🔴 **而打在 `side_p2sh` 上不成立 —— J2 2026-08-10 实测**:

```
阳性对照(2 行【已有】side_lock_daa):
   库存 59,950,126 → utxo=1 daa=59,562,352   🔴 不等
   库存 60,244,919 → utxo=1 daa=59,562,352   🔴 不等
待测 8 行: 全部 utxo=1 · daa=59,562,352 · 值 0.2 KAS
```

**根因(现查坐实)**:该市场 `side_p2sh` **只有 1 个值、被 10 行共用**;`side_redeem_script_hex` **长度 0**
⇒ **bshard 共享池形态:每个 bettor 根本没有独立的 side UTXO。** 那 0.2 KAS 是 `PS_SEED`(`pool-shard-register.mjs:79` = 20,000,000 sompi),**不是任何人的注**。
⇒ **不是「被花了」,是【从来就没有过】。**

### 🔴🔴 这一格拦下的东西,是本稿存在的理由

**若没有那 2 行阳性对照,这一跑会输出「🟢 8/8 可取 daa」** —— 而那个 daa 是**分片种子块的、十个人完全相同**。
喂进 `canonicalBetOrder` ⇒ **排序键全相等** ⇒ 退化成 tiebreak ⇒ **payoutRoot 与真值不同,且【每个委员各自重算也会一致地错】**。
🔨 **⇒ 一个「所有节点都同意」的错值,比一个不一致的错值更危险** —— 前者过得了 §3 的确定性轴,而确定性轴正是本稿用来筛路的那把尺。**确定性合格 ≠ 正确。**

### 🙋 而这个坑本仓早已写下,是我没先读到

`trade-protocol-filter.js:1194-1196` 逐字写着**不走「查 `side_p2sh` 未花费 UTXO 反推」那条路**(2026-07-08 记录的历史假阴性)。
**那条注释就是为挡这条路写的。** 我为确认该函数只读而读过这个文件,**却没读到这一段就动了手**。记 J2 账上。
🔵 **边界入档(免得下一个人重跑)**:**J1 的方法对 `spine_p2sh` 一类值有效;对 `side_lock_daa` 无效。**

---

## §6 我的判断(**标明是判断,不是结论**)

- **路 A 收益近零**(过不了确定性轴)。
- **路 B 有技术含量,而它的代价不是工作量,是信任模型** —— 具体地:**它把 betsRoot 这一格的 4-of-5 静默降成 1-of-1**(§4.2-c),且 D-008 的先例说明**这类不一致会变得结构上不可检测**。
- **路 C 是 Owner 的方向层决策,不是技术活** —— 而 rev-2 把它的标价从「426 KAS」改成 **34,892 KAS**,**这足以改变那一拍的权重**。

⇒ 🔵 **我不推荐在结算救火的时间压力下走路 B。** 若认为 34,892 KAS 值得动信任模型,那应当是一次**独立立项 + 顶格红队**,不是救火的附带产物。
⇒ 🔵 **而无论选哪条,§1-ter 第 3 条都独立成立**:**剪裁点只会前移** ⇒ 不起归档节点,这张表明天更长。**那是一条与三条出路正交的、现在就该做的事。**

---

## §7 证据层级

| 陈述 | 层级 |
|---|---|
| §1 那张表(9 盘 / 627 行 / 30,533 KAS) | ✅ `[CONFIRMED·本机只读实测 2026-08-11 02:1xZ]` |
| 「NULL 本身不挡结算」(completed 96.1% 也是 NULL) | ✅ `[CONFIRMED·实测]` |
| 9 盘中 8 盘会撞同一堵墙 | 🟠 `[推断·同代码路径]` —— **只有 j34vb 逐行实测过** |
| 链上派生永久不可得 | ✅ `[CONFIRMED·Owner 终裁 + NWT 逐层独立复核]` |
| 四个消费点及失败模式 | ✅ `[CONFIRMED·现读,行号在表内]` |
| ③ 的根由委员**各自独立重算** | ✅ `[CONFIRMED·现读 import 行注释 + `canonicalBetOrder`:66-76]` |
| 路 B 把 4-of-5 降成 1-of-1 | ✅ `[CONFIRMED·由 §4.2-b 引的 D-008 原文 + ③ 承重链推出,两端都是现读]` |
| `block_time` 各节点未必逐位相同 | 🟠 `[推断·有在册支撑,未跨节点实测]` |
| 近似误差的实际包络 | 🔴 `[NOT-MEASURED]` —— 需样本回归,而这些盘的块**已被剪** ⇒ 得另找市场取样 |
| 路 B 不触发 D-009(不动电路) | 🟠 `[推断·未验]` —— 动手前须现读电路输入 |
| `pruned_expired_waived` 那 140 盘当初怎么裁的 | 🔴 `[未查]` —— 路 C 若被选,这是**必查前置** |
| 与 NWT 独立复算差 38 行 / 38 KAS 的成因 | ✅ `[CONFIRMED·现查逐位对上:2 个零 NULL 分片]` |
| 剪裁窗深 = **1,480,722 DAA ≈ 4.05 天** | ✅ `[CONFIRMED·真实块索引 spc_daa_index 夹逼 + NWT 用同一真尺独立复核一致]` |
| ~~TN12 保留 ~9.5 天~~ / ~~「3 盘 80 KAS 3.6 天内落墙」~~ | 🔴 **已撤回(§1-sexies)** —— 尺子坏了(`deadline_daa` 是建盘估值),**两次"独立"回归共用同一坏源一起错** |
| 「此刻正在滑出的盘 = 0;32 盘 / 5,155 KAS 待到期,各起 ~4 天引信」 | ✅ `[CONFIRMED·真实 tip / 剪裁点边界]` |
| kr5l4「疑丢」与「不可得」是两回事 | ✅ `[CONFIRMED·定义级]`;而「25 天前开案时块已在墙外」是 🟠 `[推断·待查当时剪裁点留档]` |
| 「不推荐现在走路 B」 | 🔵 **判断,非结论** —— 归 Bettor(D-011 口径)/ Owner |
