# KANet 对外统一口径(Owner 订死·唯一权威版本)

> **Status**: CURRENT

**状态**: 已由 Owner 订死,即刻生效
**出处**: Owner(`qrymjvcy`)在 `dev-coord-testnet` 频道,2026-07-26 04:01 + 04:03(原话「订死!」)
**适用**: 一切对外材料、快速上手文档、外部问询答复、用户面文案

---

## ✅ 唯一权威文本(逐字·任何人不得改动一个字)

```
KANet 是一条给程序用的信任通道。
接进来的程序,能拿到三样东西:可信的身份、加密的通信、链上的结算 —— 全部记在 Kaspa 链上,任何人都能自己验。
现在是测试网。
```

🔴 **要改这段,必须回 `dev-coord-testnet` 频道,由 Owner 重新订。任何人不得就地改写、"润色"或按语境增删。**

---

## 🔴 明确禁用的说法(以及为什么)

| 禁用 | 为什么 | 证据 |
|---|---|---|
| 「任何人都能自己**查**」<br>「链上可**查**」 | 「查」暗示存在一个可点开的网页浏览器。**TN12 没有公网 explorer。** | 【实跑 2026-07-26 04:00】`nslookup explorer-tn12.kaspa.org` ⇒ `Non-existent domain`;`curl --max-time 8 https://explorer-tn12.kaspa.org/` ⇒ `http_code=000`(连不上,不是 404) |
| 「外部程序**已经能**快速接入」 | 外部程序接入是**正在建**的主干工作,今天做不到 | `kasia-console/src/api/kanet-broker.js` onboarding 仍要求 `bot_token` |

🔵 **为什么单独立这张表**:本项目代码在这一点上是诚实的 —— `buildExplorerUrl(txid, 'testnet-12')` **宁可返回 `null`,也不编一个打不开的 URL**(该处注释:「不是换个域名,是承认没有」,由 NWT 红队 `31eade42` GREEN-with-MUST-FIX 折入)。
🔴 **对外文案若写「能自己查」,等于文案退回到我们自己代码已经拒绝的那个说法** —— 在用户面上声称一个代码里明写不存在的能力。

---

## 🔴 已知差口:这句话今天在两条路径上【不成立】

订死那句要求的是「每一笔都有链上凭据、可以自己验」。**按结算路径分,现状不是一致的**(J2 于 B0-M2 实读核出):

| 路径 | 是否满足 | 依据 |
|---|---|---|
| ZK claim / `zk_close` / handoff | ✅ 成立 | 广播成功**不**推进状态;`checkUtxoLanded` 过了才写;超时零持久化 |
| `bettor-prediction-settler` | 🔴 **不成立** | `checkUtxoLanded` 引用 **0 次** |
| `kasia-console/src/services/exchange-machine.js:826-829` | 🔴 **不成立,且更糟** | 手工造 `{ confirmed: true, confirmations: 1 }` —— 不是漏了校验,是**伪造了裁决** |

⚠️ **处置(J2 提,Bettor 采纳)**:
- 🔴 **不改文案。** 文案是 Owner 订死的,且它描述的是我们要建成的东西。**该动的是那两条路径,不是那句话。**
- 🔨 **那两条的修复优先级由此上调** —— 不是因为技术上更严重了,而是因为**它们现在处在一句已生效的对外承诺的反面**。
- 📌 在它们修好之前,**对外材料引用这句话时不得额外声称"所有功能均已如此"**;这句描述的是协议承诺,不是逐路径的完工声明。

🔵 **为什么写进这份文件而不是另开一篇**:一句对外承诺与它的已知差口分开存放,读到承诺的人不会读到差口。**放在一起,才是这份文件唯一诚实的形态。**

---

## 📌 定稿经过(留档,因为过程本身是判据)

1. Bettor 拟了两版,两版都含「任何人都能自己查」。
2. **NWT 在 Owner 拍板前抓出这半句不成立**,并给出替代措辞与两格自标【未做】。
3. Bettor 实跑 DNS/curl 补掉其中一格,确认该域名至今不存在。
4. Owner 先按旧措辞发了一版;**J2 提醒不得替 Owner 判断他是"没看到"还是"看到了仍要这么说"**,应直接问。
5. Bettor 只提请改一个字(查 ⇒ 验),Owner 采纳并订死。
6. ⚠️ Bettor 首次落锁时**用了自己的转述版本而非 Owner 原话**,当场改锁为逐字原文 —— 本文件即改锁后的版本。

🔨 **教训(适用于一切"被订死"的文本)**:**订死的是原话,不是意思。** 相近的改写即使更好,也不是被订死的那一句;要改,回去重订。
