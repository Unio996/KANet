# u1 · A2 同源判定 **规范定稿 v1.0-rc**（待 @NWT 审 · 落码依据）

> **Status**: CURRENT · v1.0-rc（2026-08-12 · J2）—— **本文件是 A2 的规范唯一权威**。
> **与设计稿的分工（防同一事实两处存、一处必陈）**：
> - **规范条款 / 验收判据 / 残余敞口** ⇒ **只在本文件**，落码以本文件为准；
> - **推导、实测读数、我自己的撤销记录** ⇒ 留在 `docs/2026-08-12-u1-a2-same-origin-decidability-v0.1.md`（v0.5，证据与история稿，**非规范**）。
>
> **裁定来源（本稿只落实，不重开）**
> | 来源 | 内容 |
> |---|---|
> | @Bettor ledger **(159)** | A2 方向 = **A+C**；**§8.3 委员必须 mnemonic 型**；逃逸口必堵（取舍留设计/红队） |
> | @Bettor 频道 **19:57Z** `#p3086h.2` | **`identities-per-account` 锁死 = 1**；「同一登记根下出现第二个注册身份 = **构造性违规**，不需要任何仪式性判定」；**C 边界不变** |
> | @NWT 红队 `9d7fc088` | §5-A PUSH-BACK（措辞 + 至少一项缓解 + **V6**）；committee 抽样问题已答 |
> | @J1tn 审视 `8969aca7` | **现行码已有两个逃逸口**，形态 A 会原样继承（真执行四臂） |

---

## §1 目标与适用范围

**A2 要回答的**：两个委员身份是不是**同一份 mnemonic** 派生的。

**能与不能（这一条是规范的一部分，不是免责声明）**：
- ✅ **正向可证**：「这两个身份同源」——可由登记根相等 + 派生证明**密码学地**判定。
- 🔴 **反向不可证**：「这两个身份确实不同源」——**任何事后软件检查都做不到**。造钥方可以私下用一份 seed 派生到**另一个硬化账户**，只出示两个互不关联的根（硬化兄弟不可关联，实测在证据稿 §3）。
  ⇒ **反向保证只能来自生成仪式**（runbook §2 步骤 2 在目标宿主本地生成）。**C 边界，(159)/19:57Z 两次确认不变。**

⚠ **⇒ A1/A2 打勾 ≠ R3 有密码学保证。** 这条必须出现在**运行时输出**里（**V6**），不能只活在文档里。

---

## §2 规范条款（落码即照此）

### N1 · 登记根 = 账户层 xpub
登记根 **= `m/44'/111111'/0'` 的 xpub**。**不是** master xpub（跨不过硬化级，推不出子 pubkey），**不是** seed 指纹（与 relay pubkey 无可验链接 = 裁定要防的"换皮的 ②"）。

### N2 · 委员身份派生布局
```
m / 44' / 111111' / 0' / 0 / i
```
**登记根到每个身份的路径全程非硬化 ⇒ 验证方可从登记根推导出该身份 pubkey 并逐字节比对**（满足 (156) 硬要求）。

🔨 **N2 的作用不是"允许一根挂多身份"，恰恰相反**：它保证**同一 seed 的任何额外身份都只能落在同一个根下 ⇒ 一旦出现就【可见】**。旧布局下第二个身份藏在另一个硬化账户里 = **不可见**。

### N3 · `identities-per-account` **锁死 = 1**（@Bettor 19:57Z）
每个登记根**有且只有一个**注册身份（`i = 0`）。
🔴 **同一登记根下出现第二个注册身份 = 构造性违规，直接拒绝** —— 不需要任何仪式性判定、不需要人工裁量。
**后果（两条都要记）**：
- 泄露放大面**归零**：无兄弟可沦陷；那把子私钥本就等于该身份全沦陷，**无增量损失**。
- **零迁移成本**：现实本就 10 份 mnemonic ↔ 10 个 `is_oracle` 身份，一比一。

### N4 · 委员必须 mnemonic 型（(159) §8.3）
`privkey_encrypted`-only 身份**不入委员**（R1 的分配单位是一份 mnemonic，privkey-only 落在规则外）⇒ **登记时 fail-closed 拒绝**，拒绝理由可读。存量 2 个 privkey relay 维持现用途不受影响。

### N5 · 两个逃逸口一起钉死（@J1tn `8969aca7` · 我原先只报了第二个）
| # | 逃逸口 | 钉法 |
|---|---|---|
| **①** | `relay-manager.js:70` 全量继承 `process.env`，`:86-87` 的 `else` 分支**不删继承来的 `KASPA_PRIVKEY`**，而 `wallet.mjs:105-118` **先读 `KASPA_PRIVKEY` 命中即 return** ⇒ **mnemonic relay 的签名身份被整个替换** | **spawn 侧互斥写死**：设一个必 `delete` 另一个 |
| **②** | `KASPA_ACCOUNT_INDEX` 走继承（全仓一处读、零处写 ⇒ 只可能来自继承环境） | **不许走继承**：由 console 按 relay 显式传；解析结果 `Number.isInteger` 校验，**坏值抛而不是静默取 0/NaN** |

🔴 **N5 的作用域，必须与条款同页写死**（否则又是「局部性质写成全局保证」）：
> 钉死消除的是**无痕的、误操作即可触发的**同源隐藏路径（复制一份 env、换宿主、换运维）。
> **它不构成对敌对域运营方的约束** —— 进程跑在他自己的宿主上。对他的那一半，答案只有 §1 的 C 边界。
> 它换来的是：把「造第二个身份」从**一个环境变量**抬到**一次代码改动**（无痕 → 留痕）。

🔵 **未验一格（@J1tn 明确标注，别当已知）**：`parseInt` 坏值行为里 **`NaN` 那一支他没走到底**（要真走 wasm `deriveChild(NaN, true)`）。**落码时必须自己验，别引他那句当已知。**

### N6 · 登记根分发面收窄
账户层 xpub **只落库 / 只给验证方**，**绝不进 `dev-coord-testnet` 或任何公开面** —— 频道是**链上明文、不可撤回**（在册 `reference-dev-channel-is-onchain-plaintext-not-internal`），贴一次 = 永久公开，而它是泄露链的必要输入之一。

### N7 · 残余边界进运行时输出（**V6**）
见 §4-V6。

---

## §3 安全性陈述（带确切前提 · 含已撤销的说法）

🔴 **已撤销，别复活**：设计稿 v0.1 曾写「放大面被 R1/R3 夹住 ⇒ **R3 不受影响**」。**该句作废**（@NWT `9d7fc088` PUSH-BACK，我复核成立）。它错在**循环**：用「R1 成立」去论证「违反 R1 的后果可控」，而 **R1 是否成立正是 A2 存在来检验的那件事**。

**现行准确陈述**：
1. **在 N3（锁 1）之下**，形态 A 的兄弟泄露放大面 **= 0**（无兄弟）。这是 N3 带来的，不是论证出来的。
2. **R1 被违反时**（同 seed 跨两域），一把子私钥泄露 + 公开登记根 ⇒ **跨域** ⇒ R3 被打破。**N3 让"第二个身份"在【同根】情形下变成构造性违规而被拒；【异硬化账户】那条路仍属 C 边界（仪式域）。**
3. **委员抽样的兜底程度是 stake 分布的函数，不是常数**：@NWT 用 `pool-committee-sampler.mjs` 算出单域(2/10)两人同场 ≈ **22.2%**、且只有 2/5 票 vs **4-of-5** 门槛（我独立重算一致）——
   ⚠ **但那是【均匀抽样】的数，而抽样器是 stake 加权**（`:117-128` `hit = rand % totalStake`；`:100` 非正 stake 抛错）⇒ **stake 不等时须重算**。
   ⇒ 防线链 = **R1（运维承诺）→ R2（运维配额）→ R3（抽样数学）→ stake 锚定**，**四节无一节是密码学强制**。

---

## §4 验收判据（预登记 · 落码前写死 · 落码后逐条跑）

| # | 判据 | 怎么测 | 为什么不能省 |
|---|---|---|---|
| **V1** | 同源正向可判：同一根下两个身份 ⇒ 判 SAME-ORIGIN | 随机 seed 造两身份（非真钥） | A2 主功能 |
| **V2** | 派生证明真在跑：把登记根换成**另一个合法根**（不是乱码）⇒ 必红 | 弱注入臂 | 乱码根会被格式校验拦下，**红了不证明比对在跑**（在册：注入物须取自断言实读的字段集） |
| **V3** | 反向不谎报：确实不同源 ⇒ 判 DIFFERENT，**且输出显式声明「本判定不具反向保证」** | 阳性对照 + 读输出串 | §1 边界必须出现在运行时，不能只在文档 |
| **V4** | 同 seed **异 account index** 两身份 ⇒ 必须报「**无法判定**」，**不得**报「不同源」 | 按证据稿 §2 构造 | 🔴 fail-closed：把"我看不出来"输出成"它们不同源" = 把无知报成安全 |
| **V5** | 存量全量：跑一次 32 行，输出与证据稿 §1 一致（0 重复 address） | 全量非抽样 | 同判据 B 纪律 |
| **V6** | **残余风险进运行时输出**：含「形态 A 下单点私钥泄露（尤其非整机级窄通道）的爆炸半径 = 该 seed 下全部身份；其可控性依赖 R1 合规，而 R1 合规正是 A2 本身在检验的对象」等价表述 | 跑检查器 `grep` 输出 | @NWT 要求；镜像 V3 —— **边界不在输出里 = 读记分板的人不会翻到设计稿** |
| **V7** | **privkey-only 身份做委员登记 ⇒ 必须被拒**（fail-closed），拒绝理由可读 | 取 `privkey_encrypted` 非空 / `mnemonic_encrypted` 空的身份走登记 | 裁定写在 ledger 里**不等于码里有闸** |
| **V8** | **同一登记根下出现第二个注册身份 ⇒ 必须拒绝并报构造性违规** | 造一根两身份的登记 | 「目前恰好是 1」与「1 被检查着」是两回事——前者没有任何东西在守 |
| **V9** | **两个逃逸口各自的负测试**：①继承 `KASPA_PRIVKEY` 时 spawn 必须删它（身份不被顶掉）②`KASPA_ACCOUNT_INDEX` 不再从继承环境生效，坏值**抛**不静默 | 照 @J1tn `8969aca7` §6 四臂复现脚本改造 | N5 是条款，**没有负测试的条款等于注释** |

---

## §5 残余敞口（明写，不留给下一个人撞）

1. **反向不可证**（§1 C 边界）—— 靠生成仪式，不是密码学。
2. **N5 不约束敌对运营方** —— 只消除无痕误配路径。
3. **`parseInt` 的 `NaN` 支未验**（@J1tn 标注）—— 落码时自验。
4. **兜底程度依赖 stake 分布 + stake 锚定**（§3-3）—— 非常数。
5. **形态 A 是一条新派生代码路径**，不是配置项调整（现行叶子固定 `0/0`）⇒ 新路径**必须自带一次 §N5 式的逃逸口审视**，否则只是把洞换个名字。
6. **本机 live 现态**：两个逃逸口**当前均未生效**（4 个近期广播的本机 relay 运行时地址与 `relay_nodes` 逐字符对上，`scripts/u1-escape-hatch-live-check.cjs`）——
   ⚠ **作用域**：只覆盖近期广播过的 relay；且这是**行为证据**，排除的是「正在生效」，**不是**「变量不存在」。
   🔴 **v1.0-rc 补（@NWT 20:03Z 对 safely_absent §5 的 MUST-FIX 同族，落在我自己这条上，我自贴）**：
   这条判据的样本来自 `broadcast_messages` —— **而那个观察者本身今晚静默停摆过**（:3200 摄入腿 18:28→19:17Z 断、CONSOLE-SPAWN-DEATH 自 18:34Z）。
   ⇒ 「近期广播过的 relay」这个集合**是由一个当时部分失明的观察者圈定的**：停摆窗内某 relay 即使签了、也可能没进表 ⇒ **它落进"无话可说"那一类，而不是"已验证"那一类**。
   ⇒ **本条的失效方向是【覆盖变少】而非【结论变错】**（进了表的那 4 条仍是真实签名地址）——**但覆盖率不能拿这次的读数当常数**。
   🔨 **⇒ 落码时把这条判据的分母写成"观察者自证在岗的那段窗口"**，不要写成"近期"。**缺席的排除力 = 仪器完备度**，这句是 @NWT 今晚在别处立的，同样管我这条。
   🔴 **而"自证在岗"的合格形态，按 Codex `90215dfd` 20:18Z 对 safely_absent 的裁决【比我上面写的严】，我这条照收**：
   我原话写的是「心跳/checkpoint 证据」——**Codex 明确否掉了"墙钟心跳文本"这种形态**，要求覆盖证据钉在**单调 DAA 区间**上，且 **checkpoint 有缺口即令该区间的负臂失效（即使 value 链连续）**、**重启必须续接原覆盖边界、不得静默开一个新的"完整"窗**。
   ⇒ **本条判据的分母同样不许用墙钟心跳文本。** 我这格是本地 DB 观察（不是链上缺席证明），DAA 锚定未必逐条适用，但**"缺口即失效""重启不得静默开新窗"两条原样适用** —— 落码时按这个强度实现，别照我上一版那句弱的做。

---

## §6 证据与复现（跨节点可跑，不依赖本机）

```bash
node scripts/u1-a2-decidability-probes.mjs     # §1 事实 / 派生实验 / 硬化墙(带对照臂) / 补齐列信息量 / xpub 泄露(带对照臂) / 抽样条件
node scripts/u1-escape-hatch-live-check.cjs    # 两个逃逸口在本机是否正在生效(行为证据)
```
推导与完整读数：`docs/2026-08-12-u1-a2-same-origin-decidability-v0.1.md`（v0.5）
@J1tn 逃逸口审视与四臂复现：`docs/2026-08-12-j1-derivation-env-escape-hatch-audit-v0.1.md`（`8969aca7`）

---

## §7 撤销记录（这一节的存在本身是规范的一部分）

| 被撤销的说法 | 谁抓的 | 为什么错 |
|---|---|---|
| 「放大面被 R1/R3 夹住 ⇒ **R3 不受影响**」 | @NWT `9d7fc088` 缺口① | **循环**：用 R1 成立去证违反 R1 的后果可控 |
| 「(整机模型下)多丢几把没差」 | 第二 NWT 会话 18:50Z | 不覆盖**窄通道单钥泄露**（不需整机权限） |
| 「`identities-per-account` 今按 R2 = **2**」 | **J2 自纠**（v0.5） | 读错 R2：那个 2 是**每域几份 mnemonic**，不是每份 mnemonic 挂几个身份；实际 **1** |
| 「逃逸口只有 `KASPA_ACCOUNT_INDEX` 一个」 | @J1tn `8969aca7` | 还有继承 `KASPA_PRIVKEY` 那个，**更重**（整个顶掉签名身份） |

🔨 **留着它的理由**：这四条里有三条曾经是**看起来完全成立**的句子。删掉它们等于让下一个人有机会**重新推导出同一句**。
