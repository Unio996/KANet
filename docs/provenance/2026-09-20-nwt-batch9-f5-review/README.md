> **Status**: CURRENT（2026-09-20，NWT；对象 = `origin/coord/j2-batch9-f5-scan-strip-comments-v0` 头 `92e1da1c`（父主线 `f027f947`）；J2 F5：共享扫描器的 `stripComments` 换成 D26-scan v2.1 的状态机版）

# 批 9 F5 笔审（共享源码扫描器 stripComments，NWT F4-1）—— NWT

方法：独立检出（`D:\kanet-nwt-cand`，`92e1da1c`）；**程序化**抽出两处函数全文逐字比较；亲跑扫描器自测与两个消费该扫描器的测试；用**真实探针文件**（走真实目录遍历，跑完即删、`git status` 空）复跑我 F4 审的全部形态并加新形态与"不误报"控制臂；6 个轻量变异。**本机内存 >80% 期间只跑单文件测试**（Bettor 通知），没有跑大批量变异，也没有重跑 J2 的 10 + 18 项变异。D-021：无密钥 / 余额 / 地址。

## 结论：**GREEN，无 MUST；1 条小 SHOULD。** 可以合主线。

Bettor 让我核的三点：
1. **与 `f72b0cdb` 那份逐字相同**：✅。我用脚本从 `f72b0cdb:kasia-console/src/lib/broadcaster-utxo.test.mjs` 与 `92e1da1c:…/scan-non-test-sources.mjs` 各抽出 `stripComments` 函数全文（含函数体，按花括号配平）：**2257 字符对 2257 字符，逐字相同**（换行归一后比较）。
2. **我 F4-1 的三种漏报形态现在全红**：✅ 字符串里含 `//`（E1）、字符串里含 `/*` 且行尾字符串含 `*/`（E2）、模板字面量里含 `//`（E3）——**全部被抓**。另外我这次新加的：正则字符类里的 `/*`（E5）、单引号字符串里的 `/* not a comment`（E6）**也被抓**；F2 / F4 时的旧形态（`src/data`、`kasia-console/scripts`、根 `*.mts`、正则里 `\/\/`）**仍红**。
3. **变异不空**：✅ 我的 6 个轻量变异（s1 正则识别整体关掉、s2 字符串不因其引号闭合、s3 行注释永不结束、s4 代码态转义不消耗、s5 块注释结束不回代码态、s6 未闭合单行字符串不在换行处重同步）**4 被抓（s1 / s3 / s4 / s5）、2 存活（s2、s6）**——存活的两个都在 **over-keep 方向**（见下）。（s3 / s4 第一轮我的替换锚点没匹配上、没有真跑，重写脚本后第二轮才生效；outputs 里是第二轮结果。）

亲跑：扫描器自测 **9/0**（J2 自报 6→9）、`proto-claim-draw` **57/0**、`proto-settlement-c1` **45/0**（这两个是消费该扫描器的测试）。

## 小 SHOULD
- **F5-1（小）over-keep 方向没有自测守着**。两个存活变异 s2（字符串状态永不因其引号闭合）与 s6（未闭合的单行字符串不在换行处重同步）的**后果只会是"真注释里的标识符被当成引用"（误报）**，不是漏报——所以不影响安全方向，J2 的变异也没有点名它们。但**误报会让扫描红在无辜代码上，逼人放宽规则**（这是这类钉住测试最常见的死法）。我的控制臂 K1 / K2（整行注释 / 块注释里的引用**不**被标记）通过，但这两个存活变异恰好说明自测里没有守 s2 / s6 各自行为的对照（我读自测的 9 项断言名得出，未逐条比对实现）。补两条对照：`const s = "a"; // <引用>` ⇒ **不**报（对应 s2）；一行含未闭合引号、下一行 `// <引用>` ⇒ **不**报（对应 s6）。
- **留给 9-2b 的（Bettor 已记）**：`kasia-relay/src/…/utxo-facts.test.mjs:73` 还有一份简单正则版 `stripComments`，扫描器要提到两包共用位置后统一——同意；我在握手 V9 实现审时会核它用的是这一份。

## 没做
- 没重跑 J2 自报的 10 项状态机变异与 F4 的 18 项探针 / 变异（内存 >80% 期间不跑大批量）；我用 §3 的真实探针文件覆盖了 F4 的核心形态。
- 状态机对"模板字面量嵌套两层以上""正则 / 除法的启发式边界（`)` 后的 `/`）"等仍是启发式，我没有穷尽；它们的失效方向已知偏向 over-keep（安全），沿用 D26-scan v2.1 的口径。
