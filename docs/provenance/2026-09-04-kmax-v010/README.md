> **v0.10（接线草案 · (24) 取数器 + `hVisUb` 透传；只加不改，默认仍参数注入；真 RPC 四闸（Codex 283）未实测前一切输出 PROVISIONAL、不喂 (23) 入场闸）**：① **`hVisUb({n, wCapWindow, t0Ms, t1Ms, sampledAtMs?, certificate?})`**：可选 `sampledAtMs`（`wCapWindow` 认证时刻）∉ `[t0Ms, t1Ms]` ⇒ `reason` 含 **`WINDOW_TIME_MISMATCH`** ⇒ null（(24) v0.2 闸4）；可选 `certificate.kind ∉ {GENESIS, TRUNCATION}` 或 `missing > 0` ⇒ `NO_W_CAP` ⇒ null（闸3，绝不用更小窗）；两参缺省 ⇒ 行为与 v0.8 **逐字相同**（返回对象不加字段，既有 36 向量逐位不变）。② **`hVisUbFromEvidence(ev)`**：读 (24) `fetchCover` 输出 `{wCapWindow, certificate, arrivalWindow{t0Ms,t1Ms}, n_arrivals?, evidence?}` ⇒ 透传（`sampledAtMs` 缺省 = `certificate.sampled_at_ms ?? t1Ms`）⇒ `hVisUb`，附 `provisional: true` + `evidence_gates{G1,G2,G3}`；`n_arrivals`（own-clock 窗内新可达块数，(21) 接收计**另落**）缺 ⇒ `NO_N`。③ **`main --wcap-evidence <fetch-evidence.json>`**：不连 RPC，只读 (24) 输出 ⇒ 打印 `{schema_version: kmax-hvis/1, …hVisUbFromEvidence}`；早返分支，既有 RPC 路径逐字不动。④ **(24) 取数器 `wcap-fetch.mjs`**（只读 RPC）：`findAnchor`（沿 `selectedParentHash` 回溯到 `bs ≤ bs_top − (36,000+26,440)`，记步数）/ `fetchForward`（`getBlocks(lowHash, includeBlocks, !includeTransactions)` 幂等续游标 = 返回 `blockHashes` 末项、`== sink` 停、空页/不前进即停不算完整、记每页 `{page, low, high, count, t_recv_ms, note}`）/ `closureCheck`（未返回引用诊断表，锚自身豁免）/ **`fetchCover`**：SYNC_GATE（`isSynced ∧ daa > DAA_FLOOR`，🔴 绝不连 IBD 活节点）→ 起锚 → 拉 → **先无 anchor 重建（无条件安全）→ G1（同 (R, sink) 二次拉取 hash 集相等 ∧ 两次翻齐）∧ S 内窗 `missing=0` ∧ complete 才传 `anchor`**（NWT 审 a93f9c5e affirming note）→ 不达标几何加深 ×2 到真 genesis 止 → 输出 `{wCapWindow, certificate, arrivalWindow{t0Ms,t1Ms}, evidence{G1,G2,G3,G4,pages}, attempts}`。G2 判据 = S 内窗 `certificate.missing == 0`（引用 `past(R)` 的合法缺席不算洞）。⑤ **`wcap-synth.mjs`**（测试专用）：合成 DAG 生成器 + replay 假 RPC（`getBlocks` = antipast(low)∩past(sink) 拓扑分页含 low、≤pageSize、末页附 sink anticone；故障注入 `dropPages / hideBlocks / isSynced`）。向量：`vectors.json` +4（`D3-hvis-passthrough-ok`（F1 replay certificate 作 fixture）/ `time-mismatch` / `cert-inexact` / `cert-missing-but-kind-ok`）= **40/40**；`vectors-fetch.json` **8 条**（F1 一页到 sink G1–G4 全过 + `hVisUb` 同窗出数 / F2 pageSize 7 续游标逐位同 / F3 丢页 ⇒ INEXACT / F4 IBD ⇒ SYNC_GATE 零取块 / F5 red 密 / F6 回溯到真 genesis ⇒ GENESIS / F7 sink-anticone tip / F8 节点缺成员 ⇒ 加深到 genesis 仍 INEXACT）= **8/8**。`fetch-evidence.fixture.json` = 驱动路径 fixture（F1 certificate + 合成 3600 s 窗 + `n_arrivals`）。 ⑥ **法 3′ 接收计 `arrivalCounter(rpc, {lowHash, clock, waitFn})`**（(23) v0.10+ MUST-B：按接收不按戳）：两次本机时钟轮询各自 sink、从同一 low 锚 `fetchForward` ⇒ `n_arrivals = |reachable(t1) ∖ reachable(t0)|`、`sum_work = Σ calcWork(bits)`；两次任一不完整 ⇒ `n=null + ARRIVAL_INCOMPLETE` ⇒ `hVisUbFromEvidence` `NO_N`；只报数不判（闸在 `hVisUb` W_MIN/N_MIN）；诊断列 `n_by_stamp_window_diag / old_stamp_arrivals`（按戳窗会漏掉的旧戳陈父块）。**`runCycle(rpc)`** = 接收计（t0→t1）+ t1 时刻 `fetchCover` 认证，**共享同一 `arrivalWindow` 对象**（闸4），`certificate.sampled_at_ms = t1`，输出并入 fetch-evidence（含 `n_arrivals`）⇒ `hVisUbFromEvidence` 直接出数（PROVISIONAL）。向量 `vectors-fetch.json` +4 = **12/12**：A1 60→68 链 ⇒ n=8；A2 60→66 + 2 侧块（s5 陈父块旧戳 5500 ms）⇒ 按接收 n=8、按戳对照 7、`old_stamp_arrivals=[s5]`（MUST-B 可见证据）；A3 第二次拉取中断 ⇒ `ARRIVAL_INCOMPLETE` ⇒ NO_N；A4 全周期 60→4060（n=4000 ≥ N_MIN）⇒ `same_window_object=true`、`sampled_at ∈ 窗`、`H_vis_ub` 出真数（gates G1–G3 true）。replay 假 RPC 加 `advance(newBlocks)`（模拟轮询间出块）。 ⑦ **(B) 逐窗零静默 skip（Codex 19284783 ⑤）**：`fetchCover` 证据 `G5_bits_selfcheck = {exact_windows, checked, mismatch, skipped_inexact, skipped_no_bits, pass}`——exact 窗 100% 核 `bitsCalc == 收块 bits`，非 exact 窗不核但**显式计数**，无 `bitsCalc` 的（真 genesis / 截断根 / genesis 子块）单列；`pass = mismatch==0 ∧ checked==exact_windows`。⑧ **真 RPC 跑手 `wcap-run.mjs`**（只读；SYNC-GATE 退出码 3；`--wait-s 3600 --sleep-ms 20`；输出 `runs/fetch-evidence-<UTC>.json` + sha256，摘要行含 G1–G3 + B 五计数 + `H_vis_ub`（PROVISIONAL））= (17) 清单 ③f，MSG-284 五条真 RPC 证据来源。 ⑨ **鲁棒性（NWT 预审 ④⑤⑥）**：`fetchForward` 每页 `getBlocks` **指数退避重试 3 次**（250/500/1000 ms）+ **续游标 2 次**（重试耗尽后从同一 `low` 再来，不重头 251 页；`evidence.retries_total / resumed_from`），全部耗尽 ⇒ 该页 `FAILED_AFTER_RETRIES` ⇒ `complete=false` ⇒ G1 fail ⇒ INEXACT（fail-closed）；跑手 `--sleep-ms` 默认 50 且**自适应** `max(sleep, 上次 getBlocks 延迟×0.5)`；`evidence.vantage = single-node-da9`（Codex f7bc9057：单节点观测点非独立多点）；**G4.pass = 同一 `arrivalWindow` 对象 ∧ `sampled_at_ms ∈ [t0,t1]`**（只有 `runCycle` 注入同窗才 true；单独 `fetchCover` 为 false）；跑手启动打印 ③f 前置提醒（③d 3600 s 跑完 ∧ ③b 无新 err，由派工闸保证）。`hVisUbFromEvidence.evidence_gates` 含 G1–G5 + vantage。向量 +3 = **15/15**：R1 第 2 页前 2 次 TRANSIENT ⇒ 重试成功、与 F2 逐位同（retries=2）；R2 前 5 次 ⇒ 续游标 1 次 ⇒ 与 F2 逐位同；R3 前 20 次 ⇒ 重试+续全耗尽 ⇒ `FAILED_AFTER_RETRIES` ⇒ INEXACT w=null。replay RPC 加 `failOnLow{low: times}` 故障注入。

| 文件 | 作用 |
|---|---|
| `wcap-fetch.mjs` | (24) 取数器（`findAnchor / fetchForward / closureCheck / fetchCover / arrivalCounter / runCycle`；四闸证据 + 接收计）|
| `wcap-fetch.test.mjs` / `vectors-fetch.json` / `expected-output-fetch.json` | replay 离线测试 / 15 向量（F1–F8 取数 + A1–A4 接收计 + R1–R3 鲁棒性）/ 期望 |
| `wcap-synth.testonly.mjs` | 测试专用：生成器 + replay 假 RPC（生产禁用）|
| `wcap-run.mjs` | 真 RPC 跑手（只读；SYNC-GATE；runCycle ⇒ fetch-evidence + G1–G5）|

```bash
node docs/provenance/2026-09-04-kmax-v010/wcap-fetch.test.mjs                                   # 期望 15/15 PASS(replay, 不连节点)
node docs/provenance/2026-09-04-kmax-v010/kmax-cost.mjs --wcap-evidence <fetch-evidence.json>    # 透传出 H_vis_ub(PROVISIONAL; 四闸未实测不喂入场闸)
# 真 RPC 四闸实测(步 0 后, Bettor 派): fetchCover(rpc) ⇒ fetch-evidence.json ⇒ 上式
```

> **v0.10.1（2026-09-04 · NWT GREEN · Bettor 批入库）**：本目录 = (21)/(24) v0.10 自含包（14 文件），从 `scratch/_j2_kmax_v010_staging/` 入库；历史（v0.5–v0.9.2）见 `docs/provenance/2026-08-27-kmax/README.md`（原地不动）。相对 v0.10 staging 的改动仅三处：`wcap-synth.mjs` → `wcap-synth.testonly.mjs`（名字即边界，生产路径 import 它 = lint 红）；`wcap-run.mjs` `--out` 必传（缺 ⇒ 退出码 1 打 usage，证据不得静默落 gitignored 目录）+ 头注释与代码对齐（`--sleep-ms` 默认 50 自适应）+ 仓根由 `import.meta.url` 向上找 `kasia-console/package.json` 解析（不写死盘符；若本目录被复制进某个 worktree，会解析到那个 worktree 自己的根——自洽）。已知非阻塞：`wcap-run.mjs` 的「③f 前置提醒」先于 usage 打印。
> 跑法：`cd /d/kanet-tn12/kasia-console && node ../docs/provenance/2026-09-04-kmax-v010/wcap-run.mjs --out D:/kanet-tn12/docs/provenance/2026-09-04-kmax-v010/runs [--wait-s 3600] [--sleep-ms 50]`（只读；SYNC-GATE 退出码 3；输出 PROVISIONAL，四闸未实测不喂 (23) 入场闸）。
