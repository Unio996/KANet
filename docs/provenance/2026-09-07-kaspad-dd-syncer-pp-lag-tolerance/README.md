# kaspad D-d — configurable IBD syncer pruning-point lag tolerance (`--ibd-syncer-pp-lag-tolerance`)

> **Status**: CURRENT (2026-09-07)

- **Commit**: `3d017b6d8d593ed58d558b69c4374a174e819f79` on branch `j2-dc-self-trigger-ibd` (= D-c `a39c60d2` + D-d `c8820392` + NWT review fixes MUST-1/SHOULD-1). Base lineage: `4d0a9e30` (D-b) ← `1b3046fb` (D-a) ← `7b1e18cc`.
- **Artifact**: `D:\kaspad-live\dc-3d017b6d\kaspad.exe` · sha256 `6d5bcebebd528862d6adbc28ba7baab6f21ea74c373325fef0e208fbd1d5106f` · 40,629,248 B · embedded `3d017b6d` ×3 (full hash ×1) · `--help` shows the three D-c flags + `--ibd-syncer-pp-lag-tolerance`. `--version` prints `kaspad 1.1.1-toc.1` without hash (same as D-b/D-c); liveness is judged by the first log line `kaspad v1.1.1-toc.1-3d017b6d`.
- **Build**: real clone `D:\rusty-kaspa-dc2` on a *branch* HEAD, `build.sh` (`-j 2`, `CARGO_TARGET_DIR=D:\rusty-kaspa-dc\target-dc`), attempt 4 = `build.log`. Attempts 1–3 are kept as evidence of three separate traps: source checked out mid-build (killed), detached HEAD (utils/build.rs embeds no hash), stale build-script cache (empty output carries no rerun-if-changed → `touch utils/build.rs`).
- **Incident**: 2026-09-07T00:57Z — our pruning point jumped index 60→69 in one step; the only syncer in our future (136.243.93.17) has a pruning point stalled at our index 41 ⇒ upstream `get_n_last_pruning_points(4)` rejected it every IBD round ("could not be easily recognized"), ~60 s disconnect loop, sink frozen.
- **Deployed value: `--ibd-syncer-pp-lag-tolerance=0` (unlimited).** 16 was tried first and was **not enough**: the true gap is **28 pruning-point indices** — MUST-1 line at 03:03:20Z `table position none (window 16)` (rejected), then with 0 at 03:05:54Z `lag 28 pruning-point indices (tolerance flag = 0)` (accepted, `syncing ahead from current pruning point`). A bounded value (e.g. 32) is deferred to a future planned window; not worth an extra restart. Default in the binary stays 4 (upstream semantics).
- **Review**: NWT `docs/2026-09-07-NWT-redteam-dd-syncer-pp-lag-tolerance-review-v0.1.md` (v0.1 → v0.3, code + artifact GREEN-final). Design/hunks page: `scratch/_j2_dd_design_and_hunks_2026-09-07T01-15Z.md`. Runbook: `scratch/_j2_Dc_switch_admin_runbook_PREPARED_2026-09-06.md` (D-d overlay section).
- **Known issues (observed live, not fixed in this artifact)**:
  - D-c `IBD self-trigger: peer … not eligible until it completes a first IBD with us (lag Ns)` is emitted once per non-syncer peer **after every completed IBD round** (idle check resumes), not once per process: 07:04:40/04:55/05:20Z and again 07:23:49/23:52/24:02Z for the same three churn peers (≈20 lines/h at the ≈9 min cycle; not per reconnect — 5 lines vs 181 reconnects in 20 min). Tolerable. SHOULD (upstream PR draft): dedupe by peer key for the process lifetime, or only log when lag ≥ threshold.
  - Step ② live cadence for reference: self-trigger #1 07:10:43Z → 07:14:35Z (3 m 52 s), #2 07:19:47Z → 07:23:45Z (3 m 58 s, 4,739 blocks); interval 544 s (band 525–975 s).
- **Files**: `patch.diff` (= `git diff a39c60d2 3d017b6d`, sha256 prefix `0efc624ecf1e8eeb`), `COMMIT.txt`, `build.sh`, `build.log`, `build-attempt{1,2,3}-*.log`, `MANIFEST.sha256` (includes the exe).
