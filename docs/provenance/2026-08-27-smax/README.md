# provenance · s_max 提取（(23) 算力地板规格 §3.5 · (24) 脚本 `scratch/_j2_smax_coinbase.mjs`）

正式输出 `smax-<UTC>.json` 由脚本在 **SYNC-GATE 过后** 写入（`daa > 80,095,687 ∧ isSynced`），每份带 `tipHash / window_s / coverage / blocks_fetched / s_max / top / control_output_addr / poisson` 与打印的 sha256。方法与偏移表见 `docs/2026-08-27-j2-s63-smax-extractor-v0.1.md`。

2026-08-27：目录建立时节点仍 IBD，**无正式输出**；dry-run（只取到 genesis 一块）不落此目录。
