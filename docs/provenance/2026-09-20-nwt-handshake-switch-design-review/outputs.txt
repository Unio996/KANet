# NWT 握手开关设计审(52f6133d) —— 读码/调用图证据(只读; 未改任何文件、未碰任何进程)。独立检出 D:\kanet-nwt-cand @ 52f6133d。

## 1. relay 里 acceptHandshake 的全部调用点(grep -rnE "acceptHandshake" kasia-relay/src | 非测试)
chain.mjs:136            export async function acceptHandshake(params)          [定义]
relay.mjs:4              import { ..., acceptHandshake, sendKaspa, ... } from "./chain.mjs"
relay.mjs:256            const draft = await acceptHandshake({ address: peer });       <- doAcceptHandshake(peer)   [设计稿未覆盖]
rpc-listener.mjs:582     const draft = await acceptHandshake({ address: hs.remoteAddress });    <- catchUpHistory 第 1 段  [落点 3]
rpc-listener.mjs:986     const draft = await acceptHandshake({ address: senderAddress });        <- processHandshake step 5 之后 [落点 2]

## 2. doAcceptHandshake 何时会被调用(relay.mjs)
:10   const RELAY_MODE = process.env.RELAY_MODE || "indexer";
:285-286  if (conv.status === "pending_incoming") { await doAcceptHandshake(conv.contactAddress); }     (在 poll() 内)
:300-311  if (RELAY_MODE === "rpc") { startRpcListener().catch(err => { log("Falling back to indexer mode..."); setInterval(poll, POLL_MS); }); } else { setInterval(poll, POLL_MS); }
relay-manager.js:224   const relayMode = await getConfig('relay_mode') || process.env.RELAY_MODE || 'rpc';    (console 拉起 relay 时传入)
=> poll() 在两种情形下运行: rpc 监听器启动失败的回落; config_entries.relay_mode = 'indexer'。

## 3. pending 行的创建与消费(console)
services/ingest-service.js:86-140  inbound handshake 的 ingestMessage: 关系已 accepted/confirmed/active => 跳过; 旧行 failed/expired => 重置为 pending; 无行 => INSERT handshake_accept/pending (source='ingest')
api/ingest.js:252-296              create_and_claim: 已 active => {claimed:false}; INSERT OR IGNORE pending_actions; 随即 claimPendingAction()
消费者(全仓 grep getPendingHandshakes|claimPendingAction|create_and_claim|pending-handshakes, 非测试):
   getPendingHandshakes 仅被 api/ingest.js:300 (/ingest/pending-handshakes) 调用; 该路由的调用方 = relay 追赶第 1 段(rpc-listener.mjs:553/567) 与 relay 实时 step 5(:960/966) 与 agent-mind/action-executor.mjs:651(出站 handshake_init, 由 autoHandshake 默认关另闸)
过期: 除 failPendingAction(执行失败超过 max_retries => 'expired', catchup-service.js:62-72) 外, 全仓没有按时间清理/过期 pending 行的代码。
getPendingHandshakes: ORDER BY created_at ASC LIMIT 100 (catchup-service.js:10-32)。

## 4. 汇总行与既有先例
rpc-listener.mjs:725 (注释) "…(它坏着的时候打的也是 0 —— 19551 次 catch-up done 全是 0/0/0)"
rpc-listener.mjs:659-670  catchupCommEnabled = KANET_CATCHUP_COMM === 'on'; 关闭态 log('catch-up comm: DISABLED (KANET_CATCHUP_COMM!=on) — historical backfill skipped by design, not fixed'); 汇总行 :728 写 DISABLED 而非 0
grep "catch-up done|catch-up handshakes" 于 kasia-console/src、scripts、kaspa-scout: 无程序消费(仅一个旧脚本样例)。
