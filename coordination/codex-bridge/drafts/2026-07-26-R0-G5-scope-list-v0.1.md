# R0-G5-CLOSEOUT — 不可变范围清单 v0.1

| 字段 | 内容 |
|---|---|
| `batch` | `R0-G5-CLOSEOUT` |
| DRI | J2 |
| 方向/调度 | Bettor |
| 红队 | NWT |
| 独立复审 | Codex |
| 卡权威原文 | bridge `c45acd37` v1.2 §7（R0-1 / R0-2 / R0-3） |
| 活动基线 | pre-fix WIP `557554fd5ba8f4ba110b016b273f596c6cfbe121` |
| 本文用途 | 把范围从 commit message 里搬出来固定下来（Codex 要求：**不许让范围只活在 commit message 里**） |

---

## ① 源 commit

```
0e184eb0033bb56125d7798ff066804ea39b3385a
标题: WIP(m0c1-gate): G5 v2 B1-B6 落码(1-12条修复进行中·未完成·落袋保存)
```

该 commit message 自述：**「NWT/KANet-UI 深审攻出的 1-12 条修复**尚未开始改**。」**

**Codex 裁定（Bettor 03:43 转，逐字）**：R0 十二项 = 该 commit message 里编号 1–12 的那份。
依据：v1.2 把活基线钉在配对的 WIP `557554fd`，而"已公开列出的十二项"指的就是那个声明它们未完成的 WIP commit 所附的编号清单；v1.2 里不存在另一份十二项。

---

## ② 每项编号 + 原文逐字

> 以下整块**逐字**取自 `git log -1 --format='%b' 0e184eb0`，未改写、未重排、未补标点。

```
待修(NWT+KANet-UI 深审 1-12 条·全是"静默容错"同一病根):
1-7 B2: scope 路径缺失/symlink-junction 必须 throw(现静默 continue/跟进)·scope 清单
进响应体+烤进 treeDigest·fileCount 进响应+G5 比对·对应 negative 测试
8 预算门静默失效: amount_kas 非法/NaN 让整本账变 NaN·预算门恒 false(NWT 实测
4.8+'oops'+0.1=NaN·NaN>5 为 false·已花的 4.8 从账上消失)——逐条 throw
9 tmp 孤儿分支 continue 跳过 state 白名单校验(主路径严旁路松)——校验提到分支之前
10 health.js 外层 catch{} 加 console.error(不改 fail-closed·只让运维能查)
11 evidence-file symlink 检查(按 P1-2 同款 lstatSync 口径)
12 reconcile list/evidence 逐条容错(属排障路径·坏 journal 不该整个崩)
```

### 🔴🔴 而"逐项"这个要求在 1–7 上**做不到** —— 报出来，不自行发明

**R0-2 第 2 项要求「十二项修复逐项映射到 file/blob/test」。而源清单的结构是：**

```
编号 1-7  ⇒ 【合并写成一行】, 未逐条拆开
编号 8    ⇒ 独立一条
编号 9    ⇒ 独立一条
编号 10   ⇒ 独立一条
编号 11   ⇒ 独立一条
编号 12   ⇒ 独立一条
```

🔴 **而 `1-7` 那一行里用 `·` 分隔的子句只有【四个】**：

1. `scope 路径缺失/symlink-junction 必须 throw(现静默 continue/跟进)`
2. `scope 清单进响应体+烤进 treeDigest`
3. `fileCount 进响应+G5 比对`
4. `对应 negative 测试`

⇒ **七个编号，四个子句。二者对不上，而源文本没有给出 1/2/3/4/5/6/7 各自是哪一条。**

🔨 **⇒ 处置：我【不】把它拆成七条。**
理由：拆分等于**我来定义编号 1–7 各自是什么** —— 那是范围定义，不是范围转录。而 Codex 的裁定授权的是"照这份清单做"，不是"由 J2 补全这份清单"。

⚠️ **⇒ 这一格【开着】，需要一句裁定**：
- 要么由 Codex/NWT/KANet-UI（原始提出这 1–12 条的人）给出 1–7 的逐条边界；
- 要么把 `1-7` 整体当作**一个范围单元**处理（那样"十二项"的实际可操作单元是 **6 个**：`1-7` + `8` + `9` + `10` + `11` + `12`）。

🔵 **在此之前，本清单按 6 个单元列，而编号仍写作源文本的 `1-7 / 8 / 9 / 10 / 11 / 12`，不重编号。**

---

## ③ 预期文件 / 测试

⚠️ **本节全部标【推论】** —— 源清单只给了症状描述与文件名片段，没有给 blob/test 的绑定。下表是我按描述**推**出的目标，**未逐个实读确认**。

| 单元 | 原文关键指认 | 预期文件（推论） | 预期测试（推论） | 当前状态 |
|---|---|---|---|---|
| `1-7` | `B2` · `scope` · `treeDigest` · `fileCount` · `G5 比对` | `kasia-console/src/lib/runtime-scope-dirs.mjs` · `kasia-console/src/lib/load-bearing-digest.mjs` · `kasia-console/src/api/health.js` | 需新增 negative 测试（源文本明写「对应 negative 测试」是这一单元的一部分） | **未开始**（源自述） |
| `8` | `预算门` · `amount_kas` · `NaN` | 🔴 **未定位** —— 源文本未给文件名 | 需新增（逐条 throw 的负测试） | **未开始**（源自述） |
| `9` | `tmp 孤儿分支` · `state 白名单校验` | 🔴 **未定位** —— 源文本未给文件名 | 需新增 | **未开始**（源自述） |
| `10` | `health.js 外层 catch{}` | `kasia-console/src/api/health.js`（源文本直接点名） | 无（只加日志，不改行为） | **未开始**（源自述） |
| `11` | `evidence-file symlink 检查` · `lstatSync` | 🔴 **未定位** —— 源文本未给文件名 | 需新增 | **未开始**（源自述） |
| `12` | `reconcile list/evidence` | 🔴 **未定位** —— 源文本未给文件名 | 需新增 | **未开始**（源自述） |

🔴 **六个单元里有四个我定位不到文件。** 这不是"我没找"，是**源清单在那四条上只给了功能描述**。
🔨 **⇒ 定位它们是 R0 的第一步实际工作**，而它属于「为证明某一项而加的澄清」（Codex 明许），不属于「夹带」。

### ⚠️ 状态列全为「未开始」的依据

**唯一依据是源 commit message 自述**：「1-12 条修复**尚未开始改**」。
🔴 **标【转述】** —— 我**没有**逐项去代码里核实"它是否真的未被改过"。
⇒ 若其中某项此后被别人改过，本表会把它错报成"未开始"。**核实它属于 R0 的下一步，不属于本清单。**

---

## ④ 明令禁止第十三项功能扩张

```
🔴 本清单所列 6 个单元（源编号 1-7 / 8 / 9 / 10 / 11 / 12）之外的任何改动，一律不做。
🔴 不许夹带新能力。
🔴 不许无关清理。
✅ 允许：为证明某一项而加的澄清与测试。
```

**并继承 v1.2 §7 R0-1「禁止范围」逐字**：
> 不得新增能力、不准 G5 POST、不准重启、不准 re-arm、不准 grant、不准 reconcile release、不准签名/广播/资金动作。

**并继承 §16.3**：R0 不得顺手实现 B0-O3 / O4 / O5。
**并继承 §16.1**：`BLOCKED_DO_NOT_RUN_G5` 仍在 force —— **R0 关闭只表示"G5 原型变成可审对象"，不表示允许运行**（§7 R0-3）。

---

## ⑤ NWT 审查引用

| 项 | 内容 |
|---|---|
| 红队 | NWT（v1.2 §7 R0-1 指定） |
| 红队顺位 | 第 4（§16.2「NWT 依次审」清单） |
| 本清单送审状态 | v0.1，**未送审** |
| 已知将被压的点 | 本文 ②「1–7 拆不开」那一格；③ 四个未定位文件；状态列的【转述】依据 |

**并适用今晚立起的交付纪律**（皆由 NWT 红队或 Bettor 裁定产生）：
1. 数子句 / 找量词，量词要给总判；
2. 限定必须在**结论位自带**，不靠交叉引用；
3. 证据四级标注：【实读】【实跑】【推论】【转述】；
4. 子句映射表 —— **数目相等不构成覆盖**；
5. 凡在交付物里点名某类缺陷，须在同一份产物内 grep 一遍该类并写出命中数（含 0）。

---

## ⑥ 自查：本文点名过的缺陷类型，在本文内的命中数

> 依据 Bettor 22:12 立的机械要求 + J2 22:13 的"学到时当场回扫"。

| 我在本文点名的类型 | 本文内 grep 结果 |
|---|---|
| 「限定与结论分居」 | **命中 0** —— ②「拆不开」与 ③「四个未定位」的限定都写在该格结论位内，未挪到别节 |
| 「量词未给总判」 | **命中 0** —— ③ 表格后已给总判（「六个单元里有四个我定位不到文件」） |
| 「拿描述当内容」 | **命中 1** 🔴 —— ③ 整节是从**描述**推目标文件，已在节首标【推论】且逐格标"未定位"，未升格为结论 |
| 「数目相等掩盖缺失」 | **命中 1** 🔴 —— 正是 ② 那一格：「十二项」这个数与实际可操作单元数（6）不等，而若不数就会照"十二"去找十二个东西 |

---

## ⑦ 覆盖边界

```
✅【实读】源 commit 0e184eb0 的 commit message（git log 直读，未经转述）
✅【实读】v1.2 §7 / §16.1 / §16.2 / §16.3（git show c45acd37 直读）
🔴【转述】Codex 关于"十二项出处"的裁定 —— 来源 Bettor 03:43/03:44 频道转述，我未见 Codex 原文
🔴【推论】③ 全表的文件/测试映射
🔴【未做】未逐项到代码里核实"是否真的未开始改"
🔴【未做】四个未定位单元的文件定位
🔴【开着】1–7 的逐条边界，需要裁定（见 ②）
```

**本清单产出过程零写入主仓**（`041e2421` 未动）**· 未碰 live · 未跑 G5 任何部分 · 未构造/签名/广播。**

---

**本文件是 R0 范围的**本体**。此后任何"这算不算 R0 范围内"的争议，以本文为准；本文与频道分段不一致时，以本文为准。**
