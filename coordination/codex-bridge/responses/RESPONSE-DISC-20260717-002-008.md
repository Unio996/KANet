# RESPONSE-DISC-20260717-002-008

- created_at_utc: 2026-07-18T12:58:00Z
- from: Codex / external architecture reviewer
- to: Bettor, J1tn, NWT and settlement/voter maintainers
- task: KANET-JEPU1-STALE-SIG-RECOVERY-001
- discussion: DISC-20260717-002
- type: code-review-result
- reply_to: RESPONSE-DISC-20260717-002-007
- related: commits `d060e8722c25e912f8e273a88c8e71d6652c7cc7`, `b862c6e0369d72981fe08280ad34e061dee4007e`; `docs/2026-07-18-jepu1-surgery-order.md`

## Verified code-level findings

1. `d060e872` correctly removes the known cross-node plain-JSON signing path in `trade-protocol-filter.js::handlePoolOracleTxSignReq`: it serializes through the shared `toSettleSafeJsonTxHex` helper and sets `safe_json:true`. The helper extraction also removes the previous local copy from `bettor-prediction-voter.js` without changing its body.
2. `b862c6e0` adds a repository-wide producer scan plus safe-json round-trip behavior test and, importantly, the first run found and fixed an additional live plain-JSON site in `processPoolRefundDisagreementTxSign`. This is strong evidence that the machine clamp adds real value rather than merely documenting the manual enumeration.
3. The proposed soft-invalidation is safer and more auditable than physical deletion: the original rows remain in-place, the collector/voter dedup keys are released by changing `event_type`, and rollback is explicit.
4. Gate-A is valid: committed code is not evidence that the canonical process is running it. Restart/load verification must precede re-signing.
5. Gate-B's zero-orphan conclusion is relevant because a surviving `sign_resp` re-ingest path could otherwise recreate stale signatures after soft-invalidation.

## Assessment against MSG-008 acceptance conditions

- **Condition 1: satisfied for the known production sites relevant to jepu1.** The third path and the newly discovered refund-disagreement path now use the shared serializer.
- **Condition 2: substantially satisfied, with one limitation.** The test scans the tree and caught a missed site. However, its positive check is partly file-level (`usesHelper || hasSafeFlag`) rather than proving every call site in a multi-call-site file binds its own `tx_hex` to the helper result and its own command to `safe_json:true`. The local ±300-character raw-JSON check reduces risk but is not a full AST/call-site proof. Keep this as a follow-up hardening item; it does not block the narrowly scoped jepu1 recovery because the relevant diffs were inspected directly.
- **Condition 3: not yet fully recorded in the repository bridge.** The surgery order uses a broad `payload LIKE '%jepu1%'` placeholder and records only ID prefixes in the bridge. Before mutation, the immutable audit/approval record must contain the exact full market id, the settle tx identity (`f9e64afc...`) and/or canonical sighash identity, all five full row IDs, all five full signer pubkeys, input index, timestamps and payload hashes. The actual UPDATE may use the five exact full IDs, but the precondition query and audit record must prove those IDs belong to this exact tx/sighash and market, not merely contain the substring `jepu1`.
- **Condition 4: direction accepted but pending execution artifact.** Soft-invalidation preserves the canonical evidence, but the promised pre-surgery snapshot with full payload hashes must be committed or otherwise immutably receipted before the UPDATE.
- **Condition 5: pending by design.** No settle broadcast should occur until all five replacement signatures independently verify against the canonical safe-json-derived sighash.
- **Condition 6: pending.** NWT review is recorded; Owner/delegated 188 KAS money-path authorization is still absent.

## Required next action

1. Do not execute yet.
2. Complete a repository-visible pre-surgery manifest containing the exact full selector identity and hashes described above; avoid relying on external-channel messages as the only source for full IDs.
3. After Owner/delegated authorization, restart and prove the canonical console loaded `d060e872` + `b862c6e0`, then execute the soft-invalidation transaction with `changes()==5` or abort.
4. Record the before/after DB receipts and independently verified replacement signatures before settlement submission.
5. Track an AST- or call-site-level strengthening of `settle-safe-json.test.mjs` so a safe flag elsewhere in the same file cannot mask a future raw producer.

This review does **not** authorize restart, database mutation, re-signing, rebroadcast, settlement or movement of the 188 KAS path.