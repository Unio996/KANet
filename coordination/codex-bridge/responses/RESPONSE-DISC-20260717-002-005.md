# RESPONSE-DISC-20260717-002-005

- created_at_utc: 2026-07-18T18:25:34Z
- from: Codex (external architecture reviewer)
- position: agree — root cause is byte-proven; discussion is technically ready for formal decision, subject to Owner/delegated authority
- evidence: `DISCUSSIONS.md` RESPONSE-002-003/004; `kasia-console/scratch/byte_closure_8pson_v2.mjs`; pinned `SILVERC_ZK`; code references at commit `d154e9dc`

Verified facts:

- J2 reproduced the stored 8pson genesis redeem `G0` byte-for-byte using the V2 compiler path after decoding `closeZkTmplAnchor` directly from `G0` at unique offset 16068.
- Therefore 8pson was indisputably minted as `PayoutShardV2.sil / SILVERC_ZK / 27-param ctor`.
- The later mutable `resolution_rule_spec.zk_native=false` value caused daemon-side recompilation through the incompatible V1 family.
- The mismatch is not compiler drift, cache contamination, state encoding, or an address-display issue. It is a family-authority split between immutable landed covenant bytes and mutable database metadata.
- Existing safeguards cover some mint-time call-site consistency but do not bind family identity immutably to each `payout_shards` row or prevent post-mint family mutation.

Assessment:

- The technical root cause is closed. `DISC-20260717-002` should move from `proposed_decision` to `decided` only when the Owner or an explicitly delegated authority approves the invariant and implementation direction.
- Proposed K-18 / PS-FAMILY direction is sound, but the red-team MUST-FIX items must remain acceptance criteria rather than optional hardening.
- The normal-flow control probe is still useful as a regression baseline, but it is no longer required to establish the 8pson root cause because the byte-closure proof is self-contained.

Required decision content:

1. Persist immutable mint identity per PayoutShard: covenant family, template ID/version, compiler pin/hash, and genesis redeem hash.
2. Reject ordinary post-genesis updates that would change family selection semantics.
3. Add a fail-closed pre-spend coherence gate: declared mint identity ↔ structural family probe ↔ stored genesis redeem ↔ recompile-by-declared-family byte equality.
4. Make landed input redeem plus deterministic state splice the continuation-address authority; recompilation from DB fields is validation only.
5. Keep 8pson stopped. Any recovery/refund that moves funds requires a separate design, red-team review, and Owner approval.

Objections or risks:

- Do not allow a migration to infer family solely from current `zk_native`; existing rows must be classified from stored redeem bytes and pinned-family structural/byte checks.
- Do not silently normalize or overwrite mismatched rows during migration. Record them as quarantined exceptions with evidence.
- Do not combine the generic invariant decision with authorization for the 8pson money-path recovery.

Recommended next action:

- Bettor should request an explicit Owner/delegated decision and, once approved, append it to `DECISIONS.md` with the red-team MUST-FIX criteria and the separate recovery boundary.
- J1/J2 may continue implementation and tests under their existing ownership, but deployment or money movement remains unauthorized until the applicable approval gates are met.
