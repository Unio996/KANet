# Codex review — Oracle simnet green-light status

Canonical bridge baseline checked first: `1c650e809eaa6ef152b71c4c4ca4299ef12dcd0c`; compare against `coord/codex-bridge` was identical (0 commits, 0 changed files). Five bridge blobs were re-read from Git, not inferred from timestamps: TO-CODEX `01b94acecb3b364501a3bd524b45a16c6718b2fe`; DISCUSSIONS `313bb29aabc3fe906c721beb528735400de2969c`; STATUS `c4be60e4c4380e1401f2f718d17d94dc19ff7809`; DECISIONS `895334928a0ff58c1b9ca795ea3a27d328005fa4`; FROM-CODEX `0023782bbe6f0fa649100ac726f1c4fbadd3e769`.

Because the bridge had no delta, I compared the directly related active branch from prior checkpoint `e032b7ff849b6d8273efde39d7763c4bf83aa4d8` to current `bshard-m3-deploy` HEAD `8d2289addacc33001e2fc38d7e062a9f6d99a6d3`: 28 commits ahead, 0 behind. The actual changed-file set is overwhelmingly Telegram/read-only-shell/capability work and is not collaboration feedback for the Oracle/settlement review. I did not treat those unrelated commits as such.

One directly relevant status commit exists in that range: `d5b803f910084d26db6b07c45ce84b944d914c5c`, ledger entry 1611, recording Owner green-light for J2 to run the Oracle simnet E2E from HEAD `c2352d9182f1b171dd8183bba48f1332a56af18b` with the agreed environment/§8 contract. This is authorization to execute the simnet test, not evidence that the test ran or passed. The active-branch file diff since the prior checkpoint contains no Oracle/settlement implementation or test artifact changes; therefore I do not upgrade any Oracle gate from this commit alone.

Codex judgment: the simnet run may proceed under the already bounded non-production scope. Acceptance still requires actual immutable evidence for both the happy path and the adverse freeze arm previously requested: judged-market create → derive → verdict → full configured grace → promote → close → convert → claim, plus `close selected/prepared → durable freeze → first send/replay = zero broadcast`, followed by the refund terminal path. A green-light ledger entry cannot substitute for those receipts.

Open MUSTs remain unchanged: (1) prepared-but-not-yet-broadcast close must be blocked by a later durable freeze at the final send/replay boundary without rebuilding/re-signing; (2) full configured `GRACE_MS + CLOSE_PIPELINE_MARGIN` must fit, otherwise freeze/refund rather than runtime grace compression; (3) same-tick settlement fee-outpoint reservation/exclusion remains required for multi-ready concurrency. No production funds-path authorization follows from this review.
