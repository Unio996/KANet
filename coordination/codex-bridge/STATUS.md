# Codex ↔ KANet coordination status

Last updated: 2026-07-19 (Git/blob-grounded; document timestamps are non-authoritative)

| Item | Owner | State | Current step | Next action | Blocker | Related |
|---|---|---|---|---|---|---|
| `KANET-CODEX-BOOTSTRAP-001` | Bettor (coordination) | `stood_down` | Owner decided to keep GitHub file coordination and not deploy the host Gateway/MCP runtime | None; retain PR #3 only as historical assessment | None | `DEC-20260717-001`, PR #3 |
| `DISC-20260717-001` / `KANET-ARCH-PRIORITY-001` | J2 (Gate 0 owner); Bettor (landing verification); Codex (review); Owner/delegate (Batch 1/2 authority) | `gate0_delivered_kanet_verified_codex_artifact_pending` (DEC-20260719-001) | J2/Bettor/NWT report all 3 Gate 0 artifacts delivered and internally verified. Reported measured blast radius is **15 stranded markets / ~1679 bets / 67192.52 KAS**, replacing the prior unmeasured estimate. Reported falsification of j34vb/9jaty rescue shows endBlock reachability is necessary but not sufficient because bet-level accepting-block evidence was pruned and `side_lock_daa` failed loud. Codex accepts the delivery receipt and architecture implication, but cannot independently inspect the cited artifact because abbreviated commit `5f07f43c` does not resolve and the report is not accessible on the visible repository refs | Bettor provided the accessible closure artifact: full report copied byte-identical to `coordination/codex-bridge/evidence/2026-07-19-gate0-report.md` with a provenance header (full SHA `5f07f43c6c25f9e6bbbe0c5f771d7f8b38565d9f`, source branch `bshard-m3-deploy` [not on origin, hence the copy], run source commit `6da0f162…`, raw-set sha256, per-market table for arithmetic recompute). Codex to independently verify arithmetic/dedup/restore from the evidence copy. Refund execution and Batch 1/2 remain separately gated | Codex independent verification now unblocked (artifact accessible on `coord/codex-bridge`). Refund broadcast, production code/DB migration and money-path actions remain unauthorized | `DEC-20260719-001`; `coordination/codex-bridge/evidence/2026-07-19-gate0-report.md`; report SHA `5f07f43c6c25f9e6bbbe0c5f771d7f8b38565d9f`; raw-set sha256 `99d9f10635a2823479ff4029c5c6d1d77d7ca8da83a136083739d2793c6748d9`; `responses/RESPONSE-DISC-20260717-001-005.md` |
| `DISC-20260717-002` / `KANET-COVENANT-DERIVATION-001` | Bettor (discussion owner); J2 + J1tn (SS evidence); Codex (external assessment) | `decided` (DEC-20260718-001) | Byte-closure proof reproduced 8pson genesis redeem exactly through V2. Root cause confirmed: mutable `zk_native` flag diverged from immutable landed V2 bytes → daemon V1 recompilation. **Owner approved (2026-07-18 "采用+ABC"): adopt PS-FAMILY/K-18 invariant + accept red-team 3 MUST-FIX + 8pson → fail-closed refund** | J1+J2 implement the K-18 coherence gate (4 components, 3 MUST-FIX as acceptance); 8pson refund goes through separate design→red-team→Owner money-path approval | No 8pson money movement until its refund is separately approved; K-18 gate implementation in progress | `DISCUSSIONS.md` RESPONSE-002-003/004; `responses/RESPONSE-DISC-20260717-002-005.md`; `responses/RESPONSE-DEC-20260718-001-CODEX-ACK.md`; commit `2976d95d74249a9ebed71748b139d7fbe40203e6`; tx `a7d67850...` |
| `KANET-JEPU1-STALE-SIG-RECOVERY-001` | Bettor (gate); J1tn (patch/design); NWT (red-team); Owner/delegate (money-path authority) | `resigned_but_node_rejects` | Restart, five-row soft-invalidation and five-signature recollection were executed and verified against the locally derived safe-json sighash. The node still rejects settle tx `f9e64afc...`; no 188 KAS moved. Codex review confirms the local oracle proves hand-written preimage == local wasm signing path, not equivalence to the exact wire transaction and running node runtime | Capture a fail-closed last-boundary wire receipt; reconstruct the unsigned object from final submitted bytes; compare signing-relevant fields and exact UTXO context with `phase2_tx_obj`; obtain the node/runtime-computed input-0 sighash from the exact running node commit. If node hash differs, field-diff the preimage; if it matches, inspect signature-script assembly, pubkey ordering and covenant branch | Exact wire-object / UTXO-context / running-node sighash equivalence is unproven. Blind rebroadcast adds no evidence | `responses/RESPONSE-DISC-20260717-002-007.md`; `responses/RESPONSE-DISC-20260717-002-008.md`; `responses/RESPONSE-DISC-20260717-002-009.md`; commits `d060e872`, `b862c6e0`, `bd9b46a2`, `6340d9cb`, `5d50f568` |

## Verified facts

- The active persistent collaboration channel is `coordination/codex-bridge/` on branch `coord/codex-bridge`.
- The channel contains shared context, structured persistent discussion, directional mailboxes, canonical status and a final decision ledger.
- Codex has GitHub repository write access but no direct KANet-host access.
- The runtime Gateway/MCP deployment remains explicitly stood down by Owner decision.
- `DISC-20260717-001` is authority-closed by `DEC-20260719-001`. KANet-side agents report Gate 0 delivered and verified; Codex has accepted the receipt but awaits an accessible full commit/ref or copied evidence artifact for independent repository verification.
- The prior ~54,275 KAS / 1,526-user figure is retired. The replacement **15 markets / ~1679 bets / 67192.52 KAS** is supported by a KANet-side report summary and independent Bettor/NWT checks, but Codex has not yet inspected the underlying report body or raw query artifact.
- The reported j34vb/9jaty result demonstrates that durable endBlock capture alone cannot recover bets whose canonical accepting-block evidence was not persisted before pruning; Batch 1 design must include bet-level evidence continuity, not only endBlock capture.
- `DISC-20260717-002` is technically root-cause closed by byte-exact V2 reproduction and authority-closed by `DEC-20260718-001`; implementation and the separately governed 8pson refund remain pending.
- V1 and V2 use different templates, constructor shapes and pinned compiler families. The observed 2614-byte G0/G1 difference is explained by cross-family recompilation.
- The generic risk remains: any post-mint flag mutation, backfill, migration or partial repair can reproduce the mismatch unless covenant family and mint artifacts are bound immutably and checked before spend.
- Existing rows must not be migrated by trusting current `zk_native` alone; family classification must be derived from stored redeem bytes and pinned-family checks.
- The jepu1 signing patch covers the known cross-node path and an additional refund-disagreement path found by the repository scan. The test should later be hardened from partly file-level checks to per-call-site/AST-level binding.
- The jepu1 restart, database soft-invalidation and re-signing were executed and locally verified; settlement did not succeed and no funds moved.
- `verify-settle-sigs.mjs` validates its manual sighash against the local `kaspa-wasm` signing path over the stored object. It does not establish byte identity with the final relay-submitted transaction, node-resolved UTXO context or exact running-node TOC sighash implementation.
