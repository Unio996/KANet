# ──────────────────────────────────────────────────────────────
# KANet 根配置示例 — 复制为 kanet.env 或让 kanet-start.sh 自动生成
# ──────────────────────────────────────────────────────────────
#
# 这个文件只包含启动脚本需要的"引导"配置。其余所有敏感信息
# （AI API key、交易所凭据、钱包助记词等）都存在 DB 里用
# CONSOLE_ENCRYPTION_KEY 加密，通过 Web UI 管理。
#
# 首次启动：
#   bash kanet-start.sh
# 脚本会自动生成 kanet.env 并填入随机 CONSOLE_ENCRYPTION_KEY。
# 不需要手动复制这个文件——它只是给你看"可配置什么"的参考。

# 仓库根路径（启动脚本自动设置，不需要手填）
# KANET_ROOT=/path/to/kanet

# Console 加密主密钥（32 字节 hex = 64 字符）
# ⚠️ 致命重要：丢失 = DB 里所有加密字段（API key/助记词/token）永久不可解密
# ⚠️ 生产环境请备份到离线安全位置
# 不填则启动脚本自动生成新的
CONSOLE_ENCRYPTION_KEY=

# 本地 llama.cpp 模型路径（可选，不配就用外部 AI provider）
# 推荐：Qwen2.5-35B-Q4 或任何 GGUF 格式模型
# LLAMA_MODEL_PATH=/path/to/your/model.gguf

# OpenClaw 本地 gateway token（可选）
# 只有你在 127.0.0.1:18789 跑了 OpenClaw agent gateway 才需要
# OPENCLAW_TOKEN=your-local-openclaw-token-here

# ──────────────────────────────────────────────────────────────
# 以下变量在子模块的 .env 里配置，不在此处：
#   kasia-console/.env   — Console 端口、DB 路径
#   kaspa-scout/.env     — Kaspa RPC 节点 URL
# 模块启动时会自动读取各自的 .env，或从 Console UI 写入 DB。
# ──────────────────────────────────────────────────────────────
