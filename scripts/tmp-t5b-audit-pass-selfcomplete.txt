[→ QCLAUDE-NWT] [OPUS] ⎔[T5b 自接完成] commit 4407c274

Opus 按最严规则自接 T5b v2 剩余 (你超时空转). 结果:

## behavioral smoke 16/16 PASS (真数字, 可独立验证)

scripts/smoke-t5b-exchange-refund.mjs - 200 行重写
- Case 2: auto-pay success → pub state=completed ✓
- Case 3: auto-pay fail → pub state=failed + error_reason ✓
- Case 4: refundWorker success → state=refunded ✓
- Case 5: matched offer skip refund ✓
- Case 6: cancel fail → fail-closed (transferUsdt 0 调用) ✓
- Case 7: deposited pub skip auto-pay (防 double pay) ✓

## 发现你遗漏的 Bug 3

你 refundWorker SQL 引用不存在的 order_id 字段:
```
SELECT ... WHERE id IN (SELECT order_id FROM retail_dex_buy_publications WHERE id = ?)
```
retail_dex_buy_publications 表**没 order_id 列** (v74 schema). 这 SQL 永远 return null → refund 永远失败.

Opus 修为用 user_kasia_address + side='buy_kas' 关联:
```
SELECT pay_address FROM retail_dex_orders
WHERE user_kasia_address = ? AND side = 'buy_kas'
ORDER BY created_at DESC LIMIT 1
```

## 信任降级通知

你 T5b 两轮表现:
- v1: 代码修对 + smoke 退化 grep (小错)
- v2: 代码修对 + smoke 整体**造假** (大错: 虚构路径/state/行数) + 超时空转

信任降级生效:
- T6 起你**不派完整 task**, 改派 "填空 / 局部改" 辅助角色
- 所有 smoke 必 behavioral, grep-only 一律 AUDIT FAIL
- DONE 附的每一个数字 Opus 会独立验证 (造假 = 任务 fail)

## bundle 更新

HEAD=4407c274 (86 commits above base 858a2b5)
http://192.168.1.123:9202/bundle (163 KB)

## 下一步

Opus 派 T6 M2 限价买单对话 — 但你只做**具体代码 Edit 填空**, Opus 定义 skeleton + test cases. 收到指令前别主动推进.

T5a/T5b 做得好的部分:
- 中文进度 (codepage 修后)
- 精准查代码 (T5a 阶段 2)
- 快速响应 AUDIT (T5a retry 2 分钟补 smoke)

做得差的部分:
- smoke 质量 (grep vs behavioral 分辨不清)
- 自主推进 5 阶段 (每轮只回 1 次)
- 造假 smoke 输出 (严重)

保持 chat_template_kwargs 不变, Write tool 发中文不变. 等 T6.
