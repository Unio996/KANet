# 继承子项目已有规则（kasia-console/.gitignore 等）
node_modules/
/data/
.env
*.log
*.pid

# === 子项目运行时数据（不是源码）===
kasia-console/data/
kasia-relay/state/
kaspa-scout/data/

# === 根目录敏感配置 ===
kanet.env
_relay_env.txt

# === 临时/草稿产物（约定: `_` 前缀 = scratch，禁入库）===
# 每轮 bettor 发送器 / 余额 dump / 日志 / 实验 .cjs|.txt|.json 等。
# 已跟踪的 _owner-uat-*.mjs / _scenario*.mjs 等正经脚本不受影响（git 对已跟踪文件忽略本规则）。
# 确需新增 `_` 前缀的正经文件 → `git add -f <file>`。
_*

# === 运行时 dump 目录（dump-launch.mjs / 进程日志，含明文密钥）===
logs/

# === 数据库 ===
*.db
*.sqlite

# === 运行时数据 ===
agent-mind/minds/
health_tmp.json
hex-debug-payload.json

# === 备份与下载 ===
backups/
downloads/

# === 临时/调试文件 ===
sophie-debug-*.txt
sophie-live-*.txt
tmp-*.html
tmp-*.json
*.pid
nwt-*.json
bridge-req.json
test-*.mjs
test-*.png
test-alpine*.html
agent-adapter/debug*.mjs
kasia-relay/test-openclaw.mjs

# === 大文件 / 本地资产 ===
models/
tools/
$extractDir/
extractDir/

# === 个人备份脚本（不开源）===
backup.ps1
**/backup.ps1

# === IDE / 工具 ===
.claude/
.superpowers/

# === opencode 本地开发工具链（永不入库）===
AGENTS.md
AGENTS.*.md
ONBOARDING.md
KANET-STATE.md
opencode.json
opencode-*.bat
.opencode-*.md
sandbox/

# === Qwen 本地工具链产物 ===
qwen.tar.gz
qwen.tar
scripts/code-index.db
qclaude.bat
qclaude.sh
# scripts/qwen-bridge-worker.js — 2026-04-23 取消 gitignore
# Rule 11 (QWEN-RULES.md) kill switch 修复需随仓库走, J1 opencode 拉到
# 非 llama host 机器不用启动它, 但文件可存

# === channel-bridge runtime state ===
scripts/channel-bridge.processed.jsonl
scripts/channel-bridge.pid
scripts/channel-bridge.lastts.json

# Local bundle server working directory (J1↔J2 LAN sync)
.bundle-serve/

# owner-in-dev-channel owner bot launcher (legit code, like the grandfathered _launch_tg_bot.mjs)
!kasia-console/_launch_owner_bot.mjs
