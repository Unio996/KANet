# TASK T-2026-04-22-05 v0.3.2: Retail-Proxy Agent（单模式）

**执行者**：QClaude
**出题人**：Opus 4.7
**优先级**：P0（Agent=DEX 叙事首个实例）
**预计工作量**：~150 行新代码 + 1 新 Agent 配置

---

## 前置已完成

- ✅ **Step G hedge 门控** 已加（commit 待 Opus 一并 commit）— `_executeHedge:815` 入口检查 `offer.meta.hedge_enabled !== true` → skip
- ✅ **cross-chain-verify V3.5** 已验证：sender 软告警，**不影响 retail 代发 paid**
- ✅ **协议字段**：`exchange_offers.taker / taker_payment_address / payment_tx` 齐备
- ✅ **handleExchangeAccept / handleExchangePaid**：server-side 就位，接受任何 sender 的广播

## 设计决策（不争论）

1. **单模式 `broker_proxies`** —— retail-proxy Agent 代用户发 `accept_v1` + `paid_v1`，作 taker-of-record
2. **mobile Kasia 只做 3 件事**：聊天 / 收 KAS / 外链钱包付款。不升级、不做 Exchange UI
3. **retail-proxy = 系统级 Agent**，不是用户身份。reputation 归属 retail-proxy 正确
4. **雷 1 sender mismatch**：V3.5 软告警，不拒，**忽略**
5. **retail-proxy 钱包**：仅需 KAS gas 几个 + 临时 taker KAS 持有（收到立刻转发用户）

---

## 架构

```
手机用户 (Kasia 协议)
  │ DM "买 50 KAS"
  ▼
retail-proxy Agent (新建)
  │ skill: retail-proxy.mjs
  ├─ 1. intent 识别（正则）
  ├─ 2. 查 /api/exchange/offers?status=open&market_key=KAS|USDT
  ├─ 3. 选最优价 + 报价 DM + 30s confirm
  │    "50 USDT (BNB) → 1558 KAS, maker: kaspa:xxx, 有效 30s, 回 YES"
  ├─ 4. 用户 YES → broadcast kanet_exchange_accept_v1
  │                  (taker=proxy-agent address, offer_id=xxx)
  ├─ 5. DM 付款指引
  │    "付 50 USDT 到 0xMakerBnb (BNB 链)，付完发 txhash 回来"
  ├─ 6. 等用户 DM txhash
  ├─ 7. broadcast kanet_exchange_paid_v1
  │                  (offer_id=xxx, payment_tx=<user's txhash>)
  ├─ 8. 轮询 /api/exchange/offers/:id → completed
  ├─ 9. KAS 到 proxy-agent 钱包 → sendKaspa 给用户 Kasia 地址
  └─ 10. DM "到账啦，<N> KAS"
```

## 组件清单

### 新建

| 文件 | 职责 | 预估 |
|---|---|---|
| `agent-mind/src/skills/retail-proxy.mjs` | 核心 skill，状态机 10 步 | ~180 行 |

### 改动

| 文件 | 改动 |
|---|---|
| `agent-mind/src/skills/registry.mjs` | 注册 retail-proxy skill |
| `kasia-console/src/services/skills.js` | `registerMindSkills()` 扫描到此 skill |

### 不动

- `kasia-console/src/api/exchange.js`（publish/accept 端点不改）
- `kasia-console/src/services/exchange-machine.js`
- `kasia-console/src/services/exchange-verifiers.js`
- `kasia-console/src/services/trade-protocol-filter.js`（handleExchange*不动，Step G 由 Opus 已加）
- 所有 migrate.js（不加表）

## skill 内部状态机

每个 retail-proxy 在内存维护一个 `conversationState: Map<user_address, {stage, offer_id, quote, accept_tx, paid_tx, started_at}>`。

**stages**：
```
IDLE                     → 初始/完成后
WAITING_CONFIRM          → 已报价，等用户 YES
AWAITING_PAYMENT_TXHASH  → 已 accept，等用户 DM txhash
AWAITING_COMPLETION      → 已 paid 广播，轮询等 completed
DELIVERING               → 已 completed，正在转发 KAS
```

**并发**：一个 user_address 同时只一个 active conversation。同 user 再来新请求 → 回"你有进行中订单，先完成/取消"。

**超时**：
- `WAITING_CONFIRM` 60s 无 YES → 清空，回"超时，请重发"
- `AWAITING_PAYMENT_TXHASH` 600s 无 txhash → 清空 + 链上 cancel offer
- `AWAITING_COMPLETION` 900s 无 completed → 告警 Owner

## 关键代码要点

### intent 识别正则

```js
const TRADE_INTENT_REGEX = [
  /换\s*(\d+(?:\.\d+)?)\s*(USDT|KAS)\s*(?:成|换|为|to)\s*(KAS|USDT)/i,
  /buy\s+(\d+(?:\.\d+)?)\s+(KAS|USDT)/i,
  /我要(?:买|卖|换)\s*(\d+(?:\.\d+)?)\s*(KAS|USDT)/i,
];
```

匹配第一条 = trade intent。否则 skill 不激活（让其他 reactive 走）。

### 查 offer

```js
const offers = await fetch(`${consoleUrl}/api/exchange/offers?status=open&market_key=${market}`).then(r => r.json());
// 挑最便宜 SELL KAS（用户 买 KAS）或最贵 BUY KAS（用户 卖 KAS）
// 检查 offer.expires_at > now + 60s（要给 confirm 窗口留缓冲）
```

### 广播 accept

```js
const acceptPayload = {
  t: 'kanet_exchange_accept_v1',
  offer_id: offer.id,
  taker: proxyAgentAddress,
  taker_chain: 'bnb',
  taker_payment_address: null, // V3.5 不填，软告警
};
await sendCommandAsync(relayId, {
  type: 'send_broadcast',
  channel: 'kanet-exchange',
  message: JSON.stringify(acceptPayload),
});
```

### 广播 paid（用户 DM 回 txhash 后）

```js
const paidPayload = {
  t: 'kanet_exchange_paid_v1',
  offer_id: offer.id,
  payment_tx: userTxhash,
};
// 同样 sendCommandAsync send_broadcast
```

### 轮询 completed → 转发 KAS

```js
// 10s 间隔轮询 /api/exchange/offers/:id
// status === 'completed' && delivery_tx 有值 → KAS 已到 proxy-agent 钱包
// 调 /api/relay/:id/send (sendKaspa) 转给用户 Kasia 地址
// （扣除 proxy-agent gas，可以一期全部转给用户）
```

### 安全栏

- 单笔最大 `max_trade_usdt = 100`（config，一期硬编码防滥用）
- 每日总额 `daily_limit_usdt = 500`（防夜里被连环买穿 proxy 钱包）
- `busy_trading` flag：一个 proxy-agent 同时只一个链路在进行（多用户来 → 排队或拒绝）

## 验收剧本

**Step 1**：新建 Agent `retail-proxy-alice`，挂 `retail-proxy` skill，fund KAS ≥ 100

**Step 2**：Martin 手机 Kasia 给 alice 发 DM "换 10 USDT 成 KAS"

**Step 3**：alice 3 秒内报价（从当前 seeder 挂单里选最优）

**Step 4**：Martin 回 YES

**Step 5**：alice 广播 accept + DM 付款地址

**Step 6**：Martin MetaMask 付 10 USDT (BNB)

**Step 7**：Martin DM txhash 给 alice

**Step 8**：alice 广播 paid，状态机自动 verified → delivered → completed

**Step 9**：alice 收到 KAS → `sendKaspa` 给 Martin 手机 Kasia 地址

**Step 10**：alice DM "到账啦，N KAS"

**Martin 手机收到 KAS = 第一笔 non-Owner retail 真实成交**。里程碑。

## 工作顺序（给 QClaude）

1. **Step A**：读 `agent-mind/src/skills/self-awareness.mjs` 看现有 skill 结构。写 `retail-proxy.mjs` 骨架（gatherContext / formatForBrain / canActivate 三接口）。**验证**：skill 注册成功，Agent 日志看到 "retail-proxy skill loaded"

2. **Step B**：实现 intent 识别 + conversationState 内存管理。**验证**：dry-run 发 "买 50 KAS"，skill 返 stage=WAITING_CONFIRM 的 log

3. **Step C**：实现查 offer + 报价 DM。**验证**：用户 DM 后收到报价文本，含 maker BNB 地址

4. **Step D**：实现用户 YES → broadcast accept。**验证**：链上 offer 状态变 `matched`

5. **Step E**：实现等 txhash → broadcast paid。**验证**：用户 DM txhash 后 offer.payment_tx 被写入

6. **Step F**：实现轮询 → sendKaspa 转发。**验证**：completed 后用户 Kasia 钱包收到 KAS

7. **Step G**：✅ 已由 Opus 做完（hedge 门控）

8. **Step H**：加超时处理 + 并发 busy_trading flag。**验证**：同一 user 并发请求被拒

9. **Step I**：端到端 Martin 手机剧本。**成功 = 交付**

## 交付铁律

- 自己测试，全部通过才能交付
- 每 step 完成 → `[QCLAUDE][DONE Step X]` 到 kanet-dev，Opus 审 → PASS 进下一步
- 碰到 spec 歧义 → `[QCLAUDE][QUESTION Step X]` 到 kanet-dev，等 Opus/Martin 答
- 不要 git commit（Opus 统一 commit）
- 不要动"不动"清单里的文件

## 预估总时间

纯编码 2-3 天 + 测试 1 天 = **~4 天**（QClaude 节奏）

## 完成后报告格式

发到 `kanet-review` 频道：

```
[QCLAUDE] [DONE] T-22-05-v0.3.2 Retail-Proxy
Files:
  - agent-mind/src/skills/retail-proxy.mjs (+X new)
  - agent-mind/src/skills/registry.mjs (+Y)
  - kasia-console/src/services/skills.js (+Z) 
Acceptance:
  - Step A-F 各自测 ✅/❌
  - Step I 端到端 Martin 手机剧本 ✅/❌
  - Timeout + 并发安全栏 ✅/❌
  - 第一笔 retail completed tx hash: <xxx>
```

**spec 完。**
