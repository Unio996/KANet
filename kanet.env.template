# KANet Configuration — copy this file to kanet.env and fill in your values
# DO NOT share this file — it contains sensitive keys

# KANet root directory (change to your installation path)
KANET_ROOT=D:/Anthropic

# Encryption key for stored secrets (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
CONSOLE_ENCRYPTION_KEY=<generate-a-64-char-hex-string>
